# 작업 지시서 I — 마법서 · 덱 · 층 보상 교체 · 실전 덱 연결

> # ⛔ 보류 — 이대로 구현하지 마라 (2026-09-07 저녁)
>
> **이 지시서는 v2의 「매 턴 덱에서 손패 3장을 뽑는다」 위에 통째로 지어져 있다.**
> 같은 날 저녁 `GAME_DESIGN.md`가 **v3**로 개정되면서 그 모델이 사라졌다 —
> 이제 **주문 슬롯(속성 지정·매 턴 돌아감) × 덱 칸(서클 지정·고정)** 2층 구조다
> (`GAME_DESIGN.md` **4.2절**).
>
> **그대로 만들면 방금 지운 것을 만든다.**
>
> | 이 지시서가 전제하는 것 | v3에서는 |
> |---|---|
> | 매 턴 덱에서 손패 3장 무작위 | **주문 슬롯 3~5칸이 각자 슬롯머신처럼 돌아간다** |
> | 덱 = 활성화 주문을 서클 슬롯표에 채운 것 | **덱 칸은 「서클」만 지정한다. 주문은 전투 중 추첨된다** |
> | 시전 강도 보통/전력 | **삭제.** 서클이 DC와 마나를 정한다 |
> | 1대1 전투 | **다중 적** |
>
> **살아남는 부분도 있다** — 마법서 활성화·강화 수치(`GROWTH.md` 4·5절),
> 층 보상 교체, 시작 덱 문제(작업 5-1). **재작성 시 그대로 가져다 쓴다.**
>
> 다음 순서는 `HANDOFF.md` 2절을 본다. **엔진 지시서가 먼저다.**

발행: 2026-09-07, 기획 창.

근거 문서: **`GROWTH.md` 3·4·5절**(이 지시서의 모든 수치가 거기 있다) ·
`GAME_DESIGN.md` 5.4절 · `SPELL_CIRCLES.md` · `UI_DESIGN.md` 5-5절

> ### 🪟 창 운영
> **새 창(`/clear` 후).** 스크린샷은 **두 장**이면 된다 (★층 보상 화면 · 결과 화면).

---

## 0. 목적 (한 줄)

**게임이 v2의 성장 구조로 돌게 만든다 — 마법서를 모아 마법을 익히고,
익힌 마법이 서클 슬롯대로 덱이 되고, 그 덱으로 실제 전투를 한다.**

## 0-1. 왜 이 넷이 한 지시서인가

넷은 **하나의 사슬**이라 따로 넣으면 어느 것도 동작하지 않는다.

```
마법서를 모은다  →  주문이 활성화·강화된다  →  슬롯표대로 덱이 짜인다  →  전투가 그 덱으로 돈다
   (작업 2)            (작업 1)                  (작업 3)                (작업 4)
```

지금은 **사슬의 네 칸이 전부 비어 있다.** 특히 마지막 칸 —
`battle_controller.dart:80`의 `Battle(...)`에 **`deck` 인자가 없어서**
`hand_deck.dart`(이미 만들어 둔 것)가 **시뮬레이터에서만 돌고 실제 게임은
v1처럼 손패 3장이 고정**이다. **밸런스 곡선 열 차례를 전부 「덱에서 뽑는」
전제로 쟀다.** 이 연결이 이번 지시서의 가장 중요한 한 줄이다.

## 0-2. 절대 지킬 것

- **`GROWTH.md`의 표를 이 지시서에 옮겨 적지 않았다.** 그 문서를 읽고 옮겨라.
  **두 곳에 같은 표가 있으면 반드시 어긋난다**
- **밸런스 수치를 바꾸지 마라** — `hp_scale`·`atk_scale`·보스 HP·층수·
  주문 수치(`base_damage`·`dc_modifier`)·폭주·적 데이터는 **읽기 전용**이다
- **서클 슬롯표(`circle_slots.json`)를 고치지 마라.** 측정 14~23의 시뮬 덱이
  이 표의 **레벨 90 행**이다 (`GROWTH.md` 3.1절)
- **`tool/` 측정 도구의 로직·수치를 고치지 마라.** 작업 5에서 **이름 바뀐 것을
  따라가는 수정만** 허용한다 (컴파일이 깨지므로 불가피하다)
- `core/check.dart` · `combo.dart` · `surge.dart` · `enemy_turn.dart`를 **열지 마라**
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어 · 상태 관리는 `provider`만
- `flutter analyze` 경고 0
- **`dart format`을 파일 전체에 돌리지 마라** — 고친 부분만 손으로 정렬한다
- ⚠ **`lib/core/constants.dart`가 299줄이다.** 한 줄만 더해도 300줄 규칙을 깬다.
  **`lib/core/level.dart`처럼 갈라 내고 `constants.dart`가 `export`하는 방식**을 쓰라

---

# 작업 1 — 마법서 (수집 · 활성화 · 강화)

## 1-A. 🔴 먼저: `Spell` 모델에 `circle`이 없다

`assets/data/spells.json`의 70종에 **`circle` 칸이 이미 있는데
`lib/models/spell.dart`가 그 칸을 안 읽는다.** 그래서 지금까지
`tool/measure_profile.dart`와 `test/spell_circle_test.dart`가 **JSON을 직접
다시 읽어** 서클을 알아냈다.

**`Spell`에 `circle`을 추가하라** (`fromJson`/`toJson` 포함). 이것이 없으면
이 지시서의 나머지가 전부 JSON을 다시 읽는 꼴이 된다.

## 1-B. 저장 구조 — **보유 장수 하나만 저장한다**

`MetaState`에 **주문 id → 보유 마법서 장수** 하나만 둔다.

> ### ★ 왜 「활성화 버튼」·「강화 버튼」을 만들지 않나
> **마법서는 주문별로 따로 쌓인다** (「화염구의 마법서」). 그래서 유저가
> **어디에 쓸지 고를 여지가 없다** — 화염구의 마법서는 화염구에만 쓴다.
> 유저의 선택은 **★층에서 어느 종을 고르나** 하나뿐이고, 그건 작업 2에 있다.
>
> → **활성화 단계와 강화 단계는 보유 장수에서 계산으로 나온다.**
> 소비하지 않고 **누적**한다. 버튼도, 확인 창도, 되돌리기도 필요 없다.

- `toJson`/`fromJson`에 넣는다
- ⚠ **옛 저장본에 이 칸이 없어도 앱이 죽으면 안 된다** — 없으면 빈 상태로 읽어라
  (`clearedStageKeys`·`level`이 이미 그렇게 돼 있다. 같은 방식을 쓰라)

## 1-C. 계산식 (전부 `GROWTH.md` 4.1절)

| 무엇 | 규칙 |
|---|---|
| 활성화 | **서클 × 5장** |
| 강화 상한 | **11 − 서클** 단계 |
| 강화 n단계에 **더** 드는 장수 | **서클 × 5 × n** |
| 강화 효과 | `base_damage` **+10%씩 합산** (원본 대비). **회복·방어막도 같은 배수** |

**누적 장수 → 강화 단계**는 위 세 줄에서 바로 나온다. 표를 새로 만들지 마라.

- **강화가 함께 올리는 효과는 `heal`·`shield` 둘뿐이다.**
  나머지(`check_bonus`·`extra_die`·`delay_enemy`·`mana_restore`)는 **올리지 마라** —
  이유는 `tool/measure_growth.dart` 주석에 적혀 있다 (3.4절 확률표에 직접 개입하거나,
  지연 상한 2턴과 충돌하거나, 최대 마나가 3~5뿐이라 의미가 없다)
- 반올림은 `tool/measure_growth.dart`의 `upgradedSpell`과 **같아야 한다**
  (`(원본 × 배수).round()`). 검산 5-C가 이것을 대조한다

## 1-D. 시작 상태 — **1서클 3종을 활성화한 채로 시작한다**

레벨 1의 슬롯은 **1서클 3칸**뿐인데 마법서가 0장이면 **덱이 비어 게임이 시작되지
않는다.** 그래서 시작 시 아래 3종을 **활성화 수량(1서클이므로 각 5장)**만큼 준다.

```
cinder_shield   chill_touch   soul_nip
```

> **이 3종은 임의로 고른 것이 아니다.** `tool/measure_profile.dart`의
> `kCircle1Priority` **앞 3장**이고, 측정 16~23이 **1지역 덱으로 실제로 쓴 것**이다.
> 셋 다 위력 5에 효과가 덤이라 3장짜리 초기 덱이 교착하지 않는다
> (측정 16에서 위력 0짜리를 먼저 넣었더니 200턴 안전장치가 걸렸다).

- ⚠ **기존 `kStarterSpellIds`(`fireball`·`frost_arrow`·`mana_shield`)는 못 쓴다.**
  앞 둘이 **2서클**이라 레벨 1 슬롯에 들어가지 않는다. 위 3종으로 **바꿔라**
- 시작 장수는 매직 넘버로 박지 말고 **1-C의 활성화 식에서 계산**한다

---

# 작업 2 — 층 보상 교체 (v1 잔재 제거)

## 2-A. `floorEvent`에 **한 줄**을 더한다

`GROWTH.md` 4.5절의 유사코드 그대로다. **더하는 줄은 마지막 한 줄뿐이다** —
보스·상점·유물 세 줄은 **지금 코드 그대로 둔다.**

```
★이 하나도 없으면 남은 첫 층을 ★로 한다      ← 이 줄만 새로 더한다
```

**왜**: 지금은 **6층 이하 스테이지에 ★층이 0개**라 **1·2지역 16 스테이지 전체에
마법서 층이 없다.** ★층이 마법서의 주 획득처가 되는 순간 초반이 통째로 빈다.

- ⚠ `GROWTH.md` 4.5절의 유사코드 셋째 줄에 **「층수 5 이상이면」이라는 조건이
  잘못 적혀 있었다.** 2026-09-07에 정정했으니 **정정 후 문서를 읽어라.**
  그 조건을 넣으면 4층 스테이지 6개가 유물 보상을 잃는다
- `FloorEvent.spellReward` → **`bookReward`로 이름을 바꾼다.** 뜻이 바뀌었다.
  `kSpellRewardInterval`도 같이 바꾼다
- `floor_tile.dart`의 라벨 「주문 보상」 → **「마법서」**

## 2-B. ★층 보상: 「주문 3장 중 1택」 → **「마법서 3종 택1, 고른 종 3장」**

**화면과 조작은 그대로 살고 의미만 v2가 된다.** 새 화면을 만들지 마라 —
`reward_screen.dart`의 배너 카드 3장 구성을 그대로 쓴다.

| 지금 (v1 잔재) | 바꾼 뒤 |
|---|---|
| 주문 3장을 보여준다 | **마법서 3종**을 보여준다 |
| 고르면 **「교체할 손패를 고르세요」** 단계가 더 뜬다 | **없앤다.** 고르면 바로 끝 |
| 손패 한 칸이 교체된다 | 그 종의 마법서 **3장**이 쌓인다 |

- 카드에는 **주문 이름 · 서클 · 「보유 N장」**과, 그 3장으로 **활성화되거나
  강화 단계가 오르면 그 사실**을 한 줄로 보여준다 (「곧 활성화」·「2강 → 3강」)
- 「건너뛰기」는 그대로 남긴다

## 2-C. 그 밖의 획득처 (`GROWTH.md` 4.2절)

| 어디 | 장수 |
|---|---|
| 일반 전투 승리 | 1장 |
| 보스 전투 승리 | 5장 |
| ★층 | 3장 (3종 택1) |

- **난이도 배율을 마법서에 곱하지 마라.** 하드·데스의 보상은 각인이다
- **상점에서 파는 마법서는 이번에 만들지 마라** (상점 경제는 별도)

## 2-D. 어느 주문의 마법서가 나오나 (`GROWTH.md` 4.3절)

**지역별 서클 풀 안에서 무작위 균등.** 12지역치 서클 범위는 그 절의 표에 있다.

- `assets/data/regions.json`에 **칸을 신설**한다 (절대 규칙 2 — 수치는 JSON에).
  **그 밖의 칸은 한 값도 건드리지 마라**
- **이미 만강인 주문은 후보에서 뺀다** (4.2절)
- 후보가 하나도 안 남으면 **그 층은 마법서를 주지 않는다.** 억지로 주지 마라

## 2-E. 지워야 할 v1 잔재

`run_controller.dart`에서 **아래 셋을 지운다.**

| 무엇 | |
|---|---|
| `_rollSpellOffers()` · `spellOffers` · `takeSpell(chosen, slotIndex)` · `skipSpellReward()` | 손패를 교체하는 개념 자체가 사라진다 |
| `RunState.handIds` | 덱이 대신한다 (작업 3). **쓰는 곳을 전부 정리하고 지워라** |
| `meta.unlockedSpellIds`를 보상 추첨에서 쓰던 조건 | 마법서 활성화가 그 역할을 한다 |

- ⚠ **`MetaState.unlockedSpellIds` 칸 자체는 지우지 마라.** 「영구 강화」 메뉴가
  아직 그것을 팔고 있고, 그 메뉴는 **패시브 트리 지시서가 통째로 걷어낸다.**
  **덱 구성이 그 값을 보지 않게만** 하면 된다

---

# 작업 3 — 덱 (자동 구성)

## 3-A. 규칙

**덱 = 「활성화된 주문」을 「그 레벨의 서클 슬롯표」에 맞춰 자동으로 채운 것.**

- 슬롯 수는 `GameData.circleSlotsAt(level)` (이미 있다)
- 서클 c의 칸에는 **활성화된 c서클 주문만** 들어간다
- 활성화된 것이 슬롯보다 적으면 **그 칸은 빈다.** 다른 서클로 메우지 마라
- 덱에 담기는 주문은 **강화가 씌워진 사본**이다 (1-C)

## 3-B. 🔴 채우는 순서 — 측정과 **같아야 한다**

`tool/measure_profile.dart`의 `_pickCircle` · `_circleOrder` · `kCircle1Priority`가
**측정 16~23이 실제로 쓴 순서**다. **같은 순서를 `lib/`에 두어라.**

| 서클 | 순서 |
|---|---|
| **1서클** | `kCircle1Priority` 순 (위력을 지킨 유틸 먼저), 그다음 나머지 |
| **2서클 이상** | **위력 큰 순 → 같으면 id 순**, 그리고 **속성이 겹치지 않게** |
| 속성 제약으로 칸이 남으면 | 순서대로 메운다 |

- **`tool/`을 `lib/`에서 import 하지 마라.** 반대 방향(`test/`가 둘 다 읽어 대조)은
  이미 `test/deck_profile_test.dart`가 하고 있다. **검산 5-A가 그 방식으로 대조한다**
- **덱 편성 화면(수동)은 이번에 만들지 마라** (`UI_DESIGN.md` 5-5는 다음 지시서다)

---

# 작업 4 — 🔴 실전을 덱으로 돌린다 (**이번 지시서의 핵심**)

`RunController.buildBattleController()` → `BattleController.startBattle(...)` →
`Battle(...)`로 **덱이 흘러가게** 만든다.

- `Battle`은 **이미 `deck` 인자를 받는다.** `hand_deck.dart`도 **이미 있다.**
  **중간의 두 칸이 안 이어져 있을 뿐이다**
- 덱을 넘기면 `Battle.startTurn()`이 **매 턴 손패를 다시 뽑는다.** 그게 v2 4.2절이다
- `startBattle`의 `hand` 인자는 **첫 턴 전에 쓰이는 초기값**이다. 덱에서
  뽑히도록 이어졌는지 **동작으로 확인하라** (검산 5-B)

> **`hand_deck.dart`를 고치지 마라.** 이미 검증돼 있다 (`test/deck_test.dart`).

---

# 작업 5 — 함께 얹는 것 (작은 것 둘)

## 5-1. 변종 경험치 범위 — **✅ 사용자 승인 완료**

`lib/screens/run_controller.dart:96`

```
isVariant: currentEnemy.variantId != kNormalVariantId   ← 지금 (변종 전부)
isVariant: currentEnemy.variantId == kAncientVariantId  ← 이렇게
```

`kAncientVariantId`는 **이미 `constants.dart:176`에 있다.**

**왜**: 변종 전부에 ×1.5를 주면 **10지역에서 만렙**이 되어 9서클 덱(21장)으로
11·12지역을 돈다. 곡선은 **레벨 90 행(19장·9서클 없음)**으로 쟀으므로
**조건 B1·B2의 근거가 무너진다** (`HANDOFF.md` 4-1절에 지역별 도달 레벨 3열 비교표).

- `GROWTH.md` 1.3·1.4절은 **기획 창이 이미 고쳤다.** 문서를 또 고치지 마라
- `lib/core/level.dart`의 `battleExp` 자체는 **그대로 둔다.** 부르는 쪽 한 줄만 바뀐다

## 5-2. 결과 화면 — 한 줄만

이번 판에서 **얻은 마법서 장수**와 **새로 익힌(활성화된) 주문 수**를 보여준다.

- ⛔ **연출·애니메이션을 만들지 마라.** 경험치 줄 옆에 숫자만 붙이면 된다
- ⛔ **「영구 강화」·「도달 층수」 문구 3건은 이번에 건드리지 마라.**
  셋 다 **「영구 강화」 메뉴와 한 화면에 묶여 있고, 그 메뉴는 패시브 트리 지시서가
  통째로 걷어낸다.** 지금 고치면 두 번 고친다 (검토 25와 같은 판단)

---

# 작업 6 — 검산

## 6-A. ★ 덱이 측정과 같은가 (**가장 중요한 검사**)

**모든 주문이 활성화되고 강화가 0인 상태**에서 자동 덱을 짓고 대조한다.

| # | 검사 | 기대 |
|---|---|---|
| 1 | **레벨 90 덱** vs `profileDeck(spells, circles, 12)` | **id 목록이 완전히 같다** |
| 2 | 레벨 99 덱 | **21장** |
| 3 | 레벨 1 덱 | **3장** — 작업 1-D의 3종 |
| 4 | 어느 레벨에서도 덱이 **3장 이상** | 손패 3장을 못 채우면 원칙 4가 깨진다 |

> 1번이 이 지시서의 **밸런스 안전장치**다. `kDeckSlots[12]`는 슬롯표 **레벨 90 행**과
> 같은 값이므로, 순서 규칙이 어긋나면 여기서 잡힌다.
> `test/deck_profile_test.dart`가 이미 `tool/measure_profile.dart`를 import 하고 있다.

## 6-B. ★ 실전이 덱으로 도는가 — **동작으로 걸어라**

「`deck` 인자가 있는가」 같은 **문자열·존재 검사로 때우지 마라.**

- 활성화 주문이 슬롯보다 **많은** 상태로 전투를 만들고, **여러 턴을 돌려
  손패에 뜬 주문 id 집합이 3장을 넘는지** 본다 (고정 손패면 절대 안 넘는다)
- 손패는 **언제나 덱 안의 주문**이다

## 6-C. 마법서 계산식

| # | 검사 | 기대 |
|---|---|---|
| 1 | 만강까지 총 장수 (서클 1~9) | `GROWTH.md` 4.1절 표와 **전부 일치** |
| 2 | 만강 위력 배수 | 1서클 **×2.00** … 9서클 **×1.20** |
| 3 | 강화 상한 | 11 − 서클을 **넘지 않는다** |
| 4 | 활성화 직전 장수 | 덱에 **안 들어간다** |
| 5 | `heal`·`shield`만 함께 오른다 | 나머지 효과는 원본 그대로 |
| 6 | 위 2·5를 `tool/measure_growth.dart`의 `upgradedSpell`과 **대조** | 값·반올림이 같다 |

## 6-D. 층 보상

| # | 검사 | 기대 |
|---|---|---|
| 1 | **스테이지 100개 전부** | ★층이 **1개 이상** |
| 2 | 층수 3·4·5·6·7·8·10 배치 | `GROWTH.md` 4.5절 표(**정정본**)와 일치 |
| 3 | 보스 1 · 상점 1 · 유물 ≤1 | **지금 테스트 그대로 통과해야 한다** |
| 4 | 보통 한 바퀴 마법서 총량 | **1,584장** (`GROWTH.md` 4.4절) |
| 5 | 만강인 주문 | 드랍 후보에 **안 나온다** |
| 6 | 지역별 드랍 서클 | 4.3절 표와 일치 (12지역 전수) |

> 4번은 **각 스테이지를 한 번씩만, 보통 난이도로** 돈 값이다.
> 기획 창이 스크립트로 **1,584에 정확히 맞는 것을 확인했다.** 안 맞으면 보고하라 —
> **임의로 수치를 고치지 마라.**

## 6-E. 저장

| # | 검사 |
|---|---|
| 1 | **마법서 칸이 없는 옛 저장 JSON**을 읽어도 죽지 않는다 |
| 2 | 저장·복원 왕복 (`toJson` → `fromJson`) |
| 3 | `regions.json`의 **신설 칸 외에 바뀐 값이 없다** (12지역 전수 대조) |

---

# 🔴 미리 찾아 둔 「반드시 깨지는 기존 테스트」

**옳게 고칠수록 깨진다. 아래는 고쳐도 되는 것들이다** — 헤매지 마라.
(`flutter test` 현재 **313개** 통과 상태에서 출발한다)

| 파일·줄 | 무엇 | 어떻게 |
|---|---|---|
| `test/run_test.dart:211` | 「주문 보상 층 승리 후 3종 → 교체 동작」 | **마법서 보상 검사로 다시 쓴다** |
| `test/meta_test.dart:149` | 「epic 주문은 해금 전에는 보상에 안 나온다」 | **지운다** — 마법서 활성화가 그 역할을 한다 |
| `test/meta_test.dart:163` | 「해금한 epic 주문은 보상에 나올 수 있다」 | **지운다** (같은 이유) |
| `test/progression_test.dart:207` | 「3층 스테이지에는 유물 보상이 없다」 | 1층이 **battle → ★**로 바뀐다. 기대값 수정 |
| `test/progression_test.dart:214` | 「층수별 배치 — 5·8·10층」 | **5층만** 1층이 ★로 바뀐다. 8·10층은 **이름만** 바뀐다 |
| `test/progression_test.dart:190` | 「스테이지 100개 전부에서 배치 규칙」 | **★ 1개 이상**을 단언에 **더한다** |
| `test_shots/capture_test.dart:143` | `run.spellOffers`에 직접 대입 | 마법서 보상으로 고친다 |
| `lib/screens/map_screen.dart:101` | `run.spellOffers.isNotEmpty` 분기 | 마법서 보상으로 고친다 |
| `tool/sim_core.dart:112·240` | `FloorEvent.spellReward` 참조 | ⚠ **이름만 따라가라.** `spellReward()` 함수의 **동작·수치는 그대로 둔다** |

> **`tool/sim_core.dart`의 보상 동작을 v2로 바꾸지 마라.** 곡선 재측정은
> **별도 지시서(순서 6)**다. 여기서 손대면 측정 14~23과 비교가 끊긴다.

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. **실제로 앱을 띄워** 확인하고, **실제 기기 비율(400×860)**로 찍은 480px 축소본을
   `reports/img/30_books/`에 남긴다 — **★층 보상 화면 · 결과 화면 두 장**
3. `PROGRESS.md`에 기록
4. 🔴 **`reports/30_마법서_덱.md`를 「작업을 시작할 때」 먼저 만들고,
   한 덩어리가 끝날 때마다 한 줄씩 덧붙여라.** 끝에 몰아 쓰지 마라
   (양식 `reports/_TEMPLATE.md`). 끊기면 **「다음: ~」 한 줄**을 남긴다
   - 맨 앞에 **「6-A 1번(레벨 90 덱 = 측정 덱)이 통과했는가」 한 줄**
   - 판단·제안은 **「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **덱 편성 화면(수동)** · 마법서 도감 화면 (`UI_DESIGN.md` 5-5 — 다음 지시서)
- ❌ **상점에서 마법서 판매** (상점 경제는 별도)
- ❌ **패시브 트리 · 각인 · 장비**
- ❌ **「영구 강화」 메뉴 · v1 잔재 문구 3건** (패시브 트리 지시서가 함께 걷어낸다)
- ❌ **보스 호위 · 연전 · 전조** (별도 지시서)
- ❌ 서클 슬롯표 · 밸런스 수치 · 주문 수치 · 층수 · 적 데이터 수정
- ❌ `core/check.dart`·`combo.dart`·`surge.dart`·`enemy_turn.dart`·`hand_deck.dart`
- ❌ **`tool/`의 로직·수치 변경** (이름 따라가기만 허용)
- ❌ 경험치·마법서 획득 연출 공들이기
- ❌ `dart format`을 파일 전체에 돌리기
