# REVIEW — 전체 검수 보고서 (2026-08-31)

GAME_DESIGN.md / CLAUDE.md 기준으로 지금까지 만든 것 전부를 검수했다.
**아무것도 고치지 않았다.** 발견 사항만 정리한다.

검수 방법:
- 데이터: 전용 검수 스크립트로 4개 JSON 전수 검사 (수량·분포·스키마·범위·참조)
- 엔진: `flutter test` 재실행 (62개), `flutter analyze` 재실행
- 코드: lib/test/tool 전 파일 정독 + 줄 수 측정
- 텍스트: surges.json 80종 전부 다시 읽고 품질 평가

## 종합 판정

| 영역 | 판정 |
|---|---|
| 데이터 수량·분포 | ✅ 전부 기획서와 일치 |
| id 중복·스키마·참조 | ✅ 위반 0건 (문서화된 예외 2건) |
| 판정 엔진 vs 3.4절 확률표 | ✅ 일치 (재실행으로 재확인) |
| surges 텍스트 품질 | ⚠️ 80종 중 4~5종 손질 권장 |
| CLAUDE.md 코드 규칙 | ⚠️ 위반 3건 (아래 상세) |
| 전투 로직 정합성 | ⚠️ "행동 예고" 원칙 위반 2건 (중요) |

---

## 1. JSON 수량·등급 분포 — ✅ 합격

| 파일 | 목표 | 실제 | 판정 |
|---|---|---|---|
| spells | 70 (common 30/rare 25/epic 15) | 70 (30/25/15) | ✅ |
| spells 속성 | 5종 균형 | fire/frost/arcane/shadow/nature 각 14 | ✅ |
| spells effect 보유 | 30% | 21개 (30.0%) | ✅ |
| surges | 80 (자해30/반전20/소환20/도박15/이득15%) | 80 (24/16/16/12/12 = 정확히 그 비율) | ✅ |
| enemies | 30 (보스 4) | 30 (보스 4, tier 1~5에 6/5/5/5/5) | ✅ |
| relics | 40 (18/14/8, 확률 관련 70%) | 40 (18/14/8, 28개=70%) | ✅ |

- 대미지 범위(common 5~9 / rare 8~14 / epic 12~22), dc_modifier 범위
  (0 / 0~1 / 1~3), 주문 설명 20자 이내, 유물 판정 보정 최대 +3,
  보스 HP 85~95 + 2페이즈, 적 패턴 3~4개 행동 — **전부 전수 검사 통과.**
- tier별 HP도 4.4절 곡선 범위 안 (t1 12~16, t2 20~24, t3 28~32, t4 36~40, t5 42~45).

## 2. id 중복·스키마 위반·참조 깨짐 — ✅ 합격

- id 중복: **0건** (4개 파일 안에서도, 파일끼리 겹치는 것도 없음)
- 이름 중복: 0건
- 스키마 외 키 / 키 누락: 0건 (surges의 `category`는 BLOCKERS 판단 5로 문서화됨)
- effect type: 주문 6종·폭주 10종 허용 목록만 사용, 위반 0건
- **태그 참조 양방향 검사 통과:** 폭주 태그 중 어떤 주문도 안 쓰는 "고아
  태그" 없음. 모든 주문이 최소 35종 이상의 폭주와 매칭 (최소: ember_dart 35종)
- 문서화된 예외 2건만 존재: 마력방패·치유 약초의 base_damage 0 (판단 4)

## 3. surges.json 텍스트 품질 — ⚠️ 4~5종 손질 권장

80종을 전부 다시 읽었다. "마법이 실패했다"류의 밋밋한 문장은 없지만,
아래는 다른 76종에 비해 약하다 (구체적 장면 대신 추상어·군더더기):

| id | 현재 문제 | 개선 방향 |
|---|---|---|
| `free_echo` | "세상에 이런 일이"는 군더더기. 마나가 벽에 튕긴다는 그림이 안 그려짐 | 장면을 만들 것 (예: 쓴 마나가 물방울로 맺혀 지팡이를 타고 돌아온다) |
| `warm_glow` | 사고가 아니라 그냥 예쁜 문장. 폭주다운 의외성이 없음 | "실패가 좋은 일이 되는 순간"이 보이게 |
| `echo_cast` | "판정도 다시 하란다" — 게임 용어가 세계관 바깥에서 들어옴 | 판정 언급 없이 장면만 |
| `overload` | "위력은 세 배, 반동도 진짜다" — 수치 설명이지 장면이 아님 | 임계점을 넘는 순간의 묘사로 |
| `all_in` | "통장은 비었지만" — 현대 은어. 유머 톤이 의도라면 유지 가능 | 톤 방침 결정 필요 (아침 판단) |

**구조적 지적 1건:** 반전형 16종 중 5종(mirror_world, time_stutter,
echo_cast, reversed_incantation, drunk_compass)이 전부 `force_reroll`
단독 효과다. 이름·문장은 다르지만 플레이어 체감은 같은 사건 5개.
2~3종을 다른 효과 조합으로 바꾸면 반전형이 더 풍성해진다.

## 4. 판정 엔진 vs 3.4절 확률표 — ✅ 합격 (재확인)

검수 시점에 `flutter test`를 다시 돌렸다: **62개 전부 통과, analyze 경고 0.**

- 몬테카를로 10만 회(시드 고정)가 DC 7/10/13 전부 표와 오차 1% 이내
- 몬테카를로와 별개로 **216가지 전수 계산**이 표와 소수점까지 일치
  (대성공 62.5/25.9/4.6%, 실패 0.5/9.3/37.5% 등)
- 트리플=무조건 대성공, 뱀눈=무조건 폭주, 페어 +3, 족보 우선순위(216가지
  전수), 마력 축적 3칸 확정 대성공 — 전부 테스트로 강제되고 있음
- 전제: 대성공 경계 = DC+3 (BLOCKERS 판단 1, **아침 확정 필요** — 3.2절
  본문의 +5와 모순. 표 기준으로 +3 채택함)

## 5. CLAUDE.md 코드 규칙 — ⚠️ 위반 3건

1. **`lib/core/battle.dart` 363줄 — "파일 300줄 초과 시 분리" 위반.**
   제안: 폭주 효과 적용부(_applySurge, _applySurgeEffect, _forceReroll,
   SummonUnit)를 `lib/core/surge.dart`로 빼면 두 파일 다 300줄 안쪽이 되고,
   원래 폴더 구조 계획(core/surge.dart)과도 맞는다.
2. **`test/check_test.dart` 331줄 — 같은 규칙 위반** (테스트 파일도 예외
   조항이 없음). 제안: 족보 테스트를 combo_test.dart로 분리.
3. **매직 넘버 1건:** battle.dart 217행 `_random.nextInt(4)` — 아슬아슬
   반동 종류 수 4가 하드코딩. `constants.dart`에 `kGrazeBacklashKinds = 4`
   같은 상수로 빼야 한다.

그 외는 준수: 함수 40줄 초과 없음(최장 _applyGrade 28줄), 주석 전부 한국어,
모델 전부 fromJson/toJson 보유, lib에 print() 없음, 서버 통신 없음,
주사위 눈을 대미지로 쓰는 코드 없음(전수 확인).

참고 (위반은 아니지만): `lib/main.dart`는 아직 Flutter 템플릿(영어 주석,
카운터 앱) 그대로다. UI 단계에서 교체 예정이므로 문제는 아니나,
`test/widget_test.dart`(카운터 테스트)는 main.dart를 갈아엎는 순간 깨진다.

## 6. 전투 로직 정합성 — ⚠️ 예고 원칙 위반 2건 (가장 중요한 발견)

기획서 4.3절: "다음 행동은 항상 화면에 **예고**된다 (유저가 계산할 수
있어야 함)". 현재 로직은 두 상황에서 예고가 거짓말을 하게 된다:

1. **강타(charge) 대기 중 예고가 틀림.** 적이 강타를 예고하면
   `_pendingChargeValue`에 저장되는데, 패턴 인덱스는 이미 다음 칸으로
   넘어가 있다. 그래서 `telegraph`(예고 표시용 값)는 "다음다음 행동"을
   보여주지만 실제로 다음 턴에 터지는 건 강타다. UI를 붙이는 순간
   유저가 잘못된 정보로 판단하게 된다.
   제안: `telegraph`가 `_pendingChargeValue > 0`이면 강타를 돌려주도록.
2. **보스 2페이즈 전환 시 예고가 틀림.** 전환 검사가 적 턴 시작 시점이라,
   유저가 1페이즈 행동을 보고 시전했는데 실제로는 2페이즈 패턴의 첫
   행동이 나온다.
   제안: 전환을 대미지 적용 직후로 옮기고, 전환 시 예고를 갱신해서
   유저가 새 예고를 보고 시전하게 할 것.

둘 다 지금은 화면이 없어서 안 보이지만, **전투 화면(다음 단계)을 만들기
전에 고치는 게 맞다.**

### 그 외 사소한 지적 (버그는 아님, 알고 있으라는 수준)

- 소환수 처리(`_tickSummons`)가 시전할 때만 돌아간다. 손패가 전부
  봉인돼 시전을 못 하는 턴에는 소환수도 멈춘다. 드문 상황이지만 규칙을
  정해두는 게 좋다.
- 마력 축적 3칸 확정 대성공이 트리플(어차피 대성공)에도 소모된다.
  게이지가 아깝게 낭비되는 느낌을 줄 수 있다. "트리플이면 게이지 유지"
  같은 배려를 검토할 만하다 (수치 변경이라 아침 판단).
- 시뮬레이터 통계에서 force_reroll 폭주가 재굴림에 성공하면 같은 시전이
  두 번 집계된다 (실패 1 + 새 등급 1). BALANCE.md의 판정 분포가 1~2%p
  왜곡됐을 수 있다. 결론을 뒤집을 수준은 아님.
- tier 3 적 풀(HP 28~32)이 5층(기획 HP 28)에도 나오므로 5층에서 HP 32
  적을 만날 수 있다. 층별이 아니라 tier별 풀이라 생기는 ±4 편차다.

## 7. 앞으로 필요한 것 (결함 아님, 남은 일)

- **아이콘 이름은 전부 미검증 추정치다.** JSON의 icon 필드는
  game-icons.net풍으로 지어놨지만 실제 존재 여부를 확인 안 했다
  (예: "double-fire", "chalk-outline-murder"는 없거나 톤이 안 맞을 수
  있음). 아이콘 내려받는 단계에서 하나씩 대조하고 `ATTRIBUTION.md`를
  그때 만들어야 한다 (절대 규칙 6).
- **유물 효과 대부분이 아직 미구현.** 전투 로직은 checkModifier(판정 +N)만
  받는다. extra_reroll, free_rerolls, reroll_ones, pair_bonus_up 등은
  전투 화면/런 진행을 만들 때 붙여야 한다.
- 미도달 단계(계획대로): 전투 화면, 지도/보상, 메타 성장·저장, 손맛,
  광고·결제, 안드로이드 빌드. §9 완료 정의 중 "APK 실기기 실행"은
  Android SDK 설정도 선행돼야 한다.

## 조치 결과 (수정 라운드 완료)

사용자 지시("수정해")로 아래를 반영했다. `flutter test` 64개 전부 통과,
`flutter analyze` 경고 0. 시뮬레이션 재실행 후 BALANCE.md 수치 갱신.

| 지적 | 조치 |
|---|---|
| §6-1 강타 대기 중 예고 거짓말 | ✅ telegraph가 예고된 강타를 보여주도록 수정 + 테스트 추가 |
| §6-2 보스 2페이즈 예고 거짓말 | ✅ 전환을 적 턴 종료 후로 이동 (예고된 행동은 반드시 실행) + 테스트 추가. 원래 제안과 다른 방식인 이유는 BLOCKERS 판단 8 |
| §5-1 battle.dart 363줄 | ✅ 폭주 처리를 `core/surge.dart`(SurgeSystem)로 분리 → 300줄 |
| §5-2 check_test.dart 331줄 | ✅ 족보·주사위 테스트를 `combo_test.dart`로 분리 → 256줄 |
| §5-3 매직 넘버 nextInt(4) | ✅ `kGrazeBacklashKinds` 상수화 |
| §3 밋밋한 폭주 텍스트 5종 | ✅ free_echo·warm_glow·echo_cast·overload 재작성, all_in은 은어 제거("마지막 한 방울까지") |
| §3 force_reroll 5종 몰림 | ✅ reversed_incantation → seal_spell, drunk_compass → force_reroll+마나 손실 (순수 force_reroll 3종만 유지) |
| §6 소환수 시전 시에만 행동 | ✅ 적 턴 시작 시 행동하는 규칙으로 확정 |
| §6 시뮬레이터 이중 집계 | ✅ 로그 파싱 폐기, Battle 통계 카운터 직접 집계 |

**수정 안 한 것 (사용자 결정 필요):** 대성공 경계 DC+3 확정, 트리플 시
게이지 유지 여부, 아이콘 검증(다운로드 단계), main.dart 템플릿(UI 단계).

## 우선순위 요약 (아침에 볼 것)

1. **[결정]** 대성공 경계 DC+3 확정 여부 (판단 1) — 모든 밸런스의 전제
2. **[수정 권장]** 예고 거짓말 2건 (§6) — UI 만들기 전에
3. **[수정 권장]** battle.dart 파일 분리 + 매직 넘버 1건 (§5) — 분리하는 김에 §6과 함께
4. **[손질 권장]** surges 4~5종 텍스트 + force_reroll 5종 몰림 (§3)
5. **[결정]** 폭주 텍스트의 유머 톤("영끌", "통장") 유지 여부
