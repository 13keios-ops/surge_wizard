# 작업 지시서 B — 적·지역·스테이지 데이터 (구현 창용)

발행: 2026-09-02, 기획 창. **사용자 승인 완료된 작업이다.**

근거 문서: **`ENEMIES.md`** (보스 12종·변종·tier 풀) · **`STAGES.md`** (지역 12·스테이지 100)

> **읽는 순서**: 이 문서 → `ENEMIES.md` 2·3·5·6절 → `STAGES.md` 2·3·4절 → 작업 시작.
> **표를 이 지시서에 옮겨 적지 않았다.** 두 곳에 같은 표가 있으면 반드시 어긋난다.
> **근거 문서를 보고 옮겨라.**

> ### 🪟 창 운영
> **이 지시서는 새 창(`/clear` 후)에서 시작한다.** 앞 대화가 필요 없다.
> **스크린샷을 볼 일이 없는 작업**이므로 한 창에서 끝까지 간다.
> (`CLAUDE.md` 「새 창이냐 이어서냐」)

---

## 0. 이 작업의 목적 (한 줄)

**진행 구조가 「10층 단선」에서 「지역 12 · 스테이지 100 · 난이도 3」으로 바뀌었다.**
그에 필요한 적·지역·스테이지 데이터를 만든다.

## 0-1. 절대 지킬 것

- **JSON 데이터와 테스트만 고친다. `lib/` 코드는 한 줄도 고치지 마라.**
  새 파일 3종을 읽을 **모델·로더는 다음 지시서의 일이다.** 지금은 파일만 만든다.
  검산은 테스트에서 `dart:io`로 JSON을 직접 읽어 확인한다 (로더 불필요).
- **`Enemy.fromJson`은 모르는 키를 무시한다.** 그래서 `region` 필드를 넣어도
  앱이 죽지 않는다. 그 상태가 정상이다.
- **수치를 임의로 바꾸지 마라** (절대 규칙 3). 이 지시서에서 바뀌는 기존 수치는
  **승인받은 보스 3종뿐이다.**
- 한글 JSON을 **PowerShell 텍스트 명령으로 고치지 마라.** Edit/Write 도구만 쓴다.
  (지시서 A가 쓴 「비ASCII 바이트 수 단언」 방식이 좋았다. 이번에도 권한다)
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것.

---

# 작업 1 — `assets/data/enemies.json`

## 1-A. 보스 9종 추가 (30종 → 39종)

`ENEMIES.md` **2절**(이름·id·icon·색조) + **3절**(수치·패턴·2페이즈)대로 추가한다.

기존 보스와 형식이 같다. **`region` 필드만 새로 붙는다.**

```json
{
  "id": "boss_elder_slime",
  "name": "태고의 슬라임",
  "icon": "slime",
  "hp": 40,
  "tier": 1,
  "is_boss": true,
  "region": 1,
  "pattern": [ ... ],
  "phase2_hp_threshold": 20,
  "phase2_pattern": [ ... ]
}
```

- `tier`는 그 보스가 속한 지역의 **주력 tier**를 쓴다 (`ENEMIES.md` 6절 표)
- **2페이즈는 12종 전부 있어야 한다.** 규칙은 `ENEMIES.md` 3절
  「2페이즈 수치 규칙」 — 임계 = HP 50%, 패턴 값 = 1페이즈 ×1.2
- 신규 9종의 2페이즈 `label`은 1페이즈 문구를 더 격하게 바꾼 것으로 짓는다

## 1-B. ✅ 승인된 수치 변경 — 기존 보스 3종

**2026-09-02 사용자 승인 완료.** `ENEMIES.md` 3절 표대로.

| 보스 | HP | 일반 공격 | 강타 | `phase2_hp_threshold` |
|---|---|---|---|---|
| `boss_archlich` | 90 → **330** | 8 → **14** | 14 → **30** | 45 → **165** |
| `boss_void_titan` | 95 → **400** | 8 → **16** | 14 → **34** | 47 → **200** |
| `boss_inferno_dragon` | 92 → **480** | 9 → **18** | 15 → **38** | 46 → **240** |

- **`phase2_pattern`의 값도 함께 올린다** (1페이즈 새 값 × 1.2, 반올림)
- **`label`(문구)은 전부 그대로 둔다.** 수치만 바꾼다
- **`region`을 붙인다** — 리치 10 · 거인 11 · 화염룡 12

## 1-C. `boss_dice_devourer` — 손대지 않는다

**수치·패턴·2페이즈 전부 현행 유지.** `region` 키를 **아예 넣지 않는다**
(또는 `null`). 이벤트 던전용으로 아껴 둔 보스다 (`ENEMIES.md` 2절).

## 1-D. 일반 적 26종 — 손대지 않는다

`region`도 붙이지 않는다. 일반 적은 지역이 아니라 **tier 풀**로 배정된다.

---

# 작업 2 — `assets/data/variants.json` (신규)

`ENEMIES.md` **5.1절** 표 그대로 6종.

```json
[
  { "id": "normal",  "name_prefix": "",      "tint": null,      "hp_mul": 1.0, "atk_mul": 1.0 },
  { "id": "hungry",  "name_prefix": "굶주린", "tint": "#C8503C", "hp_mul": 0.8, "atk_mul": 1.3 }
]
```

- `normal`은 `name_prefix`가 빈 문자열, `tint`가 `null`이다
- 나머지 5종은 `ENEMIES.md` 5.1절의 색조 코드를 그대로 쓴다

---

# 작업 3 — `assets/data/regions.json` (신규)

지역 12개. `ENEMIES.md` **6절**(tier 풀·지역 배율) + **5.3절**(변종 가중치)
+ `STAGES.md` **2절**(이름·테마·스테이지 수·보스)을 한 항목에 모은다.

```json
{
  "id": 1,
  "name": "마나의 숲",
  "theme": "forest",
  "boss_id": "boss_elder_slime",
  "stage_count": 8,
  "hp_scale": 1.00,
  "atk_scale": 1.00,
  "tier_pool": { "1": 90, "2": 10 },
  "variant_weights": { "normal": 80, "hungry": 20 }
}
```

- `theme`는 영문 소문자로 — `forest` / `dungeon` / `desert` / `tower`
  (기획서 6.1절의 「숲(녹)」「사막(설원)」 같은 변형은 **테마 4종 중 하나 + 색조**로
  처리하는 것이 원칙이다. 색조 값은 **이 작업에서 정하지 않는다.** 그래픽 쪽 일이다)
- `tier_pool`·`variant_weights`는 **0인 항목을 넣지 않는다.** 합은 각각 100
- **12지역 이름은 「고룡의 둥지」다.** 「미지의 던전」이 남아 있으면 안 된다

---

# 작업 4 — `assets/data/stages.json` (신규)

스테이지 **정확히 100개.** `STAGES.md` **3절**(부제) + **4절**(층수·권장 레벨).

```json
{
  "id": "r1_s8",
  "region_id": 1,
  "index": 8,
  "subtitle": "태고의 웅덩이",
  "floors": 5,
  "recommended_level": 8,
  "is_boss_stage": true
}
```

- `id`는 `r{지역}_s{순번}` 형식
- `index`는 화면에 로마숫자로 바꿔 보여줄 정수다. **JSON에 로마숫자를 넣지 마라**
- `subtitle`은 **보스 스테이지 12개에만** 넣는다. 나머지 88개는 키를 넣지 않는다
- 이름은 데이터에 넣지 않는다 — 화면에서 `지역명 + 로마숫자`로 만든다

---

# 작업 5 — 검산 테스트 추가

## 5-A. `test/enemy_data_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | 적 **39종**, id 중복 0 |
| 2 | `is_boss`가 **13종** (지역 12 + 포식자 1) |
| 3 | ★ `region`을 가진 보스가 **정확히 12종**이고, 값이 **1~12 각각 하나씩** |
| 4 | `boss_dice_devourer`만 보스 중 `region`이 없다 |
| 5 | 일반 적 26종은 `region`이 없다 |
| 6 | 보스 12종의 HP가 `ENEMIES.md` 3절 표와 일치 (지역 순 40→480 단조 증가) |
| 7 | ★ 보스 13종 전부 `phase2_hp_threshold`와 `phase2_pattern`이 있고,<br>**임계 = `hp ~/ 2`** 다 (포식자 제외 — 현행값 44 유지) |
| 8 | 보스 이름에 **「감독관」·「문지기」·「수호상」**이 없다 (`ENEMIES.md` 2절 이름 규칙) |

## 5-B. `test/region_stage_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | `variants.json` 6종, id 중복 0, `normal`의 배율이 1.0 |
| 2 | `regions.json` 12개, id 1~12 각각 하나씩 |
| 3 | ★ `tier_pool` 12행이 **각각 합 100** |
| 4 | ★ `variant_weights` 12행이 **각각 합 100**, 키가 `variants.json`의 id 안에 있음 |
| 5 | `boss_id`가 `enemies.json`에 **실제로 존재**하고 `is_boss`가 참 |
| 6 | `scale`이 1.00 → 5.00으로 **단조 증가** |
| 7 | 12지역 이름이 **「고룡의 둥지」** |
| 8 | `stages.json` **정확히 100개**, id 중복 0 |
| 9 | 지역별 스테이지 수 = **8·8·8·10·10·10·10·8·8·8·6·6**, `stage_count`와 일치 |
| 10 | ★ **각 지역의 마지막 스테이지만** `is_boss_stage` = true (총 12개) |
| 11 | ★ `subtitle`을 가진 스테이지가 **정확히 그 12개** |
| 12 | ★ **모든 스테이지의 `floors`가 10 이하**, 총합 **740** |
| 13 | `floors`가 지역 안에서 **감소하지 않는다** |
| 14 | `recommended_level`이 지역 안에서 **증가**하고, 지역 경계에서 끊기지 않는다 (1 → 99) |

---

# 작업 6 — ⚠ 기존 테스트 2건 손봐야 한다 (미리 허가한다)

**기획 창이 미리 확인했다. 아래 2건은 데이터를 옳게 옮겨도 반드시 걸린다.**
지시서 A 때처럼 뒤늦게 발견하지 않도록 먼저 적어 둔다.

## 6-A. `battle_test.dart` — 2페이즈 테스트가 **깨진다**

```dart
test('보스전에서 2페이즈 전환이 일어날 수 있다', () {
  final boss = enemies.firstWhere((e) => e.id == 'boss_archlich');
  ... playerHp: 200, ...
```

리치 HP가 90 → 330이 되면 **2페이즈 임계가 165**다. 초보 손패
(화염구 7 / 서리화살 6 / 마력방패 0)로 165를 깎으려면 25~30턴이 걸리는데,
새 공격력(14 / 강타 30)이면 **플레이어가 체력 200으로 10턴을 못 버틴다.**

- **허가**: `playerHp: 200` → **`playerHp: 2000`** 으로 올린다.
  이 값은 원래 주석이 "보스 후반까지 살아남도록 체력만 넉넉히"라고 밝힌
  **인위적 값**이므로 성격이 바뀌지 않는다
- 그래도 실패하면 `maxTurns`를 올리지 말고 **보고서에 적어라.** 기획 창이 판단한다

## 6-B. `battle_test.dart` — 테스트 **이름**이 낡는다

```dart
test('모든 적 30종과의 전투가 전부 정상 종료된다', () {
```

적이 39종이 되므로 **이름을 「39종」으로 고친다.** 반복문은 `enemies` 전체를 돌므로
로직은 그대로 두면 된다.

## 6-C. 그 밖에 기존 테스트를 고치지 마라

위 2건 외에 `flutter test`가 깨지면 **데이터를 잘못 옮긴 것이다.**
테스트를 고쳐서 통과시키지 말고 **데이터를 고쳐라.**

---

# 마무리

1. `flutter analyze` 경고 0
2. `flutter test` 전부 통과 (152개 + 신규 22개 안팎)
3. **`reports/09_데이터B_적지역스테이지.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - **보스 12종의 HP·공격·강타·2페이즈 임계를 표로 넣을 것**
   - **지역 12행의 tier 풀 합·변종 가중치 합·층수 합계를 표로 넣을 것**
   - 6-A의 `playerHp`를 얼마로 올렸고 그때 몇 턴에 2페이즈가 떴는지 적을 것
   - 옮기다 발견한 근거 문서의 오류가 있으면 **고치지 말고 보고서에 적어라**
4. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ 새 파일 3종을 읽는 **모델·로더·화면** (다음 지시서)
- ❌ 변종을 **런타임에 곱하는 코드** (다음 지시서 — 지금은 규칙 데이터만 만든다)
- ❌ 지역별 테마 **색조 값** 결정 (그래픽 작업)
- ❌ 난이도 3단계(보통·하드·데스) **배율** — 아직 정해지지 않았다 〔시뮬〕
- ❌ 새 효과 4종 구현 (지시서 C)
- ❌ 밸런스 수치 조정 — 승인된 보스 3종 외에는 전부 금지
