# 작업 지시서 — 이미지 에셋 파이프라인 (그래픽 창용)

> ## ⛔ 보류 중 (2026-09-01)
> 파이프라인 설계(96×96 격자·팔레트 양자화·굽기)는 **그대로 유효하다.**
> 다만 **원화가 바뀐다** — 노인이 아니라 **젊은 마법사**이고, 통짜가 아니라
> **몸 + 모자 + 로브 + 지팡이 4레이어**다(기획서 v2 5.5절).
> `art_src/wizard_front.png`(노인·통짜)는 **쓰지 않는다.**
> **새 원화가 들어오면 지시서를 갱신해 재개한다.**

발행: 2026-09-01, 기획 창. **사용자 승인 완료.**
선행: `reports/04_graphics_pilot.md`(손도트 파일럿 — 품질 미달로 방식 전환),
`ART_PIPELINE.md`(에셋 방침), `reports/img/05_grid_probe.png`(격자 실측 근거).

> **방식이 바뀌었다.** 캐릭터를 손으로 찍지 않는다. 사용자가 이미지 생성 모델로
> 원화를 받아 `art_src/`에 넣으면, **그것을 게임용 스프라이트로 변환하는
> 파이프라인을 만드는 것**이 이번 작업이다.

---

## 0. 왜 바뀌었나 (배경)

손도트 3안 파일럿(보고서 04) 결과, C안(전면 손도트)까지 갔는데도 모바일 RPG
수준에 못 미쳤다. 원인은 Flutter도 코드도 아니고, **도트를 보면서 고치는 반복을
할 수 없다는 것**이었다. 30종을 이 방식으로 끌면 품질도 어중간하고 비용이 최악이다.

사용자가 이미지 생성 모델로 마법사 원화를 받아왔고, **품질이 비교가 안 되게 좋다.**
그래서 **원화는 밖에서 받고, 우리는 그것을 게임에 맞게 변환·변형·고정한다.**

---

## 1. 확정 사양

| 항목 | 값 | 근거 |
|---|---|---|
| **스프라이트 격자** | **96 × 96** | 48은 얼굴이 죽고 64도 눈이 사라진다. 96부터 눈·눈썹·수염이 살아남는다 (`reports/img/05_grid_probe.png` 실측) |
| **캐릭터당 색 수** | **48~64색** | 195,011색 원본을 64색으로 줄여도 그림이 안 죽는 것을 확인했다 |
| **게임 전체 공용 팔레트** | **192색 내외** | 모든 캐릭터가 같은 팔레트에서 색을 뽑아야 30종이 한 화풍으로 묶인다 |
| **저장 형식** | PNG (RGBA, 투명 배경) | 96×96 PNG 한 장 ≈ 3~6KB. 30종 ≈ 150KB |
| **원화 입력 위치** | `art_src/<이름>.png` | 사용자가 채운다 |
| **결과물 위치** | `assets/sprites/<이름>.png` | pubspec에 등록 |

**화면 표시는 정수 배율로만 한다** (1× 또는 2×). 1.3배 같은 값을 쓰면 도트가
불규칙하게 뭉개져 픽셀아트로 안 보인다. 지금 화면의 실제 표시 크기를 재서
가장 가까운 정수 배율에 맞춰라. **맞추느라 레이아웃이 조금 바뀌는 건 허용한다.**

---

## 2. 파이프라인 (`tool/` 에 CLI 도구로 만든다)

`dart run tool/import_sprite.dart <입력> <출력이름>` 형태. **Flutter import 금지**
(CLAUDE.md 규칙 — `tool/`은 순수 Dart여야 `dart run`이 동작한다).
Dart로 이미지 처리가 어려우면 `tool/` 안에 파이썬 스크립트를 두어도 좋다.
이 PC에 Pillow가 설치돼 있는 것을 확인했다.

### 단계

1. **배경 제거** — 마젠타(#FF00FF 계열)를 투명으로. **단색이 아니라 노이즈가
   있다**(실측: 배경만 색이 수천 가지). 정확히 비교하지 말고 **색상 범위로 판정**하라
   (R>190, B>190, G<110 정도).
2. **여백 잘라내기 + 정사각 정렬** — `getbbox()`로 자르고, 가로세로 중 긴 쪽에
   맞춰 정사각 캔버스에 담되 **발이 아래 변에 닿게** 배치한다 (캐릭터마다 키가
   달라도 바닥선이 맞는다).
3. **96×96으로 축소** — `BOX`(면적 평균). `NEAREST`로 줄이면 얇은 선이 통째로 사라진다.
4. **알파 이진화** — 축소하면 가장자리에 반투명이 생긴다. 128 기준으로 0 아니면 255로.
   **이걸 안 하면 도트 가장자리가 흐려져 픽셀아트가 아니게 된다.**
5. **공용 팔레트로 양자화** — 3절 참조.
6. **외곽선 정리** — 실루엣 바깥 한 겹을 **그 자리 색의 가장 어두운 단계**로
   덮는다 (검정 아님 — `ART_PIPELINE.md` 3절 규칙).
7. **저장 + 해시 기록** — `assets/sprites/<이름>.png` 와 체크섬.

### 결과 확인

`test_shots/`를 확장해 **변환 전/후 + 실제 전투 화면에 올린 모습**을 480px 시트
한 장으로 뽑는다. 손으로 캡처하지 마라.

---

## 3. 공용 팔레트 — 이게 화풍 통일의 핵심이다

생성 모델은 캐릭터마다 조금씩 다른 그림체·색감을 낸다. **모든 캐릭터를 하나의
공용 팔레트로 양자화하면 색이 통일되어 한 게임처럼 보인다.**

- 지금은 원화가 마법사 하나뿐이다. **팔레트를 확정하지 말고, 확장 가능하게 만들어라.**
  - `assets/sprites/palette.json` 에 색 목록을 저장한다
  - 새 원화가 들어오면 팔레트에 **없는 색만 추가**하고, 있는 색은 재사용한다
  - 상한 192색에 도달하면 가장 가까운 색으로 흡수한다
- 팔레트에는 **작업 1에서 확정한 UI 색**(`kWizardTint` 청록, `kGold` 등)을 미리 넣어라.
  캐릭터 색이 UI 색과 같은 계열로 수렴한다.

---

## 4. 변형 — 원화 한 장에서 코드로 파생시킬 것

**받은 원화를 그대로 쓰기만 하는 게 아니다.** 아래는 전부 코드로 만든다.
이게 생성 횟수를 줄이는 핵심이다.

| 변형 | 방법 | 쓰이는 곳 |
|---|---|---|
| **피격** | 흰색 물들임 + 세로 찌그러짐 | 이미 `flashAmount`가 있다 |
| **시전** | 앞으로 기울임 | 이미 있다 |
| **숨쉬기** | 상하 미세 이동 | 이미 있다 |
| **보스 크기** | 1.5배 (정수 배율 주의) | 보스 4종 |
| **좌우 반전** | 수평 뒤집기 | 필요 시 |
| **색조 변형** | 팔레트 색상만 회전 | **같은 몬스터의 다른 색 변종** — 생성 횟수를 크게 아낀다 |

**주의**: 색조 변형으로 만든 변종에 **청록을 쓰지 마라.** 청록은 마법사 전용이다
(검토 03 확정 규칙).

---

## 5. 굽기·고정은 그대로 간다

방식이 PNG로 바뀌어도 사용자가 걱정한 **"그릴 때마다 달라진다"**는 그대로 막는다.

1. `assets/sprites/*.png` 가 곧 구워진 결과물이다. 런타임 생성이 없다.
2. **`test/sprite_test.dart`를 PNG 체크섬 검사로 교체한다.** 파일이 한 바이트라도
   바뀌면 테스트가 빨간불. 의도적으로 바꿀 때만 체크섬을 갱신한다.
3. 승인된 그림 전체를 `docs/sprites/sheet_v1.png`로 남긴다.

---

## 6. 기존 코드는 어떻게 되나

- `art/pixel_sprite.dart`의 도트 문자열 렌더러는 **당장 지우지 마라.**
  주문·유물 아이콘 17종이 아직 그걸 쓴다. 캐릭터만 PNG로 옮긴다.
- `art/sprite_map.dart`의 적↔스프라이트 매핑은 PNG 이름을 가리키게 바꾼다.
- **성능은 좋아진다.** 지금은 스프라이트 하나에 `drawRect`를 2,000번 가까이
  호출하고 매 프레임 다시 그린다. PNG는 `drawImageRect` 한 번이다.
- `CLAUDE.md` 폴더 구조 설명의 *"그림은 전부 코드로 그린다(외부 이미지 없음)"*가
  낡았다. **기획 창이 갱신한다. 건드리지 마라.**

## 7. 지금 할 일 / 하지 말 것

**이번 작업의 범위는 「마법사 정면 1종을 끝까지 통과시키는 것」이다.**
원화가 하나뿐이므로 30종 작업은 아직 못 한다.

- ✅ 파이프라인 도구 제작 + 마법사 1종 변환 + 팔레트 파일 생성
- ✅ 게임 화면에 실제로 올려 보고 시트 뽑기 (변환 전/후 + 전투 화면)
- ✅ 표시 배율을 정수로 맞추기
- ❌ 나머지 29종 — 원화가 없다. 사용자가 생성 중이다
- ❌ 손도트 스프라이트 삭제 — 원화가 다 들어올 때까지 그대로 둔다
- ❌ 작업 1의 수정 2건(기호 충돌·결과 화면 숫자 색) — 이 다음에 한다

`flutter test` 139개 통과 + `flutter analyze` 경고 0 유지.
보고는 `reports/05_art_pipeline.md`.
