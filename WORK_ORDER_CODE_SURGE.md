# 작업 지시서 C — 폭주 효과 4종 구현 + 팝업 6갈래 (구현 창용)

발행: 2026-09-02, 기획 창. **사용자 승인 완료된 작업이다.**

근거 문서: **`SURGE_DESIGN.md`** (1절 화면 규칙 · 2절 6갈래 · 4절 효과 규약)

> **읽는 순서**: 이 문서 → `SURGE_DESIGN.md` 1·2·4절 → 작업 시작.

> ### 🪟 창 운영
> **새 창(`/clear` 후)에서 시작한다.**
> 지시서 A·B와 달리 **`lib/` 코드를 고치고 화면을 확인하는 작업**이다.
> 팝업 확인용 스크린샷은 **가로 480px로 줄인 사본 1~2장만** 본다
> (`CLAUDE.md` 토큰 절약). 다 끝나면 창을 놓아준다.

---

## 0. 이 작업의 목적 (한 줄)

**데이터에는 6갈래 폭주 80종이 들어갔는데 코드가 새 효과 4종을 몰라서,
지금 역류 20종·연쇄 9종·오발 일부가 조용히 아무 일도 안 하고 있다.** 그걸 메운다.

이게 끝나야 **시뮬레이션 수치가 의미를 갖는다.**

## 0-1. 절대 지킬 것

- **`core/check.dart`·`core/combo.dart`를 고치지 마라.** 3.4절 확률표를 검증하는
  테스트 37개가 걸려 있다. 이 작업은 **판정이 아니라 판정 이후**만 건드린다
- **매직 넘버 금지.** 전부 `core/constants.dart`에 상수로 (0.4 · 4 · 1.2 등)
- 파일 300줄 / 함수 40줄 규칙. `core/surge.dart`는 지금 108줄이라 여유가 있지만
  **넘으면 분리**한다
- 주석은 한국어. `debugPrint()` 사용 (`print()` 금지)
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것

---

# 작업 1 — 새 효과 4종 구현

전부 `lib/core/surge.dart`의 `_applyEffect` **`switch`에 case를 더하는 일**이다.
기존 10종은 손대지 않는다.

## 1-A. `shield` — 가장 쉽다

```dart
case 'shield':
  b.shield += e.value;
```

`Battle.shield`가 이미 있고 `dealToPlayer`가 먼저 흡수한다. `_applySpellEffect`의
`'shield'` case와 똑같이 하면 된다.

## 1-B. `enemy_heal`

적을 `e.value` 만큼 회복시킨다. **`enemy.hp`(최대치)를 넘지 않는다.**

```dart
case 'enemy_heal':
  b.enemyHp = min(b.enemy.hp, b.enemyHp + e.value);
```

## 1-C. ★ `self_damage_spell` — 역류의 정체성

`SURGE_DESIGN.md` **4절 「역류·자해 계산 규칙」 4가지를 전부** 지켜야 한다.

```
기준 위력 = max(spellPower(spell), kBacklashMinPower)   // 상수 4
피해      = (기준 위력 × e.value / 100) 반올림
```

- ⚠ **시전 강도 배율(`it.power`)을 곱하지 마라.** 곱하면 전력 실패가 곧 자살이 되어
  아무도 전력을 안 쓴다. **이 지시서에서 가장 틀리기 쉬운 곳이다**
- ⚠ **`kBacklashMinPower`(4)가 필요한 이유**: `mana_shield`·`healing_herb`는
  `base_damage`가 0이라 그대로 계산하면 역류 피해가 0이다.
  보조 주문이 "실패해도 안전한" 지배 전략이 된다

## 1-D. ★ `chain_cast` — 연쇄

**손패의 다른 주문 |N|개**가 터진다. 양수면 적에게, **음수면 나에게.**

- `Battle._autoCastOther`(스트레이트용)와 **로직이 거의 같다.** 재사용하거나
  공통부를 뽑아 쓰되, 아래 차이를 지킬 것
- **시전 강도 배율을 곱하지 않는다** (1-C와 같은 이유). `spellPower(other)` 그대로
- 시전한 주문 자신과 봉인된 주문은 **제외**한다 (`castableIndexes` 활용)
- **서로 다른 주문을 |N|개** 고른다. 손패가 모자라면 있는 만큼만
- **양수일 때만** 그 주문의 부가 효과를 적용한다. 나에게 터지는데
  적을 지연시키면 말이 안 된다
- ⚠ **연쇄가 또 폭주를 부르지 않는다.** 판정을 다시 하지 않으므로 구조상 안전하지만,
  구현하다 `castSpell`을 부르지 않도록 주의할 것

## 1-E. ★ 자해 피해 상한 — 합산해서 자른다

`SURGE_DESIGN.md` 4절 2번.

> **한 번의 폭주에서 발생한 자해 피해의 「총합」이 최대 체력의 40%를 넘지 않는다.**
> (`self_damage_spell` + `self_damage` + 음수 `chain_cast` 를 **합산한 뒤** 자른다)

- **개별 효과마다 자르면 안 된다.** 효과가 둘 붙은 폭주에서 상한이 무의미해진다
- 자른 **뒤에** `dealToPlayer`를 부른다 (방어막은 그다음에 흡수)
- 구현: `applySurge`가 효과들을 훑어 **자해 합계를 먼저 구하고**, 상한을 적용한 뒤
  한 번에 입히는 방식이 가장 단순하다
- 상수 `kSurgeSelfDamageMaxRatio = 0.4`

---

# 작업 2 — 폭주 팝업을 6갈래로

## 2-A. 색상표 교체 — `lib/widgets/surge_popup.dart`

지금 `_categoryColors`가 **옛 5종**(`self_harm`/`reversal`/`summon`/`gamble`/`boon`)
기준이라 새 6갈래가 전부 기본색으로 뜬다.

| `category` | 갈래 | 색 (`pixel_ui.dart`의 기존 상수를 우선 쓸 것) |
|---|---|---|
| `backlash` | **역류** | 붉은 계열 (`kHpRed`) |
| `misfire` | **오발** | 보라 계열 |
| `fizzle` | **불발** | 무채·회색 계열 |
| `amplify` | **증폭** | 주황·금 계열 (`kGold`) |
| `summon` | **소환** | 초록 계열 (`kHpGreen`) |
| `chain` | **연쇄** | 파랑 계열 (`kManaBlue`) |

- 없는 색은 `pixel_ui.dart`에 상수로 추가한다. **팔레트 밖의 색을 쓰지 마라**
- **불발이 무채색인 것이 중요하다.** 「아무 일도 없었다」가 색으로도 읽혀야 한다

## 2-B. ★ 레이아웃 — 갈래(큰 글씨) → 숫자 → 한 줄

`SURGE_DESIGN.md` 1절이 정한 순서다. 지금은 이름이 크고 문장이 작다.

```
[아주 크게]   역류!          ← 갈래 이름 (6종 중 하나)
[크게]        −14            ← 결과 숫자
[작게]        마력이 거슬러 오른다   ← surge.text
[아주 작게]   지팡이 역류      ← surge.name (부제)
```

- **큰 글씨는 갈래**다. `surge.name`이 아니다
- 갈래 한글 이름(역류/오발/불발/증폭/소환/연쇄)은 **화면 문자열**이므로
  `category` 값과 함께 매핑 표로 둔다

## 2-C. 숫자를 만들려면 결과를 기록해야 한다

**지금 팝업은 숫자를 모른다.** `SurgeEvent`에는 효과 정의만 있고
"실제로 몇 대미지가 들어갔나"가 없다.

- `SurgeSystem`에 **`String? lastSurgeSummary`** 를 만든다
- `applySurge`가 효과를 적용하면서 결과를 문자열로 모은다
  - 예: `−14` · `마나 +2` · `×3` · `방어막 6` · `적 +8` · 불발이면 **빈 문자열**
  - 효과가 둘이면 공백으로 잇는다 — `−10  마나 −1`
- `battle_controller.dart`가 `lastSurge`를 넘기듯 이 값도 넘긴다
- **불발은 숫자 칸이 비는 것이 정상이다.** 「−」 같은 것을 억지로 넣지 마라

## 2-D. 낡은 주석 하나

`lib/models/surge_event.dart` 21행:

```dart
/// 유형: self_harm / reversal / summon / gamble / boon (통계·디버깅용)
```

→ **6갈래(`backlash`/`misfire`/`fizzle`/`amplify`/`summon`/`chain`)로 고친다.**
주석뿐이지만 다음 사람이 오해한다.

---

# 작업 3 — 테스트

## 3-A. `test/surge_effect_test.dart` (신규)

**결정적으로 검사할 것.** 폭주를 추첨에 맡기지 말고 `SurgeEvent`를 손으로 만들어
`applySurge`에 태운다 (`run_test.dart` 132행의 방식이 좋은 본보기다).

| # | 검사 |
|---|---|
| 1 | `shield`: 방어막이 오르고, 다음 피해를 먼저 흡수한다 |
| 2 | `enemy_heal`: 적 체력이 오르되 **`enemy.hp`를 넘지 않는다** |
| 3 | ★ `self_damage_spell 100`: 화염구(위력 7)로 **정확히 7** 피해 |
| 4 | ★ **강도 배율이 곱해지지 않는다** — 「보통」과 「전력」에서 자해가 **같다** |
| 5 | ★ 위력 0 주문(`mana_shield`)에서 **최소 기준값 4**가 적용된다 |
| 6 | ★ **자해 상한** — 자해 효과를 여러 개 단 폭주에서 총합이 최대 체력의 40%로 잘린다 |
| 7 | `chain_cast 1`: 손패의 **다른** 주문 1개가 적에게 터진다 (자기 자신 아님) |
| 8 | `chain_cast -1`: 그 피해가 **나에게** 들어오고, 그 주문의 부가 효과는 발동하지 않는다 |
| 9 | `chain_cast 2`: **서로 다른** 주문 2개가 터진다 |
| 10 | 봉인된 주문은 연쇄에 뽑히지 않는다 |
| 11 | `fizzle`(효과 없음): 체력·마나·적 체력이 **하나도 안 변한다** |
| 12 | `lastSurgeSummary`가 갈래별로 기대한 문자열을 만든다 (불발은 빈 문자열) |

## 3-B. ⚠ 기존 테스트 — 미리 조사했다

**기획 창이 `test/` 전체를 훑었다.** 아래 2건 외에는 걸릴 것이 없다.

### 3-B-1. `test/run_test.dart` 132행 — 고쳐도 된다 (허가)

```dart
category: 'boon',          // ← 옛 갈래 이름
effects: const [GameEffect(type: 'heal', value: 0)],
```

손으로 만든 시험용 폭주라 데이터 파일과 무관해 **깨지지는 않는다.** 다만 옛 이름이라
헷갈린다. **`category: 'fizzle'` + `effects: const []`로 바꿔도 좋다.**
(불발이 "마나에 손대지 않는 무해한 폭주"라는 그 테스트의 의도에 더 정확히 맞는다)

### 3-B-2. `test/battle_test.dart` 249행 — 깨지지는 않지만 확인할 것

```dart
test('실패(뱀눈): 주문 미발동 + 폭주 + 마력 축적 +2', () {
  ...
  expect(b.enemyHp <= hpBefore, isTrue);
```

**`enemy_heal`이 구현되면 폭주가 적 체력을 올릴 수 있다.** 이 테스트는 전투 첫 시전이라
적이 최대 체력이고 `enemy_heal`은 최대치에서 잘리므로 **통과한다.**
그래도 **실제로 통과하는지 확인**하고, 만약 깨지면 **테스트를 고치지 말고 보고하라.**
1-B의 상한 처리가 빠졌다는 뜻이다.

### 3-B-3. 그 밖의 기존 테스트를 고치지 마라

`flutter test`가 깨지면 **구현이 틀린 것이다.** 특히 `check_test`·`combo_test`가
깨지면 판정 엔진을 건드린 것이므로 되돌려라.

---

# 마무리

1. `flutter analyze` 경고 0
2. `flutter test` 전부 통과 (175개 + 신규 12개 안팎)
3. **화면 확인** — 전투를 돌려 폭주 팝업을 실제로 띄운다.
   **가로 480px로 줄인 스크린샷 1~2장**만 보고 판단한다.
   갈래가 크게, 숫자가 그다음, 문장이 작게 나오는지만 확인하면 된다
4. **`reports/10_코드폭주_효과팝업.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - 3-A의 3·4·5·6번(역류 계산) 결과를 **수치 표로** 넣을 것
   - 팝업 스크린샷 1장을 **경로로** 적을 것 (보고서에 이미지를 붙이지 마라)
   - 근거 문서 오류를 발견하면 **고치지 말고 적어라**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ `Region`·`Stage`·`EnemyVariant` 모델과 로더 (지시서 D)
- ❌ 변종을 런타임에 곱하는 코드 (지시서 D)
- ❌ `run_controller`의 「10층 단선」 구조 교체 (지시서 D)
- ❌ `Spell`에 `circle` 필드 읽히기 (지시서 D)
- ❌ 판정 엔진(`check.dart`·`combo.dart`) 수정
- ❌ 밸런스 수치 조정 — 이 지시서에 승인된 수치 변경은 **없다**
