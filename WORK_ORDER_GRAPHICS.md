# 작업 지시서 — 그래픽 개선 (구현 창용)

> ## ⛔ 보류 중 (2026-09-01)
> **기획서가 v2로 전면 개정됐다.** 작업 1(팔레트·폰트)은 완료·승인됐고(보고서 03),
> **작업 2(캐릭터)는 방식이 통째로 바뀌었다** — 손도트가 아니라 외부 원화를
> 변환해 쓴다. 캐릭터도 노인이 아니라 **젊은 마법사 + 장비 4레이어**다.
> 작업 3·4도 v2 기준으로 다시 써야 한다.
> **새 지시서가 나올 때까지 이 문서로 작업하지 마라.**

발행: 2026-09-01, 기획 창. **사용자 승인 완료된 작업이다.**
근거 문서: `ART_PIPELINE.md`(에셋 방침·제작 파이프라인), `GRAPHICS_PLAN.md`(5단계 계획),
`refs/DICERO_ANALYSIS.md`(화면·연출 분석).

> **읽는 순서**: 이 문서 → `ART_PIPELINE.md` → 작업 시작.
> `refs/dicero*/` 스크린샷 200장은 **한 번에 열지 마라.** 필요한 것만 골라 연다.

---

## 0. 이 작업의 목적 (한 줄)

**"조악하다"는 인상을 없앤다.** 원인은 해상도가 아니라 ①칙칙한 색 ②성격 없는 캐릭터다.

## 0-1. 밸런스 창과 파일이 겹친다 — 반드시 읽을 것

밸런스 작업(`WORK_ORDER_BALANCE.md`)과 **아래 파일이 겹친다.**

| 파일 | 밸런스 창이 하는 일 | 그래픽 창이 하는 일 |
|---|---|---|
| `widgets/dice_widget.dart` | 각인 주사위 테두리 색 | 주사위 그림 손질 |
| `widgets/battle_fx.dart` | 확정 굴림 연출 | 연출 강화 전반 |
| `widgets/shop_dialog.dart` | 상점 상품 4종 | 상점 화면 디자인 |

**규칙**:
1. **한 번에 한 창만 작업한다.** 동시에 돌리지 않는다.
2. **작업을 시작할 때 위 3개 파일은 기억에 의존하지 말고 반드시 다시 읽는다.**
   다른 창이 그 사이 고쳤을 수 있다.
3. 밸런스 창의 **작업 4·5(확정 굴림·각인 주사위)는 이 지시서가 끝난 뒤에** 한다.
   순서를 지키면 겹침이 사실상 사라진다.
4. **밸런스 창이 작업 3(상점 경제)을 아직 안 했다.** 상품 4종이 곧 들어온다.
   그래픽 작업 1에서 `widgets/shop_dialog.dart`는 **팔레트 상수만 갈아끼우고
   구조·배치는 건드리지 마라.** 상점 화면 디자인은 상품이 들어온 뒤(작업 3)에 한다.

## 0-2. 절대 지킬 것

- **게임 규칙·수치를 건드리지 마라.** 이 지시서는 그림만 다룬다 (절대 규칙 3).
- 외부 에셋은 **CC0 / CC BY / OFL만**, 도입하면 `ATTRIBUTION.md`에 기록 (절대 규칙 6).
  **캐릭터·몬스터는 직접 그린다. CC0는 참고용으로만 본다** (사용자 결정).
- 상용 게임(딸깍 다이스 포함)의 그림·폰트를 추출하거나 베끼지 않는다.
- `flutter analyze` 경고 0, `flutter test` 95개 전부 통과 상태로 끝낼 것.
- 화면은 고정 높이 대신 `Expanded` + `LayoutBuilder` (600px 기기에서 넘친다).

## 0-3. 토큰 관리 (실제로 한도를 태운 적 있다)

- 스크린샷은 **가로 480px로 축소한 사본만** 본다. 원본(1080폭)은 4배 비싸다.
- **여러 장을 볼 땐 한 장의 시트로 합쳐서** 본다. 낱장으로 여러 번 열지 마라.
- 한 작업이 끝날 때마다 `PROGRESS.md`를 갱신하고 **창을 초기화**해도 좋다.
  문서만 읽으면 맥락이 복원되게 기록해둘 것.

---

# 작업 1 — 아트 디렉션 교체 + 폰트 (효과 최대 / 비용 최소)

## 1-0. 실제 화면에서 확인된 문제 (2026-09-01, `reports/img/02_weak_fix.png`)

기획 창이 실제 전투 화면을 눈으로 확인하고 뽑은 목록이다. **이걸 없애는 게 작업 1이다.**

1. **마법사가 배경에 묻힌다** ← 가장 심각.
   보라 로브 + 보라 배경이라 실루엣이 안 떨어진다. 주인공이 안 보인다.
2. **패널 테두리가 배경과 거의 안 갈라진다.** `kBorderDim`이 바탕과 너무 붙어 있다.
3. **적이 갈색 덩어리로 읽힌다.** 배경 산맥과 명도가 비슷해 형태가 안 잡힌다.
4. **화면에서 가장 튀는 게 손패 카드다.** 속성색(주황·파랑·보라)이 제일 채도가 높아
   시선이 카드로 간다. **주인공이어야 할 주사위보다 카드가 튄다** —
   기획서 9.5절 1번("주사위가 화면의 주인공")에 어긋난다.
5. **글씨가 시스템 폰트**라 픽셀 그림과 따로 논다 (1-B에서 해결).

> 잘 되어 있는 것: **주사위는 흰색이라 잘 보인다.** 이 대비는 유지하라.

## 1-A. 팔레트

**위치**: `lib/widgets/pixel_ui.dart` 상단 색 상수 + `lib/art/stage_backdrop.dart` 층 테마

### 방향 — "전체를 밝게"가 아니라 "명도 대비를 벌린다"

`GRAPHICS_PLAN.md`는 배경을 밝은 청회색~흰색으로 바꾸라고 적었지만, **전투 화면까지
밝게 만들면 안 된다.** 이 게임의 핵심 연출은 **발광**(마법 이펙트·폭주·대성공 플래시)이고,
빛은 어두운 바탕에서만 빛난다. 밝은 배경으로 통일하면 이펙트가 통째로 죽는다.

따라서 이렇게 나눈다:

| 영역 | 방향 |
|---|---|
| UI 패널·보상·지도·타이틀·메타 | **밝게** — 여기가 "칙칙하다"의 주범이다 |
| 전투 무대 **위쪽**(먼 곳) | 중간 명도로 **올린다** — 적 실루엣이 뜬다 |
| 전투 무대 **발밑**(가까운 곳) | 어둡게 **유지** — 이펙트가 빛날 자리 |

### 시작값 (여기서 출발해 눈으로 조정하라 — 확정값이 아니다)

```
kBgDeep     0xFF1A1526 → 0xFF232042   바탕. 보라 단일톤에서 벗어나 푸른기를 넣는다
kBgPanel    0xFF2E2745 → 0xFF3A3560
kBgPanelLit 0xFF423761 → 0xFF524B84
kBorderDim  0xFF564776 → 0xFF6E68A8   지금은 바탕과 너무 붙어 테두리가 안 보인다
kTextDim    0xFFA79CC0 → 0xFFB8B0D8   지금 설명 글씨가 묻힌다
kTextMain   0xFFF2EEFA               (유지)
```

**강조색은 3개로 제한한다** (지금은 색이 너무 많아 눈이 쉴 곳이 없다):
- 금 `0xFFF2C14E` (유지) — 선택·중요
- 빨강 `0xFFE8546B` (유지) — 체력·피해
- **청록 `0xFF4FD1C5` (신규)** — 마나·마력 축적

> `kChargePurple 0xFFC77DE8`은 배경이 보라라서 묻힌다. **청록으로 교체를 검토하라.**
> 바꾸면 마력 축적이 한눈에 들어온다.

### 규칙

- **명도 5단계가 확실히 벌어져야 한다.** 인접한 두 색의 밝기 차이가 애매하면 둘 중
  하나를 없애라. 지금 문제는 색이 없어서가 아니라 **전부 비슷한 밝기**여서다.
- 층 테마 3종(얼음/비전/화염)은 **채도를 한 단계씩 올린다.**
- 팔레트 밖의 색을 코드 여기저기에 직접 쓰지 마라. 전부 `pixel_ui.dart` 상수로.

### 검증

타이틀·지도·전투·보상·결과 5개 화면을 캡처해 **한 장의 시트로 합쳐** 사용자에게 보인다.
개정 전/후를 나란히 놓을 것.

## 1-B. 한글 픽셀 폰트 도입 ★ 비용 대비 체감이 가장 큰 항목

지금은 시스템 기본 폰트라 둥글고 흐려서, 픽셀 그림과 따로 논다.

- **후보**: **Galmuri (갈무리)** — OFL 1.1. 한글 픽셀 폰트로 가장 확실하다.
  대안: 둥근모꼴 / Neo둥근모 (자유 이용이나 조건이 배포처마다 다르다).
- **받기 전에 라이선스 페이지를 눈으로 확인할 것.** 확인이 안 되면 받지 말고 사용자에게 묻는다.
- `pubspec.yaml`에 폰트 등록 → `main.dart` 테마의 `fontFamily`로 전역 적용.
- **`ATTRIBUTION.md`에 출처·작가·라이선스·받은 날짜를 기록한다.**

### 픽셀 폰트를 쓸 때 주의 (이걸 놓치면 오히려 더 지저분해진다)

- 픽셀 폰트는 **정수 배율에서만 또렷하다.** 폰트의 기준 픽셀 크기가 11이면
  `fontSize`를 11, 22, 33으로 쓴다. 13, 18 같은 어중간한 값은 글자가 뭉갠다.
  **지금 코드 곳곳의 fontSize를 전부 정수 배율로 정리해야 한다.**
- 결과 화면의 거대 숫자(84pt)처럼 큰 글씨는 큰 사이즈 폰트를 따로 쓰거나
  정수 배율로 키운다.

---

# 작업 2 — 캐릭터 재작업 + 굽기 (가장 오래 걸린다)

**반드시 `ART_PIPELINE.md` 1절·3절을 읽고 시작하라.** 여기엔 요약만 적는다.

## 2-0. 먼저 파일럿 — 캐릭터 **한 장**만 그린다 (2026-09-01 사용자 지시)

30종에 착수하기 전에 **도달 가능한 품질의 상한**을 확인한다.
`GRAPHICS_PLAN.md` 0절이 *"캐릭터 원화 품질은 손으로 도트를 찍는 방식으로는
한계가 뚜렷하다"*고 적었는데, 그 한계가 실제로 어디인지 한 장으로 확인하는 것이다.

### 대상

**마법사 정면 48×48 한 캐릭터.** 다른 캐릭터는 그리지 않는다.

이유: 가장 오래 화면에 보이고, 색(`kWizardTint` 청록)이 확정됐고,
얼굴·손·지팡이처럼 **작은 요소가 뭉개지는지**가 가장 잘 드러난다.

### 3안을 만들어 나란히 보인다

| 안 | 방식 |
|---|---|
| **A. 기준선** | 지금 방식 그대로 — `SpriteBuilder`의 자동 명암만. 비교 기준이므로 새로 그리지 않고 현재 마법사를 그대로 쓴다 |
| **B. 혼합** | `ART_PIPELINE.md` 3절 방식 — 자동 명암으로 부피를 잡고 **특징을 손으로 덧찍는다** |
| **C. 전면 손도트 + 현대적 채색** | 처음부터 손으로 찍는다. **색 수를 늘리고**(캐릭터 하나에 8~16색) **제한적 안티앨리어싱**을 쓴다 |

> **제한적 안티앨리어싱이란**: 렌더러의 부드럽게 하기 기능을 켜는 게 아니라,
> **경계선 픽셀에 중간색을 손으로 한 칸 넣는** 기법이다(selective AA).
> 도트의 선명함은 유지하면서 계단현상만 완화한다. 렌더러 설정은 그대로 두어라.

### 시트 구성 (가로 480px 한 장)

3안 × 3열로 배치한다.

| | ① 실제 게임 크기 | ② 4배 확대 | ③ 전투 배경 위 |
|---|---|---|---|
| A | | | |
| B | | | |
| C | | | |

- **②(4배 확대)가 없으면 도트 품질을 판단할 수 없다.** 반드시 넣어라.
- **③(배경 위)이 없으면 게임에서 사는지 알 수 없다.** 단독으로 예쁜 것과
  복잡한 배경 위에서 실루엣이 뜨는 것은 다른 문제다.
- `test_shots/` 캡처 도구를 확장해 만든다. 손으로 찍지 마라.

### 이 단계에서 하지 않을 것

- **굽기 구조(2-A)를 아직 만들지 마라.** 파일럿은 버릴 수 있어야 한다.
  안이 정해진 다음에 굽는다.
- 다른 캐릭터·적을 그리지 마라.
- 스프라이트 데이터를 기존 파일에 덮어쓰지 마라. 파일럿용 임시 파일에 둔다.

### 렌더러 제약이 걸리면 그것도 보고하라

`art/pixel_sprite.dart`는 도트를 팔레트 글자(명암 단계)로 표현한다.
**안 C처럼 색을 8~16색으로 늘리려면 렌더러를 확장해야 할 수 있다.**
막히면 임의로 크게 뜯어고치지 말고, **무엇이 막았고 고치면 얼마나 드는지**를
보고서 7절에 적어라. 그것도 판단 재료다.

### 판정 기준 (사용자가 볼 것)

1. 실루엣이 한눈에 읽히는가 (흑백으로 봐도 마법사인가)
2. 4배 확대에서 도트가 선명한가, 뭉개진 곳은 없는가
3. 얼굴·손·지팡이 보석 같은 작은 요소가 살아 있는가
4. 전투 배경 위에서 뜨는가
5. **모바일 RPG 픽셀아트로 내놓을 수 있는 수준인가**

### 결과에 따른 분기 — 이게 파일럿의 목적이다

| 결과 | 다음 |
|---|---|
| **통과** | 채택된 안의 방식으로 2-A(굽기) → 30종 전체 진행 |
| **미달** | **CC0 캐릭터 팩을 베이스로 변형하는 방식으로 전환**을 검토한다. `ART_PIPELINE.md` 2절의 "캐릭터는 직접 그린다"를 사용자와 다시 상의한다. 절대 규칙 6은 이미 완화돼 있어 CC0 도입 자체는 가능하다 |

**파일럿 결과를 보고하고 반드시 멈춘다.** 통과 여부는 사용자가 정한다.

## 2-A. 먼저 굽기 구조부터 만든다 (그림보다 이게 먼저다)

지금 캐릭터는 `art/sprite_builder.dart`가 **실행할 때마다 계산해서** 그린다.
게임 실행 중엔 안 변하지만, **다음에 생성 코드를 고치면 손대지 않은 캐릭터까지
통째로 바뀐다.** 사용자가 가장 걱정하는 지점이므로 구조로 막는다.

1. **굽기(bake)**: 완성된 스프라이트의 픽셀을 도트 문자열로 뽑아
   `lib/art/sprites_baked_characters.dart` 등에 박아둔다. 게임은 이 파일만 읽는다.
   `sprite_builder.dart`는 "새 캐릭터를 만들 때 쓰는 도구"로 역할이 바뀐다.
2. **해시 잠금**: 스프라이트마다 내용 해시를 저장하고 `test/sprite_test.dart`가 검사한다.
   한 픽셀이라도 바뀌면 테스트가 빨간불. 의도적으로 바꿀 때만 해시를 갱신한다.
3. **대장(臺帳)**: 승인된 그림 전체를 `docs/sprites/sheet_v1.png`(가로 480px)로 저장.

> **굽기 구조를 먼저 만들고, 그 위에서 캐릭터를 그려라.** 순서가 반대면 다시 굽는
> 작업을 30번 하게 된다.

## 2-B. 그리는 순서

**마법사(앞·뒤) → 보스 4종 → 일반 적 26종**. 가장 오래 보이는 것부터.

**5~6종 그릴 때마다 시트를 만들어 사용자에게 보인다.**
30종을 다 그린 뒤에 화풍이 어긋나 있으면 전부 다시 그려야 한다.

## 2-C. 전 캐릭터 공통 규칙 — 이게 "우리 화풍"이 된다

- **마법사**: 어깨 폭 > 모자챙 폭 (실루엣이 뭉치지 않게). 지팡이 보석은 항상 발광.
- **적에게 청록을 쓰지 않는다** (2026-09-01 확정). 마법사 로브가
  `kWizardTint 0xFF45C9C0` 청록으로 확정됐으므로, 청록을 마법사 전용으로 비워두면
  **화면에 청록이 보이는 순간 그게 나라는 게 즉시 읽힌다.** 색이 정체성이 된다.
- **모든 적**: 눈이 크고, **포인트색을 딱 하나만** 가진다. 나머지는 무채색에 가깝게.
  적마다 **한눈에 각인되는 특징 하나**를 정한다
  (슬라임=지나치게 큰 눈 / 해골=빈 눈구멍 속 불씨 / 로브=후드 안의 빛 두 점 / 용=뿔+이빨).
- **보스**: 일반 적의 1.5배 이상 + 실루엣에 **비대칭 요소**(한쪽 뿔, 접힌 날개 하나).
- **외곽선**: 검정이 아니라 **그 캐릭터 색의 가장 어두운 단계**를 쓴다.
- 작업 1에서 확정한 팔레트 **밖으로 나가지 않는다.**
- **실루엣을 흑백으로 먼저 확인한다.** 검은 덩어리 상태에서 뭔지 알아보이지 않으면
  색을 칠해도 실패다. 여기서 걸러라.

## 2-D. 피격/시전 변형

지금은 색만 번쩍인다. **형태가 변하는 프레임**을 추가한다
(피격 = 뒤로 젖혀지며 찌그러짐 / 시전 = 앞으로 내밀며 늘어남).

## 테스트

- [ ] 기존 검사(줄 길이·크기·색 글자) 유지
- [ ] **해시 고정 검사 추가** ← 핵심
- [ ] 실루엣이 캔버스 밖으로 나가지 않음

---

# 작업 3 — UI를 딸깍 패턴으로 통일 (남은 것 마무리)

이미 적용된 것: 보상 가로 배너 카드, HP 숫자 겹치기, 피해 배너, 좌측 노드 트랙,
패널 모서리 장식, 입체 버튼. **아래 4개가 남았다.**

| # | 항목 | 내용 |
|---|---|---|
| 3-A | **조합표 상시 열람** | 전투 중 좌하단 버튼 → 족보 4종과 효과를 언제든 확인. 규칙을 외우게 만들지 마라 |
| 3-B | **현재 성립 족보 상시 배너** | 주사위를 굴린 뒤 "페어! +3" 같은 배너가 **계속 떠 있게**. 지금은 팝업이 터지고 사라져서, 리롤을 고민하는 동안 뭐가 성립했는지 안 보인다 |
| 3-C | **일시정지에서 보유 유물 확인** | 유물 40종의 효과를 기억할 수 없다. 아이콘+설명 목록 |
| 3-D | **리본 배너 헤더 통일** | 지금 화면마다 헤더 모양이 다르다. 결과 화면의 리본 배너로 통일 |
| 3-E | **보상 등급색을 새 팔레트에 맞춘다** | common/rare/epic이 아직 옛 색이다(검토 03). 기획서 9.5절 5번 "등급 색이 한눈에"는 지킬 것 |
| 3-F | **예고 기호를 아이콘 스프라이트로** | 공격·힘모으기·회복·보스를 글자 기호 대신 16×16 아이콘으로. 위젯 구조를 건드리므로 여기서 한다 (검토 03) |

> **3-B가 가장 중요하다.** 리롤 판단에 직접 쓰이는 정보인데 지금은 사라진다.

---

# 작업 4 — 연출 강화 (손맛)

| # | 항목 | 내용 |
|---|---|---|
| 4-A | **판정 결과를 화면 중앙에 거대하게** | 「대성공」「폭주」가 화면을 덮으며 터진다. 딸깍 다이스가 조합 확정 순간에 하는 것 |
| 4-B | **히트스톱** | 명중 순간 40~80ms 정지 후 튕겨나가게. 코드 몇 줄인데 타격감이 확 산다 |
| 4-C | **화면 흔들림 3단계** | 약(일반 타격) / 중(대성공) / 강(폭주). 지금은 한 종류뿐 |
| 4-D | **대미지 숫자 크기를 피해량에 비례** | 3과 30이 같은 크기로 뜨면 안 된다 |
| 4-E | **적 처치 연출** | 파편이 사방으로 터지며 사라진다. 지금은 그냥 없어진다 |
| 4-F | **주사위를 도트로 다시 그린다** | 화면에서 안티앨리어싱이 켜진 **유일한 물체**다. 사용자 방침("선명한 도트, 제한적 안티앨리어싱")에 어긋나고, 하필 화면의 주인공 자리다. **흰색 대비는 그대로 유지**할 것 — 유지하라고 한 건 밝기 대비이지 렌더링 방식이 아니다 (검토 03) |

> **작업 4는 밸런스 창의 작업 4·5가 끝난 뒤에 시작한다.** `battle_fx.dart`·
> `dice_widget.dart`를 두 창이 함께 쓰기 때문이다. 순서는 아래와 같이 확정됐다.
>
> `그래픽 2 → 그래픽 3 → 밸런스 3·4·5 → 그래픽 4`

- 진동(`HapticFeedback`)을 각 단계에 맞춰 붙인다 (기획서 6절).
- **배경 애니메이션이 계속 돌면 위젯 테스트의 `pumpAndSettle`이 영원히 안 끝난다.**
  전투 화면 테스트는 `pump(시간)`으로 넘길 것 (LESSONS 기록됨).

---

# 작업 순서와 보고

1. **작업 1(팔레트+폰트)을 끝내고 멈춰서 화면 시트를 보고**한다.
   색은 취향이 갈리므로 **사용자 확인 없이 다음으로 넘어가지 마라.**
2. 확인 후 작업 2(캐릭터) → 3(UI) → 4(연출).
3. 작업 2는 5~6종마다 시트를 보인다.
4. 각 작업 후 `flutter test` + `flutter analyze`, 그리고 **실제로 실행해 눈으로 확인.**
5. 작업이 끝날 때마다 `PROGRESS.md`를 갱신한다 (창을 초기화해도 복원되도록).

## 보고 형식 (사용자는 비전공자다)

- 무엇을 왜 바꿨는지 한국어로 짧게
- **개정 전/후를 나란히 놓은 480px 시트 한 장**
- 테스트 개수와 통과 여부
- 판단이 필요하면 임의로 정하지 말고 물어본다

## 하지 말 것

- 게임 규칙·수치 변경 (밸런스 창 담당)
- 밸런스 창의 작업 4·5(확정 굴림·각인 주사위) 선점
- 기획서(`GAME_DESIGN.md`) 수정 — 문서는 기획 창 담당이다
- 상용 게임 에셋 추출·모방
- 스크린샷 난사 — 화면을 한 번 보고 판단해 한 번만 누른다
