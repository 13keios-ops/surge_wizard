# 작업 지시서 K — 덱·봇 수정 + 결정용 재측정

발행: 2026-09-02, 기획 창. **작업 1은 사용자 승인 완료된 수치 변경이다.**

근거 문서: **`SPELL_CIRCLES.md`**(1서클 절 · 7서클 절) · 검토 16

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**

---

## 0. 목적 (한 줄)

**측정 12·14·15·16이 전부 전제가 흔들린 상태였다. 세 가지를 고쳐 그 전제를 다 맞추고,
「각인·층수·성장 곡선을 확정할 수 있는 첫 측정」을 만든다.**

## 0-1. 무엇이 틀려 있었나 (검토 16)

| # | 문제 | 고칠 것 |
|---|---|---|
| 1 | **1지역 덱에 적을 깎을 주문이 1장뿐**이라 전투가 교착했다 (200턴) | 채우는 **순서** |
| 2 | **봇이 유틸을 안 쓴다** — 덱의 31~50%가 유틸인데 시전은 1.6~10% | 봇 **정책** |
| 3 | `soul_reaper`가 위력·회복 둘 다 최고라 후반을 독점했다 | **수치** (승인됨) |

## 0-2. 절대 지킬 것

- **작업 1 외에 밸런스 수치를 바꾸지 마라.** 완주율이 낮아도 그대로 적어라
- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — ✅ 승인된 수치 변경 1건

`assets/data/spells.json`의 **`soul_reaper`(영혼 수확) `base_damage` 15 → 13.**

- **`effect`(회복 5)와 `dc_modifier`(+1)는 그대로 둔다**
- **이 한 값 외에 어떤 수치도 건드리지 마라**
- 코드·테스트 어디도 `soul_reaper`를 직접 참조하지 않는다 (기획 창이 확인했다)

---

# 작업 2 — 1서클 채우기 순서 (측정 프로필)

`tool/measure_profile.dart`의 `kCircle1Priority`를 고친다.

### 지금 (틀림)

```
치유 약초(0) → 마력방패(0) → 잿불 방패(5) → 냉기 손길(5) → 영혼 갉기(5)
```

### 고칠 것

```
잿불 방패(5+방어4) → 냉기 손길(5+지연) → 영혼 갉기(5+회복2)
  → 치유 약초(0+회복5) → 마력방패(0+방어6)
```

**「위력을 지킨 유틸 3종」이 먼저다.** 1서클 공격 주문도 전부 위력 5이므로,
이 셋은 **공격 주문과 위력이 같으면서 효과가 덤인** 주문이다.

- 1지역 덱(3칸) = **위력 5 세 장, 전부 유틸 효과 포함** → 교착이 사라진다
- 1서클 슬롯이 5칸이 되는 후반에는 치유 약초·마력방패가 자연히 들어온다
- **검산**: 모든 지역의 덱에서 **`base_damage` > 0인 주문이 2장 이상**인지 확인하는
  단언을 넣어라 (`SPELL_CIRCLES.md` 1서클 절의 규칙)

---

# 작업 3 — ★ 봇 최소 유틸 정책

## 3-A. 정책 내용 (기획 창 지정)

`tool/sim_core.dart`의 주문 선택에 **두 줄만** 더한다.

```
1) 체력 < 최대 체력의 40%  이고 손패에 회복(heal) 주문이 있으면
   → 회복량이 가장 큰 것을 시전
2) 적이 강타를 예고했고  그 피해 ≥ 현재 체력 + 방어막  이며
   손패에 방어막(shield) 주문이 있으면
   → 방어막이 가장 큰 것을 시전
3) 그 밖에는 지금과 같다 — 가장 센 주문
```

- **「사람이라면 당연히 하는 것」만 흉내 낸다.** 그 이상 똑똑하게 만들지 마라
- 시전 강도(`chooseIntensity`)는 **건드리지 마라**
- 1·2번으로 고른 주문도 **봉인·마나 조건은 똑같이 적용**된다

## 3-B. ★ 두 정책을 한 번의 실행에서 모두 돌린다

⚠ **측정 15에서 `kBotFullManaReserve`가 `const`라 두 번 돌리고 파일을 옮기는
수고가 있었다.** 같은 일을 반복하지 마라.

- 정책을 **`const`가 아니라 인자로** 받게 한다 (예: `BotPolicy { greedy, utility }`)
- **한 번 실행에 두 정책 결과를 나란히** 낸다
- 기본값은 **`utility`**(새 정책). `greedy`는 비교용이다

---

# 작업 4 — 재측정

측정 15·16과 **같은 조건**이라야 비교가 된다.

- 지역 12 × 난이도 3 = **36조합 × 300판**, 시드 **2개**(20260831 · 20260902)
- 체력·마나 프로필은 **그대로**
- 출력 **`reports/data/measure5_output.txt`**

## 내보낼 표

### 4-A. ★ 완주율 — 3단 비교

| 지역 | 측정 15 (고정 손패) | 측정 16 (덱·틀림) | **이번 greedy** | **이번 utility** |

**`greedy` 열이 측정 16보다 좋아졌다면 「덱 순서 수정」의 효과**,
**`utility` 열이 `greedy`보다 좋아졌다면 「봇 정책」의 효과**다. 둘을 갈라 읽을 수 있다.

### 4-B. 손패에 무엇이 잡히고 무엇을 썼나

| 지역 | 덱 크기 | 덱 유틸 비중 | **시전 유틸 비율 (greedy / utility)** | 회복 시전 비율 | 평균 시전 위력 |

**시전 유틸 비율이 1.6~10%에서 얼마나 올랐는지**가 작업 3의 성패다.

### 4-C. 전투당 굴림 + 각인 기대 적립

측정 12·14·15·16과 **한 표에 나란히.** 각인 기대 적립도 다시 계산한다
(문서 기재 13~19% / 측정 16은 8.5~11.9%였다).

### 4-D. 전투당 턴 수 (일반 / 보스)

측정 16의 보스 9.34턴은 **1·2지역 교착이 끌어올린 값**이었다.
**교착이 사라졌는지**가 여기서 보인다.

### 4-E. 200턴 안전장치에 걸린 전투 수

**0이어야 한다.** 0이 아니면 작업 2가 덜 된 것이다.

---

# 작업 5 — 테스트

| # | 검사 | 어디 |
|---|---|---|
| 1 | **모든 지역 덱에 `base_damage` > 0인 주문이 2장 이상** | 새 테스트 |
| 2 | 1지역 덱이 「잿불 방패 · 냉기 손길 · 영혼 갉기」다 | 새 테스트 |
| 3 | `soul_reaper` `base_damage`가 **13**이다 | `spell_circle_test`에 덧붙임 |
| 4 | 7서클 5종의 위력이 **12~13 범위**다 | 위와 같은 곳 |

> ⚠ **기존 테스트가 깨지면 구현이 틀린 것이다.** 기획 창이 확인한 바로는
> `soul_reaper`를 참조하는 테스트가 없어 **작업 1로는 아무것도 안 깨진다.**

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드 정상 종료, `reports/data/measure5_output.txt` 생성
3. `BALANCE.md`에 **「9차 — 덱·봇 수정」** 절을 맨 위에 추가 (기존 보존)
   - ⚠ **8차 절에 「덱 구성 오류로 무효」라고 한 줄 달아 둘 것** (검토 16)
4. **`reports/17_덱봇수정_재측정.md`** 작성
   - **4-A 3단 비교표를 맨 앞에**
   - 4-E(안전장치 0건)를 명시
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ 작업 1 외의 수치 조정 — **완주율이 낮아도 만지지 마라**
- ❌ 체력·마나 프로필 변경 (측정 15·16과 비교해야 한다)
- ❌ `chooseIntensity`(시전 강도) 변경
- ❌ 봇을 더 똑똑하게 만들기 (3-A의 두 줄만)
- ❌ 화면 · 덱 UI · 마법서 시스템
- ❌ 각인·층수 표 확정 (이 결과를 보고 기획 창이 정한다)
