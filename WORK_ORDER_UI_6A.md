# 작업 지시서 6-A — 전투 화면 재설계 (UI 개선)

> ⛔ **끝난 지시서다. 현재 기준이 아니다** (검토 31 — **구조 승인 / 수정 3건.**). 지금 상태는 `HANDOFF.md`.

발행: 2026-09-07 저녁, 기획 창.

> ### 🪟 창 운영
> **새 창(`/clear` 후).** 이번에 새로 깐 **`superdesign` · `frontend-design`
> 스킬은 세션 시작 때 실리므로, 이 창을 새로 열어야 쓸 수 있다.**

---

## 0. 이 지시서가 왜 나왔나

**사용자가 지금 UI가 마음에 들지 않는다고 했다** (2026-09-07).

그동안은 「`UI_DESIGN.md`가 기준」이었는데, **그 기준대로 만든 결과가
마음에 안 든다는 것이므로 기준 자체를 다시 본다.**

> 🔴 **이번 작업에서 `UI_DESIGN.md`는 「지켜야 할 규칙」이 아니라
> 「지금까지 이랬다」는 자료다.** 사용자가 명시적으로 뒤집었다.
> 다만 **취향이 아니라 물리인 것**(3절)은 그대로 남는다.

---

## 1. 🔴 가장 중요한 것 — **v2 화면을 고치지 마라**

**이 작업은 「지금 화면 다듬기」가 아니라 「v3 화면 새로 짜기」다.**

`GAME_DESIGN.md`가 v3로 개정되면서 전투 화면의 **구성 요소 자체가 바뀌었다.**
지금 화면을 예쁘게 다듬으면 **곧 지울 것을 다듬는 것**이 된다.

| 지금 화면(v2) | **v3에서** |
|---|---|
| 손패 3장 (덱에서 뽑은 주문 카드) | **주문 슬롯 3~5칸.** 매 턴 슬롯머신처럼 돌아간다 |
| **시전 강도 「보통 / 전력」 버튼** | 🔴 **삭제.** `cast_controls.dart`가 아직 이걸 그리고 있다 |
| 적 1마리 | **다중 적.** 적마다 HP바·레벨·데미지 숫자 |
| 리롤 = 마나 1/2/3 체증 | **층당 2회 무료.** 남은 횟수를 보여줘야 한다 |
| — | **던전 이름 + 층/스테이지** (상단), **일시정지/설정** (우상단) |

**그래서 시안은 처음부터 v3 요소로 그린다.** 근거는 `GAME_DESIGN.md` **6.7절**
(사용자가 원하는 화면이 표로 다 적혀 있다) + **4.2절**(주문 슬롯/덱 칸) +
**4.4절**(다중 적 구도).

---

## 2. 측정된 결함 — 이건 취향이 아니라 버그다

### 2-1. 조작부가 화면 높이를 따라가지 않는다

`lib/screens/battle_screen.dart`의 구조가 이렇다:

```
Column
 ├ Expanded( BattleStage )        ← 남는 높이를 전부 먹는다
 └ Padding( Column(min) [ 손패, DiceTray ] )   ← 내용 크기 그대로. 고정이다
```

조작부가 **`MainAxisSize.min` 체인**이라 **화면이 작아져도 안 줄어든다.**
실측 **고정 377dp**이고, **640dp 기기에서 화면의 59%**를 먹는다.

**→ 조작부를 화면 높이에 비례시키는 구조로 바꾼다.** 이게 6-A의 핵심이다.
비율 목표는 시안에서 정하되, **작은 기기에서 장면이 조작부보다 작아지면 안 된다.**

### 2-2. 이미 반영된 것 (또 하지 마라)

2026-09-07에 이미 손봤다 (`reports/29_*` 9절). **되돌리지 말 것:**

- 장면:조작부 46:52 → **55:44**
- 무대 여백 제거 (배경이 화면 끝까지)
- 주사위 88 → **64** (`kDiceSize`)
- 마법사 상한 142 → **160** (`kWizardMaxSize`)

---

## 3. 취향이 아니라 물리인 것 — **이건 못 바꾼다**

`UI_DESIGN.md`를 자료로 내리더라도 아래는 남는다. **이유가 미감이 아니라
기계이기 때문이다.**

| 제약 | 왜 |
|---|---|
| **픽셀 폰트는 정수 배율에서만 또렷하다** | Galmuri11 = 12·24·36·48·84 / Galmuri9 = 10·20·30. **어긋나면 글자가 뭉갠다.** `pubspec.yaml`에 적혀 있다 |
| **터치 대상은 최소 44dp** | 손가락 크기. 접근성 기본 |
| **적 상자 밀도 「화면 한 칸 = 2.0dp」** | 118dp → 1.61dp, 90dp → **1.23dp**. Phase 1이 반려된 바로 그 문제다 (`UI_DESIGN.md` 1-2-b). **다중 적을 가로로 4마리 세우면 여기 걸린다** |
| **다중 적은 원근으로 흩는다** | 가로 배치는 4마리에서 472dp로 화면을 넘는다 (`GAME_DESIGN.md` 4.4절 실측표) |
| **배경 = 밝은 파스텔 / 캐릭터 = 진한 톤** | 2026-09-07 사용자 지시 (`GAME_DESIGN.md` 6.1절) |

**이 다섯 개 말고는 전부 다시 제안해도 된다.** 색·배치·정보 위계·버튼 모양·
주사위 표현 전부 열려 있다.

---

## 4. 할 일

### 작업 1 — 지금 화면을 눈으로 확인한다

`test_shots/`에 화면을 찍는 도구가 이미 있다. **전투 화면을 640dp(작은 기기)와
900dp(보통) 두 높이로 찍어** 2-1의 59% 문제를 눈으로 확인하고 보고서에 남긴다.

🔴 **스크린샷은 가로 480px로 줄인 사본만 본다** (`CLAUDE.md` 토큰 절약).

### 작업 2 — `superdesign`으로 시안을 뽑는다

**`superdesign` 스킬을 써서 전투 화면 시안을 3안 뽑는다.**
`frontend-design`은 그 안에서 미적 판단(타이포·색·위계)에 쓴다.

시안마다 **다르게 가져갈 축**을 하나씩 잡는다. 예:

| 안 | 축 |
|---|---|
| A | **장면을 최대로** — 조작부를 최소로 접고 배경·캐릭터를 크게 |
| B | **조작을 손에 가깝게** — 아래쪽에 무게, 엄지 닿는 곳에 리롤/시전 |
| C | **정보 위계 재구성** — 던전/층·적 상태·내 상태·주사위를 새 순서로 |

> ⚠ **시안은 HTML이라 Flutter 코드가 되지 않는다.** 그래도 상관없다 —
> 이 단계에서 정하는 것은 **배치·비율·색·정보 위계**이고, 그건 프레임워크와
> 무관하다. Flutter로 옮기는 것은 작업 4다.

**세 안 모두 v3 요소로 그린다** (1절 표). 다중 적은 **2마리와 4마리** 두 경우를
보여준다 — 4마리가 들어가는지가 이 설계의 시험대다.

### 작업 3 — 사용자에게 비교시킨다

**한 장의 비교 시트로 보여준다.** 사용자는 `GAME_ART_ROADMAP.md` 10절대로
**네 가지만 판단한다** — 같은 게임처럼 보이나 / 자연스럽나 / 가리나 / 마음에 드나.

🔴 **규격·밀도·폰트 배율·터치 크기 검증은 네가 한다. 사용자에게 찾아 달라고 하지 마라**
(`CLAUDE.md` 작업 방식).

**여기서 멈추고 승인을 받는다. 승인 없이 작업 4로 가지 마라.**

### 작업 4 — 승인된 안을 Flutter로 옮긴다

- `lib/core/layout.dart`의 상수를 **화면 높이 비례 계산**으로 바꾼다
- `battle_screen.dart`의 `Column` 구조를 비율 기반으로 바꾼다
- 🔴 **`cast_controls.dart`의 시전 강도(`CastIntensity`) UI를 지운다** — v3에서 삭제됐다
- **주문 슬롯 5칸 자리를 만든다.** 안 산 칸은 잠금 표시 (`GAME_DESIGN.md` 4.2절)
- **다중 적 자리를 비워 둔다** — 엔진은 아직 1대1이다. **레이아웃만 미리 받아둔다**

### 작업 5 — 검증

- `flutter analyze` 경고 0건
- `flutter test` 전부 통과 (지금 **316개**)
- **640 / 900dp 두 높이에서 다시 찍어** 조작부 비율이 따라오는지 확인
- 픽셀 폰트가 정수 배율을 벗어나지 않았는지 **직접 세어 확인**

---

## 5. 하지 마라

- ❌ **엔진을 건드리지 마라.** 다중 적·주문 슬롯 추첨·족보·리롤은 **다음 지시서**다.
  이번엔 **화면 레이아웃만** 한다
- ❌ `GAME_DESIGN.md` 등 기획 문서를 고치지 마라 (기획 창 소유)
- ❌ 승인 없이 작업 4로 넘어가지 마라
- ❌ 배경 원화를 새로 뽑지 마라 — 배경 8종 재발주는 **별도 건**이다.
  지금 있는 `bg_forest`로 시안을 만든다

---

## 6. 보고서

🔴 **시작할 때 `reports/31_UI재설계_6A.md`를 먼저 만든다** (양식 `reports/_TEMPLATE.md`).
**한 덩어리가 끝날 때마다 한 줄씩 덧붙인다.** 끝에 몰아 쓰지 마라 —
토큰 한도로 끊기면 판단이 통째로 사라진다.

사용자에게는 **「보고서 `reports/31_UI재설계_6A.md` 작성 완료」 한 줄만** 말한다.

---

## 7. 끝나면

`UI_DESIGN.md`가 **낡았다.** 이번 결과로 갱신해야 하지만 **그건 기획 창이 한다.**
보고서에 **「`UI_DESIGN.md`의 어느 절이 무효가 됐는지」**를 목록으로 남겨라.
