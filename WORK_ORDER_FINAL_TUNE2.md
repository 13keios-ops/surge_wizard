# 작업 지시서 Q — 12지역 배율 조정 + 곡선 조건 최종 판정

발행: 2026-09-04, 기획 창. **작업 1은 사용자 승인 완료된 수치 변경이다.**

근거 문서: **`GAME_DESIGN.md` 6.2절**(곡선 조건 — **B가 B1·B2로 쪼개졌다**) ·
`ENEMIES.md` · 검토 22(`reports/22_최종조정_측정_REVIEW.md`)

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**

---

## 0. 목적 (한 줄)

**곡선 작업의 마지막 한 칸이다. 이번 측정으로 밸런스 곡선을 닫는다.**

## 0-1. ★ 판정 기준이 바뀌었다 — B가 B1·B2로 쪼개졌다

`GAME_DESIGN.md` 6.2절을 **반드시 읽어라.** 옛 「B 12지역 25~45%」는 없다.

| # | 조건 | 측정 22 |
|---|---|---|
| **A** | 상한 **단조 감소** · 인접 낙차 **20%p 이내** (흔들림 선 **±1.0%p**) | ✅ 최대 16.0%p |
| **B1** | **11지역 상한 25~45%** | ✅ 27.7% |
| **B2** | **12지역 상한 15~25%** | ⚠ 13.3% / 19.0% |
| **C** | 도입(1~3) 하한 **70%+**, 5~10지역 간격 **30%p+** | ✅ |

**이번 목표는 B2를 두 시드 모두에서 맞추면서 A·B1·C를 깨지 않는 것이다.**

> **왜 목표가 25~45%에서 15~25%로 바뀌었나** — 검토 22의 3절이다.
> 11지역이 이미 27.7%로 밴드 바닥이라, 12지역을 25% 위로 올리라는 것은
> **최종 지역을 앞 지역과 같은 난이도로 만들라**는 뜻이 되어 **조건 A(단조 감소)와
> 양립할 수 없었다.** 목표를 낮춘 것이 아니라 **모순을 없앤 것**이다.

## 0-2. 절대 지킬 것

- **작업 1 외에 수치를 바꾸지 마라.** 목표에 못 미쳐도 그대로 적어라
- **적 수치 · 보스 HP · `atk_scale` · 난이도 배율 · 1~11지역 `hp_scale` ·
  모든 지역의 층수 · 성장 프로필 · 강화율 · 상점 회복 · 전투 승리 회복을
  건드리지 마라**
- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — ✅ 12지역 `hp_scale` 하향

`assets/data/regions.json`. **12지역 한 칸만** 바꾼다.

| 지역 | 지금 | **바꿀 값** |
|---|---|---|
| 11 심연의 균열 | 3.20 | **손대지 마라** |
| **12 고룡의 둥지** | **3.50** | **3.35** |

- **`atk_scale` 1.45는 그대로다.** 보스 HP 405도 그대로다
- **1~11지역 `hp_scale`은 한 칸도 바꾸지 마라**

### 1-A. ⚠ 왜 3.1~3.2가 아니라 3.35인가

보고서 22의 6-1은 3.1~3.2를 권했으나 **실행 불가능하다.**
**11지역이 이미 3.20**이라 최종 지역 일반 적이 앞 지역보다 약해지고,
`hp_scale` **단조 증가 불변식**이 깨진다.

**3.35는 11지역보다 위이면서(3.20 < 3.35) 증가폭을 +0.30 → +0.15로 줄인다.**
12지역의 +0.30은 12개 지역 통틀어 가장 컸다.

---

# 작업 2 — ⚠ 기존 테스트 (기획 창이 조사했다)

| 위치 | 조치 |
|---|---|
| `test/region_stage_test.dart` **6-1** — 「`hp_scale`이 표와 같고 1.00 → **3.50**으로 단조 증가」 | `kHpScales`의 12번째와 `regions.last` 단언을 **3.35**로. **제목의 3.50도 3.35로.** **단조 증가 검사는 손대지 마라** — 3.20 < 3.35라 그대로 통과한다 |
| `test/region_stage_test.dart` **25행 `kHpScales` 목록의 마지막 값 3.50** | **3.35로** |
| `test/region_stage_test.dart` **158행 `lateScales`의 `12: 3.50`** | **3.35로** |
| `test/region_stage_test.dart` **6-1b** — 「1~8지역 `hp_scale` 불변」 | **손대지 마라.** 그대로 통과한다 |
| `test/progression_test.dart` **76~108행** — 검사 4·5가 `buildEnemy(..., 3.5, 1.45, ...)`를 **인자로 직접 넘긴다** | **동작은 그대로 통과한다** (배율 함수의 단위 테스트라 지역 데이터를 안 읽는다). 다만 **제목에 「12지역 ×3.5」라고 적혀 있어 거짓말이 된다.** 인자와 제목을 **3.35로** 함께 고쳐라 |
| `test/loader_test.dart` 적 HP 범위 단언 (있다면) | 상한이 배율에서 나오면 **새 값으로**. 보스 HP 405는 불변이다 |

**그 밖이 깨지면 구현이 틀린 것이다.**

---

# 작업 3 — 측정

측정 22와 **같은 조건** (36조합 × 시드 2 × 하한/상한 × greedy/utility).
출력 **`reports/data/measure11_output.txt`**

## 3-A. ★ 판 수를 지역별로 다르게 잡는다 (신규)

측정 22에서 **12지역 상한이 시드에 따라 13.3 ↔ 19.0%로 흔들렸다**
(완주 40판 vs 57판). 지금 표본으로는 통과·미달을 못 가른다.

| 지역 | 조합당 판 수 |
|---|---|
| 1~11 | **300판** (지금과 같다 — 재현 검산에 필요하다) |
| **12** | **1000판** |

- **판 수는 인자로 받아라.** 상수로 박지 마라
- **1~11지역이 300판이어야 기준 조건이 측정 22를 소수점까지 재현한다**

## 3-B. 조건 2개로 기여를 확인한다

| 조건 | 12지역 `hp_scale` |
|---|---|
| 기준 (= 측정 22 재현) | **3.50** |
| **새 배율 (채택)** | **3.35** |

- **기준 조건의 1~11지역이 측정 22와 소수점까지 같아야 한다.** 다르면 보고하라
- **12지역은 판 수가 300 → 1000으로 달라졌으므로 값이 달라도 정상**이다.
  그 사실을 출력에 한 줄 적어라
- 조건은 **인자로** 받아라

## 3-C. ★ 판정 — 곡선 조건 A · B1 · B2 · C

### 표 1 — 곡선과 낙차

| 지역 | 하한 | 상한 | 상한 낙차 | 간격(상−하) |

### 표 2 — 조건 판정

| 조건 | 요구 | 실측 (시드1) | 실측 (시드2) | 판정 |
|---|---|---|---|---|
| A | 단조 감소 · 낙차 ≤ 20%p (흔들림 ±1.0%p) | | | |
| **B1** | **11지역 상한 25~45%** | | | |
| **B2** | **12지역 상한 15~25%** | | | |
| C | 도입 하한 70%+ / 5~10지역 간격 ≥ 30%p | | | |

**네 조건을 한 줄로 답하라** — 「A✅ B1✅ B2✅ C✅」인지.
**「A 엄격 판정」 줄은 측정 22처럼 그대로 유지하라.**

## 3-D. 그 밖에 낼 표

| # | 표 | 비고 |
|---|---|---|
| 1 | 상점 층 **도달률 · 남은 체력 %** | 12지역 54.0% / 57.0%에서 어떻게 움직이는지 |
| 2 | 지역별 **보스만 승률** (9~12) | **1~11은 바뀌면 안 된다.** 12지역은 난수 흐름으로 움직일 수 있다 — 측정 22처럼 설명을 붙여라 |
| 3 | 전투당 굴림 | 측정 12~23을 한 표에. **각인 기대 적립 표는 더 내지 마라 — 확정됐다** (아래 참조) |
| 4 | 전투당 턴 수 · **200턴 안전장치** | 0이어야 한다 |
| 5 | **12지역 한 판의 평균 턴 수** | 25.69턴에서 얼마나 움직이는지 (원칙 6) |

> ⚠ **「전투당 피해 %」는 내지 마라** (생존 편향).

> ### ✅ 각인 수치표는 이미 확정됐다 — 다시 계산하지 마라
> 기획 창이 검토 22에서 **실측 4.49굴림 기준으로 확정**해
> `ENGRAVINGS.md` 4절을 다시 썼다. **1회 적립당 수치가 전부 두 배가 됐다**
> (흔함 전설 5% → **10%**, 희박 전설 20% → **40%**).
> **이번 측정에서 각인 기대 적립 표를 내지 말고, 값을 재계산하지도 마라.**
> 전투당 굴림 숫자만 표 3에 이어 적으면 된다.

---

# 작업 4 — 테스트

| # | 검사 |
|---|---|
| 1 | 12지역 `hp_scale`이 **3.35** |
| 2 | **1~11지역 `hp_scale`이 안 바뀌었다** (12칸 전수 대조) |
| 3 | `hp_scale`이 여전히 **단조 증가**한다 |
| 4 | **`atk_scale` 12칸이 한 값도 안 바뀌었다** |
| 5 | 12지역 **보스 HP가 405 그대로**, 모든 지역 **층수가 안 바뀌었다** (총합 728) |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드 정상 종료, `reports/data/measure11_output.txt` 생성
   (**측정 22의 `measure10_output.txt`를 덮어쓰지 마라**)
3. `BALANCE.md`에 **「15차 — 12지역 배율」** 절을 맨 위에 추가
4. **`reports/23_배율조정_최종측정.md`** 작성
   - **맨 앞에 「A·B1·B2·C 네 조건이 통과했는가」 한 줄**
   - 곡선·낙차 표
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **적 수치 · 보스 HP · `atk_scale` · 난이도 배율 · 1~11지역 배율 ·
  층수 · 성장 프로필 · 회복 · 강화율 조정**
- ❌ 구간 밴드 목표표 재발행
- ❌ 「전투당 피해 %」 표
- ❌ **각인 수치 재계산** (확정됐다)
- ❌ `lib/`에 마법서·강화 시스템 구현
- ❌ 화면 작업
