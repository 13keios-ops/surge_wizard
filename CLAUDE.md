# CLAUDE.md — surge_wizard

이 파일은 이 프로젝트에서 작업하는 Claude가 매 세션 시작 시 읽는 규칙이다.
**작업 전에 반드시 `GAME_DESIGN.md`를 먼저 읽을 것.**

---

## 문서 지도 (새 세션은 여기부터)

대화가 초기화돼도 이 문서들만 읽으면 맥락이 복원된다. **필요한 것만 골라 읽어라.**

| 문서 | 내용 | 언제 읽나 |
|---|---|---|
| `HANDOFF.md` | **새 창은 여기부터.** 지금 어디까지 왔고 다음에 뭘 하나 | **항상, 제일 먼저** |
| `GAME_DESIGN.md` | **기획서 v2**. 모든 판단의 기준 | **항상** |
| `docs/GAME_DESIGN_v1.md` | 옛 기획서 (2026-09-01 대체됨) | v1 결정의 배경이 궁금할 때만 |
| `PROGRESS.md` | 단계별 완료 현황과 무엇을 어떻게 만들었는지 | **항상** |
| `BLOCKERS.md` | 내가 내린 판단과 그 근거 (기획서에 없던 것들) | 설계 의문이 생기면 |
| `UI_DESIGN.md` | **화면 구도 + 원화 규격 + 톤 통일 규칙 (v2)** | **그림·UI 작업 시 항상** |
| `ART_PROMPT.md` | 사용자가 GPT 그래픽 창에 넣은 **지침 원문** (기록, 고치지 말 것) | 에셋이 들어올 때 |
| `ASSET_LIST.md` | **에셋 46장 목록 + 각각의 생김새** (GPT에 그대로 전달하는 문서) | 그림 발주 시 |
| `ASSET_LIST_EQUIP.md` | **장비(지팡이 15종) 목록** — 위 문서의 짝 | 장비 발주 시 |
| `ASSET_LIST_BG.md` | **배경 8종 목록** — 12지역을 8장으로 덮는 대응표 포함 | 배경 발주 시 |
| `art_raw/README.md` | **GPT 원본을 어디 넣나** + 후처리 흐름 + **장비 검수 절차** | 에셋이 들어올 때 |
| `GRAPHICS_PLAN.md` | ⛔ 폐기. v1 픽셀아트 전제 → `UI_DESIGN.md`로 대체 | 읽지 마라 |
| `ART_PIPELINE.md` | ⛔ 폐기. v1 픽셀아트 전제 → `UI_DESIGN.md`로 대체 | 읽지 마라 |
| `DICE_DESIGN.md` | ⛔ **폐기.** v1 기준 + 옛 확률표 | 읽지 마라 |
| `SURGE_DESIGN.md` | **폭주 6갈래 재설계 + 80종 전문**. surges.json의 기준 | 폭주 작업 시 |
| `SPELL_CIRCLES.md` | **주문 70종 서클 1~9 분류표** + 마법서 수치. spells.json의 기준 | 주문·덱 작업 시 |
| `PASSIVE_TREE.md` | **패시브 100노드** (5갈래 × 20) + 비용·개방 조건 | 성장·트리 작업 시 |
| `ENGRAVINGS.md` | **각인 18종 × 5등급 수치표** + 획득 확률 | 각인·보상 작업 시 |
| `GROWTH.md` | **레벨·경험치 곡선 · 서클 슬롯표(확정) · 마법서 드랍** | **성장·덱·마법서 작업 시 항상** |
| `ENEMIES.md` | **보스 12종 · 변종 6종 · tier 풀 배분표** | 적·난이도 작업 시 |
| `STAGES.md` | **지역 12 · 스테이지 100 이름·층수표** | 진행 구조 작업 시 |
| `WORK_ORDER_DATA_A.md` | 구현 창 지시서 — 폭주 80종 + 주문 서클 | ✅ 완료 (검토 08 승인) |
| `WORK_ORDER_DATA_B.md` | 구현 창 지시서 — 보스·변종·지역·스테이지 | ✅ 완료 (검토 09 승인) |
| `WORK_ORDER_CODE_SURGE.md` | 구현 창 지시서 — 폭주 효과 4종 + 팝업 6갈래 | ✅ 완료 (검토 10 승인) |
| `WORK_ORDER_INTENSITY.md` | 구현 창 지시서 — 시전 강도 2단계 개정 | ✅ 완료 (검토 11 승인) |
| `WORK_ORDER_SIM.md` | 구현 창 지시서 — 시뮬레이션 측정 | ✅ 완료 (검토 12 승인) |
| `WORK_ORDER_PROGRESSION.md` | 구현 창 지시서 — 진행 구조 교체 (엔진층) | ✅ 완료 (검토 13 승인) |
| `WORK_ORDER_SIM2.md` | 구현 창 지시서 — 시뮬 재측정 | ✅ 완료 (검토 14 승인) |
| `WORK_ORDER_SCALE_FIX.md` | 구현 창 지시서 — 지역 배율 분리 + 재측정 | ✅ 완료 (검토 15 승인) |
| `WORK_ORDER_DECK_SIM.md` | 구현 창 지시서 — 덱에서 손패 뽑기 | ✅ 완료 (검토 16 — 구현 승인) |
| `WORK_ORDER_DECK_FIX.md` | 구현 창 지시서 — 덱·봇 수정 + 재측정 | ✅ 완료 (검토 17 승인) |
| `WORK_ORDER_GROWTH_SIM.md` | 구현 창 지시서 — 성장 반영 측정 | ✅ 완료 (검토 18 승인) |
| `WORK_ORDER_HEAL_FIX.md` | 구현 창 지시서 — 회복 수정 + 재측정 | ✅ 완료 (검토 19 승인) |
| `WORK_ORDER_ATTRITION.md` | 구현 창 지시서 — 소모전 완화 | ✅ 완료 (검토 20 승인) |
| `WORK_ORDER_LATE_CURVE.md` | 구현 창 지시서 — 후반 곡선 정렬 | ✅ 완료 (검토 21 승인) |
| `WORK_ORDER_FINAL_TUNE.md` | 구현 창 지시서 — 12지역 층수 + 곡선 조건 판정 | ✅ 완료 (검토 22 승인) |
| `WORK_ORDER_FINAL_TUNE2.md` | 구현 창 지시서 — 12지역 배율 3.35 + 최종 판정 | ✅ 완료 (검토 23 승인 — **곡선 종료**) |
| `WORK_ORDER_SCREENS.md` | 구현 창 지시서 — 지역·스테이지·난이도 화면 · 지도 · 파일 분리 | ✅ 완료 (검토 24 승인) |
| `WORK_ORDER_SCREENS2.md` | 구현 창 지시서 — 화면 잔손질 3건 | ✅ 완료 (검토 25 승인) |
| `WORK_ORDER_LEVEL.md` | **구현 창 지시서** — 레벨·경험치·서클 슬롯표 | ★ 지금 발행된 지시서 |
| `WORK_ORDER_BALANCE.md` | ⛔ 보류. 옛 지시서 — 밸런스 개정 5개 작업 | v1 기준이라 낡음 |
| `WORK_ORDER_GRAPHICS.md` | **구현 창 작업 지시서** — 그래픽 4개 작업 | 그래픽 창이 코드를 고치기 전 |
| `WORK_ORDER_ART_PIPELINE.md` | **구현 창 작업 지시서** — 외부 원화를 게임 스프라이트로 변환 | 캐릭터 에셋 작업 시 |
| `refs/DICERO_ANALYSIS.md` | 참고 게임의 **화면 배치·연출** 분석 | UI·연출 작업 시 |
| `refs/DICERO_SYSTEMS.md` | 참고 게임의 **시스템·수치** 분석 | 시스템 설계 시 |
| `BALANCE.md` | 밸런스 시뮬레이션 결과와 우려 지점 | 수치 논의 시 |
| `REVIEW.md` | 전체 검수 보고서 | 품질 점검 시 |

`refs/dicero*/`에는 참고용 스크린샷 200장 이상이 있다. **한 번에 다 열지 마라.**

## 창 운영 (사용자가 두 개를 나눠 쓴다)

- **기획 창** — 검토·계획·의사결정. 문서를 읽고 쓰며 이미지를 거의 안 본다.
  오래 유지된다. 계획은 `.md` 파일로 남겨 구현 창에 넘긴다.
- **구현 창** — 코드 작성·빌드·화면 확인. 스크린샷이 쌓여 무거워지므로
  주기적으로 초기화한다.

두 창이 같은 파일을 동시에 고치면 충돌한다. **기획 창은 문서(.md)만,
구현 창은 코드를 고치는 것을 원칙으로 한다.**

### 구현 창 운영 — 지시서 하나 = 창 하나 (2026-09-01 개정)

**역할별 창을 계속 유지하지 않는다.** 「밸런스 창」「그래픽 창」을 오래 살려두니,
쉬었다 돌아온 창의 기억이 낡아 남의 작업을 덮어쓸 위험이 있었다.

**규칙**

1. **작업 지시서 하나에 창 하나.** 새 지시서를 주기 직전에 그 창을 `/clear`로
   초기화한다 (창을 새로 열어도 되지만 `/clear`가 더 간편하다).
   → 모든 작업이 항상 파일을 새로 읽고 시작하므로 낡은 기억 문제가 사라진다.
2. **한 번에 한 창만 작업한다.** 동시에 돌리지 않는다.
3. `/clear` 해도 잃는 것이 없다. 작업 결과는 코드·`PROGRESS.md`·`reports/`에 남는다.
4. 그래픽 작업 창은 스크린샷이 쌓여 무거워지므로 **특히 오래 끌지 않는다.**

### ★ 새 창이냐 이어서냐 — 판단 가이드 (2026-09-02 신설)

**작업을 시작하기 전에 매번 이 판단을 하고, 사용자에게 어느 쪽인지 먼저 말한다.**

#### 한 줄 기준

> **"지금까지의 판단이 .md 파일에 다 남아 있나?"**
> 남아 있으면 **새 창이 싸다.** 안 남아 있으면 **먼저 남기고** 새 창으로 간다.

문서에 남은 것은 새 창이 30초면 다시 읽는다. 대화에만 있는 것은 영영 사라진다.

#### 이어서 한다 (같은 창 유지)

| 조건 | 이유 |
|---|---|
| **다음 작업이 방금 한 작업의 결과를 전제로 할 때** | 새 창은 그 연결을 못 본다 |
| 문서(.md)만 다루고 **이미지를 안 볼 때** | 텍스트는 누적 비용이 완만하다 |
| 같은 파일을 계속 고칠 때 | 파일 상태를 이미 알고 있다 |
| 사용자가 방금 방향을 뒤집었을 때 | 왜 뒤집혔는지가 대화에만 있다 |

> **실제 사례 (2026-09-02)**: 기획 과제 6개를 한 창에서 이어서 했다.
> 그래서 **4번(각인)을 만들 때 3번(패시브 그림자 갈래)과 겹치는 것을 발견**했고,
> **6번(층수)을 만들 때 5번(보스 수치)이 플레이어 체력 곡선에 걸려 있는 것을 봤다.**
> 창을 나눴으면 둘 다 놓쳤을 것이다. **설계 작업은 이어서 하는 편이 옳다.**

#### 새 창 / `/clear` 한다

| 조건 | 이유 |
|---|---|
| **스크린샷을 3장 이상 봤을 때** | 이미지는 매 요청마다 전부 다시 실려 **비용이 제곱으로 는다** |
| **역할이 바뀔 때** (기획 → 구현, 밸런스 → 그래픽) | 읽어야 할 문서가 통째로 다르다 |
| **작업 지시서가 바뀔 때** | 「지시서 하나 = 창 하나」 원칙 |
| 앞 작업이 **완료돼 문서·코드에 다 남았을 때** | 대화를 들고 갈 이유가 없다 |
| 모델이 앞부분 결정을 **놓치기 시작할 때** | 이미 한계를 넘었다는 신호다 |

#### 창을 바꾸기 전에 반드시 할 것

1. **대화에만 있는 판단을 문서에 옮긴다** — 설계면 설계 문서, 작업이면 `PROGRESS.md`,
   판단 근거면 `BLOCKERS.md`, 인수인계면 `HANDOFF.md`
2. **사용자가 뒤집은 결정은 「왜 뒤집혔는지」까지** 적는다. 안 적으면 다음 창이
   같은 실수를 반복한다
3. **한 건씩 대조한다.** 「대충 남겼겠지」로 넘기지 말고, 이 창에서 나온 판단을
   **목록으로 세어** 각각 어느 파일에 들어갔는지 확인한다.
   사용자에게 **「지금 지워도 되는지」를 먼저 말한다**
4. 그다음에 `/clear` 한다

#### ★ 새 창을 연 직후에 반드시 할 것 (2026-09-04 신설)

> **`HANDOFF.md`의 「아직 안 한 것」은 결론보다 훨씬 빨리 썩는다.**

인수인계 문서는 **끝난 일은 잘 남기는데 안 끝난 일은 방치된다.**
「순서 N에 함께 넣을 것」 같은 표는 그 순서가 지나가면 **아무도 안 읽는 죽은
문장**이 되고, 새 창은 그것을 「옛날 기록」으로 보고 넘긴다.

**그래서 새 창은 시작할 때 이렇게 한다.**

1. `HANDOFF.md`에서 **⬜ · ❌ · 「미구현」 · 「아직」 · 「~할 것」**이 붙은 항목을 뽑는다
2. **각각이 아직 유효한지 코드·데이터로 직접 확인한다** (`grep` 한 번이면 된다).
   문서 말만 믿지 마라 — **이미 구현됐는데 표에만 남아 있는 것**이 섞여 있다
3. 결과대로 `HANDOFF.md`를 **그 자리에서 고친다.** 끝난 것은 ✅로,
   아직인 것은 **어느 지시서에서 처리할지**까지 적는다

> **실제 사례 (2026-09-04)**: 「순서 7에 함께 넣을 것 — 미구현 3건」 표가
> 순서 7이 끝난 뒤로도 여섯 지시서 동안 방치돼 있었다. 확인해 보니
> **1건은 이미 구현됐고, 2건(보스 호위·연전·전조)은 여전히 미구현**이었다.
> 그 2건이 없으면 **하드·데스가 수치 배율만 다른 똑같은 전투**다.

### 창끼리 주고받는 법 — 보고서 (2026-09-01~)

사용자가 결과를 복사해 나르지 않는다. **파일로 주고받는다.**

- 구현 창은 작업이 끝나면 **`reports/NN_창이름_작업.md`를 새로 만든다.**
  양식은 `reports/_TEMPLATE.md`. 사용자에게는 **"보고서 <경로> 작성 완료"
  한 줄만** 말하고 내용을 대화에 길게 붙여넣지 않는다.
- 기획 창은 그 파일을 읽고 **`reports/NN_..._REVIEW.md`**로 검토 결과를 남긴 뒤
  `reports/INDEX.md`를 갱신한다.
- **구현 창은 항상 새 파일만 만든다.** 남의 파일을 고치지 않으므로 충돌이
  구조적으로 불가능하다.

### 파일 소유권 (누가 고치는가)

| 파일 | 소유 |
|---|---|
| `lib/`, `test/`, `tool/`, `assets/` 코드·데이터 | 구현 창 |
| `PROGRESS.md`, `BALANCE.md`, `BLOCKERS.md`, `ATTRIBUTION.md` | 구현 창 |
| `reports/NN_*.md` (자기 보고서) | 만든 구현 창 |
| `GAME_DESIGN.md`, `GRAPHICS_PLAN.md`, `ART_PIPELINE.md`, `DICE_DESIGN.md` | 기획 창 |
| `WORK_ORDER_*.md`, `CLAUDE.md`, `reports/INDEX.md`, `reports/*_REVIEW.md` | 기획 창 |

---

## 프로젝트 개요

주사위 3개(3d6)로 **주문 시전 성공 여부를 판정**하는 캐주얼 로그라이트 모바일 게임.
Flutter로 개발하며 안드로이드와 iOS를 목표로 한다.

**사용자는 소프트웨어 비전공자이며 코드를 직접 읽지 않는다.**
따라서 아래를 지킬 것:
- 무엇을 왜 하는지 한국어로 짧게 설명한 뒤 작업한다
- 사용자가 직접 해야 하는 일(터미널 명령, 파일 다운로드, 콘솔 설정)은 **번호를 매겨 단계별로** 알려준다
- 전문 용어를 쓸 때는 괄호로 한 줄 설명을 붙인다
- 에러가 나면 원인을 추측하지 말고 실제 로그를 확인한 뒤 고친다

---

## 개발 환경 (중요)

이 PC에서는 `flutter` 명령이 PATH에 없을 수 있다. 없으면 아래 전체 경로를 써라.

```
D:\Tools\flutter\bin\flutter.bat
```

- Flutter 버전: 3.44.2
- OS: Windows
- 셸: PowerShell

PowerShell에서 현재 세션에만 PATH를 추가하려면:

```powershell
$env:Path += ";D:\Tools\flutter\bin"
```

**iOS 빌드는 이 PC에서 불가능하다** (맥 필요). 안드로이드까지만 진행한다.
Android SDK가 아직 설정되지 않았더라도 `flutter test`(단위 테스트)는
Dart만 있으면 동작한다. 테스트 기반 작업은 그대로 진행할 것.

---

## 절대 규칙

1. **서버 통신 금지.** 백엔드, REST API 호출, 로그인, 클라우드 저장 없음. 게임은 비행기 모드에서 완전히 동작해야 한다. 예외는 광고(`google_mobile_ads`)와 결제(`in_app_purchase`)뿐이다.
2. **게임 데이터는 전부 `assets/data/*.json`.** 코드에 수치를 하드코딩하지 않는다.
3. **밸런싱 수치를 임의로 바꾸지 않는다.** `GAME_DESIGN.md`의 확률표(3.4절)가 기준이다. 수치를 바꿔야 한다면 먼저 사용자에게 이유와 함께 물어본다.
4. **스코프를 넘지 않는다.** `GAME_DESIGN.md` 10절 "하지 않을 것" 목록에 있는 기능은 제안도 하지 않는다.
5. **주사위는 대미지가 아니라 판정에만 쓴다.** 눈금 합계가 대미지가 되는 코드를 작성하지 않는다.
6. **외부 에셋은 자유 라이선스만, 출처를 반드시 남긴다.** (2026-09-01 완화)
   - 쓸 수 있는 것: **CC0 / CC BY / OFL** 등 상업적 사용과 재배포가 허용된 에셋.
     game-icons.net(CC BY 3.0), Kenney(CC0), OpenGameArt의 CC0/CC BY 항목 등.
   - 도입하면 **반드시 `ATTRIBUTION.md`에 출처·작가·라이선스를 기록**한다.
   - **절대 금지**: 다른 상용 게임(딸깍 다이스 포함)의 이미지·폰트·사운드를
     추출하거나 베껴 쓰는 것. 참고 자료(`refs/`)는 **설계를 배우는 용도**이며
     그 그림 자체를 게임에 넣지 않는다.
   - 라이선스가 불분명하면 쓰지 말고 사용자에게 묻는다.

---

## 폴더 구조

실제 구조 (2026-09-01 기준). 그림 관련은 전부 `lib/art/`에 모여 있다.

```
lib/
  main.dart                # 앱 진입 + 테마 + 데이터/저장 로드
  art/                     # ★ 그림은 전부 코드로 그린다 (외부 이미지 없음)
    pixel_sprite.dart      # 스프라이트 자료구조 + 팔레트(7단계 명암) + 렌더러
    sprite_builder.dart    # 도형을 겹치면 명암·외곽선을 자동 계산해주는 생성기
    sprites_characters.dart# 마법사·슬라임·짐승·박쥐·버섯·해골·고블린·거구
    sprites_monsters.dart  # 기사·유령·골렘·용·악마·로브·날개
    sprites_items.dart     # 주문/유물 아이콘 17종 (16×16)
    sprite_map.dart        # 적/주문/유물 → 스프라이트+색조 매핑
    stage_backdrop.dart    # 전투 배경 (달·산·기둥·안개·통로, 층별 3테마)
    spell_fx.dart          # 마법 이펙트 (차징→비행→명중)
  core/
    dice.dart / check.dart / combo.dart / constants.dart
    battle.dart            # 전투 진행 (300줄 제한 때문에 아래로 분리)
    surge.dart             # 폭주 추첨·효과·소환수
    enemy_turn.dart        # 적 턴·강타 예고·보스 2페이즈
    cast_intensity.dart    # 시전 강도
    relic_powers.dart      # 유물 효과 합산
  models/                  # spell / enemy / relic / surge_event / effect
                           # run_state(한 판) / meta_state(영구)
  data/
    parser.dart            # Flutter 의존 없는 순수 파서 (CLI 도구도 씀)
    loader.dart            # assets 로드
  screens/
    title / map / battle / reward / result / meta
    battle_controller.dart # 전투 화면 상태 (provider)
    run_controller.dart    # 한 판 진행 상태 (provider)
    meta_controller.dart   # 영구 강화 상태 (provider)
  widgets/
    pixel_ui.dart          # 공통 팔레트·패널·버튼·막대·리본·뱃지
    battle_stage.dart      # 전투 무대 (원근 구도 + 이펙트 + 캐릭터)
    battle_fx.dart         # 화면 플래시·흔들림·피해 배너·족보 팝업
    dice_widget.dart / spell_card.dart / reward_banner_card.dart
    result_banner.dart / surge_popup.dart / shop_dialog.dart
  services/
    save_service.dart      # shared_preferences
    sfx_service.dart       # 효과음 + 진동
    (ad_service / iap_service — 7단계에서 만든다)
assets/
  data/*.json              # 주문70 / 폭주80 / 적30 / 유물40
  sfx/*.wav                # tool/gen_sfx.dart 가 합성해 만든다
tool/                      # CLI 도구 (Flutter import 금지)
  simulate.dart            # 밸런스 시뮬레이션
  meta_progression.dart    # 메타 페이싱 시뮬레이션
  sim_core.dart / gen_sfx.dart / mc_report.dart
refs/                      # 참고 자료 (게임에 넣지 않음 — 절대 규칙 6)
```

---

## 코딩 규칙

- 파일 하나가 300줄을 넘으면 분리한다
- 함수 하나가 40줄을 넘으면 분리한다
- 주석은 **한국어**로 쓴다
- 모델 클래스는 반드시 `fromJson` / `toJson`을 갖는다
- 매직 넘버 금지. `core/constants.dart`에 상수로 정의한다
- 상태 관리는 `provider`만 사용한다. 다른 라이브러리를 도입하지 않는다
- `print()` 대신 `debugPrint()`를 쓴다

---

## 테스트 규칙

**판정 엔진(`core/`)은 UI 없이 단위 테스트로 먼저 검증한다.** 이게 가장 중요하다.

`test/check_test.dart`에 최소한 아래를 포함한다:

- 3d6 합계 분포가 216가지 경우와 일치하는지 (몬테카를로 10만 회, 오차 1% 이내)
- DC 7 / 10 / 13 각각에서 4단계 결과 비율이 `GAME_DESIGN.md` 3.4절 표와 일치하는지
- 트리플은 판정값과 무관하게 항상 대성공인지
- 뱀눈은 항상 폭주인지
- 페어 +3 보정이 정확히 적용되는지
- 족보 우선순위(트리플 > 스트레이트 > 뱀눈 > 페어)가 지켜지는지
- 마력 축적 3칸에서 다음 시전이 무조건 대성공인지

**테스트가 통과하기 전에는 UI 작업으로 넘어가지 않는다.**

---

## 작업 순서 (현재 위치: 그래픽 개선)

1. ✅ 판정 엔진 + 단위 테스트 (UI 없음)
2. ✅ 게임 데이터 JSON 생성 (주문70/폭주80/적30/유물40)
3. ✅ 전투 화면
4. ✅ 지도 / 런 진행 / 보상
5. ✅ 메타 성장 + 저장
6. ✅ 손맛 (진동·효과음·연출)
7. ⬜ 광고 + 결제 — **사용자 지시로 보류 중**
8. ⬜ 안드로이드 빌드

**2026-09-01 — 기획서가 v2로 전면 개정됐다. 위 순서는 v1 기준이라 낡았다.**
지금은 **기획 단계**다. 두 구현 창(밸런스·그래픽)은 **멈춰 있다.**
새 지시서가 나오기 전까지 코드를 고치지 마라. v2에서 무엇이 살아남고
무엇이 바뀌는지는 `GAME_DESIGN.md` 12절을 볼 것.

각 단계가 끝나면 **실제로 실행해서 동작을 확인한 뒤** 다음으로 넘어간다.

---

## 작업 방식

- 큰 작업은 먼저 계획을 세워 사용자에게 보여주고 동의를 받은 뒤 실행한다
- 한 번에 한 단계씩 한다. 여러 단계를 뭉쳐서 처리하지 않는다

### ★ 지시서를 넘길 때의 출력 형식 (2026-09-04, 사용자 요청)

**기획 창이 작업 지시서를 발행했으면, 답변 맨 끝에 사용자가 그대로 복사해
붙일 수 있는 전달 문장을 낸다.** 그리고 **그 구현 창을 `/clear` 할지도 함께**
말한다.

```
> 이제 구현 창에 「`WORK_ORDER_SCREENS.md`를 확인하고 그대로 진행해」라고
> 전달하시면 됩니다.
> **구현 창은 `/clear` 하고 시작하세요.**
```

- **설명을 길게 붙이지 마라.** 전달 문장과 `/clear` 여부만
- `/clear` 판단은 「지시서 하나 = 창 하나」 원칙을 따른다 —
  **지시서가 바뀌면 새 창**이 원칙이고, 예외일 때만 이유를 한 줄 붙인다
- **사용자는 비전공자이고 두 창을 손으로 오간다.** 매번 전달 문장을 직접
  지어내게 하지 마라
- 코드를 고친 뒤에는 `flutter analyze`를 돌려 경고가 없는지 확인한다
- 잘 모르는 부분을 아는 척하지 않는다. 확인이 필요하면 확인한다고 말한다

### 토큰 절약 (중요 — 실제로 한도를 태운 적 있다)

이미지는 대화에 **누적**되고 매 요청마다 전부 다시 실려 간다. 그래서 비용이
장수에 비례하는 게 아니라 제곱으로 불어난다. 반드시 지킬 것:

- 화면 확인용 스크린샷은 **가로 480px로 축소한 사본을 만들어** 그것만 본다.
  원본(1080폭)은 4배 비싸다.
- 스크린샷은 **판단에 꼭 필요할 때만** 연다. 나머지는 파일로만 저장한다.
- 자동 조작할 때 **좌표를 찍어 맞히려 하지 마라.** 화면을 한 번 보고
  좌표를 판단해 **한 번만** 누른다. 난사하면 어디서 막혔는지 확인하느라
  스크린샷을 더 쓰게 된다. (2026-08-31 실제로 이렇게 한도를 태웠다)
- 대화가 무거워지면 사용자에게 **새 창 또는 초기화**를 권한다.

---

## LESSONS (작업하며 배운 것을 여기에 계속 추가할 것)

<!-- 실수했거나 사용자가 정정한 내용을 한 줄씩 기록한다. 다음 세션의 나에게 남기는 메모다. -->

- **한글 파일을 PowerShell 텍스트 명령으로 고치지 말 것.** `Get-Content`에
  `-Encoding UTF8`을 빼먹으면 한글이 전부 깨진다. 파일 수정은 Edit/Write
  도구만 쓴다. (2026-08-31, battle_test.dart 인코딩 파손 후 복구)
- **기획서 내부 모순은 확률표(3.4절)가 이긴다.** 3.2절 본문(대성공 DC+5)과
  3.4절 표(DC+3 기준)가 달랐고, 절대 규칙 3번에 따라 표를 채택했다.
  경계값은 `constants.dart`의 `kCritMargin` 하나만 바꾸면 전환된다.
- **CLI 도구(tool/)에서 lib 코드를 쓰려면 Flutter import를 피해야 한다.**
  `package:flutter/services.dart`가 섞이면 `dart run`이 죽는다. 그래서
  파서(`lib/data/parser.dart`)와 로더(`lib/data/loader.dart`)를 분리했다.
- **JSON 데이터는 만들 때마다 즉시 파싱 검증.** PowerShell
  `ConvertFrom-Json`으로 개수·id 중복·통계를 바로 확인하니 뒤에서
  터지는 일이 없었다.
- **네이티브 파일(windows/·android/ 등)에는 한글 주석 금지.** C++ 러너는
  /WX(경고=오류) + CP949 해석 때문에 UTF-8 한글 주석이 빌드 실패를 낸다.
  "주석은 한국어" 규칙은 Dart 파일에만 적용한다. (2026-08-31)
- **Windows에서 플러그인 첫 빌드는 개발자 모드 필요.** shared_preferences
  등 플러그인 추가 후 데스크톱 빌드가 symlink 오류로 실패하면 사용자에게
  개발자 모드(ms-settings:developers)를 켜달라고 안내한다.
- **`build` 폴더를 직접 지우지 말 것.** `native_assets`가 사라져 설치
  단계에서 빌드가 깨진다. 반드시 `flutter clean`을 쓴다. (2026-08-31)
- **위젯 테스트에서 버튼 텍스트는 `textContaining`으로 찾자.** 버튼에
  이모지를 붙이는 순간 `find.text` 정확 일치가 깨진다.
- **픽셀아트를 손으로 찍으면 반드시 검증 테스트를 함께 둔다.**
  (`test/sprite_test.dart`) 줄 길이가 한 칸만 어긋나거나 공백이 섞여도
  화면에서는 티가 잘 안 나는데, 테스트는 즉시 잡아낸다. 실제로 공백 29곳을
  이 방법으로 찾았다. (2026-08-31)
- **화면은 고정 높이 대신 `Expanded` + `LayoutBuilder`로 짠다.** 데스크톱
  창에서는 멀쩡해도 작은 기기(=위젯 테스트 기본 600px)에서 넘친다.
- **픽셀아트를 손으로 찍지 말고 도형으로 조합한다.** 손도트는 명암을 못 넣어
  단색 덩어리가 된다. `art/sprite_builder.dart`가 도형에서 명암·외곽선을
  자동 계산한다. 실루엣이 뭉치지 않게 **어깨를 모자보다 넓게** 같은
  형태 대비를 의식할 것. (2026-08-31)
- **배경 애니메이션이 계속 돌면 `pumpAndSettle`이 영원히 안 끝난다.**
  전투 화면 테스트는 `pump(시간)`으로 넘긴다.
- **`const` 목록 안에서 문자열 메서드를 쓸 수 없다.** 스프라이트를 런타임에
  생성하면 그 맵·리스트는 `final`이어야 한다.
- **손으로 계산한 값을 실측처럼 표에 넣지 마라.** 검토 18에서 「전투당 피해 =
  적 공격 × 전투 턴」으로 75%를 냈는데 **실측은 41.5%**였다. 적은 매 턴 공격하지
  않고(방어·회복 턴, 강타 예고 턴), 방어막이 막고, 전투가 중간에 끝난다.
  **계산값은 「추정」이라고 못 박고, 판단의 근거로 쓰기 전에 반드시 재라.**
  (2026-09-03, 검토 19 — 구현 창이 잡아냈다)
- **밸런스 지표에 생존 편향이 없는지 본다.** 「전투당 피해 %」는 개선될수록
  더 깊은 층이 표본에 섞여 **평균이 도로 올라간다.** 소모전은 **판 단위**
  (「상점 층 도달 시 남은 체력 %」)로 재야 한다. (2026-09-03, 검토 19)
- **한국어 이름·용어는 동음이의어를 확인하고 짓는다.** 게임 안에서는 한자 없이
  한글로만 보이므로, 읽는 사람이 다른 뜻으로 받아들이면 그걸로 끝이다.
  실제로 「용암 침(타액)」「눈덩이 사출」「수액 분출」「감정(鑑定→感情)의 각인」
  「사고(事故→思考)의 각인」이 걸렸고, 문서 용어 「정액값」도 「고정값」으로 고쳤다.
  **새 이름을 지을 때마다 소리 내어 한 번 읽어 볼 것.** (2026-09-02 사용자 지적)
- **적·보스 이름은 직함이 아니라 형상으로 짓는다.** 「갱도의 감독관」「탑의 문지기」
  「신전의 수호상」은 무슨 일을 하는지만 알려주고 **어떻게 생겼는지를 하나도
  알려주지 않는다.** 「무쇠 트롤」「목 없는 검은 기사」처럼 **이름을 들으면 화면에
  무엇이 설지 즉시 그려져야** 한다. (2026-09-01 사용자 지적)
- **`dart format`을 파일 전체에 돌리지 마라.** 이 저장소는 git이 아니라 되돌릴
  이력이 없다. 파일이 통째로 재정렬되면 **무엇이 이번 작업의 변경인지 사람이
  구분할 수 없어** 검토가 불가능해진다. **고친 부분만 손으로 정렬한다.**
  (2026-09-02, 검토 11 — `battle_screen.dart`가 통째로 재정렬됐다)
- **검산 조건에 「낱말이 0개」 같은 문자열 규칙을 걸지 마라.** 지시서가 한쪽에서는
  「주석에 이유를 남겨라」, 다른 쪽에서는 「그 낱말이 0줄이어야 한다」고 요구해
  서로 모순됐다. **검산은 동작을 걸어야지 문자열을 걸면 안 된다.**
  (2026-09-02, 검토 11)
- **검산 테스트는 「값이 유효한가」가 아니라 「값이 옳은가」를 봐야 한다.**
  「테마가 4종 중 하나면 통과」는 11지역이 `tower`여야 하는데 `dungeon`인 것을
  못 잡았다. **허용 목록 검사로는 부족하고 기대값 표와 대조해야** 잡힌다.
  (2026-09-02, 검토 09 2차)
- **지시서를 쓰기 전에 「개수·범위를 단언하는 기존 테스트」를 전수 조사하라.**
  데이터를 옳게 옮길수록 반드시 깨지는 낡은 단언이 있다
  (`loader_test.dart`의 `enemies.length == 30`, `boss.hp inInclusiveRange(85,95)` 등).
  **미리 찾아 지시서에 「이건 고쳐도 된다」고 적어 두면** 구현 창이 헤매지 않는다.
  지시서 A·B에서 연속으로 놓쳤다. (2026-09-02, 검토 08·09)
- **문서의 값을 고쳤으면 그 값을 인용한 다른 절을 함께 훑어라.** 12지역 이름을
  `STAGES.md`에서 「고룡의 둥지」로 바꿨는데 `ENEMIES.md`의 표 두 곳에 「미지의 던전」이
  남아 있었다. 구현 창이 발견해 보고했다. (2026-09-02, 검토 09)
- **장비 에셋 규격은 「캐릭터에 붙여 보고」 정한다.** 지팡이를 따로 놓고 보면
  화려한 게 좋아 보이지만, **2.5등신 캐릭터에 붙이면 얼굴을 가린다.**
  실제로 합성해 보니 **헤드가 위에서 44% 아래로 내려오면 얼굴을 덮고,
  몸통이 길이의 20%를 넘으면 몸을 덮었다.** 반대로 **지팡이를 신장보다 8% 길게**
  하면 헤드가 머리 위 빈 공간으로 솟아 폭 322px짜리도 안 가렸다.
  **규격 숫자를 문서에 적기 전에 반드시 합성해 볼 것.** (2026-09-04)
- **에셋 요청서에 적지 않은 것은 「최소」로 나온다.** 지팡이 15종의 등급 차이를
  **헤드 크기로만** 정의했더니, 몸통은 15종 전부 똑같은 가는 막대가 됐다.
  실측하니 **전설(길이의 3.1%)이 v1 일반 등급(4.9%)보다 가늘었다.**
  **한 부위로만 등급을 정의하지 말고, 부위별로 값을 다 적을 것.**
  (2026-09-04, 지팡이 v2·v3 실패)
- **숫자 규격을 적기 전에 「그 숫자가 무엇을 잘라내는지」 확인하라.**
  「색 24~32색」→ 정확히 24색 양자화로 볼륨 소멸.
  「캔버스 예: 400×800」→ 320×660으로 나와 질감 담을 자리 소멸.
  「헤드 전체 폭 150px」→ 궤도·별처럼 **틈이 많은 장식**까지 잘려 화려함 소멸.
  세 번 다 **생성기는 규격을 100% 지켰다.** 잘못은 전부 요청서에 있었다.
  (2026-09-04)
- **「막힌 덩어리」와 「흩어진 요소」를 나눠 제한하라.** 얼굴을 가리는 것은
  bounding box가 아니라 **연속된 불투명 덩어리**다. v1 전설 지팡이는 폭 352px이지만
  최대 덩어리는 138px으로 얼굴(169px)보다 작았다. **폭 하나로 묶어 제한하면
  화려함만 죽는다.** (2026-09-04)
- **내 측정이 무엇을 말하는지 한 번 더 물어라.** `wizard_body.png`를 32색으로
  양자화했을 때 오차가 4.4/255라서 「32색이면 충분」이라 적었는데, 원본의 실제
  고유색은 **53,269개**였다. **「양자화 오차가 작다」와 「팔레트가 작다」는 다른 말이다.**
  (2026-09-04)
- **판타지 관습을 거스르면 유저가 먼저 알아챈다.** 용을 중반 지역 보스로 뒀더니
  "용은 끝판왕 느낌인데 일찍 나온다"는 지적이 바로 나왔다. 테마가 맞아도
  **관습이 정해 둔 격(格)이 우선**이다. (2026-09-01)
- **밸런스 목표를 쓸 때 「곡선의 마지막 점」을 따로 생각하라.** 「종반(11~12) 상한
  25~45%」라고 한 줄로 묶었더니, 11지역이 밴드 바닥(27.7%)에 앉는 순간
  **12지역더러 앞 지역과 같아지라는 뜻**이 됐다. 그런데 옆에 있는 조건 A는
  **단조 감소**를 요구했다. **단조 감소하는 곡선의 마지막 점은 앞 점과 같은
  밴드에 들어갈 수 없다.** 배율·층수를 다섯 번 만져도 안 풀린 이유가 이것이다.
  → B1(11지역) · B2(12지역)로 쪼갰다. (2026-09-04, 검토 22)
- **구현 창의 조정 제안은 「옆 칸 값」을 확인하고 받아라.** 보고서 22가
  「12지역 `hp_scale`을 3.1~3.2로」 권했는데 **11지역이 이미 3.20**이었다.
  그대로 냈으면 최종 지역 적이 앞 지역보다 약해지고 단조 증가 테스트가 깨졌다.
  **구현 창은 자기가 잰 칸만 보고, 옆 칸을 안 본다.** (2026-09-04)
- **「측정을 한 번 더 하고 확정하자」를 습관으로 만들지 마라.** 각인 수치표를
  네 번 미뤘는데, 근거값(전투당 굴림)은 **측정 18부터 다섯 번 연속
  4.39~4.65에 머물러 있었다.** 값이 안정됐으면 다른 조건이 미달이어도
  **그 값에 의존하는 표는 확정할 수 있다.** (2026-09-04, 검토 22)
- **에셋 검수는 「그림을 보기 전에 숫자부터」.** 규격에서 떨어진 것은 눈으로
  볼 필요가 없고, 스크린샷은 대화에 누적돼 비용이 제곱으로 는다.
  재는 방법을 `art_raw/README.md`에 절차로 박아 뒀다. (2026-09-04)
- **관문의 「수」를 줄이는 것과 관문을 「넘을 힘」을 키우는 것은 다르다.**
  12지역 층수를 10 → 8로 줄였더니 도달률이 15%p 열렸는데 완주율은 2.3%p만
  올랐다. **층에 붙어 있던 회복(전투 승리·상점)도 함께 줄어** 보스 앞 체력이
  3.5%p 내려갔기 때문이다. 같은 자리에 배율을 −0.15 줬더니 **도달률과 남은
  체력이 함께 올라** 완주율이 5.1%p 올랐다. **후반 완주율은 배율부터 보라.**
  (2026-09-04, 측정 22 vs 23)
- **측정 조건을 지역마다 다르게 하면 「합산 지표」의 저울이 깨진다.**
  12지역만 판 수를 300 → 1000으로 올렸더니 전투당 굴림이 4.49 → 4.84로
  오른 것처럼 보였는데 **게임은 하나도 안 바뀌었다.** 12지역 비중이 커진 것뿐이다.
  → **표본을 늘릴 때는 전 지역을 같이 늘려라.** 실행 시간 1분보다
  **다음 창이 표를 오독할 위험**이 비싸다. (2026-09-04, 검토 23)
- **인수인계 문서의 「아직 안 한 것」은 결론보다 빨리 썩는다.** 끝난 일은
  누구나 ✅를 붙이는데, **안 끝난 일은 그 단계가 지나가면 아무도 안 읽는
  죽은 문장**이 된다. 「순서 7에 함께 넣을 것」 표가 여섯 지시서 동안
  방치됐고, 확인해 보니 1건은 이미 구현, 2건은 여전히 미구현이었다.
  → **새 창은 시작할 때 `HANDOFF.md`의 미완료 항목을 코드로 검증하고
  그 자리에서 고친다** (「새 창을 연 직후에 반드시 할 것」).
  (2026-09-04, 사용자가 「흐름을 잃고 반복할까 걱정된다」고 물어서 발견)
- **「어둡게 한다」를 명암 막(scrim)으로 구현하면 바탕색마다 결과가 다르다.**
  잠긴 지역 카드에 같은 농도의 검은 막을 씌웠더니, **밝은 주황 테마(사막)가
  어두운 초록 테마(숲)보다 여전히 밝아** 「잠긴 칸이 열린 칸보다 눈에 띄는」
  화면이 나왔다. 막의 농도를 올려도 안 풀린다 — 테마마다 필요한 농도가 다르다.
  → **상태(잠김/열림)를 색으로 나타낼 때는 막을 덮지 말고 「그 상태 전용 색」을
  따로 준다.** (2026-09-04, 검토 24)
- **화면은 「가장 작은 데이터」로 한 번 봐라.** 층수가 3~10으로 가변인데
  10층 기준으로 짜면 **3층에서 화면 절반이 빈다.** 그리고 도입 지역이 3~7층이라
  **유저가 초반 내내 그 빈 화면을 본다.** 가변 데이터를 그리는 화면은
  **최소·최대 양쪽 스크린샷을 반드시 받아라.** (2026-09-04, 검토 24)
