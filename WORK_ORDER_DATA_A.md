# 작업 지시서 A — 폭주·주문 데이터 반영 (구현 창용)

발행: 2026-09-02, 기획 창. **사용자 승인 완료된 작업이다.**

근거 문서: **`SURGE_DESIGN.md`** (폭주 80종) · **`SPELL_CIRCLES.md`** (서클 분류)

> **읽는 순서**: 이 문서 → `SURGE_DESIGN.md` 1~4절 → 작업 시작.
> 80종 전문과 서클 배정표는 근거 문서에 있다. **이 지시서에 옮겨 적지 않았다.**
> 두 곳에 같은 표가 있으면 반드시 어긋나기 때문이다. **근거 문서를 보고 옮겨라.**

> ### 🪟 창 운영
> **이 지시서는 새 창(`/clear` 후)에서 시작한다.** 앞 대화가 필요 없다 —
> 필요한 판단은 전부 근거 문서에 적혀 있다.
> **스크린샷을 볼 일이 없는 작업**이므로 한 창에서 끝까지 간다.
> (`CLAUDE.md` 「새 창이냐 이어서냐」 참조)

---

## 0. 이 작업의 목적 (한 줄)

**폭주가 "재미있는 문장 + 곁다리 효과"에서 "전투 결과의 6갈래"로 바뀌었고,
주문에 서클 1~9가 생겼다.** 그 두 가지를 데이터에 반영한다.

## 0-1. 절대 지킬 것

- **JSON 데이터만 고친다. `lib/` 코드는 한 줄도 고치지 마라.**
  새 효과 4종(`self_damage_spell` 등)의 **구현은 다음 지시서(C)의 일이다.**
  `core/surge.dart`의 `switch`에는 `default`가 없으므로 **모르는 효과 타입은
  조용히 무시된다. 앱이 죽지 않는다.** 그 상태가 정상이다.
- **`rarity` 필드를 지우지 마라.** `run_controller.dart` · `reward_screen.dart` ·
  `meta_screen.dart` · `sim_core.dart` · `loader_test.dart` 5곳이 쓰고 있다.
  `circle`을 **추가**하는 것이지 교체가 아니다.
- **수치를 임의로 바꾸지 마라** (절대 규칙 3). 이 지시서에서 바뀌는 수치는
  **승인받은 `gaia_wrath` 하나뿐이다.**
- 한글 JSON을 **PowerShell 텍스트 명령으로 고치지 마라.** Edit/Write 도구만 쓴다
  (`CLAUDE.md` LESSONS 1번 — 실제로 인코딩이 깨진 적 있다).
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것.

---

# 작업 1 — `assets/data/surges.json` 80종 전면 교체

## 1-A. 무엇을 하나

기존 80종을 **전부 버리고** `SURGE_DESIGN.md` 5절의 80종으로 교체한다.
id·이름·문장·가중치·태그·효과가 전부 바뀐다.

## 1-B. 항목 형식

기존과 같다. `category` 값만 6종으로 바뀐다.

```json
{
  "id": "staff_backflow",
  "name": "지팡이 역류",
  "text": "마력이 거슬러 오른다",
  "category": "backlash",
  "weight": 35,
  "tags": ["any"],
  "effects": [{ "type": "self_damage_spell", "value": 100 }]
}
```

| `category` | 갈래 |
|---|---|
| `backlash` | 역류 |
| `misfire` | 오발 |
| `fizzle` | 불발 |
| `amplify` | 증폭 |
| `summon` | 소환 |
| `chain` | 연쇄 |

## 1-C. 특히 주의할 것

1. **불발 11종은 `"effects": []` 가 정상이다.** 비어 있다고 빠뜨린 게 아니다
2. **태그는 `any` + 속성 5종(`fire` `frost` `arcane` `shadow` `nature`)만 쓴다.**
   기존의 `explosion` `life` `sky` `earth` `shield` `rune` `light` `void` `time`
   `mind` `curse` `swarm`는 **폭주 쪽에서 전부 사라진다.**
   `spells.json`의 부가 태그는 **그대로 둔다** (그 태그를 가진 폭주가 없을 뿐이다)
3. 가중치는 **한 자리도 틀리면 안 된다.** 6갈래 비율이 가중치로만 보장된다

---

# 작업 2 — `assets/data/spells.json` 서클 필드 추가

## 2-A. `circle` 필드 추가 (70종 전부)

`SPELL_CIRCLES.md` 2절의 배정표대로 `circle`(정수 1~9)을 넣는다.
**`rarity`는 그대로 둔다.**

```json
{ "id": "fireball", "name": "화염구", "circle": 2, "rarity": "common", ... }
```

## 2-B. ✅ 승인된 수치 변경 1건

**`gaia_wrath`(대지의 분노)의 `dc_modifier`를 `2` → `3`으로 바꾼다.**

9서클 5종 중 자연만 +2라 자연이 지배 속성이 된다. **2026-09-01 사용자 승인 완료.**
**이 하나 외에 어떤 수치도 건드리지 마라.**

---

# 작업 3 — 검산 테스트 추가

**데이터가 맞는지는 눈으로 보지 말고 테스트로 잡는다.**
(`CLAUDE.md` LESSONS — 픽셀아트 공백 29곳을 이 방법으로 찾았다)

## 3-A. `test/surge_data_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | 폭주가 **정확히 80종**이고 id 중복이 없다 |
| 2 | `category`가 **6종 중 하나**다 |
| 3 | 갈래별 종수 = 역류 20 · 오발 18 · 불발 11 · 증폭 11 · 소환 11 · 연쇄 9 |
| 4 | 갈래별 가중치 합 = **400 / 320 / 240 / 240 / 240 / 160** (총 1600) |
| 5 | `tags`가 **`any` 또는 속성 5종**만 쓴다 |
| 6 | `any` 항목 40종의 가중치 합 = **1000**, 속성별 8종의 합 = **각 120** |
| 7 | ★ **주문 70종 각각에 대해** 후보 풀(= any + 그 주문 속성)의 갈래 비율이<br>**25 / 20 / 15 / 15 / 15 / 10%와 정확히 일치**한다 |
| 8 | `fizzle` 11종은 `effects`가 비어 있다 |

> **7번이 이 작업의 핵심 검사다.** 오차 허용 없이 정확히 일치해야 한다.
> 어긋나면 가중치나 태그를 잘못 옮긴 것이다.

## 3-B. `test/spell_circle_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | 70종 전부 `circle`이 있고 **1~9 범위**다 |
| 2 | 서클별 종수 = **10·10·10·10·10·5·5·5·5** (합 70) |
| 3 | ★ **모든 서클에 5속성이 전부 존재**한다 |
| 4 | `circle`↔`rarity` 대응이 **100% 일치** (1~3=common / 4~6=rare / 7~9=epic) |
| 5 | 9서클 5종의 `dc_modifier`가 **전부 3**이다 |

## 3-C. 기존 테스트

`loader_test.dart` 등 **139개가 계속 통과해야 한다.** 깨지면 데이터를 잘못 옮긴 것이다.
(`rarity`를 지우면 깨진다 — 0-1 참조)

---

# 마무리

1. `flutter analyze` 경고 0
2. `flutter test` 전부 통과
3. **`reports/NN_데이터A_폭주주문.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - 3-A 7번 검사 결과(주문 70종의 갈래 비율)를 **표로 넣을 것**
   - 옮기다 발견한 근거 문서의 오류가 있으면 **고치지 말고 보고서에 적어라**
4. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ 새 효과 4종 구현 (지시서 C)
- ❌ 폭주 팝업 UI 6갈래 색상 (지시서 C)
- ❌ `variants.json` · `regions.json` · `stages.json` (지시서 B)
- ❌ 보스 추가 (지시서 B)
- ❌ 밸런스 수치 조정 — 승인된 `gaia_wrath` 하나 외에는 전부 금지
