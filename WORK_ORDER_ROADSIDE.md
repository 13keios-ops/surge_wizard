# 작업 지시서 — 걸어가는 연출을 **레이어 방식으로 교체**

발행: 2026-09-13, 기획 창.
근거: `GAME_DESIGN.md` **6.7절 ①** (2026-09-12 개정) · `ART_REQUEST_LAYERS.md`.

> ### 🪟 창 운영
> **구현 창. `/clear` 하고 시작한다.** 에셋이 없어도 끝까지 갈 수 있는 작업이다.

---

## 0. 한 줄

**배경 판을 미는 지금 코드를 걷어내고, 길 옆 물체가 원근 경로를 따라
내려오며 커지게 바꾼다.**

---

## 1. 왜 갈아엎나 — 먼저 읽어라

`WORK_ORDER_BG_SCROLL.md`로 만든 **그림 전체를 위아래로 미는 방식은 폐기됐다.**
테스트가 통과하고 있어서 멀쩡해 보이지만 **기제가 틀렸다.**

원근 그림에서 물체의 크기는 **그림의 어느 높이에 있느냐**로 정해진다. 그림을 위로
밀면 **작게 그려진 먼 나무가 가까운 자리로 내려와** 크기가 틀린 것이 드러난다.

참고 게임 실측(`refs/dicero` 03 대 16, 밝기 보정 후)에서도 **달·산·지평선·길·보스
실루엣은 완전히 같았고 좌우 기둥과 크리스탈만 자리와 크기가 달랐다.**

🔴 **배경 판은 고정이다. 움직이는 것은 길 옆 물체뿐이다.**

### 겹의 순서 (게임이 이렇게 쌓는다)

| 뒤 → 앞 | 무엇 | 이번 지시서 |
|---|---|---|
| 1 | 성 (지역별 목표 건물) | ⬜ 다음에 |
| 2 | 보스 음영 + 빛나는 눈 | ⬜ 다음에 — **이번 범위 아님** |
| 3 | **길 옆 물체** (나무·바위·덤불) | ★ **이번에 만든다** |
| 4 | 배경 판 (하늘·먼 산·지평선·길) | ★ **고정으로 되돌린다** |
| 5 | 몬스터 · 캐릭터 | 그대로 |

---

## 2. 지금 코드에서 무엇을 남기고 무엇을 지우나

| 대상 | 처리 |
|---|---|
| `backdropProgress()` · `backdropAlignment()` (`art_sprite.dart:112·116`) | 🔴 **지운다.** 판은 안 움직인다 |
| `ArtBackdropView` 의 슬라이드 (`_slide` 컨트롤러 · 애니메이션) | 🔴 **지운다.** `Alignment.bottomCenter` 고정으로 되돌린다 |
| `test/backdrop_slide_test.dart` | 🔴 **지운다** (대체 테스트를 새로 쓴다) |
| **`WalkingWizardView`** · `kArtWizardWalk` · `kArtWizardWalkReady` · `wizardWalkFrame()` | ✅ **그대로 남긴다.** 캐릭터가 걷는 것은 맞는 연출이다 |
| `kBackdropSlide` (900ms, `layout.dart:124`) | ✅ **남기되 이름을 `kWalkTransition` 으로** 바꾼다. 배경이 아니라 걸음의 길이다 |

**남는 것과 지우는 것을 헷갈리지 마라.** 캐릭터는 걷고, 배경 판은 안 움직이고,
길 옆 물체가 흐른다.

---

## 3. 원근 경로 — 이것이 이번 작업의 전부

물체 하나는 **거리(distance)** 하나만 들고 있으면 된다. 나머지는 전부 계산이다.

```
t = 0  → 소실점 (아주 멀다, 아주 작다)
t = 1  → 화면 아래 (가장 가깝다, 가장 크다)

화면 y      = lerp(소실점y, 바닥y, t)
크기 배율   = t
중심에서 x  = ±(바닥에서의 좌우 거리) × t
```

**t에 선형으로 비례하는 것이 진짜 원근이다.** 화면 y와 크기가 둘 다 거리의
역수에 비례하므로, t를 그렇게 잡으면 물체가 **소실점에서 뻗어 나오는 직선**을
따라 움직인다. 별도 보정이 필요 없다.

### 거리 → t

```
t = k / distance          (k 는 상수)
```

**거리를 등속으로 줄이면** t가 뒤로 갈수록 빨라진다. 가까워질수록 빨리 스쳐
가는 것이 실제로 그렇게 보인다. **t를 직접 등속으로 올리지 마라** — 멀리서 너무
빠르고 가까이서 너무 느려 어색하다.

### 한 스테이지에 얼마나 가나

한 층을 넘길 때 **거리를 일정량 줄인다.** 그 값은 `core/constants.dart`에
상수로 둔다. **매직 넘버 금지.**

- 한 지역(8~10층)을 걷는 동안 물체가 **두세 번 지나가는 정도**가 기준이다
- `t > 1` 이 된 물체는 **맨 뒤로 되돌린다** (개수는 항상 일정)

### 좌우 배치

- 물체는 **길 바깥쪽**에만 선다. 가운데 세로 띠는 비운다 (캐릭터 자리)
- 좌우 각각 **2~3개**면 충분하다. 더 넣지 마라
- 🔴 **같은 (지역, 층)이면 항상 같은 배치**여야 한다. 화면을 다시 그릴 때마다
  자리가 바뀌면 안 된다 → **(지역id, 층)으로 시드를 만든 난수**를 쓴다

---

## 4. 에셋이 없다 — 그래도 끝까지 간다

길 옆 물체 원화(`side_tree_big` · `side_rock` · `side_bush`)는 **아직 없다**
(`ART_REQUEST_LAYERS.md`로 발주 예정).

🔴 **에셋을 기다리지 마라.** 없으면 **단색 사각형**을 그려서 움직이게 한다.
그림이 들어오면 **그리는 부분 한 줄만** 바꾸면 되게 짜라.

- 있으면 원화, 없으면 사각형 — `kArtWizardWalkReady` 와 같은 방식의 한 줄 분기
- 사각형이라도 **경로·크기·속도는 진짜와 똑같이** 움직여야 한다. 그래야 지금
  판정할 수 있다

---

## 5. 🔴 파일 길이

| 파일 | 지금 | 규칙 |
|---|---|---|
| `lib/art/art_sprite.dart` | **274줄** | 300 |
| `lib/widgets/battle_stage.dart` | **298줄** | 300 |

**둘 다 코앞이다.** 길 옆 물체는 **새 파일로 빼라** (`lib/art/roadside.dart` 등).
`battle_stage.dart` 에는 **겹 하나를 얹는 한 줄**만 들어가야 한다.

---

## 6. 테스트 (필수)

`test/roadside_test.dart` 를 새로 쓴다. **UI 없이 도는 것부터.**

| 무엇 | 기대 |
|---|---|
| t=0 | 소실점 위치 · 크기 배율 0 에 가깝다 |
| t=1 | 화면 바닥 · 크기 배율 1 |
| 단조성 | t가 커지면 y도 크기도 **단조 증가** |
| 좌우 | t가 커질수록 중심에서 **멀어진다** |
| 거리 등속 | 같은 시간 동안 **가까울수록 t가 더 많이 는다** |
| 재활용 | t>1 이면 맨 뒤로. **개수가 변하지 않는다** |
| 🔴 결정성 | 같은 (지역, 층)은 **항상 같은 배치** |
| 🔴 `floors == 1` | 0으로 나누지 않는다 |

`flutter test` **전부 통과**해야 한다 (지금 328개, 슬라이드 테스트 삭제분 제외).
`flutter analyze` 경고 0.

---

## 7. 보고서

🔴 **작업을 시작할 때** `reports/36_길옆물체.md` 를 먼저 만든다.
양식은 `reports/_TEMPLATE.md`. **한 덩어리 끝날 때마다 한 줄씩 덧붙인다.**
끊기면 「다음: ~」 한 줄을 남긴다.

사용자에게는 **"보고서 reports/36_길옆물체.md 작성 완료" 한 줄만** 말한다.

---

## 8. 하지 않을 것

- **보스 음영·성 레이어** — 다음 지시서다. 이번에 건드리지 마라
- 배경 판을 다시 움직이게 하는 것
- 네비게이션 구조 변경
- 밸런스 수치·데이터 파일
- 물체를 **무한히 늘리는 것** — 좌우 2~3개씩이면 충분하다
