# 작업 지시서 H — 시뮬 재측정 (지역 12 × 난이도 3)

발행: 2026-09-02, 기획 창.

근거 문서: `ENEMIES.md` 5.3·6·8절 · `STAGES.md` 1절 · `ENGRAVINGS.md` 3·4절 ·
`SPELL_CIRCLES.md` 부록

> ### 🪟 창 운영
> **새 창(`/clear` 후).** `tool/`과 `lib/core/stage_runner.dart` 한 곳만 만진다.
> **스크린샷이 필요 없다.** 출력은 파일로 남기고 대화에 통째로 붙이지 마라.

---

## 0. 목적 (한 줄)

**진행 구조가 들어왔으니, 이제 「전투당 굴림 수」·「층당 턴 수」를 우리가 실제로 만들
게임에서 다시 잰다.** 각인 수치표와 층수 상한이 이 값을 기다리고 있다.

## 0-1. ⚠ 이번에도 「측정」이다. 조정이 아니다

> **밸런스 수치를 하나도 바꾸지 마라.** 이상해 보여도 **보고서에 적어라.**
> 조정은 기획 창이 결과를 보고 별도 지시서로 낸다.
> 유일한 예외가 **작업 1**(지시서 D가 빠뜨린 규칙을 채우는 것)이다.

## 0-2. ★ 이번 측정의 핵심 문제 — 플레이어가 성장하지 않는다

지역 12 일반 적은 HP 225, 보스는 480이다. 그런데 **레벨·패시브·장비가 아직 없어
플레이어는 체력 20 · 화염구 7에 머문다.** 그대로 재면 **후반은 전부 전멸**이라
아무것도 알 수 없다.

**그래서 「가정 성장 프로필」을 넣고 잰다** (작업 2). 이것이 `ENEMIES.md` 8절의
전제 **「체력 20 → 70」이 실제로 성립하는지**를 검증하는 방법이다.

> **이 프로필은 설계 확정이 아니라 측정용 발판이다.**
> 측정 결과가 「이 곡선이면 종반이 불가능하다」로 나오면 **곡선을 고칠지
> 적을 고칠지는 기획 창이 정한다.**

---

# 작업 1 — 지시서 D가 빠뜨린 규칙 하나 (측정 전에)

## 1-A. 난이도별 변종 보정

`ENEMIES.md` 5.3절 마지막 줄이 정한 규칙인데 **지시서 D가 옮기지 않았다.**

> **하드는 「고대의」 +10%p, 데스는 +20%p를 더하고 같은 양을 「기본」에서 뺀다.
> 기본이 0 밑으로 내려가면 다음으로 비중이 큰 변종에서 뺀다.**

- `lib/core/stage_runner.dart`에 함수 하나 (10줄 안팎)
- **`ancient` 가중치가 0인 지역(1~7)에도 적용한다.** 없던 변종이 생기는 것이 맞다 —
  하드·데스는 강해진 뒤 다시 도는 난이도이고, **금빛 적이 섞이는 것이
  「난이도가 올랐다」는 가장 빠른 신호**다
- 보정 후에도 **가중치 합이 100**이어야 한다

### 테스트 (`progression_test.dart`에 덧붙일 것)

| # | 검사 |
|---|---|
| 1 | 보정 후 12지역 × 3난이도 = **36가지 전부 합이 100** |
| 2 | 하드에서 `ancient`가 **원래 + 10**, 데스는 **+20** |
| 3 | 1지역(원래 `ancient` 0)에서도 하드는 10, 데스는 20이 된다 |
| 4 | 「기본」이 모자란 지역에서 **다음으로 비중이 큰 변종에서 빠지고**, 음수가 안 생긴다 |

> ⚠ **보스 변종은 이 보정과 무관하다.** 난이도로 고정돼 있다
> (`normal`/`shadow`/`ancient`). 건드리지 마라.

---

# 작업 2 — ✅ 가정 성장 프로필 (기획 창 지정 · 〔측정용 가정〕)

`tool/`에만 둔다. **`lib/`에 넣지 마라.** 게임 코드가 아니라 측정 발판이다.

지역 진입 시점의 권장 레벨(`STAGES.md` 4절)을 기준으로 잡았다.
체력은 **Lv1에 20 → Lv99에 70**의 선형 보간, 서클은 `SPELL_CIRCLES.md` 부록의
슬롯 개방표를 따랐다.

| 지역 | 진입 Lv | **최대 체력** | **최고 서클** | 최대 마나 |
|---|---|---|---|---|
| 1 | 1 | **20** | 1 | 3 |
| 2 | 8 | **24** | 1 | 3 |
| 3 | 16 | **28** | 2 | 3 |
| 4 | 25 | **32** | 3 | 3 |
| 5 | 35 | **37** | 4 | 4 |
| 6 | 45 | **42** | 5 | 4 |
| 7 | 55 | **48** | 5 | 4 |
| 8 | 65 | **53** | 6 | 4 |
| 9 | 72 | **56** | 6 | 5 |
| 10 | 80 | **60** | 7 | 5 |
| 11 | 87 | **64** | 7 | 5 |
| 12 | 94 | **67** | 8 | 5 |

**손패 구성**: 그 지역의 **최고 서클 주문 3장**을 쓴다.
`spells.json`의 `circle` 필드로 고르고, **속성이 겹치지 않게** 3장을 뽑는다
(같은 속성만 쓰면 폭주 태그가 한쪽으로 쏠린다).

- 유물·패시브·각인은 **넣지 않는다.** 변인을 늘리면 원인을 못 가린다
- 리롤은 기본 규칙 그대로 (최대 3회, 비용 1/2/3)

---

# 작업 3 — 측정

`tool/measure.dart`를 확장하거나 `tool/measure_stage.dart`를 새로 만든다.
**`simulate.dart`는 건드리지 마라** (기준선).

- **지역 12 × 난이도 3 = 36조합**, 조합당 **300판**
- 각 판은 그 지역의 **스테이지 하나**를 돈다.
  대표 스테이지는 **그 지역의 마지막(보스) 스테이지**로 고정한다
  (보스 전투가 포함돼야 보스 턴 수를 잰다)
- 시드 **2개**로 돌려 흔들림을 확인한다
- 출력은 **`reports/data/measure2_output.txt`**

## 내보낼 표 5가지

### 3-A. ★ 전투당 굴림 횟수 — 각인 수치표가 기다리는 값

| 지역별 · 난이도별 | 전투당 평균 굴림 | 전투당 판정 | 판정당 리롤 |

**그리고 전체 평균으로 각인 기대 적립을 다시 계산해 표로 낼 것**
(측정 12의 4-A2와 같은 형식 — `E[min(X, 상한)]`, X는 이항분포).

| 조건 유형 | 1굴림 확률 | 상한 | **실측 기대 적립** | 전설 기대 보너스 | 문서 기재(13~19%) |

### 3-B. ★ 전투당 턴 수 — 층수 상한이 기다리는 값

| 지역별 · 난이도별 | 일반 전투 평균 턴 | 보스 전투 평균 턴 |

⚠ **초로 환산하지 마라.** 턴당 몇 초인지는 사람이 플레이해야 안다.

### 3-C. ★ 어디서 막히는가 — 지역 × 난이도 완주율

| 지역 | 보통 | 하드 | 데스 |

**이 표가 `ENEMIES.md` 8절의 「체력 20 → 70」 전제를 검증한다.**
`GAME_DESIGN.md` 6.2절의 의도와 대조해 적을 것:

| 구간 | 지역 | 의도 |
|---|---|---|
| 도입 | 1~3 | **무과금으로 쾌적하게** |
| 중반 | 4~7 | 파밍이 필요해진다 |
| 종반 | 8~12 | 오래 누적해야 한다. **단 「불가능」이 아니라 「오래 걸림」** |

> **완주율 0%가 나오는 칸이 있으면 그대로 적어라.** 그게 이 측정의 결론이다.
> **적 수치를 낮추지 마라.**

### 3-D. 변종 등장 비율 (작업 1 검증)

지역 × 난이도별로 6변종이 **실제로 가중치대로 나오는지.**
특히 **하드/데스에서 「고대의」가 +10 / +20%p** 만큼 늘었는지.

### 3-E. 폭주 자해 상한 — 이제 체력이 지역마다 다르다

측정 12에서는 체력 20 고정이라 상한이 8 고정이었다.
**이번엔 체력이 20~67로 오르므로 상한도 8~26으로 오른다.**

| 지역 | 최대 체력 | 상한 | 자해 폭주 수 | **잘린 비율** |

> **검토 10의 미결(「후반 역류가 고정값이 되는가」)이 여기서 답이 나온다.**
> 잘린 비율이 후반으로 갈수록 **떨어지면** 상한이 제 역할만 하는 것이고,
> **그대로거나 오르면** % 방식이 무의미해진다는 뜻이다.

---

# 작업 4 — 정리

## 4-A. `BALANCE.md` 갱신

- 기존 내용을 지우지 말고 **「6차 — 진행 구조 반영」** 절을 맨 위에 추가
- 3-A ~ 3-E를 표로
- **해석은 짧게, 판단은 하지 마라**

## 4-B. 테스트

`test/progression_test.dart`에 작업 1의 4항목을 덧붙인다.
**측정 코드 자체에는 테스트를 새로 만들지 않아도 된다** — 측정 12의
`measure_sanity_test.dart`가 이미 굴림 세기와 자해 상한을 검사한다.

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드에서 정상 종료, `reports/data/measure2_output.txt` 생성
3. **`reports/14_시뮬재측정.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - **3-C(지역 × 난이도 완주율)를 맨 앞에** 둘 것. 이번 측정의 결론이다
   - 측정 12의 값과 **나란히 놓아 무엇이 얼마나 변했는지** 보이게 할 것
   - 두 시드의 차이를 적을 것
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
4. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ **밸런스 수치·확률표·데이터 조정** — 승인된 수치 변경은 **없다**
- ❌ 가정 성장 프로필을 `lib/`에 넣기 (측정 발판이지 게임 코드가 아니다)
- ❌ 턴 수를 초로 환산하기
- ❌ **보스 등장 방식 3종**(전조→본체·호위·연전) — 별도 지시서다.
  이번 측정은 **보스 1체 기준**이고, 그 사실을 보고서에 적을 것
- ❌ 화면 · 선택 UI (지시서 G)
- ❌ 레벨·패시브·각인·장비의 **실제 구현** (가정 프로필로 흉내만 낸다)
- ❌ `simulate.dart` 수정
