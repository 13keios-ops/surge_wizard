# 그래픽 개선 계획

> ## ⛔ 폐기됨 (2026-09-02) — 읽지 마라
> **v1 픽셀아트 전제로 쓰인 5단계 계획이다.**
> 사용자가 **「GPT 원화를 그대로 크게 쓴다」**로 결정했다.
> → **`UI_DESIGN.md`가 대체한다** (화면 구도 · 자산 규격 · 자산 우선순위 포함).

작성: 2026-09-01. 근거 자료: `refs/DICERO_ANALYSIS.md`(화면·연출),
`refs/DICERO_SYSTEMS.md`(시스템), 스크린샷 `refs/dicero*/`.

---

## 0. 먼저 — 솔직한 진단

지금 우리 그림이 딸깍 다이스보다 못한 이유는 **해상도가 아니라 두 가지**다.

1. **아트 디렉션(색·빛)이 칙칙하다.** 딸깍은 밝은 하늘색~흰색 바탕에 채도 높은
   포인트색을 쓴다. 우리는 화면 전체가 어두운 보라 한 톤이라 뭘 그려 넣어도
   묻힌다. **이건 코드로 100% 해결 가능하고, 체감 변화가 가장 크다.**

2. **캐릭터가 "도형 덩어리"다.** 지금 스프라이트는 타원·다각형을 겹쳐 자동
   명암을 입히는 방식(`art/sprite_builder.dart`)이라 **부피는 있지만 성격이 없다.**
   딸깍 캐릭터는 흰 머리 + 빨간 목도리 + 큰 눈처럼 **한눈에 각인되는 특징**이
   있는데, 자동 생성으로는 그게 안 나온다.

> **한계를 분명히 해둔다**: 배경·이펙트·UI·연출은 코드로 딸깍 수준에 근접할 수
> 있다. 하지만 **캐릭터 원화 품질은 손으로 도트를 찍는 방식으로는 한계가 뚜렷하다.**
> 그래서 5단계에 "진짜 에셋 도입"을 선택지로 넣었다. 결정은 사용자 몫이다.

---

## 1단계 — 아트 디렉션 교체 (효과 최대 / 비용 최소)

**목표**: 화면을 밝고 선명하게. "칙칙하다"는 인상을 먼저 없앤다.

- `widgets/pixel_ui.dart` 팔레트를 전면 교체
  - 배경: 어두운 보라 단일톤 → **밝은 청회색 ~ 흰색 그라데이션**
  - 캐릭터/적: 채도를 올리고 서로 보색 대비를 준다
  - 강조색은 금색·빨강·청록 3개로 제한 (지금은 색이 너무 많다)
- 명암 대비를 키운다: 밝은 곳은 더 밝게, 그림자는 더 진하게
- 층 테마(얼음/비전/화염) 3종의 채도를 전부 한 단계씩 올린다

**검증**: 타이틀·지도·전투 화면을 캡처해 나란히 비교한다.

## 2단계 — 캐릭터 스프라이트 재작업

**방식**: 자동 생성만으로는 안 되므로 **혼합**으로 간다.
`SpriteBuilder`로 부피(실루엣·명암)를 만들고, 그 위에 **특징을 손으로 찍는다.**

- 마법사(가장 오래 보임)부터: 실루엣이 뚜렷하도록
  **어깨 > 모자챙** 비율 유지 + 펄럭이는 망토 + 지팡이 보석 발광
- 적은 **한눈에 구분되는 특징 하나씩** 부여
  (슬라임=큰 눈, 해골=빈 눈구멍, 로브=후드 속 빛, 용=뿔+이빨)
- 크기 대비를 살린다: 보스는 일반 적의 1.5배 이상
- 각 스프라이트에 **피격/시전 변형** 프레임을 추가 (지금은 색만 번쩍인다)

**검증**: `test/sprite_test.dart`가 폭·명암 단계를 이미 검사한다. 여기에
"실루엣이 화면 밖으로 안 나감" 검사를 추가한다.

## 3단계 — UI를 딸깍 패턴으로 통일

분석 문서에서 검증된 패턴만 가져온다 (이미 일부 적용됨 → 나머지 마무리).

| 항목 | 상태 |
|---|---|
| 보상 카드 가로 배너 + 「추천」 뱃지 | ✅ 적용됨 |
| HP 숫자를 바 위에 겹치기 | ✅ 적용됨 |
| 화면 가장자리 피해 배너 | ✅ 적용됨 |
| 좌측 노드 트랙 | ✅ 적용됨 |
| **조합표를 전투 중 언제나 열람** (좌하단 버튼) | ⬜ 미적용 |
| **현재 성립 족보를 상시 배너로** | ⬜ 미적용 |
| **일시정지에서 보유 유물 확인** | ⬜ 미적용 |
| **리본 배너 헤더 통일** | 부분 적용 |
| 패널 모서리 장식·입체 버튼 | ✅ 적용됨 |

## 4단계 — 연출 강화 (손맛)

- **배수/판정 결과를 화면 중앙에 거대하게** 터뜨린다
  (딸깍은 조합 확정 순간 노란 「×5.95」가 화면을 덮는다)
- **히트스톱**: 명중 순간 40~80ms 정지 후 튕겨나가게
- 화면 흔들림을 **3단계**로 (약: 일반 타격 / 중: 대성공 / 강: 폭주)
- 대미지 숫자 크기를 **피해량에 비례**시킨다
- 적 처치 시 파편이 터지며 사라지는 연출

## 5단계 — 외부 에셋 도입 범위 ✅ 결정됨 (2026-09-01)

**사용자 결정: 기본 재료만 CC0에서 가져오고, 캐릭터·몬스터는 직접 그린다.**

- 가져오는 것: **한글 픽셀 폰트(최우선)**, 배경 타일·질감
- 직접 그리는 것: 마법사·몬스터 30종 (CC0는 참고 자료로만 본다)
- 한 번 그린 그림은 **굽기(bake) + 해시 잠금**으로 박제해 다시는 저절로 변하지 않게 한다

상세 방침·후보 팩·제작 파이프라인은 **`ART_PIPELINE.md`** 로 분리했다.
2단계는 대체되지 않고 `ART_PIPELINE.md` 3절의 절차로 진행한다.

---

## 진행 순서 (2026-09-01 개정)

1단계(아트 디렉션) **+ 폰트 교체** → 2단계(캐릭터) → 3단계(UI) → 4단계(연출)

**이유**: 팔레트를 바꾸면 캐릭터 색을 어차피 다시 맞춰야 하므로, 1 직후에 2를
하면 재작업이 없다. 3·4는 캐릭터와 독립적이라 뒤로 밀려도 손해가 없다.
(원래 순서는 1→3→4→2였으나, 사용자가 "어설프면 플레이하는 맛이 안 산다"며
캐릭터 품질을 중시해 2를 앞으로 당겼다.)

## 작업 시 지켜야 할 것

- **스크린샷은 가로 480px로 축소해서 확인한다.** 원본(1080폭)은 토큰을 4배 먹는다.
- 한 단계 끝날 때마다 빌드→실행→캡처로 **눈으로 확인**한다.
- `flutter test`(2026-09-01 기준 136개)와 `flutter analyze` 경고 0을 유지한다.
- 게임 규칙·수치는 건드리지 않는다 (CLAUDE.md 절대 규칙 3).
