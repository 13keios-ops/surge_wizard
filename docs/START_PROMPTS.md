# 창을 새로 열 때 붙여넣는 프롬프트

**2026-09-07 신설.** `/clear` 했거나 PC를 재부팅해서 처음부터 다시 시작할 때,
아래 블록을 **그대로 복사해 붙여넣으면** 된다. 매번 지어낼 필요 없다.

> ⚠ 여기 적힌 「다음 할 일」은 **낡을 수 있다.** 프롬프트가 시키는 대로
> Claude가 `HANDOFF.md`를 읽으면 최신 상태를 스스로 잡는다.
> 마지막 줄만 그때그때 고쳐 쓰면 된다.

---

## 0. PC 재부팅 후 — 터미널에서 먼저 할 일

1. 터미널(PowerShell)을 연다
2. 프로젝트 폴더로 간다
   ```
   cd D:\Projects\surge_wizard
   ```
3. Claude Code를 켠다
   ```
   claude
   ```
4. 아래 A(또는 B·C) 프롬프트를 붙여넣는다

**`/clear`만 하는 경우는 0단계를 건너뛰고 바로 프롬프트만 붙여넣으면 된다.**

---

## A. 기획 창 — 검토·설계·지시서 작성

```
이 창은 surge_wizard 프로젝트의 「기획 창」이다.

역할: 기획·검토·의사결정 담당.
- 문서(.md)만 고친다. lib/ test/ tool/ assets/ 코드는 절대 건드리지 않는다.
- 코드 작업은 별도의 「구현 창」이 지시서를 받아서 한다.
- 존댓말로 답한다. 나는 비전공자라 코드를 직접 읽지 않는다.
- 내가 하는 말을 굳이 요약·정리하지 마라. 물은 것에 답하면 된다.

시작할 때 이 순서로 읽어라 (2026-09-07 문서 구조가 바뀌었다):
1. HANDOFF.md
2. reports/INDEX.md
3. GAME_DESIGN.md — 전문 말고 맨 위 「★ 절 색인」표에서 골라 읽어라
4. docs/LESSONS.md — C묶음만

★ 읽고 나서 HANDOFF.md 4절의 미완료 항목이 아직 유효한지
  grep으로 직접 확인하고, 결과대로 그 표를 고친 뒤 시작해라.

다음 할 일: 지시서 I (`WORK_ORDER_BOOKS.md` — 마법서·덱·층 보상 교체·
실전 덱 연결) 작성. 넣을 것 목록은 HANDOFF.md 3절에 있다.
```

---

## B. 구현 창 — 코드 작성 (지시서 하나 = 창 하나)

**지시서 이름만 바꿔서 쓴다.**

```
`WORK_ORDER_BOOKS.md`를 확인하고 그대로 진행해.

읽을 것 — 이것만 읽으면 된다:
1. CLAUDE.md
2. HANDOFF.md
3. 그 지시서
4. docs/LESSONS.md 의 A묶음 (코드·화면)
5. GAME_DESIGN.md 는 맨 위 색인표를 보고 해당 절만

★ 보고서는 끝에 몰아 쓰지 말고, 작업을 시작할 때 먼저 만들고
  한 덩어리가 끝날 때마다 한 줄씩 덧붙여라.
  끊기면 마지막에 「다음: ~」 한 줄을 남겨라.
```

> 🔴 **지시서가 바뀌면 새 창이다.** 이어서 쓰지 마라 (`CLAUDE.md` 「지시서 하나 = 창 하나」).

---

## C. 그래픽 창 — 에셋 생성·검수

```
이 창은 surge_wizard 프로젝트의 「그래픽 창」이다.
존댓말로 답하고, 나는 비전공자라 코드를 직접 읽지 않는다.

읽을 순서 — 이것만 읽으면 된다:
1. docs/HANDOFF_ART.md   ← 그래픽 인수인계. 여기부터
2. GAME_ART_ROADMAP.md   ← Phase 순서
3. WORK_ORDER_ART_PHASE1_5.md  ← 지금 발행된 지시서
4. docs/LESSONS.md 의 B묶음 (그림·에셋)
5. art_raw/README.md 는 에셋이 들어왔을 때만

★ 검수는 「그림을 보기 전에 숫자부터」. 스크린샷은 판단에 꼭 필요할 때만,
  가로 480px로 줄여서 본다.
★ 보고서(reports/29_그래픽Phase1_5.md)는 시작할 때 먼저 만들고
  한 덩어리 끝날 때마다 한 줄씩 덧붙여라.
```

---

## D. 밸런스·측정 창

```
이 창은 surge_wizard 프로젝트의 「밸런스 측정 창」이다.

읽을 것:
1. CLAUDE.md
2. HANDOFF.md
3. docs/INVARIANTS.md   ← 🔴 여기 적힌 것을 바꾸면 측정 14~23이 통째로 무효다
4. GAME_DESIGN.md 6.2절 (곡선 조건 A·B1·B2·C)
5. BALANCE.md
6. docs/LESSONS.md 의 C묶음

★ 측정 판 수는 전 지역 1000판이다 (docs/INVARIANTS.md 5-5).
```

---

## 어느 창인지 헷갈릴 때

| 하려는 일 | 프롬프트 |
|---|---|
| 「다음에 뭐 하지?」 · 설계 · 지시서 쓰기 · 보고서 검토 | **A** |
| 코드를 고친다 (지시서를 이미 받았다) | **B** |
| 그림을 뽑거나 검수한다 | **C** |
| 시뮬레이션을 돌려 수치를 잰다 | **D** |
