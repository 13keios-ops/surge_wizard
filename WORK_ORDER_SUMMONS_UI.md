# 작업 지시서 — 소환수를 화면에 그린다

> ⛔ **끝났다 (2026-09-13).** 보고서 `reports/39_소환수표시.md`.

발행: 2026-09-13, 기획 창. 근거: `reports/36_길옆물체.md` 화면 확인에서 발견.

## 0. 왜

**소환수를 그리는 코드가 어디에도 없다.** `lib/widgets/`·`lib/screens/` 를 다 뒤져도
`summons` 를 읽는 곳이 없다. 그래서 지금 이렇게 된다.

- 폭주로 아군 소환수가 나와도 **화면에 아무 일도 안 일어난다**
- 🔴 **하드의 호위가 매 턴 나를 때리는데 화면에 이유가 없다** (검토 37에서 들어왔다)

**맞는 것이 화면에 보이지 않는 것은 버그다.** 플레이어가 왜 죽는지 모른다.

## 1. 무엇을 그리나

`lib/core/surge.dart` 의 `SurgeSystem.summons` — `SummonUnit(power, turnsLeft)`.
**위력이 양수면 아군(적을 때린다), 음수면 적대(나를 때린다)**.

| 소환수 | 어디에 | 어떻게 |
|---|---|---|
| **아군** (power > 0) | 마법사 **옆**, 조금 뒤 | 작게. 마법사보다 확실히 작아야 한다 |
| **적대** (power < 0) | 적 **옆**, 조금 뒤 | 같은 크기감. 붉은 기 |

- **여러 체가 있을 수 있다.** 좌우로 조금씩 어긋나게 늘어놓는다
- **남은 턴 수**를 작게 보여 준다. 호위는 사실상 무한이라 **큰 수는 숨긴다**
- 원화가 없다 — **기존 코드 스프라이트나 단색 실루엣**으로 충분하다.
  길 옆 물체와 같은 방식으로 나중에 그림만 갈아 끼운다

## 2. 어디를 고치나

- `lib/widgets/battle_stage.dart` — **298줄이다.** 겹 하나를 얹는 **한 줄**만 넣고
  본체는 **새 파일**로 뺀다 (`lib/widgets/summon_row.dart` 등)
- 그리는 순서: **배경 겹 → 길 옆 물체 → 소환수 → 몬스터·캐릭터**

## 3. 테스트

| 무엇 | 기대 |
|---|---|
| 아군 1체 | 마법사 쪽에 하나 그려진다 |
| 적대 1체 | 적 쪽에 하나 그려진다 |
| 둘 다 · 여러 체 | 개수만큼 그려지고 좌우가 안 겹친다 |
| 없을 때 | 아무것도 안 그려진다 (빈 위젯) |
| 호위(999턴) | 남은 턴 수를 **안 보여 준다** |

`flutter analyze` 0 · `flutter test` 전부 통과.

## 4. 하지 않을 것

- 소환수 **전투 규칙** 변경 — 그리기만 한다
- 소환수 원화 발주 — 코드 그림으로 충분하다
- 다중 적 — 별도 과제다
