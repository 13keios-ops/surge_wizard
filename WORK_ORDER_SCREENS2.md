# 작업 지시서 G-2 — 화면 잔손질 3건

발행: 2026-09-04, 기획 창. **작은 지시서다. 오래 끌지 마라.**

근거: **검토 24**(`reports/24_화면_지역스테이지_REVIEW.md` 3절) ·
`WORK_ORDER_SCREENS.md` 2-A · `UI_DESIGN.md` 5-2·5-3·5-4

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷을 쓴다** —
> 가로 480px 축소본만 보고, 판단에 꼭 필요할 때만 열어라 (`CLAUDE.md` 토큰 절약).

---

## 0. 목적 (한 줄)

**지시서 G의 기능은 다 됐다. 눈으로 봐야 보이는 것 세 가지만 고친다.**

## 0-1. 절대 지킬 것

- **잠금 규칙·화면 흐름·클리어 기록을 건드리지 마라.** 검토 24에서 승인됐다
- **밸런스 수치와 `assets/data/*.json`은 읽기 전용**이다
- `core/` 판정 코드 · `tool/` 측정 도구를 **열지 마라**
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` **293개 전부 통과**
- **`dart format`을 파일 전체에 돌리지 마라** — 고친 부분만 손으로 정렬한다

---

# 작업 1 — 🔴 잠긴 지역 카드를 확실히 어둡게

## 1-A. 지금 무엇이 잘못됐나

**6·7지역(사막 계열)이 열려 있는 1지역보다 밝다.** 지시서 G의 2-A가
「잠긴 지역은 어둡게」를 요구했는데 **결과가 반대로 보인다.**

**원인**: `region_card.dart`가 잠긴 카드 위에 검은 막(`foregroundDecoration`)을
덮는데, **테마마다 바탕색 밝기가 너무 다르다.**

| 테마 | 아래쪽 색 | |
|---|---|---|
| forest (1지역) | `0xFF4E9455` | 어두운 초록 |
| ember (6·7지역) | `0xFFE8894F` | **밝은 주황** |
| frost | `0xFF7FA3E8` | 밝은 파랑 |

**막의 농도를 올려서는 안 풀린다** — 테마마다 필요한 농도가 다르다.

## 1-B. ★ 이렇게 고쳐라

> **잠긴 카드는 테마 색을 아예 쓰지 않는다. 공통 회색조 한 벌로 칠한다.**

- 잠긴 카드 12장이 **균일하게 어둡고**, **열린 카드만 색을 갖는다**
- 이 화면의 목적은 **「어디까지 열렸나」가 한눈에 읽히는 것**이다.
  잠긴 카드끼리 테마가 구분될 필요가 없다
- **회색조 색은 `lib/art/region_art.dart` 안에서 정한다.**
  그 파일이 「지역 카드 겉모습을 정하는 한 곳」이다 (검토 24에서 칭찬한 구조다)
- **배경 원화가 들어오면** 잠긴 카드는 그 그림을 회색조로 깔면 되므로
  이 구조가 그대로 산다

## 1-C. 검산

- 잠긴 12장의 **바탕 밝기 차이가 눈에 띄지 않는다**
- **열린 카드가 잠긴 어느 카드보다도 밝다**
- 480px 스크린샷으로 **1지역만 열린 상태**를 확인해 보고서에 넣어라

---

# 작업 2 — 🟡 짧은 스테이지의 빈 화면 채우기

## 2-A. 지금 무엇이 잘못됐나

**3층짜리 1지역 Ⅰ에서 지도 위아래에 큰 여백이 남는다.** 세로 가운데로
모아도 **칸이 3개뿐이라** 빈다. **스테이지 선택 화면도 8칸이면 아래 절반이 빈다.**

**도입 지역(1~3)이 3~7층이라 유저가 초반 내내 이 화면을 본다.**

## 2-B. 이렇게 고쳐라

> **남는 높이에 맞춰 칸을 키운다.**

| 화면 | 무엇 |
|---|---|
| **지도** | 층 칸의 높이를 **남는 세로 공간 ÷ 층수**로 정한다. 3층이면 칸이 크고, **10층이면 지금 크기**가 된다 |
| **스테이지 선택** | 격자 칸의 높이를 같은 방식으로. 보스 카드는 지금 비율을 유지한다 |

- **칸에 상한과 하한을 둬라.** 3층에서 칸이 우스꽝스럽게 커지면 안 되고,
  10층에서 지금보다 작아지면 안 된다. **상한·하한은 `constants.dart`에 상수로**
- **10층 스테이지의 지금 모습이 기준이다.** 거기서 안 바뀌어야 한다

## 2-C. 검산

- **3층 · 10층 양쪽 스크린샷**을 보고서에 넣어라
- 10층 화면이 **지금과 같아야 한다** (넘치거나 스크롤이 생기면 안 된다)
- 기존 검사 7(3층·10층 칸 수)이 **그대로 통과**해야 한다

---

# 작업 3 — 🟡 결과 화면 문구 (v1 잔재)

`lib/screens/result_screen.dart` **57행**.

| | 지금 | **바꿀 값** |
|---|---|---|
| 클리어 | `'탑을 정복했다!'` | **`'스테이지 클리어!'`** |
| 실패 | `'정말 아쉽네요!'` | **그대로 둬라** |

- **「탑」은 v1 잔재다.** 지금 깬 것은 스테이지 하나이고, 탑은 9·10지역의
  이름일 뿐이다
- **같은 화면의 「도달 층수」는 스테이지 층수 기준이라 맞다.** 건드리지 마라
- 이 화면에 **v1 잔재 문구가 더 있는지 훑어보고**, 있으면 보고서에 적어라.
  **고치지는 말고 적기만 하라** (문구는 기획 창 몫이다)

---

# 작업 4 — 테스트

**새 테스트를 많이 만들지 마라.** 이번 작업은 대부분 눈으로 보는 것이다.

| # | 검사 |
|---|---|
| 1 | 기존 **293개가 전부 그대로 통과**한다 |
| 2 | 지도가 **3층·10층 양쪽에서 넘치지 않는다** (기존 검사 7 확장) |
| 3 | 결과 화면 문구가 **「탑」을 포함하지 않는다** |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. **실제로 앱을 띄워** 확인하고 **480px 축소본**을 `reports/img/25_run/`에 남긴다
   — **지역 선택 · 지도(3층) · 지도(10층) · 결과** 네 장이면 충분하다
3. `PROGRESS.md`에 기록
4. **`reports/25_화면_잔손질.md`** 작성
   - 맨 앞에 **「세 가지가 다 고쳐졌는가」 한 줄**
   - 작업 1의 **전후 비교** (잠긴 카드가 균일하게 어두워졌는지)
   - 작업 3에서 **찾은 v1 잔재 문구 목록** (고치지 말고 적기만)
   - 판단·제안은 **「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ 잠금 규칙 · 화면 흐름 · 클리어 기록 변경
- ❌ 밸런스 수치 · `assets/data/*.json`
- ❌ **「주문 보상」 규칙 변경** (마법서 지시서에서 한다)
- ❌ 철수 · 소탕 · 덱 · 장비 · 각인 · 레벨
- ❌ `core/` 판정 코드 · `tool/` 측정 도구
- ❌ 새 화면 추가 · 파일 분리 (검토 24에서 끝났다)
- ❌ `dart format`을 파일 전체에 돌리기
