# 작업 지시서 P — 12지역 층수 하향 + 곡선 조건 판정

발행: 2026-09-03, 기획 창. **작업 1은 사용자 승인 완료된 수치 변경이다.**

근거 문서: **`STAGES.md` 4절**(12지역 층수) · **`GAME_DESIGN.md` 6.2절**(곡선 조건) · 검토 21

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**

---

## 0. 목적 (한 줄)

**곡선은 이미 건강하다. 남은 문제는 최종 지역 하나뿐이고, 손잡이는 층수다.**

## 0-1. ★ 판정 기준이 「곡선 조건」으로 바뀌었다

`GAME_DESIGN.md` 6.2절이 **재개정**됐다. **구간별 밴드를 버렸다** —
매끄러운 곡선에 계단 목표를 씌우면 경계(4·7지역)에서 반드시 어긋나기 때문이다.

| # | 조건 | 측정 21 |
|---|---|---|
| **A** | 상한이 **단조 감소**하고 인접 낙차 **20%p 이내** | ✅ 최대 16.7 |
| **B** | **12지역 상한이 25~45%** | ❌ **11.0%** |
| **C** | 도입(1~3) 하한 **70%+**, 5~10지역 **하한·상한 간격 30%p 이상** | ✅ 37~56%p |

**이번 목표는 B 하나를 맞추면서 A·C를 깨지 않는 것이다.**

## 0-2. 절대 지킬 것

- **작업 1 외에 수치를 바꾸지 마라.** 목표에 못 미쳐도 그대로 적어라
- **적 수치 · 보스 · `hp_scale` · `atk_scale` · 난이도 배율 · 1~11지역 층수 ·
  성장 프로필 · 강화율 · 상점 회복 · 전투 승리 회복을 건드리지 마라**
- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — ✅ 12지역 층수 하향

`assets/data/stages.json`. **12지역 6개 스테이지만** 바꾼다.

| 스테이지 | Ⅰ | Ⅱ | Ⅲ | Ⅳ | Ⅴ | Ⅵ(보스) |
|---|---|---|---|---|---|---|
| 지금 | 9 | 10 | 10 | 10 | 10 | 10 |
| **바꿀 값** | **7** | **8** | **8** | **8** | **8** | **8** |

- **11지역 이하는 한 값도 바꾸지 마라**
- 지역 합 59 → **47**, **총 층수 740 → 728**
- `recommended_level`·`subtitle`·`is_boss_stage`는 **그대로**

## 1-A. ⚠ 층수가 줄면 이벤트 배치가 자동으로 바뀐다

`floorEvent(floor, floors)`가 층수에서 계산하므로 **코드는 안 고쳐도 된다.**
다만 배치가 이렇게 바뀐다 — **확인만 하라.**

| 층수 | 배치 |
|---|---|
| 8층 | ⚔ ⚔ ★ ◈ ⚔ ★ ♥ ⚠ |
| 7층 | ⚔ ⚔ ★ ◈ ⚔ ♥ ⚠ |

**주문 보상이 2 → 2개, 유물 1개, 상점 1개.** 10층과 성격이 같다. 문제없다.

---

# 작업 2 — ⚠ 기존 테스트 (기획 창이 조사했다)

| 위치 | 조치 |
|---|---|
| `test/region_stage_test.dart` **「층수 총합 740」** | **728**로 |
| `test/region_stage_test.dart` 지역별 층수 합 목록 (있다면) | 12지역 59 → **47** |
| `test/region_stage_test.dart` **「모든 스테이지 10층 이하」** | **손대지 마라.** 그대로 통과한다 |
| `test/region_stage_test.dart` **「층수가 지역 안에서 감소하지 않는다」** | **손대지 마라.** 7·8·8·8·8·8은 비감소다 |
| `test/region_stage_test.dart` **`floorEvent` 100개 검사** | **손대지 마라.** 7·8층도 규칙을 만족한다 |

**그 밖이 깨지면 구현이 틀린 것이다.**

---

# 작업 3 — 측정

측정 21과 **같은 조건** (36조합 × 300판 × 시드 2 × 하한/상한 × greedy/utility).
출력 **`reports/data/measure10_output.txt`**

## 3-A. 조건 2개로 기여를 확인한다

| 조건 | 12지역 층수 |
|---|---|
| 기준 (= 측정 21 재현) | 옛 값 (9·10) |
| **새 층수 (채택)** | 새 값 (7·8) |

- **기준 조건이 측정 21과 소수점까지 같아야 한다.** 다르면 보고하라
- 조건은 **인자로** 받아라

## 3-B. ★ 판정 — 곡선 조건 A·B·C

**구간 밴드 표는 더 내지 마라.** 대신 이렇게 낸다.

### 표 1 — 곡선과 낙차

| 지역 | 하한 | 상한 | **상한 낙차** |

### 표 2 — 조건 판정

| 조건 | 요구 | 실측 | 판정 |
|---|---|---|---|
| A | 단조 감소 · 낙차 ≤ 20%p | | |
| B | 12지역 상한 25~45% | | |
| C | 도입 하한 70%+ / 5~10지역 간격 ≥ 30%p | | |

**세 조건을 한 줄로 답하라** — 「A✅ B✅ C✅」인지.

## 3-C. 그 밖에 낼 표

| # | 표 | 비고 |
|---|---|---|
| 1 | 상점 층 **도달률 · 남은 체력 %** | 12지역 도달률이 39.0%에서 얼마나 오르는지가 핵심 |
| 2 | 지역별 **보스만 승률** (9~12) | **바뀌면 안 된다** — 보스도 배율도 안 건드렸다 |
| 3 | 전투당 굴림 + 각인 기대 적립 | 측정 12~21과 한 표에. **B가 통과하면 이 값으로 각인을 확정한다** |
| 4 | 전투당 턴 수 · **200턴 안전장치** | 0이어야 한다 |
| 5 | **12지역 한 판의 평균 턴 수** | 층수가 줄어 한 판이 얼마나 짧아졌는지 (원칙 6) |

> ⚠ **「전투당 피해 %」는 내지 마라** (생존 편향).

---

# 작업 4 — 테스트

| # | 검사 |
|---|---|
| 1 | 12지역 6스테이지 층수가 **7·8·8·8·8·8** |
| 2 | **1~11지역 층수가 안 바뀌었다** |
| 3 | 층수 총합이 **728** |
| 4 | 모든 스테이지가 여전히 **10층 이하**, 지역 안에서 **비감소** |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드 정상 종료, `reports/data/measure10_output.txt` 생성
3. `BALANCE.md`에 **「14차 — 12지역 층수」** 절을 맨 위에 추가
4. **`reports/22_최종조정_측정.md`** 작성
   - **맨 앞에 「A·B·C 세 조건이 통과했는가」 한 줄**
   - 곡선·낙차 표
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **적 수치 · 보스 · 배율 · 1~11지역 · 성장 프로필 · 회복 · 강화율 조정**
- ❌ 구간 밴드 목표표 재발행
- ❌ 「전투당 피해 %」 표
- ❌ `lib/`에 마법서·강화 시스템 구현
- ❌ 화면 · **각인 수치표 확정** (B가 통과하면 **기획 창이** 다음 지시서에서 한다)
