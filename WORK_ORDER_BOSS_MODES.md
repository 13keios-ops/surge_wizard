# 작업 지시서 — 보스 등장 방식 3종 (전조 · 호위 · 연전)

> ⛔ **끝났다 (2026-09-13).** 보고서 `reports/37_보스등장방식.md`.
> `lib/core/boss_modes.dart` 신설. `HANDOFF.md` 4절 미구현 3건 중 2건이 닫혔다.

발행: 2026-09-13, 기획 창.
근거: `GAME_DESIGN.md` **4.4절** · `ENEMIES.md` **4절** · `HANDOFF.md` 4절 미구현 목록.

> ### 🪟 창 운영
> **구현 창. `/clear` 하고 시작한다.** 엔진 작업이고 화면은 거의 안 본다.

---

## 0. 왜 지금 하나

지금 **하드와 데스가 수치 배율만 다른 똑같은 전투**다. 난이도를 올려도
**적이 더 아플 뿐 다르게 싸우지 않는다.** 기획서 4.4절이 정해 둔 세 방식이
하나도 안 들어가 있다 (`lib/core/stage_runner.dart:30` 에 미구현 주석).

---

## 1. 🔴 시작 전에 읽어라 — **다중 적은 없다**

`GAME_DESIGN.md` 4.4절이 「다중 적을 정식으로 도입했으므로 우회가 필요 없다」고
**완료형으로** 적어 두었다. **문서만 그렇고 코드에는 없다.**
`lib/core/battle.dart:44` 는 아직 `final Enemy enemy;` **한 마리**다.

**그래서 「호위」는 이번에 「진짜 호위 몹」으로 만들지 않는다.**
이미 있는 **적대 소환수**로 만든다 (아래 3절). 다중 적이 들어오는 날 올리면 된다.

⚠ **다중 적을 이 지시서에서 시작하지 마라.** 전투 모델·대상 선택·적 턴·주문 효과·
화면·테스트가 전부 걸리는 별도 과제다.

---

## 2. 전조 → 본체 (지역 8~12 한정)

**1층에서 약화판 보스를 한 번 만나고, 마지막 층에서 본체와 재대결한다.**

| | 값 | 근거 |
|---|---|---|
| 적용 지역 | **8~12만** | `ENEMIES.md` 4절 — 층이 10층을 넘어 길어지는 구간 |
| 약화판 HP | **본체의 40%** | 같은 절 |
| 약화판 공격 | **본체의 70%** | 같은 절 |
| 난이도 | **전 난이도 공통** (보통에도 나온다) | 4절 표 밖의 별도 규칙이다 |

- 뽑는 자리는 `pickEnemy()` 다. **1층이고 지역이 8~12면** 일반 추첨 대신 보스를 약화판으로
- 약화판도 **보스 변종 규칙을 그대로** 따른다 (난이도로 고정, 추첨하지 않는다)
- 🔴 **약화판은 2페이즈를 쓰지 않는다.** 40% HP에서 절반 전환은 의미가 없다
- 수치는 `core/constants.dart` 에 상수로. **매직 넘버 금지**

## 3. 호위 (하드)

**보스가 적대 소환수 1체를 데리고 나온다.**

| | 값 |
|---|---|
| 적용 | **하드 난이도의 보스 층**만 |
| 위력 | **보스 일반 공격 × 0.4** (`ENEMIES.md` 4절) |
| 수 | **1체** |

- 🔴 **새 시스템을 만들지 마라.** `lib/core/surge.dart` 의 `SummonUnit` 을 쓴다.
  **power 가 음수면 적대**이고, 적 턴 시작에 나를 때린다 (`surge.dart:177`)
- 전투가 **시작될 때** 한 체 넣는다. 폭주로 생긴 소환수와 **같은 목록에 섞인다**
- 보스를 쓰러뜨리면 전투가 끝나므로 **호위를 따로 정리할 필요가 없다**
- ⚠ 폭주로 생긴 아군 소환수와 **상쇄 규칙이 이미 있는지** 먼저 확인하라.
  없으면 만들지 마라 — 이번 범위가 아니다

## 4. 연전 (데스)

**본체를 쓰러뜨리면 곧바로 2체째가 나온다.**

| | 값 |
|---|---|
| 적용 | **데스 난이도의 보스 층**만 |
| 2체째 HP | **본체의 60%** (`ENEMIES.md` 4절) |
| 2체째 공격 | 본체와 같다 |

- **같은 전투 안에서** 적을 갈아 끼운다. 화면을 새로 열지 않는다
- 플레이어의 체력·마력 축적·손패는 **이어진다.** 회복을 주지 않는다
- 2체째도 **2페이즈를 쓴다** (60% HP의 절반에서 전환)
- 🔴 **승리 판정이 2체째를 쓰러뜨린 뒤에** 나야 한다. 1체째에서 보상 화면이 뜨면 안 된다

---

## 5. 어디를 고치나

| 파일 | 무엇 |
|---|---|
| `lib/core/stage_runner.dart` | 전조 분기. **30행의 미구현 주석을 지운다** |
| `lib/core/battle.dart` | 연전(적 교체) · 호위(전투 시작 시 소환) |
| `lib/core/constants.dart` | 40% · 70% · 60% · 0.4 · 적용 지역 범위 |

⚠ **`battle.dart` 의 길이를 먼저 재라.** 300줄을 넘으면 연전·호위를 새 파일로 뺀다
(`lib/core/boss_modes.dart` 등). 기존 분리 방식(`enemy_turn.dart`)을 따르면 된다.

---

## 6. 테스트 (필수)

| 무엇 | 기대 |
|---|---|
| 전조 — 지역 8, 1층 | 보스가 나오고 **HP 40% · 공격 70%** |
| 전조 — 지역 7, 1층 | **일반 적**이 나온다 (8 미만은 적용 안 됨) |
| 전조 — 지역 8, 마지막 층 | **본체**가 나온다 (100%) |
| 전조 약화판 | **2페이즈 전환이 없다** |
| 호위 — 하드 보스 층 | 적대 소환수 **1체**, 위력 = 보스 공격 × 0.4 |
| 호위 — 보통·데스 보스 층 | 소환수 **0체** |
| 연전 — 데스 보스 층 | 1체째를 죽여도 **전투가 안 끝난다.** 2체째 HP 60% |
| 연전 — 체력 이어짐 | 2체째로 넘어갈 때 플레이어 체력·마력이 **그대로** |
| 연전 — 승리 | **2체째를 죽여야** 승리 |
| 🔴 회귀 | 보통 난이도 보스 전투가 **지금과 똑같다** |

`flutter test` **전부 통과**. `flutter analyze` 경고 0.
🔴 **밸런스 수치를 임의로 바꾸지 마라.** 위 표의 값이 기준이다.

---

## 7. 보고서

🔴 **작업을 시작할 때** `reports/37_보스등장방식.md` 를 먼저 만든다.
양식은 `reports/_TEMPLATE.md`. **한 덩어리 끝날 때마다 한 줄씩 덧붙인다.**

사용자에게는 **"보고서 reports/37_보스등장방식.md 작성 완료" 한 줄만** 말한다.

---

## 8. 하지 않을 것

- 🔴 **다중 적 도입** — 별도 과제다. 여기서 시작하지 마라
- 소환수 시스템 재작성 — 있는 것을 쓴다
- 보스 수치·배율 변경 — 4.4절 표의 값만 쓴다
- 화면·연출 — 이번은 엔진만이다
