# 작업 지시서 J — 덱에서 손패 뽑기 + 덱 기반 재측정

발행: 2026-09-02, 기획 창.

근거 문서: **`GAME_DESIGN.md` 4.2절**(손패는 덱에서 뽑는다) ·
**`SPELL_CIRCLES.md` 부록**(서클 슬롯 개방표)

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**
> 앞부분은 `lib/core/battle.dart` 기능 추가, 뒷부분은 측정이다.

---

## 0. 목적 (한 줄)

**측정 14·15는 손패를 「그 지역 최고 서클 3장」으로 고정해서 쟀는데, 그건 실제
게임 규칙이 아니고 하필 가장 불리한 조건이었다.** 진짜 규칙으로 바꿔 다시 잰다.

### 왜 이게 중요한가 (검토 15)

완주율이 **손패 위력이 아니라 「유틸이 들어 있느냐」와 정확히 상관**했다.

| 지역 | 완주율 | 손패 |
|---|---|---|
| 1·2 | **98%** | 지연 · 방어막 · 공격 |
| 8·9 | 0.1% / **0%** | 공격 14 · 공격 14 · 공격 14 |
| 10·11 | **15% / 7.2%** | **회복5** · 지연 · 공격 |
| 12 | **0%** | 공격 19 · 공격 18 · 공격 18 |

실제 덱은 슬롯표대로 **1~9서클이 섞이고**, 만렙 21장 중 **1~3서클이 12장**이라
저서클 유틸(회복·방어막·지연)이 자연히 들어온다.

## 0-1. ⚠ 이번에도 조정은 하지 마라

> **밸런스 수치를 바꾸지 마라.** 완주율이 낮게 나와도 **그대로 적어라.**
> 이번에 바뀌는 것은 **손패를 만드는 방식**뿐이다.

## 0-2. 절대 지킬 것

- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — `Battle`에 덱 뽑기 (v2 4.2절)

## 1-A. 규칙

> **매 턴 덱에서 무작위 3장을 뽑아 손패로 준다 (중복 없음).
> 직전 턴에 시전한 주문은 1턴 쉰다.**

## 1-B. ★ 기존 동작을 깨지 마라 — 덱은 선택 인자다

`Battle`은 지금 `final List<Spell> hand`를 받는다. **테스트 237개가 그 형태에
의존한다.** 그래서 **덱을 추가 인자로** 두고, 없으면 지금과 똑같이 동작하게 한다.

```dart
Battle({
  required List<Spell> hand,      // deck이 없으면 이 손패를 그대로 고정 사용
  List<Spell>? deck,              // 주면 매 턴 여기서 3장을 뽑는다
  ...
})
```

| `deck` | 동작 |
|---|---|
| `null` (기본) | **지금과 완전히 같다.** 손패 고정 |
| 있음 | `startTurn()`에서 3장을 새로 뽑아 손패를 채운다 |

**이 설계라면 기존 테스트가 한 개도 안 깨진다.** 깨지면 잘못 만든 것이다.

## 1-C. 뽑기 세부

- 덱이 3장보다 작으면 **있는 만큼만** 뽑는다 (덱 최소 크기는 3이다 — 작업 2)
- **직전 턴에 시전한 주문 1개는 이번 턴 후보에서 뺀다.**
  단 그러면 3장을 못 채우는 경우(덱 3장) **쉬는 규칙보다 3장 채우기를 우선**한다
- **봉인된 주문**(`sealedSpellIds`)은 뽑히지 않는다
- 뽑기는 `Battle`이 이미 들고 있는 `Random`을 쓴다 (시드 재현 가능해야 한다)
- `hand`가 매 턴 바뀌므로 **`final`을 풀어야 한다.** `castableIndexes`·
  `otherCastableSpells`(스트레이트·연쇄)가 새 손패를 보는지 확인할 것

## 1-D. 테스트 (`test/deck_test.dart` 신규)

| # | 검사 |
|---|---|
| 1 | `deck`이 `null`이면 손패가 턴이 지나도 **그대로다** (기존 동작 보존) |
| 2 | `deck`을 주면 매 턴 손패가 **덱에서 3장** 채워진다 |
| 3 | 손패에 **중복이 없다** |
| 4 | **직전 턴 시전 주문이 다음 턴 손패에서 빠진다** (덱이 넉넉할 때) |
| 5 | 덱이 정확히 3장이면 **쉬는 규칙보다 3장 채우기가 우선**이다 |
| 6 | 봉인된 주문은 뽑히지 않는다 |
| 7 | 같은 시드면 같은 손패 순서가 나온다 (재현성) |

---

# 작업 2 — ✅ 덱 구성 프로필 (기획 창 지정 · 〔측정용 가정〕)

`tool/measure_profile.dart`의 손패 생성을 **덱 생성으로** 바꾼다.
체력·마나 프로필은 **그대로 둔다** (측정 15와 비교해야 한다).

## 2-A. 지역별 덱 구성 — 슬롯 개방표대로

`SPELL_CIRCLES.md` 부록의 개방표에서 **그 지역 진입 레벨 이하의 마지막 행**을 쓴다.

| 지역 | 진입 Lv | 1서클 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 덱 총량 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1 | **3** | | | | | | | | **3** |
| 2 | 8 | **3** | | | | | | | | **3** |
| 3 | 16 | 3 | 1 | | | | | | | 4 |
| 4 | 25 | 3 | 2 | 1 | | | | | | 6 |
| 5 | 35 | 4 | 2 | 1 | 1 | | | | | 8 |
| 6 | 45 | 4 | 3 | 2 | 1 | 1 | | | | 11 |
| 7 | 55 | 4 | 3 | 2 | 1 | 1 | | | | 11 |
| 8 | 65 | 4 | 3 | 2 | 2 | 1 | 1 | | | 13 |
| 9 | 72 | 4 | 3 | 2 | 2 | 1 | 1 | | | 13 |
| 10 | 80 | 5 | 3 | 3 | 2 | 2 | 1 | 1 | | 17 |
| 11 | 87 | 5 | 3 | 3 | 2 | 2 | 1 | 1 | | 17 |
| 12 | 94 | 5 | 4 | 3 | 2 | 2 | 1 | 1 | 1 | 19 |

> ⚠ **레벨 1의 1서클 칸이 2 → 3으로 바뀌었다** (2026-09-02, `SPELL_CIRCLES.md` 정정).
> 덱이 3장보다 작으면 **손패 3장을 채울 수 없기 때문**이다.

## 2-B. ★ 각 슬롯을 무엇으로 채우나 — 저서클은 유틸 우선

**이 규칙이 이번 측정의 핵심이다.** 위력 순으로만 채우면 **유틸이 하나도 안 들어와**
측정 15와 같은 함정에 빠진다.

| 서클 | 채우는 순서 |
|---|---|
| **1서클** | **유틸 먼저**: `healing_herb`(회복5) → `mana_shield`(방어막6) → `cinder_shield`(방어막4) → `chill_touch`(지연) → `soul_nip`(회복2) → 그다음 공격 주문(위력 큰 순) |
| **2서클 이상** | **위력 큰 순.** 같은 서클 안에서 **속성이 겹치지 않게** 고른다 |

> **왜 1서클만 유틸 우선인가**: `SPELL_CIRCLES.md`가 1서클을
> **「최약 공격 5 + 유틸 5」**로 설계했다. 위력 5는 후반에 의미가 없고,
> **실제 플레이어라면 그 칸을 회복·방어로 채운다.** 그게 저서클이 끝까지 사는 이유다.

- 유물·패시브·각인은 **여전히 없다** (측정 15와 같은 조건)
- 봇의 주문 선택 정책(`가장 센 주문`)은 **그대로 둔다.**
  손패가 매 턴 바뀌므로 자연히 유틸도 쓰게 된다
  - ⚠ 다만 **위력 0인 주문(`mana_shield`·`healing_herb`)만 손패에 잡힌 턴**에는
    봇이 무엇을 하는지 확인할 것. 아무것도 못 하고 굳으면 보고하라

---

# 작업 3 — 재측정 (측정 15와 같은 조건)

- 지역 12 × 난이도 3 = **36조합 × 300판**, 시드 **2개**(20260831 · 20260902)
- 출력 **`reports/data/measure4_output.txt`**
- ⚠ 측정 15에서 **출력 경로 때문에 파일을 옮기는 수고**가 있었다.
  이번에는 **출력 경로를 인자나 상수로 받도록 고쳐도 좋다** (측정 로직은 건드리지 말 것)

## 내보낼 표 — 측정 15와 나란히

| # | 표 | 측정 15 값 (참고) |
|---|---|---|
| 1 | **지역 × 난이도 완주율** | 7~9·12지역 0%대, 10지역 15.0% |
| 2 | 전투당 굴림 + **각인 기대 적립** | 5.49굴림 / 전설 7.7~11.1% |
| 3 | 전투당 턴 수 (일반 / 보스) | 일반 3.56 · 보스 6.15 |
| 4 | 자해 상한에 잘린 비율 | 4% 안팎 |

## ★ 추가로 낼 표 — 손패에 무엇이 잡혔나

**이번 변경이 실제로 유틸을 손패에 넣었는지 확인해야 한다.**

| 지역 | 덱 크기 | 덱의 유틸 비중 | **시전된 주문 중 유틸 비율** | 평균 시전 위력 |

- 「유틸」 = `heal` · `shield` · `delay_enemy` · `mana_restore` · `check_bonus` ·
  `extra_die` 효과를 가진 주문
- **10·11지역만 회복을 갖던 편중이 사라졌는지**가 확인 지점이다

---

# 작업 4 — 정리

- `BALANCE.md`에 **「8차 — 덱 기반 손패」** 절을 맨 위에 추가 (기존 보존)
- **`reports/16_덱기반측정.md`** 작성 (양식 `reports/_TEMPLATE.md`)
  - **완주율 전후 대조표를 맨 앞에**
  - 「손패에 무엇이 잡혔나」 표
  - 두 시드 차이
  - **판단·조정 제안은 「판단이 필요한 것」 절에만**
- 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **밸런스 수치 조정** — 완주율이 낮아도 만지지 마라
- ❌ 체력·마나 프로필 변경 (측정 15와 비교해야 한다)
- ❌ 화면에 덱 UI 붙이기 (지시서 G)
- ❌ 마법서·덱 편성 시스템 구현 (측정용 프로필로 흉내만 낸다)
- ❌ 각인·층수 표 확정 (이 결과를 보고 기획 창이 정한다)
- ❌ 보스 등장 방식 3종 · 레벨/패시브/장비
