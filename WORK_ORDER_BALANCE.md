# 작업 지시서 — 밸런스 개정 (구현 창용)

> ## ⛔ 보류 중 (2026-09-01)
> **기획서가 v2로 전면 개정되면서 이 지시서는 낡았다.**
> 작업 1·2는 완료됐고(보고서 01·02), **작업 3·4·5는 v2 기준으로 다시 쓸 예정이다.**
> 특히 「보통」이 마나 0으로 바뀌고 「약하게」가 삭제됐으므로 수치 전제가 다르다.
> **새 지시서가 나올 때까지 이 문서로 작업하지 마라.**

발행: 2026-09-01, 기획 창. **사용자 승인 완료된 작업이다.**
근거 문서: `DICE_DESIGN.md`(설계 근거·계산), `GAME_DESIGN.md` 3.2/3.3/3.5-1/3.6/4.4/4.5절(개정 완료).

> **읽는 순서**: 이 문서 → `DICE_DESIGN.md` 0~1절(왜 고치는지) → 작업 시작.
> 기획서는 이미 개정돼 있으므로 **기획서를 다시 고칠 필요가 없다.**

---

## 0. 이 작업의 목적 (한 줄)

**「약하게」가 마나 0으로 대성공 75%를 내는 지배 전략이라 아무도 위험을 감수하지
않고, 그래서 이 게임의 유일한 차별점인 폭주가 숙련자에게 사라진다.** 이걸 고친다.

## 0-1. 절대 지킬 것

- **3.4절 확률표(순수 3d6 분포)를 건드리지 마라.** DC도 주사위 개수도 그대로다.
  고치는 것은 **판정 결과에 붙는 보상**뿐이다.
- **판정 엔진(`core/check.dart`, `core/combo.dart`)을 고치지 마라.**
  `check_test.dart` 29개 + `combo_test.dart` 8개가 순수 분포를 검증하고 있다.
  아래 1-A(약하게 대성공 없음)는 **판정이 아니라 전투 적용 시점에서** 처리한다.
  이걸 check.dart에서 하면 37개 테스트가 깨지고 확률표와의 일치도 무너진다.
- 매직 넘버 금지. 전부 `core/constants.dart`에 상수로.
- 파일 300줄 / 함수 40줄 규칙 유지.
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것.

---

# 작업 1 — 밸런스 개정 6건 (최우선)

## 1-A. 「약하게」 시전은 대성공이 되지 않는다

- **위치**: `lib/core/battle.dart` 판정 결과 적용부 (현재 153~176행 부근의 `switch`)
- **내용**: 시전 강도가 「약하게」이면, 판정 결과가 `critSuccess`여도 **`success`로
  강등**해서 처리한다. 강등은 **전투 적용 시점**에서만 일어나고, `CheckResult`
  자체(통계·UI 표시)는 원본 등급을 유지한다.
- **함께 처리할 것**: **「약하게」로 시전할 때는 마력 축적 게이지를 소모하지 않는다.**
  (게이지 3의 "확정 대성공"이 강등돼 날아가면 좌절 방지 장치가 오히려 함정이 된다.
  강등 대상이면 아예 게이지를 쓰지 않고 남겨둔다.)
- 트리플로 얻은 확정 대성공은 강등된다 (공짜로 얻은 것이므로 의도된 동작).

## 1-B. 적 행동 지연에 누적 상한 2턴

- **위치**: `lib/core/battle.dart` (`enemyDelayTurns +=` 두 곳: 157행 부근·205행 부근),
  `lib/core/surge.dart` 70행 부근(`skip_enemy_turn`)
- **내용**: 새 상수 `kMaxEnemyDelayStack = 2`. **어느 경로로 들어오든**
  `enemyDelayTurns`가 2를 넘지 않게 `min()`으로 자른다.
  (대성공 / 주문 효과 `delay_enemy` / 폭주 `skip_enemy_turn` / 나중에 붙을 서리 각인 전부)
- 소비는 지금대로 `enemy_turn.dart` 37~39행에서 턴당 1.

## 1-C. 전력 시전 위력 2.5 → 3.0

- **위치**: `lib/core/constants.dart` `kPowerFull`
- **내용**: `2.5` → `3.0`

## 1-D. 마나 환급을 강도별로

- **위치**: `lib/core/constants.dart` `kCritManaRefund`, `lib/core/battle.dart` 157행 부근
- **내용**: 단일 상수를 **강도별 값**으로 바꾼다.
  약하게 = 해당 없음(1-A로 대성공 자체가 없음) / 보통 = **1** / 전력 = **2**.
  `core/cast_intensity.dart`가 강도별 값을 들고 있으니 거기 붙이는 게 자연스럽다.

## 1-E. 리롤 마나 비용 체증 (1 → 2 → 3)

- **위치**: `lib/core/constants.dart` `kRerollManaCost`,
  리롤 처리부(`battle.dart` 또는 `screens/battle_controller.dart`)
- **내용**: `kRerollManaCost = 1` 단일 상수를
  **`kRerollManaCosts = [1, 2, 3]`** 리스트로 교체. n번째 리롤은 `kRerollManaCosts[n]`.
- **기존 규칙 유지**: 리롤은 **시전 비용을 남겨둘 수 있을 때만** 가능하다
  (마나가 음수로 가지 않게 하는 현재 로직을 그대로 둔다).
- 유물 「무료 리롤」 효과는 **가장 비싼 회차가 아니라 그 회차의 비용**을 면제한다
  (지금 로직 그대로면 된다).

## 1-F. 상점 층 5 → 4

- **위치**: `lib/core/constants.dart` `kShopFloor`
- **내용**: `5` → `4`. 상점은 그 층 **전투에서 이긴 뒤** 열린다
  (`map_screen.dart` 141행 부근). 전투를 대체하지 않는다 — 지금 구조 그대로 유지.
- 주문 보상(3·9층)·유물 보상(6층)과 겹치지 않는지만 확인.

## 작업 1의 테스트 요구사항

`test/battle_test.dart`에 추가:

- [ ] 「약하게」로 시전하면 판정이 대성공이어도 위력이 2배가 되지 않는다
- [ ] 「약하게」 시전은 마나를 환급받지 않고 적을 지연시키지 않는다
- [ ] 마력 축적 3인 상태에서 「약하게」로 시전하면 **게이지가 그대로 남는다**
- [ ] 대성공을 여러 번 내도 `enemyDelayTurns`가 2를 넘지 않는다
- [ ] 리롤 3회의 총 마나 비용이 6이다 (1+2+3)
- [ ] 「전력」 대성공이 마나 2를 환급한다

**기존 테스트가 전부 통과해야 한다** (2026-09-01 기준 136개. `sprite_test.dart`가
반복문으로 테스트를 만들어내서 파일의 `test(` 개수보다 많다). 깨지는 게 있으면 그
테스트가 옛 수치를 검증하고 있는 것이니, **수치만 갱신하고 검증 의도는 바꾸지 마라.**

---

# 작업 2 — 시뮬레이터 재측정 (작업 1과 한 묶음. 빼먹지 마라)

**작업 1의 효과는 측정하지 않으면 알 수 없다.** 지금 봇은 「보통」만 쓰다시피 해서
개정 효과가 잡히지 않는다.

- **위치**: `tool/sim_core.dart`, `tool/simulate.dart`
- **내용**:
  1. 봇에 **「전력」을 쓰는 판단**을 넣는다. 최소 기준: 적을 이번 턴에 끝낼 수 있거나,
     마나가 2 이상 남고 다음 적 공격을 버틸 체력이 있으면 전력을 시도한다.
  2. 봇에 **「약하게」 판단**도 넣는다 (마나가 0일 때의 선택지).
  3. 리롤 비용 체증을 봇이 인식하게 한다.
  4. 1000판 재실행.

## 목표 지표 (이걸로 성공/실패를 판단한다)

| 지표 | 목표 |
|---|---|
| 폭주 빈도 | 시전의 **6~10%** (개정 전 1.9%) |
| 완주율 | **15~30%** |
| 「전력」 사용 비율 | 시전의 20% 이상 (아무도 안 쓰면 1-C·1-D가 실패한 것) |
| 층별 사망률 | 3~5층이 20%를 넘지 않을 것 (1-F의 효과 확인) |

**목표를 벗어나면 수치를 임의로 더 만지지 말고 사용자에게 보고할 것.**
결과는 `BALANCE.md`에 "4차 시뮬레이션" 절로 추가한다.
(기획 창은 이 파일을 건드리지 않기로 했으니 충돌하지 않는다.)

---

# 작업 3 — 상점 경제 (마력 결정 소비처)

기획서 **4.5절**에 표가 있다. 그대로 구현한다.

- **위치**: `lib/widgets/shop_dialog.dart`, `lib/screens/run_controller.dart`(174행 부근),
  `lib/screens/meta_controller.dart`(결정 잔액), `core/constants.dart`
- **상품**: 휴식(체력 10 회복) **판당 1회 무료** / 추가 휴식 30 /
  손패 주문 1장 교체 60 / 마력 축적 +1 40 / 무작위 유물 150
- **규칙**:
  - **첫 회복은 반드시 무료로 유지할 것.** 결정은 판이 끝나야 지급되는 영구
    재화라, 첫 판 플레이어는 잔액이 0이다. 회복까지 유료로 만들면 상점이
    닫힌 것과 같아지고 1-F(절벽 대책)가 통째로 무효가 된다
  - 가격은 전부 상수로 (`kShopPriceRest` 등)
  - 결정이 모자라면 버튼이 비활성(회색)으로 보이고, 왜 못 사는지 알 수 있어야 한다
  - **같은 상품을 한 상점에서 여러 번 살 수 있다** (결정만 있으면). 소비처를
    늘리는 게 목적이므로 1회 제한을 걸지 마라
  - 유물 구매는 **이미 가진 유물을 제외**하고 뽑는다
  - 구매 즉시 `save_service`로 저장 (결정은 영구 재화다)
- **UI**: 지금 상점은 회복 버튼 하나뿐이다. 상품 4개가 들어가면 화면이 넘칠 수 있으니
  **`Expanded` + `LayoutBuilder`로 짜라** (CLAUDE.md LESSONS — 600px 기기에서 넘친다).

## 테스트

- [ ] 결정이 모자라면 구매가 거부된다
- [ ] 구매 후 결정 잔액이 정확히 줄고 저장·복원된다
- [ ] 유물 구매가 이미 가진 유물을 주지 않는다

---

# 작업 4 — 확정 굴림 (Confirm Roll)

기획서 **3.2절**. 대성공이 뜨면 주사위 1개를 더 굴려 **4 이상이면 위력 2배 → 3배.**

- **위치**: `lib/core/battle.dart`, 연출은 `lib/widgets/battle_fx.dart` / `dice_widget.dart`
- **규칙**:
  - 「약하게」는 대성공 자체가 없으므로 확정 굴림도 없다
  - 성공 시 배율은 `kCritDamageMultiplier`(2.0)가 아니라
    새 상수 `kConfirmedCritMultiplier = 3.0`
  - 성공 기준은 상수 `kConfirmRollThreshold = 4`
  - **주사위는 `DicePool`을 통해 굴린다** (시드 고정이 되어야 테스트가 가능하다)
- **연출** (그래픽 4단계와 함께 해도 되지만, 로직은 지금 넣어라):
  화면이 잠깐 멈추고 → 주사위 하나가 **천천히** 굴러가고 → 4 이상이면 화면이 터진다.
  이게 이 게임에서 가장 짜릿한 순간이 되어야 한다.

## 테스트

- [ ] 시드를 고정해 확정 굴림 4 이상이면 3배, 3 이하면 2배가 나오는지
- [ ] 「약하게」에서는 확정 굴림이 발생하지 않는지

---

# 작업 5 — 각인 주사위

기획서 **3.5-1절**. 판정값에 영향을 주지 않고, 특정 눈이 보이면 부가 효과만 준다.

- **위치**: `lib/core/dice.dart`(각인 정보 보유), `lib/core/battle.dart`(효과 적용),
  `assets/data/relics.json`(각인 유물 추가), `lib/widgets/dice_widget.dart`(색 표시)
- **각인 4종**: 불(6이면 적에게 화염 2) / 서리(1이면 적 지연 1) /
  비전(짝수면 마나 +1) / 그림자(홀수면 마력 축적 +1)
- **규칙**:
  - **판정값 계산에 절대 끼어들지 않는다.** 효과는 판정이 끝난 뒤 별도로 적용한다
  - 부가 효과는 **주문의 성패와 무관하게** 발동한다 (실패해도 불의 6은 터진다)
  - 각인은 **유물로만** 획득한다. `relics.json`에 4종 추가
    (「불의 각인석」 등, rarity는 rare 권장). **효과 타입 신규 추가**: `attune_die`,
    value로 어느 주사위인지·어느 각인인지 지정
  - 서리 각인의 지연도 **1-B의 누적 상한 2턴**에 걸린다
  - **각인이 하나도 없으면 개정 전과 100% 동일하게 동작해야 한다** ← 이게 검증 핵심
  - **적별 상성은 만들지 마라** (기획서 10절 스코프 방어)
- **UI**: 각인된 주사위는 **테두리 색으로** 구분한다 (불=주황, 서리=하늘, 비전=보라,
  그림자=회보라). 잠금 표시(금색)와 헷갈리지 않게 할 것.

## 테스트

- [ ] 각인이 없으면 판정 결과와 대미지가 개정 전과 동일하다
- [ ] 불의 각인 주사위가 6이면 주문이 실패해도 적이 화염 2를 받는다
- [ ] 각인이 판정값(`CheckResult.value`)을 바꾸지 않는다

---

# 작업 순서와 보고

1. **작업 1 + 작업 2를 한 묶음으로** 끝내고 **거기서 멈춰서 사용자에게 보고**한다.
   시뮬레이션 지표가 목표 범위 밖이면 다음 작업으로 넘어가지 마라.
2. 보고 후 작업 3.
3. **작업 4·5는 그래픽 작업(`WORK_ORDER_GRAPHICS.md`)이 끝난 뒤에 한다.**
   확정 굴림 연출은 `battle_fx.dart`를, 각인 주사위 색은 `dice_widget.dart`를
   건드리는데 그래픽 창이 같은 파일을 통째로 손본다. 순서를 지키면 충돌이 없다.
   작업 4·5를 시작할 때 그 두 파일은 **기억하지 말고 반드시 다시 읽어라.**
3. 각 작업이 끝날 때마다 `flutter test` + `flutter analyze`를 돌리고
   **실제로 실행해서 눈으로 확인**한 뒤 다음으로 넘어간다.

## 보고 형식 (사용자는 비전공자다)

- 무엇을 왜 바꿨는지 **한국어로 짧게**
- 시뮬레이션 지표는 **개정 전 / 후를 나란히** 표로
- 테스트 개수와 통과 여부
- 판단이 필요한 게 생기면 **임의로 정하지 말고 물어볼 것** (절대 규칙 3)

## 하지 말 것

- 확률표(3.4절)·DC·주사위 개수 변경
- 판정 엔진(`check.dart`, `combo.dart`) 수정
- 광고·결제(7단계) 착수 — 여전히 보류 중이다
- 그래픽 작업 — 별도 지시서(`ART_PIPELINE.md`, `GRAPHICS_PLAN.md`)로 진행한다
- 기획서(`GAME_DESIGN.md`) 수정 — 이미 개정돼 있다. 문서는 기획 창 담당이다
