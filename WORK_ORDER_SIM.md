# 작업 지시서 F — 시뮬레이션 측정 (구현 창용)

발행: 2026-09-02, 기획 창.

근거 문서: `GAME_DESIGN.md` 3.4절(확률표) · `SURGE_DESIGN.md` 2·4절 ·
`ENGRAVINGS.md` 3·4절 · `STAGES.md` 1절

> ### 🪟 창 운영
> **새 창(`/clear` 후)에서 시작한다.** `tool/`만 만지는 작업이라 **스크린샷이 필요 없다.**
> 숫자를 많이 뽑는 작업이므로 **출력을 통째로 대화에 붙이지 말고 파일로 남겨라.**

---

## 0. 이 작업의 목적 (한 줄)

**여러 설계 문서가 「전투당 9굴림」·「층당 몇 초」·「체력 20→70」 같은 전제 위에
숫자표를 세워 뒀는데, 그 전제를 아무도 재 본 적이 없다.** 그걸 잰다.

## 0-1. ⚠ 이 작업은 「측정」이다. 조정이 아니다

> **밸런스 수치를 하나도 바꾸지 마라.** 상수·데이터·확률표 전부 그대로 둔다.
> 「이 값이 이상하다」고 판단되면 **고치지 말고 보고서에 적어라.**
> 조정은 기획 창이 결과를 보고 결정한 뒤 별도 지시서로 낸다.
>
> 측정과 조정을 같은 창에서 하면 **무엇 때문에 숫자가 변했는지 분리되지 않는다.**

## 0-2. 절대 지킬 것

- **`lib/`은 3절이 허가한 계측 카운터 1개 외에 고치지 마라**
- `core/check.dart` · `core/combo.dart` · `test/check_test.dart`의 **단언을 바꾸지 마라**
  (작업 0의 기계적 개명만 예외)
- 매직 넘버 금지. 시뮬 설정값도 상수로
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것

---

# 작업 0 — 기계적 정리 2건 (측정 전에 먼저)

검토 11에서 정해진 것이다. **동작을 바꾸지 않는다.**

## 0-A. `kDcWeak` → `kDcLow` 개명

「약하게」가 삭제됐는데 이름만 남아 오해를 부른다.

- 닿는 파일 **3개**: `lib/core/constants.dart` · `test/check_test.dart` · `tool/mc_report.dart`
- 주석: **「3.4절 확률표 검증용 낮은 DC(7). 시전 강도가 아니다.」**
- ⚠ **이름만 바꾼다. 값(7)도, 단언도, 테스트 로직도 건드리지 마라.**
  `check_test.dart` 수정은 **이 개명에 한해서만** 허가한다

## 0-B. `lastAppliedGrade` 주석 보강

`lib/core/battle.dart`. 지금은 항상 `lastResult.grade`와 같아 오해를 부른다.

> **「현재는 항상 `lastResult.grade`와 같다. 강등·승격 규칙(확정 굴림 등)이
> 생기면 여기서 갈린다.」**

**필드는 지우지 마라.**

---

# 작업 1 — 계측 준비

## 1-A. ✅ `lib/`에 허가하는 유일한 수정 — 역류 상한 카운터

`Battle`의 **「통계·기록 (시뮬레이터·디버깅용)」 블록**에 두 개를 더한다.
그 블록은 이미 `castCount`·`surgeCount`·`gradeCounts`가 있는 자리다.

```dart
int surgeSelfDamageRaw = 0;      // 상한 적용 전 자해 합계 누적
int surgeSelfDamageApplied = 0;  // 실제로 들어간 자해 합계 누적
```

`SurgeSystem._applySelfDamage`가 자를 때 두 값을 함께 누적한다.
**이것 말고 `lib/`을 고치지 마라.**

> 왜 `lib/`에 넣나: 상한은 `SurgeSystem` 안에서 잘리므로 **도구 쪽에서는 볼 수 없다.**
> 이미 있는 통계 블록에 얹는 것이 가장 작은 변경이다.

## 1-B. `tool/sim_core.dart`의 `Tally` 확장

**`lib/`을 건드리지 않고 도구에서 셀 수 있는 것들이다.**

| 항목 | 어떻게 |
|---|---|
| **굴림 횟수** | 판정 1회 = **초기 굴림 1 + `pool.rerollCount`**. `rollWithRerolls`가 끝난 시점에 누적 |
| **리롤 횟수** | `pool.rerollCount` / `pool.paidRerollCount` 각각 |
| **폭주 갈래** | 시전 직후 `battle.surge.lastSurge?.category`를 읽어 갈래별로 센다. **`lib/` 수정 불필요** |
| **전투당 턴 수** | `battle.turnCount` (이미 있다) |
| **전투 수** | 전투가 끝날 때마다 1 |

---

# 작업 2 — `tool/measure.dart` (신규)

**`simulate.dart`를 고치지 마라.** 기존 지표(완주율·층별 사망)는 비교 기준선이므로
그대로 둔다. 새 측정은 **새 파일**에서 한다.

- 실행: `dart run tool/measure.dart`
- 판 수 **1000판**, 시드 **20260831** (기존과 같게 두어 비교 가능하게)
- **시드를 하나 더**(예: 20260902) 돌려 **두 시드의 값이 크게 다르지 않은지** 함께 적을 것
- 출력은 **콘솔 + `reports/data/measure_output.txt`** 양쪽에 남긴다

## 측정할 것 6가지

### 2-A. ★ 전투당 굴림 횟수 — `ENGRAVINGS.md`의 전제

각인 수치표 전체가 **「전투당 9굴림」** 위에 세워져 있다 (`ENGRAVINGS.md` 4절).

| 내보낼 값 |
|---|
| 전투당 평균 **굴림 횟수** (초기 + 리롤) |
| 전투당 평균 **판정 횟수**(=시전 횟수) |
| 판정당 평균 **리롤 횟수** (유료/무료 나누어) |
| 굴림 횟수 **분포** (구간별 개수 — 예: 1~4, 5~8, 9~12, 13~) |

**그리고 그 값으로 각인 기대 적립을 다시 계산해 표로 낼 것:**

| 조건 유형 | 1굴림 확률 | 상한 | 기대 적립 = `E[min(X, 상한)]` |
|---|---|---|---|
| 흔함 | 0.5 | 4회 | ? |
| 드묾 | 1/6 | 3회 | ? |
| 희박(스트레이트) | 0.111 | 2회 | ? |
| 희박(뱀눈) | 0.074 | 2회 | ? |

*(X = 이항분포. 실측 굴림 횟수를 시행 수로 넣는다)*

### 2-B. 전투당 턴 수 — `STAGES.md`의 전제

층수 상한 10층이 **「층당 30~48초, 보스 90~150초」** 가정 위에 있다.

| 내보낼 값 |
|---|
| 전투당 평균 **턴 수** (일반 적 / 보스 나누어) |
| 턴 수 **분포** |

> ⚠ **초 단위로 환산하지 마라.** 턴당 몇 초인지는 **사람이 실제로 플레이해야**
> 알 수 있고 시뮬레이터는 모른다. **턴 수까지만** 내고, 환산은 기획 창이 한다.

### 2-C. 등급 분포 — 3.4절 표와 어떻게 다른가

⚠ **검토 11이 지적한 함정**: 3.4절 표는 **「보정 0 · 리롤 없음」의 순수 분포**이고,
봇은 성공이 뜰 때까지 리롤하며 유물 보정을 받는다. **그대로 비교하면 안 된다.**

**그래서 봇을 3가지 모드로 돌려 나란히 낸다:**

| 모드 | 리롤 | 유물 | 비교 대상 |
|---|---|---|---|
| **A. 순수** | 없음 | 없음 | **3.4절 표와 직접 비교 가능** |
| **B. 리롤만** | 있음 | 없음 | 리롤이 얼마나 미는가 |
| **C. 전체** | 있음 | 있음 | 실제 플레이 |

- A 모드는 `rollWithRerolls`를 건너뛰고 한 번만 굴리면 된다
- **A 모드의 값이 3.4절 표(보통 38.9/36.1/22.2/2.8, 전력 16.7/22.2/36.1/25.0)와
  맞는지가 이 측정의 정합성 검사다.** 크게 어긋나면 **측정 코드가 틀린 것이다**

### 2-D. 폭주 6갈래 실제 발생 비율

`SURGE_DESIGN.md`가 데이터로 **25 / 20 / 15 / 15 / 15 / 10%** 를 보장했다.
**전투에서 실제로 그렇게 뽑히는지** 확인한다.

- 갈래별 발생 횟수와 비율
- 목표와의 차이(%p)
- **크게 어긋나면 추첨 코드에 문제가 있다는 뜻이다**

### 2-E. ★ 역류 상한이 얼마나 자주 무는가 — 검토 10의 미결

`SURGE_DESIGN.md` 4절의 자해 상한(최대 체력 40%)이 **후반 역류를 고정값으로
만들어 버리는지**가 미결이다 (검토 10 2-1).

| 내보낼 값 |
|---|
| 자해가 발생한 폭주 횟수 |
| 그중 **상한에 잘린 횟수와 비율** |
| `surgeSelfDamageRaw` vs `surgeSelfDamageApplied` **합계와 평균** |
| 잘린 경우의 **평균 손실량**(raw − applied) |

### 2-F. 유물 판정 보정의 영향 — 검토 11의 미결

「전력 판정값 +N」 6종 + 「전체 +N」 5종으로 **유물 40종 중 11종이 판정 보정**이다.

- 모드 B와 C의 **등급 분포 차이**를 %p로 낸다
- 이것이 2-C 표에서 자연히 나오므로 **별도 실행은 필요 없다.** 차이만 정리하면 된다

---

# 작업 3 — 결과 정리

## 3-A. `BALANCE.md` 갱신

시뮬레이션 결과를 남기는 문서다 (**구현 창 소유**).

- **기존 내용을 지우지 마라.** v1 측정치는 「v1 (참고)」로 접어 두고 **아래에 추가**한다
- 2-A ~ 2-F를 표로 정리
- **해석은 짧게, 판단은 하지 마라.** 「A이므로 B로 바꿔야 한다」는 쓰지 않는다

## 3-B. 테스트

측정 코드 자체가 틀리면 모든 숫자가 거짓말이 된다. **최소한 이건 넣어라.**

`test/measure_sanity_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | 굴림 횟수 세기: 리롤 0회면 굴림 1회, 2회 리롤하면 굴림 3회 |
| 2 | `surgeSelfDamageRaw >= surgeSelfDamageApplied` 가 항상 성립 |
| 3 | 상한에 걸리지 않는 폭주에서는 **두 값이 같다** |
| 4 | 상한에 걸리는 폭주에서 `applied == (playerMaxHp * 0.4).floor()` |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. `dart run tool/measure.dart` 가 두 시드에서 정상 종료
3. **`reports/12_시뮬측정.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - 2-A ~ 2-F를 **전부 표로**
   - **2-C의 A 모드가 3.4절 표와 맞는지**를 맨 앞에 적을 것 — 이게 맞아야 나머지가 의미 있다
   - 두 시드의 값 차이를 적을 것
   - **판단·조정 제안은 「판단이 필요한 것」 절에만** 적는다
4. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ **밸런스 수치·확률표·데이터 조정** — 이 지시서에 승인된 수치 변경은 **없다**
- ❌ 턴 수를 초로 환산하기
- ❌ `simulate.dart` 수정
- ❌ 모델·로더·진행 구조 (다음 지시서)
- ❌ 확정 굴림 구현
- ❌ 「체력 20 → 70」을 시뮬레이터로 재려는 시도 —
  **레벨·패시브·장비가 아직 없어 잴 수 없다.** 설계 결정이지 측정 대상이 아니다
