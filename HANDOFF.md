# 인수인계 — 기획 창

**최종 갱신: 2026-09-05.** 새 기획 창은 이 파일부터 읽는다.
**`GAME_DESIGN.md`(v2)가 모든 판단의 기준이다.**

> ### ★ 이 파일을 읽고 나서 **가장 먼저** 할 일
> **아래 4절 「아직 안 만든 것」이 아직 유효한지 코드로 직접 확인하라.**
> 인수인계 문서는 **끝난 일은 잘 남기는데 안 끝난 일은 방치된다.**
> 실제로 2026-09-04에 「순서 7에 함께 넣을 것」 표가 여섯 지시서 동안 방치돼
> 있었고, 확인해 보니 1건은 이미 구현, 2건은 여전히 미구현이었다.
> (`CLAUDE.md` 「새 창을 연 직후에 반드시 할 것」)

---

## 1. 지금 어디까지 왔나 (한 화면)

| 영역 | 상태 |
|---|---|
| **기획서** | v2로 전면 개정 완료. 설계 문서 7종 (`SURGE_DESIGN` `SPELL_CIRCLES` `PASSIVE_TREE` `ENGRAVINGS` `ENEMIES` `STAGES` **`GROWTH`**) |
| **데이터** | 폭주 80 · 주문 70(서클 1~9) · 적 26+보스 12 · 변종 6 · 지역 12 · 스테이지 100 — **전부 반영됨** |
| **밸런스 곡선** | ✅ **종료** (측정 14~23, 열 차례). **A✅ B1✅ B2✅ C✅** |
| **각인 수치표** | ✅ 확정 (실측 4.49굴림 기준) |
| **성장 설계** | ✅ 확정 → **`GROWTH.md`** (경험치·슬롯표·마법서) |
| **화면** | ✅ 지역·스테이지·난이도 선택 · 지도 개편 완료. `flutter test` **298개** 통과 |
| **레벨·마법서·덱** | ⬜ **지금 구현 중** — 지시서 H 발행, I 미발행 |
| **패시브·각인·장비** | ⬜ 미착수 |
| **그래픽** | ⬜ 지팡이 v4 대기. 모자·로브·배경 8종·적 26·보스 13 **발주 전** |
| **광고·결제·안드로이드 빌드** | ⬜ 보류 |

---

## 2. 다음에 할 일

| 순서 | 무엇 | 창 | 상태 |
|---|---|---|---|
| **1** | **`WORK_ORDER_LEVEL.md` (H)** — 레벨·경험치·서클 슬롯표 | 새 구현 창 | ★ **발행됨. 보고서 `reports/26_레벨_경험치.md` 대기** |
| 2 | **`WORK_ORDER_BOOKS.md` (I)** — 마법서·덱·층 보상 교체·**실전 덱 연결** | 새 구현 창 | 미발행 (3절이 넣을 것 목록) |
| 3 | 패시브 트리 (포인트를 쓰는 곳) | 새 창 | 미발행 |
| 4 | 각인 · 장비 | 새 창 | 미발행 |
| 5 | **보스 「호위」·「연전」·「전조→본체」** | 새 창 | 미발행 (4절) |
| 6 | **곡선 재측정** — 위가 다 들어오면 후반이 쉬워진다 | 새 창 | 미발행 |
| 별도 | **그래픽** — 지팡이 v4 검수 · 모자·로브·배경 8종 발주 | **별도 창** | 7절 |

---

## 3. 지시서 I(마법서·덱)에 반드시 넣을 것

**지시서 H에서는 전부 금지해 뒀다.** 근거 수치는 **`GROWTH.md`**에 있다.

1. **마법서 수집·활성화(서클×5장)·강화**(+10%/단계, 상한 11−서클)
2. **덱** — 슬롯표대로 **자동 구성부터.** 수동 편성 화면은 그다음 (`UI_DESIGN.md` 5-5)
3. **층 보상 교체** — 「주문 3장 중 1택」(v1 잔재) → **「마법서 3종 택1, 고른 종 3장」.**
   화면과 조작은 그대로 살고 의미만 v2가 된다
4. **`floorEvent` 새 줄** (`GROWTH.md` 4.5절) —
   지금은 **6층 이하 스테이지에 ★층이 0개**라 **1·2지역 16 스테이지 전체에
   마법서 층이 없다**
5. 🔴 **실전 덱 연결** (4절)
6. `RunController`의 `_rollSpellOffers` · `takeSpell(chosen, slotIndex)` · `spellOffers` 제거
7. **v1 잔재 문구 3건** (4절)

---

## 4. 🔴 아직 안 만든 것 — **새 창은 이 목록을 코드로 검증하고 시작하라**

> **2026-09-05 검증 완료 (기획 창).** 아래 6건을 코드로 다시 확인했고 **전부 아직 유효하다.**
> 지시서 H는 **아직 실행되지 않았다** — `reports/26_*` 없음, `assets/data/`에 성장 데이터 파일 없음,
> `MetaState`에 `level`/`exp` 칸 없음.

| 무엇 | 마지막 확인 | 어디서 처리 |
|---|---|---|
| **실전이 덱으로 안 돈다** — `hand_deck.dart`는 있는데 `battle_controller.dart`가 `Battle(...)`에 **`deck`을 안 넘긴다.** v2 4.2절이 **시뮬레이터에서만** 돌고 실제 게임은 **v1처럼 손패 3장 고정**이다 | 2026-09-05 | **지시서 I** |
| **보스 「호위」(소환수 동반)·「연전」(2체째)** — 코드에 미구현 주석이 있다 (`lib/core/stage_runner.dart:30`) | 2026-09-05 | 순서 5 |
| **보스 「전조 → 본체」** (8~12지역 한정) — `ENEMIES.md` 4절 | 2026-09-05 | 순서 5 |
| **캐릭터 레벨·경험치** — `MetaState`에 없다 (`hpLevel` 등은 v1 강화 레벨이지 캐릭터 레벨이 아니다) | 2026-09-05 | **지시서 H (진행 중)** |
| **v1 잔재 문구 3건** — `result_screen.dart:102` 「타이틀의 "영구 강화"에서 쓸 수 있다」 · `title_screen.dart` 버튼 「영구 강화」 · `result_screen.dart:66` 「도달 층수」 | 2026-09-05 | **지시서 I 또는 패시브** |
| **「영구 강화」 메뉴 자체** — 강화 4종(체력·마나·리롤·시작 유물)은 **패시브 트리로 흡수될 예정** (v2 12절) | 2026-09-05 | 패시브 지시서 |

> ⚠ **「보스 호위·연전·전조」가 없으면 하드/데스는 수치 배율만 다른 똑같은 전투다.**
> 밸런스 곡선은 **보통 난이도로만** 판정했으므로 곡선 종료와는 무관하지만,
> **「난이도 3단계로 분량이 3배」라고 말하려면 이 셋이 있어야 한다.**

---

## 5. 🔴 절대 흔들지 말 것

### 5-1. 밸런스 판정 기준 — 조건 A · B1 · B2 · C

`GAME_DESIGN.md` 6.2절.

| # | 조건 |
|---|---|
| **A** | 상한이 **단조 감소**하고 인접 낙차 **20%p 이내** (흔들림 선 **±1.0%p**) |
| **B1** | **11지역 상한 25~45%** |
| **B2** | **12지역 상한 15~25%** |
| **C** | 도입(1~3) 하한 **70%+**, 5~10지역 **하한·상한 간격 30%p+** |

**측정 23에서 네 조건이 두 시드 모두 통과했다.**

> **🔴 왜 B가 B1·B2로 쪼개졌나** — 옛 조건은 「종반(11~12) 상한 25~45%」
> 한 줄이었다. 그런데 **11지역이 이미 27.7%로 밴드 바닥**이라, 12지역까지
> 25% 위로 올리라는 것은 **최종 지역을 앞 지역과 같은 난이도로 만들라**는 뜻이
> 됐다. **조건 A(단조 감소)와 양립할 수 없다.**
> **단조 감소하는 곡선의 마지막 점은 앞 점과 같은 밴드에 들어갈 수 없다.**

> ⚠ **「곡선을 닫았다」의 뜻**: **곡선을 맞추려고 더 이상 배율·층수를 만지지
> 않는다.** 지금 곡선은 **유물·장비·각인·패시브 판정 보정이 없는** 프로필에서
> 닫혔으므로 그 넷이 들어오면 **후반이 쉬워진다.** 그때 한 번 더 재되
> **목표 틀(A·B1·B2·C)은 그대로 쓴다.** 다음 조정의 사유는
> **「새 시스템이 들어와서」여야지 「목표에 못 미쳐서」가 아니다.**

### 5-2. 서클 슬롯표 — 곡선의 근거다

**측정 14~23의 시뮬 덱이 `GROWTH.md` 3절 표의 레벨 90 행**이다
(19장 · 8서클 1장 — 검토 20의 기록과 정확히 일치).
**표를 바꾸면 곡선 조건이 통째로 무효가 된다.**

### 5-3. 후반 화력은 정체된다 — 적을 화력에 맞춰라

**9 → 12지역에서 만강 덱 평균 위력이 13.7 → 14.2로 4%밖에 안 오른다.**
덱이 저서클 위주이고 강화 배수도 저서클에 유리하기 때문이며 **둘 다 의도한
설계다.** → **적 곡선을 화력에 맞춰야지, 화력을 억지로 올리려 하지 마라.**

### 5-4. 배율은 표적에 맞는 손잡이, 층수는 아니다

같은 12지역에 두 손잡이를 차례로 써 본 결과다 (측정 22 vs 23).

| | 도달률 | 남은 체력 | 완주율 |
|---|---|---|---|
| **층수** 10 → 8 | +15.0%p | **−3.5%p** | +2.3%p |
| **배율** 3.50 → 3.35 | +1.5%p | **+2.4%p** | **+5.1%p** |

**층이 줄면 층에 붙어 있던 회복(전투 승리 12%·상점 30%)도 함께 줄어**
보스 앞 체력이 내려간다. **후반 완주율을 올려야 할 때는 배율을 먼저 보라.**

### 5-5. 측정 판 수 정책 — **전 지역 1000판**

측정 23은 12지역만 1000판이라 **36조합 합산 지표(전투당 굴림·턴)의 저울이
과거와 달라졌다.** 전 지역 1000판이면 **지역별 무게가 다시 같아져 300판
시절과 동일한 가중치**가 되고 정밀도만 오른다 (30초 → 1분 30초).
**다음 측정 지시서에 명시할 것.**

### 5-6. ⛔ 폐기된 목표 — 되살리지 마라

「도입 90%+ / 중반 50~80% / 후반 30~50% / 종반 15~30%」라는 **구간 밴드**는
2026-09-03에 버렸다. 매끄러운 곡선에 계단 목표를 씌우면 경계(4·7지역)에서
반드시 어긋난다. **판정은 A·B1·B2·C로만 한다.**

---

## 6. 코드 구조 메모 (다음 창이 알아야 할 것)

| 파일 | 무엇 |
|---|---|
| `lib/core/progress.dart` | **잠금 규칙 한 곳** (지역·스테이지·난이도) |
| `lib/art/region_art.dart` | **지역 카드 겉모습 한 곳.** 배경 원화가 오면 여기만 고친다 |
| `lib/core/layout.dart` | 칸 크기 상·하한. `constants.dart`가 `export` |
| `lib/widgets/pixel_ui.dart` | **export만 하는 문** (알맹이는 `pixel_palette`·`pixel_panel`·`pixel_button`) |
| `MetaState.clearedStageKeys` | 클리어 기록 (`"1-8-normal"` 꼴). 잠금·★가 여기서 나온다 |
| `lib/data/parser.dart` | Flutter 의존 없는 순수 파서 (CLI 도구도 쓴다) |

**⚠ 함정 셋**

1. **`lib/core/constants.dart`가 299줄이다.** 한 줄만 더해도 300줄 규칙을 깬다.
   → `layout.dart`처럼 갈라 내고 `constants.dart`가 `export`한다
2. **폰트에 없는 글자**: 🔒 `U+1F512` · 👑 `U+265B` · ⚔ `U+2694`.
   (★ ☆ ◈ ♥ ⚠ ◆ ▶ 와 한글·ASCII 로마숫자는 **있다.** cmap 전수 조사 완료)
   **없는 글자를 쓰면 시스템 폰트로 대체돼 그 글자만 매끈해진다**
3. **스크린샷은 실제 기기 비율(400×860)로 찍어라.** 1014px로 길게 잡고 재면
   「칸이 화면을 채우는가」를 잘못 판단한다 (`shoot()`의 `pixelRatio`)

---

## 7. 그래픽 — **별도 창에서 한다** (2026-09-04 사용자 결정)

에셋 검토는 스크린샷이 쌓여 기획 창을 무겁게 만든다. 판정 기준이
`ASSET_LIST_EQUIP.md` 2절에 숫자로 다 남아 있고 밸런스와 서로 참조하는 것이
없으므로 **나눠도 놓치는 맥락이 없다.**
**「어떻게 재는가」는 `art_raw/README.md` 「장비 검수 절차」에 있다.**

### 지팡이 — v2·v3 실패, 원인 규명 완료

**생성기(GPT) 잘못이 아니라 요청서 잘못이었다.** 생성기는 매번 규격을 100% 지켰다.

| 잘못 쓴 규격 | 실제로 일어난 일 |
|---|---|
| 「색 24~32색」 | **정확히 24색으로 양자화** → 볼륨 소멸 |
| 「캔버스 예: 400×800」 | 320×660으로 나옴 → 질감 담을 자리 없음 |
| 「헤드 전체 폭 150px」 | 궤도·별처럼 **틈이 많은 장식**까지 잘림 |
| 「눈보다 작은 요소 금지」 | **배경용 규칙**을 장비에 복사 → 나뭇결·보석 면 삭제 |
| **등급을 헤드 크기로만 정의** | 몸통이 15종 전부 최소 굵기 |

**진짜 제약은 「캐릭터에 붙였을 때」다.** 캐릭터가 **2.5등신**이라 여유가 거의 없다.
**해법**: 지팡이를 **신장보다 8~12% 길게** 만들면 헤드가 머리 위 빈 공간으로 솟아
폭 322px짜리도 아무것도 가리지 않는다.
→ **길이 108~112% · 헤드는 위쪽 30% 안 · 몸통 42px 이내** (`ASSET_LIST_EQUIP.md` 2절)

### 다음에 할 일

1. `ASSET_LIST_EQUIP.md` + `wizard_body.png` + `_example_attach.png` 3개를 GPT에 전달해 **v4**
2. **v1의 옹이·참나무·폭풍 3종은 지금도 쓸 수 있다.** 재생성이 또 실패해도 손해 없음
3. 모자·로브도 같은 규격으로 (모자는 머리카락이 밑으로 나오게, 로브는 남색 스카프 보이게)
4. **배경 8종** — 들어오면 `lib/art/region_art.dart` 한 파일만 고치면 지역 카드가 전부 그림이 된다
5. 🟡 **함께 볼 것**: 지도의 **미방문 칸(`???`)이 3층 스테이지에서 속이 비어 보인다.**
   칸이 커지면서 여백이 칸 사이에서 **칸 안으로 옮겨 갔다.** 10층에서는 촘촘해 문제없다.
   **배경 원화가 들어올 때 미방문 칸의 속을 무엇으로 채울지 함께 정한다**

---

## 8. 사용자에 대해 (문서에 안 적힌 맥락)

- 사용자는 **비전공자**이고 코드를 직접 읽지 않는다. 설명은 한국어로 짧게,
  전문 용어는 괄호로 한 줄 풀이. **존댓말**로 답한다
- 사용자가 **결정을 위임하는 편**이다. 근거를 대고 권장안을 내면 대체로 채택한다.
  **선택지만 나열하지 말고 추천을 분명히 할 것**
- **사용자가 하는 말을 굳이 요약·정리하지 마라.** 물은 것에 답하면 된다
- **기획 창은 문서(.md)만 고친다.** 코드는 구현 창 담당
- **구현 창의 보고서를 그대로 믿지 말고 수치를 직접 재현해 확인할 것.**
  실제로 그렇게 잡아낸 것들 —
  「11지역이 이미 3.20인데 12지역을 3.1~3.2로 내리라는 제안」(검토 23),
  「손으로 계산한 값을 실측처럼 적은 표」(검토 19)
- **지시서를 발행하면 답변 맨 끝에 「복붙용 전달 문장 + `/clear` 여부」를 낸다**
  (`CLAUDE.md` 「지시서를 넘길 때의 출력 형식」)

### 사용자 승인을 받은 수치 변경 (절대 규칙 3)

| 승인일 | 내용 |
|---|---|
| 2026-09-01 | `gaia_wrath`(대지의 분노) `dc_modifier` **+2 → +3** |
| 2026-09-02 | 보스 3종 HP — 리치 90→330 / 공허의 거인 95→400 / 화염룡 92→480 |
| 2026-09-03 | 후반 `hp_scale` 2차 개정 (9~12지역) · 보스 HP 10~12지역 하향 |
| 2026-09-03 | 12지역 층수 9·10 → **7·8** |
| 2026-09-04 | 12지역 `hp_scale` 3.50 → **3.35** |

### 사용자가 직접 뒤집은 판단 (같은 실수를 반복하지 말 것)

- **보스 이름을 직함으로 지었다가 지적받았다.** 「갱도의 감독관」은 형상이 안
  떠오른다 → 「무쇠 트롤」「목 없는 검은 기사」로 교체
- **용을 중반(4지역) 보스로 뒀다가 지적받았다.** 판타지에서 용은 끝판왕이다
- **주사위 포식자를 최종 보스로 뒀다가 지적받았다.** 이벤트·무한 던전용으로 아껴 둔다
- **한국어 이름은 동음이의어를 확인하고 짓는다.** 「용암 침」「감정의 각인」이 걸렸다

---

## 9. 지시서를 쓸 때

- 각 설계 문서에 **「구현 창 인수 조건」 절이 있으니 그대로 옮기면 된다**
- **표를 지시서에 복사하지 마라.** 두 곳에 같은 표가 있으면 반드시 어긋난다.
  근거 문서를 가리키고, **무엇을 어디에 넣는지와 검산 조건만** 적는다
- **「개수·범위를 단언하는 기존 테스트」를 미리 전수 조사해 지시서에 적어라.**
  옳게 고칠수록 반드시 깨지는 낡은 단언이 있다. 미리 찾아 주면 구현 창이 헤매지 않는다
- **검산은 동작을 걸어야지 문자열을 걸면 안 된다** (「그 낱말이 0줄이어야 한다」 금지)
- **허용 목록 검사로는 부족하다.** 기대값 표와 대조해야 잡힌다

---

## 10. 지난 기록 (요약)

| 시기 | 무슨 일 |
|---|---|
| 2026-09-01 | 기획서 v1 → **v2 전면 개정.** 설계 문서 6종 신설 |
| 2026-09-02 | 데이터 반영 (지시서 A~D) · **3.4절 확률표 오류 발견·정정**(뱀눈 미반영) |
| 2026-09-02~03 | **밸런스 곡선 아홉 차례** (측정 14~22). 지역 배율 분리 · 덱 기반 손패 · 마법서 강화 · 소모전 완화 · 후반 곡선 정렬 |
| 2026-09-04 | 지팡이 v2·v3 실패 원인 규명 · **곡선 조건 B를 B1·B2로 분리** · **각인 수치표 확정** |
| 2026-09-04 | **측정 23 — 곡선 작업 종료** (A✅B1✅B2✅C✅) |
| 2026-09-04~05 | **화면 재설계** (지역·스테이지·난이도·지도) + 잔손질. `flutter test` 298개 |
| 2026-09-05 | **성장 설계 → `GROWTH.md`** (경험치·슬롯표·마법서) · 지시서 H 발행 |

**상세는 `reports/INDEX.md`와 각 `reports/NN_*_REVIEW.md`에 있다.**
