# 작업 지시서 H — 레벨 · 경험치 · 서클 슬롯표

발행: 2026-09-05, 기획 창.

근거 문서: **`GROWTH.md` 1·2·3절**(이 지시서의 모든 수치가 거기 있다) ·
`GAME_DESIGN.md` 5.2·5.4절 · `PASSIVE_TREE.md` 1절

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 거의 필요 없다** —
> 결과 화면 한 장이면 된다.

---

## 0. 목적 (한 줄)

**캐릭터 레벨이 코드에도 데이터에도 없다. 성장의 뼈대를 세운다.**

## 0-1. 왜 지금인가

마법서·덱을 만들려면 **서클 슬롯표**가 있어야 하고, 슬롯표는 **레벨**에 걸려 있다.

```
마법서·덱  →  서클 슬롯표  →  캐릭터 레벨  →  경험치 곡선
```

**맨 뒤부터 세운다.** 마법서·덱은 **다음 지시서(I)**다. 이번에 만들지 마라.

> ⚠ `MetaState`의 `hpLevel`·`manaLevel`·`rerollLevel`·`startRelicLevel`은
> **v1의 영구 강화 레벨**이지 캐릭터 레벨이 아니다. **건드리지 마라.**
> 그것들은 나중에 패시브 트리로 흡수된다.

## 0-2. 절대 지킬 것

- **`GROWTH.md`의 표를 이 지시서에 옮겨 적지 않았다.** 그 문서를 읽고 옮겨라.
  **두 곳에 같은 표가 있으면 반드시 어긋난다**
- **밸런스 수치를 바꾸지 마라** — `hp_scale`·`atk_scale`·보스 HP·층수·
  주문 수치·폭주·적 데이터는 **읽기 전용**이다
- `core/check.dart` · `combo.dart` · `surge.dart` · `battle.dart`를 **열지 마라**
- **`tool/` 측정 도구를 건드리지 마라**
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어 · 상태 관리는 `provider`만
- `flutter analyze` 경고 0, `flutter test` **298개가 전부 그대로 통과**
- **`dart format`을 파일 전체에 돌리지 마라** — 고친 부분만 손으로 정렬한다
- ⚠ **`lib/core/constants.dart`가 299줄이다.** 한 줄만 더해도 300줄 규칙을 깬다.
  **`lib/core/layout.dart`처럼 갈라 내고 `constants.dart`가 `export`하는
  방식을 쓰라** (검토 24·25에서 승인된 방식이다)

---

# 작업 1 — 경험치와 레벨

## 1-A. 데이터 (절대 규칙 2 — 수치는 JSON에)

| 어디 | 무엇 |
|---|---|
| **`assets/data/regions.json`** | 지역마다 **`exp` 칸 신설.** 12개 값은 `GROWTH.md` 1.3절 표 |

**그 밖의 칸(`hp_scale`·`atk_scale`·이름·테마·variant_weights)은 한 값도 건드리지 마라.**

## 1-B. 상수

| 무엇 | 값 |
|---|---|
| 레벨 L → L+1 필요 경험치 | **`60 × L`** |
| 최대 레벨 | **99** |
| 보스 배수 | ×5 |
| 변종 배수 | ×1.5 |
| 하드 / 데스 배수 | ×1.5 / ×2.0 |

**전부 `GROWTH.md` 1.2·1.3절에 있다.** 매직 넘버로 박지 말고 상수로.

## 1-C. 저장 (`MetaState`)

| 새 칸 | |
|---|---|
| **`level`** | 기본 **1** |
| **`exp`** | 현재 레벨에서 쌓인 경험치. 기본 0 |
| **`passivePoints`** | 레벨업으로 받은 포인트. 기본 0 |

- `toJson`/`fromJson`에 **반드시** 넣는다
- ⚠ **옛 저장본에 이 칸들이 없어도 앱이 죽으면 안 된다** — 없으면 기본값으로 읽어라
  (검토 24의 클리어 기록이 이미 그렇게 돼 있다. 같은 방식을 쓰라)

## 1-D. 지급

**전투를 이길 때마다 즉시 준다.** 레벨업이 여러 번 일어날 수 있다
(초반에는 한 전투로 2레벨이 오를 수 있다 — 정상이다).

> ### ★ 그 판이 나중에 실패해도 이미 받은 경험치는 남는다
> `GAME_DESIGN.md` 6.6절 — **레벨은 영구 성장이다.**
> **이것 때문에 재도전이 곧 파밍이 된다.** 판이 끝날 때 몰아서 주면 안 된다.

- 레벨업 1회 = **`passivePoints` +1** (`PASSIVE_TREE.md` 1절)
- **포인트를 쓰는 화면은 이번에 만들지 마라.** 패시브 트리는 별도 지시서다

---

# 작업 2 — 서클 슬롯표

## 2-A. 데이터로 둔다 (하드코딩 금지)

`GROWTH.md` 3절의 **9행 표**를 JSON으로. 파일 이름·형태는 **알아서 정하되**,
「어느 레벨에서 어느 서클이 몇 칸인가」가 데이터에서 읽혀야 한다.

## 2-B. 조회 규칙

| 무엇 | 규칙 |
|---|---|
| **표에 없는 레벨** | **바로 위 칸(더 낮은 레벨의 행)을 그대로 쓴다.** 레벨 44 = 레벨 30 행 |
| 반환 | 서클 1~9별 슬롯 수 |

**이번에는 「조회할 수 있다」까지만 만든다.** 덱을 짜는 것은 지시서 I다.

## 2-C. ⚠ 이 표를 고치지 마라

**측정 14~23(열 차례)의 시뮬 덱이 이 표의 레벨 90 행**이다
(19장 · 8서클 1장). **표를 바꾸면 곡선 조건 A·B1·B2·C의 근거가 무너진다.**
`GROWTH.md` 3.1절에 그 근거가 적혀 있다.

---

# 작업 3 — 화면 (최소한만)

**새 화면을 만들지 마라.** 있는 화면에 값만 보인다.

| 어디 | 무엇 |
|---|---|
| **결과 화면** | 이번 판에서 얻은 **경험치**와 **레벨업 여부**를 보여준다 |
| **지역·스테이지 선택 화면 머리** | 검토 24가 **레벨 자리를 비워 뒀다**(`screen_header.dart` 주석 참조). **이제 숫자를 넣는다** |

- 「권장 레벨」은 이미 보이고 있다. **이제 「내 레벨」과 나란히 놓여 뜻이 생긴다**
- ⛔ **경험치 막대·레벨업 연출을 공들여 만들지 마라.** 숫자만 보이면 된다

## 3-A. ⛔ 이번에 손대지 않는 것

| 무엇 | 왜 |
|---|---|
| **「영구 강화」 메뉴와 그 문구** | v1 잔재이고 **패시브 트리로 흡수될 예정**이다. 지금 고치면 두 번 고친다 (검토 25) |
| 「도달 층수」 문구 | 같은 이유 |
| 덱 · 마법서 · 패시브 트리 화면 | 별도 지시서 |

---

# 작업 4 — 검산

## 4-A. ★ `GROWTH.md` 1.4절 표를 코드로 재현한다

**이것이 이번 작업의 핵심 검사다.**

각 지역의 모든 스테이지를 **한 번씩만** 클리어했다고 가정하고
(층 = 전투 1회, 각 스테이지의 마지막 층은 보스, 변종·난이도 배수 없음)
경험치를 누적해 **각 지역 끝의 레벨**을 낸다.

- `GROWTH.md` 1.4절의 「도달」 열과 **±0으로 같아야 한다**
- 다르면 **보고하라.** 임의로 `exp` 값을 고치지 마라
- 테스트로 둘 것 (`tool/`이 아니라 `test/`에)

## 4-B. 그 밖의 검사

| # | 검사 |
|---|---|
| 1 | 레벨 1→2가 **60**, 98→99가 **5,880**, 1→99 누적이 **291,060** |
| 2 | **레벨이 99를 넘지 않는다.** 만렙에서 경험치를 더 받아도 안전하다 |
| 3 | 한 전투로 **여러 레벨**이 올라도 포인트가 그만큼 쌓인다 |
| 4 | 보스 ×5 · 변종 ×1.5 · 하드 ×1.5 · 데스 ×2.0이 곱해진다 |
| 5 | 슬롯 조회 — 레벨 **1·9·10·44·45·99**에서 `GROWTH.md` 3절 표와 같다 |
| 6 | **슬롯 총량이 어느 레벨에서도 3 이상**이다 (손패 3장을 못 채우면 원칙 4가 깨진다) |
| 7 | 레벨 99에서 덱 총량이 **21** |
| 8 | **`level`·`exp`·`passivePoints` 칸이 없는 옛 저장 JSON**을 읽어도 죽지 않는다 |
| 9 | 저장·복원 왕복 (`toJson` → `fromJson`) |
| 10 | **`regions.json`의 `exp` 외 다른 칸이 안 바뀌었다** (12지역 전수 대조) |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
   (**기존 298개가 그대로 통과해야 한다**)
2. **실제로 앱을 띄워** 전투를 이겨 경험치가 오르는지 확인하고,
   **실제 기기 비율(400×860)**로 찍은 480px 축소본을 `reports/img/26_run/`에
   남긴다 — **결과 화면 한 장**이면 된다
3. `PROGRESS.md`에 기록
4. **`reports/26_레벨_경험치.md`** 작성
   - 맨 앞에 **「4-A 검산이 `GROWTH.md` 1.4절 표와 ±0으로 같은가」 한 줄**
   - 지역별 도달 레벨 표 (문서 값과 나란히)
   - 판단·제안은 **「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **마법서 · 덱 · 덱 편성 화면** (지시서 I)
- ❌ **패시브 트리 · 포인트를 쓰는 화면** (별도 지시서)
- ❌ **「영구 강화」 메뉴 손대기** · v1 잔재 문구 고치기
- ❌ **`floorEvent` 수정** (지시서 I)
- ❌ 밸런스 수치 · `hp_scale`/`atk_scale`/보스/층수/주문/폭주/적 데이터 수정
- ❌ 서클 슬롯표 수치 변경
- ❌ `core/check.dart`·`combo.dart`·`surge.dart`·`battle.dart` · `tool/`
- ❌ 경험치 막대·레벨업 연출 공들이기
- ❌ `dart format`을 파일 전체에 돌리기
