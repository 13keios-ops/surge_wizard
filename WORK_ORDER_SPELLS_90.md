# 작업 지시서 — 주문 70종 → 90종

발행: 2026-09-13, 기획 창.
근거: **`SPELL_CIRCLES.md` 맨 끝 「추가 20종」** (2026-09-13 확정) · `GAME_DESIGN.md` 5.4절.

> ### 🪟 창 운영
> **구현 창. `/clear` 하고 시작한다.** 데이터 + 측정 작업이다.

---

## 0. 한 줄

**`SPELL_CIRCLES.md` 가 확정한 주문 20종을 `assets/data/spells.json` 에 넣고,
곡선이 안 깨지는지 측정한다.**

이 20종이 확정돼야 **주문 아이콘 90종 발주**가 열린다.

---

## 1. 왜 20종인가 — 고를 것이 없었다

70종을 (속성 × 서클)로 세어 목표 배분과 대조했더니 **다섯 속성이 전부 똑같이
1·2·3·6서클에서 1종씩 모자랐고 넘치는 칸은 없었다.** 5 × 4 = 20이다.

**표는 `SPELL_CIRCLES.md` 에 있다. 여기 옮겨 적지 않는다** — 두 곳에 있으면 갈린다.

---

## 2. 할 일

### 2-1. `assets/data/spells.json` 에 20종 추가

- `SPELL_CIRCLES.md` 의 표를 **그대로** 옮긴다. **수치를 바꾸지 마라**
- 기존 70종의 필드 구성을 따른다 —
  `id · name · icon · element · circle · rarity · base_damage · dc_modifier · effect · description · surge_tags`
- `rarity` 는 기존 대응대로: **1~3서클 = `common` · 6서클 = `rare`** (5.4절)
- `icon` 은 지금 **코드 도트 아이콘 이름**을 쓴다. 원화는 아직 없다 —
  비슷한 기존 아이콘을 재사용하고 **새 아이콘 파일을 만들지 마라**
- `description` 은 한 줄. 기존 문장 길이에 맞춘다
- `surge_tags` 는 속성과 형태에 맞게 기존 태그 어휘에서만 고른다. **새 태그를 만들지 마라**

🔴 **효과는 코드가 아는 6종뿐이다** (`lib/core/spell_effects.dart`) —
`shield` · `heal` · `delay_enemy` · `mana_restore` · `check_bonus` · `extra_die`.
표에 그 밖의 것이 있으면 **넣지 말고 보고하라.** 새 효과 타입을 만들지 마라.

### 2-2. 🔴 측정 — 이게 진짜 작업이다

수치는 **초안**이다. 절대 규칙 3이 밸런싱을 임의로 바꾸지 말라고 정한다.
**`tool/measure*` 를 돌려 곡선이 안 깨지는지 확인한다.**

특히 이 셋을 보라. `SPELL_CIRCLES.md` 가 걱정으로 적어 둔 것이다.

| 무엇 | 왜 위험한가 |
|---|---|
| `firestorm` 17 · `avalanche` 16 | **기존 최고 대미지(14)를 넘는다.** `dc +2` 로 값을 치르게 했는데 실제로 치러지나 |
| `ice_wall` 방패 10 · `elder_wrath` 방패 10 | 방패가 커지면 **소모전 설계(측정 20)** 가 흔들린다 |
| `soul_devour` 회복 6 | 회복은 `docs/INVARIANTS.md` 의 불변식에 걸린다 |

- 🔴 **깨지면 이름은 두고 숫자만 고친다.** 그리고 **무엇을 왜 고쳤는지 보고서에 적는다**
- 🔴 **`docs/INVARIANTS.md` 를 먼저 읽어라.** 거기 적힌 불변식을 건드리면
  **측정 14~23이 통째로 무효가 된다**

### 2-3. 개수 단언 고치기

`70` 이라는 숫자가 테스트·로더·문서에 박혀 있을 수 있다. `grep` 으로 찾아 **90으로** 고친다.
서클별·속성별 개수를 세는 테스트가 있으면 **새 배분(속성당 18종)** 에 맞춘다.

---

## 3. 테스트 (필수)

| 무엇 | 기대 |
|---|---|
| 총 개수 | **90** |
| 속성별 | 다섯 속성 **각각 18종** |
| (속성 × 서클) | 1·2·3서클 **각 3종** · 4·5·6서클 **각 2종** · 7·8·9서클 **각 1종** |
| id 중복 | **없다** (⚠ `absolute_zero` 는 이미 8서클에 있다. 새것은 `avalanche`) |
| 효과 타입 | 90종 전부 코드가 아는 6종 안에 있다 |
| 기존 70종 | **한 글자도 안 바뀌었다** |

`flutter test` **전부 통과**. `flutter analyze` 경고 0.

---

## 4. 보고서

🔴 **작업을 시작할 때** `reports/38_주문90종.md` 를 먼저 만든다.
양식은 `reports/_TEMPLATE.md`. **측정 결과를 표로 남겨라** — 다음 창이 그것만 보고 판정한다.

사용자에게는 **"보고서 reports/38_주문90종.md 작성 완료" 한 줄만** 말한다.

---

## 5. 하지 않을 것

- **기존 70종의 수치·이름·id 변경**
- **새 효과 타입 추가** — 6종 밖이면 보고하고 멈춘다
- **주문 아이콘 원화** — 이번 범위가 아니다. 확정된 뒤에 따로 발주한다
- 서클 개수·덱 칸 표 변경 — 5.4절 확정본이다
