# 작업 지시서 G — 화면 재설계 (지역·스테이지·난이도 · 지도 · 파일 분리)

발행: 2026-09-04, 기획 창.

근거 문서: **`UI_DESIGN.md` 5-2·5-3·5-4절**(화면 구도) ·
**`STAGES.md`**(지역 12 · 스테이지 100) · **`GAME_DESIGN.md` 6절**(진행 구조) ·
검토 23(`reports/23_배율조정_최종측정_REVIEW.md`)

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **이 지시서는 화면을 만드는 일이라 스크린샷이 필요하다.**
> **`CLAUDE.md`의 토큰 절약 규칙을 반드시 지켜라** — 가로 480px 축소본만 보고,
> 좌표를 찍어 맞히려 하지 말고, 판단에 꼭 필요할 때만 연다.

---

## 0. 목적 (한 줄)

**데이터에는 지역 12 · 스테이지 100 · 난이도 3이 다 들어와 있는데,
화면이 아직 그것을 보여주지 못한다. 유저가 고를 수 있게 만든다.**

## 0-1. ★ 지금 무엇이 없나

| 있는 것 | 없는 것 |
|---|---|
| `regions.json` 12개 · `stages.json` 100개 · 난이도 3단계 | **그것을 고르는 화면** |
| `RunController`가 `regionId`·`stageIndex`·`difficulty`로 판을 돌린다 | **그 셋을 유저가 정하는 경로** |
| `MetaState`(재화·강화 레벨·해금 주문) | **어디까지 깼는지의 기록** |

**밸런스 곡선은 검토 23에서 닫혔다.** 수치는 확정됐으니 화면만 붙이면 된다.

## 0-2. 절대 지킬 것

- **밸런스 수치를 한 값도 바꾸지 마라.** `assets/data/*.json`은 **읽기 전용**이다
- `core/check.dart` · `combo.dart` · `surge.dart` · `battle.dart`를 **열지 마라**
- **`tool/` 측정 도구를 건드리지 마라**
- 매직 넘버 금지(`core/constants.dart`) · 파일 300줄 · 함수 40줄 · 주석 한국어
- 상태 관리는 `provider`만
- `flutter analyze` 경고 0, `flutter test` 전부 통과
- **`dart format`을 파일 전체에 돌리지 마라.** 이 저장소는 git이 아니라
  되돌릴 이력이 없다. **고친 부분만 손으로 정렬한다** (`CLAUDE.md` LESSONS)

---

# 작업 1 — 진행 기록을 `MetaState`에 넣는다

화면을 만들기 전에 **「어디까지 깼는가」를 저장할 자리**가 필요하다.

## 1-A. 저장할 것

**스테이지 하나를 난이도 하나로 클리어했다**는 사실만 있으면 잠금·별표가 전부 나온다.

| 무엇 | 형태 제안 |
|---|---|
| 클리어 기록 | `Set<String>` — `"<regionId>-<stageIndex>-<difficulty>"` 같은 키 |

- **`MetaState.fromJson`/`toJson`에 반드시 넣는다** (모델 규칙)
- **옛 저장 파일에 이 필드가 없어도 앱이 죽으면 안 된다** — 없으면 빈 집합으로 읽어라
- 판을 **클리어했을 때만** 기록한다. 죽거나 나가면 기록하지 않는다

## 1-B. ★ 잠금 규칙 (기획 창이 정했다 — 임의로 바꾸지 마라)

| 무엇 | 열리는 조건 |
|---|---|
| **1지역 Ⅰ 보통** | 처음부터 열려 있다 |
| **다음 스테이지** | 같은 지역의 **앞 스테이지를 보통으로** 클리어 |
| **다음 지역** | 앞 지역의 **마지막(보스) 스테이지를 보통으로** 클리어 |
| **하드** | **그 스테이지를 보통으로** 클리어 |
| **데스** | **그 스테이지를 하드로** 클리어 |

- **난이도 해금은 스테이지 단위다.** 지역 단위가 아니다
- 지역 카드의 **★ 3개**는 「그 지역의 **보스 스테이지**를 보통/하드/데스로
  깼는가」다 (지역 전체가 아니라 보스 스테이지 기준 — 판정이 단순하고
  유저가 「이 지역을 정복했다」로 읽는 것과 일치한다)

---

# 작업 2 — 화면 3개

**구도는 `UI_DESIGN.md` 5-2·5-3·5-4절의 그림을 그대로 따른다.**
**표를 이 지시서에 옮기지 않았다.** 그 문서를 보고 만들어라.

## 2-A. 지역 선택 화면 (신규) — `UI_DESIGN.md` 5-2

- 12개 세로 스크롤. 카드에 **번호 · 이름 · 권장 레벨 · ★ 3개**
- 잠긴 지역은 어둡게 + **해금 조건 한 줄** (「앞 지역의 보스를 보통으로 클리어」)
- **배경 원화가 아직 없다.** `stage_backdrop.dart`의 테마 색으로 카드 배경을 칠하고,
  **원화가 들어오면 갈아끼울 수 있게** 한 군데에서만 결정되게 짜라

## 2-B. 스테이지 선택 화면 (신규) — `UI_DESIGN.md` 5-3

- **난이도 3탭이 위에.** 탭을 바꿔도 스테이지를 다시 고르지 않는다
- 스테이지 격자에 **로마숫자 · 층수 · ★/☆/🔒**
- **보스 스테이지만 카드가 크고 부제(`subtitle`)가 보인다**
- **잠긴 난이도 탭은 눌리지 않고**, 왜 잠겼는지 한 줄이 보인다

## 2-C. 지도 화면 (개편) — `UI_DESIGN.md` 5-4

`lib/screens/map_screen.dart`를 **고친다** (새로 만들지 말고).

- **아래에서 위로 오르는 세로 경로.** 층수가 **3~10으로 가변**이므로
  **칸 수가 유동적**이어야 한다 (지금은 고정 층수 전제일 수 있다)
- 머리에 **`stageTitle` + 진행 막대 + 「n / m층」**
- 층 아이콘은 `floorEvent`가 주는 종류를 그대로 쓴다 (⚔ ★ ◈ ♥ ⚠)
- **다음 상대를 미리 보여준다** — 이름 · HP · 공격력. 변종이면 그 표시도

> ### ⚠ 「주문 보상(★)」은 v1 잔재다 — 이번에 없애지 마라
> 층 보상의 「주문 3장 중 1택」은 v2에 없는 규칙이고 **마법서 드랍으로
> 교체될 예정**이다 (`HANDOFF.md`). **다음 지시서에서 통째로 바꾸므로
> 이번에는 지금 동작 그대로 두고 화면만 새 구조에 맞춰라.**
> 두 번 고치지 않기 위해서다.

## 2-D. 화면 연결

```
타이틀 → 지역 선택 → 스테이지 선택(+난이도 탭) → 지도 → 전투 → … → 결과
```

- **결과 화면에서 「스테이지 선택으로」 돌아갈 수 있어야 한다.** 매번
  타이틀까지 가면 반복 플레이가 괴롭다
- 뒤로 가기(← )가 **한 단계씩** 올라가야 한다

## 2-E. ⛔ 이번에 만들지 않는 것

| 무엇 | 왜 |
|---|---|
| **철수**(`GAME_DESIGN.md` 6.4절) | 「무엇을 얼마나 챙겨 나가는가」가 미정이다. 보상 규칙이 마법서 지시서에서 바뀐다 |
| **소탕 · 방치 보상**(6.5절) | 같은 이유 + 하루 횟수·광고가 얽힌다 |
| **덱 편성 · 장비·각인 화면**(5-5·5-6) | 그 시스템 자체가 아직 없다 |
| **레벨 표시** | 레벨 시스템이 아직 없다. **자리만 비워 두고 숫자를 그리지 마라** (없는 값을 0으로 그리면 버그처럼 보인다) |

---

# 작업 3 — 300줄 넘는 파일 3개 분리

`CLAUDE.md` 코딩 규칙이다. **이번에 화면을 건드리는 김에 정리한다.**

| 파일 | 줄 수 | 조치 |
|---|---|---|
| `lib/widgets/battle_stage.dart` | 533 | 분리 |
| `lib/screens/battle_screen.dart` | 478 | 분리 |
| `lib/widgets/pixel_ui.dart` | 427 | 분리 |

## 3-A. ⛔ 이 둘은 건드리지 마라

| 파일 | 줄 수 | 왜 안 건드리나 |
|---|---|---|
| `lib/art/sprites_items.dart` | 344 | **원화로 대체될 예정**이다. 지금 쪼개면 헛일이 된다 |
| `lib/art/stage_backdrop.dart` | 327 | 같은 이유 (배경 원화 8종 발주 예정) |

## 3-B. 분리 원칙

- **동작을 하나도 바꾸지 마라.** 옮기기만 한다
- **자연스러운 이음매**로 자른다 (예: 전투 화면의 「상단 정보」/「주사위 영역」/
  「손패 영역」). **줄 수만 맞추려고 아무 데나 자르지 마라**
- 옮긴 뒤 **테스트가 전부 그대로 통과**해야 한다. 하나라도 고쳐야 했다면
  **동작이 바뀐 것**이니 보고하라

---

# 작업 4 — 테스트

**위젯 테스트를 쓸 때 `CLAUDE.md` LESSONS를 먼저 읽어라** —
버튼 텍스트는 `textContaining`으로 찾고, 전투 화면은 `pumpAndSettle` 대신
`pump(시간)`을 쓰며, 화면은 고정 높이 대신 `Expanded`+`LayoutBuilder`로 짠다.

| # | 검사 |
|---|---|
| 1 | **처음 시작하면 1지역 Ⅰ 보통만 열려 있다** — 2지역·Ⅱ스테이지·하드가 전부 잠김 |
| 2 | 1지역 Ⅰ을 보통으로 클리어하면 **Ⅱ와 「Ⅰ 하드」가 열리고**, 2지역은 **안 열린다** |
| 3 | 1지역 **보스 스테이지**를 보통으로 클리어하면 **2지역이 열린다** |
| 4 | 하드 클리어 없이 **데스는 안 열린다** |
| 5 | 클리어 기록이 **저장·복원**된다 (`toJson` → `fromJson` 왕복) |
| 6 | **클리어 기록 필드가 없는 옛 저장 JSON**을 읽어도 죽지 않는다 |
| 7 | 지도 화면이 **층수 3층 스테이지와 10층 스테이지 양쪽**에서 칸을 옳게 그린다 |
| 8 | 지역 선택 → 스테이지 선택 → 지도까지 **탭으로 도달**한다 (위젯 테스트) |

**작업 3(파일 분리)에서는 새 테스트를 만들지 마라.** 기존 테스트가
그대로 통과하는 것이 곧 검사다.

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. **실제로 앱을 띄워** 타이틀 → 지역 → 스테이지 → 지도 → 전투가
   이어지는지 확인한다. **480px 축소 스크린샷을 `reports/img/`에 남겨라**
   (대화에는 판단에 필요한 것만 연다)
3. `PROGRESS.md`에 이번 작업을 기록
4. **`reports/24_화면_지역스테이지.md`** 작성
   - **맨 앞에 「타이틀 → 지역 → 스테이지 → 지도 → 전투가 이어지는가」 한 줄**
   - 잠금 규칙 8개 검사의 결과
   - 분리한 파일 3개의 **분리 전후 줄 수**
   - 판단·제안은 **「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **밸런스 수치·`assets/data/*.json` 수정**
- ❌ 철수 · 소탕 · 방치 보상 · 부활
- ❌ 덱 편성 · 장비 · 각인 · 패시브 · 레벨 화면
- ❌ **「주문 보상」 규칙 변경** (다음 지시서에서 마법서로 교체한다)
- ❌ `lib/art/sprites_items.dart` · `stage_backdrop.dart` 분리
- ❌ `tool/` 측정 도구 · `core/check.dart`·`combo.dart`·`surge.dart`·`battle.dart`
- ❌ `dart format`을 파일 전체에 돌리기
