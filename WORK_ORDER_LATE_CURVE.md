# 작업 지시서 O — 후반 곡선 정렬 + 두 줄 목표 측정

발행: 2026-09-03, 기획 창. **작업 1·2는 사용자 승인 완료된 수치 변경이다.**

근거 문서: **`ENEMIES.md` 3·6절**(후반 하향) · **`GAME_DESIGN.md` 6.2절**(두 줄 목표) · 검토 20

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**

---

## 0. 목적 (한 줄)

**후반 플레이어 화력이 9→12지역에서 4%밖에 안 오르는데 적은 78% 오른다.
적 곡선을 화력에 맞추고, 목표를 「하한·상한 두 줄」로 다시 판정한다.**

## 0-1. ★ 목표가 두 줄로 바뀌었다

`GAME_DESIGN.md` 6.2절이 개정됐다. **한 줄(만강)로만 재던 것이 잘못이었다** —
만강 자체가 이미 큰 파밍이라, 「파밍을 다 한 플레이어」를 재면서 「파밍이
필요해야 한다」를 요구한 셈이었다.

| 구간 | 지역 | **하한 = 강화 없음** | **상한 = 만강** |
|---|---|---|---|
| 도입 | 1~3 | **70% 이상** | **90% 이상** |
| 중반 | 4~7 | **20~60%** | **80% 이상** |
| 후반 | 8~10 | **0~20%** | **40~70%** |
| 종반 | 11~12 | **0~10%** | **25~45%** |

**두 값의 간격이 곧 「그 구간이 요구하는 파밍의 양」이다.**

측정 20을 이 틀로 보면 **도입·중반·후반은 이미 통과**하고 **종반 둘만 미달**이다
(11지역 상한 14.0% · 12지역 3.0%).

## 0-2. 절대 지킬 것

- **작업 1·2 외에 수치를 바꾸지 마라.** 목표에 못 미쳐도 그대로 적어라
- **1~8지역 · `atk_scale` · 보스 공격/강타/방어 · 난이도 배율 · 층수 ·
  성장 프로필 · 강화율 · 상점 회복 · 전투 승리 회복을 건드리지 마라**
- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — ✅ `hp_scale` 9~12지역 (일반 적)

`assets/data/regions.json`. **9~12지역 네 값만** 바꾼다.

| 지역 | 1~8 | 9 | 10 | 11 | 12 |
|---|---|---|---|---|---|
| 지금 | (그대로) | 2.75 | 3.10 | 3.60 | 4.20 |
| **바꿀 값** | **손대지 마라** | **2.70** | **2.95** | **3.20** | **3.50** |

- **`atk_scale`은 한 줄도 바꾸지 마라**
- 1~8지역 `hp_scale`도 그대로다

---

# 작업 2 — ✅ 보스 HP 10~12지역

`assets/data/enemies.json`. **9지역 이하는 그대로다.**

| 보스 | id | HP | **2페이즈 임계** |
|---|---|---|---|
| 대마도사 리치 | `boss_archlich` | 330 → **310** | 165 → **155** |
| 공허의 거인 | `boss_void_titan` | 400 → **355** | 200 → **177** |
| 겁화의 화염룡 | `boss_inferno_dragon` | 480 → **405** | 240 → **202** |

## 2-A. ⚠ HP와 임계만 바꾼다

| 항목 | 조치 |
|---|---|
| `hp` | **바꾼다** (위 표) |
| `phase2_hp_threshold` | **`hp ~/ 2`로 다시 계산** (위 표와 일치해야 한다) |
| `pattern`의 공격·강타·방어값 | ❌ **한 값도 바꾸지 마라** |
| `phase2_pattern`의 값 | ❌ **한 값도 바꾸지 마라** |
| `label`·`icon`·`region`·`tier` | ❌ 그대로 |

**「보스 공격 = 플레이어 최대 체력의 25%」는 여전히 맞다.**
바뀌는 것은 **HP와 그에 딸린 임계뿐**이다.

---

# 작업 3 — ⚠ 기존 테스트 (기획 창이 조사했다)

| 위치 | 조치 |
|---|---|
| `test/region_stage_test.dart`의 `kHpScales` | **9~12지역 네 값을 새 값으로.** 단조 증가 검사 유지 |
| `test/progression_test.dart` 「12지역 tier5 적 HP = 기본 × 4.2」 | **× 3.50으로** 고친다 |
| `test/progression_test.dart` 「12지역 보스 HP 480」 검사 | **405로** 고친다 |
| `test/enemy_data_test.dart` 보스 HP 표 대조 (있다면) | 새 값 3개로 |
| `test/enemy_data_test.dart` **임계 = `hp ~/ 2`** 검사 | **손대지 마라.** 새 값에서도 그대로 통과해야 한다 |
| **「보스 공격 ≥ 일반 적 평균 공격」 검사 (S5)** | **손대지 마라.** 공격값을 안 바꾸므로 통과해야 한다 |
| `test/region_stage_test.dart` **`atk_scale` 검사** | **손대지 마라** |

**그 밖이 깨지면 구현이 틀린 것이다.** 특히 **S5나 임계 검사가 깨지면
공격값이나 임계를 잘못 건드린 것이다.**

---

# 작업 4 — 측정

측정 20과 **같은 조건** (36조합 × 300판 × 시드 2 × 하한/상한 × greedy/utility).
출력 **`reports/data/measure9_output.txt`**

## 4-A. ★ 조건 3개로 기여를 가른다

| 조건 | `hp_scale` | 보스 HP |
|---|---|---|
| 기준 (= 측정 20 재현) | 옛 값 | 옛 값 |
| **배율만** | 새 값 | 옛 값 |
| **둘 다 (채택)** | 새 값 | 새 값 |

- **기준 조건이 측정 20과 소수점까지 같아야 한다.** 다르면 보고하라
- 조건은 **인자로** 받아라 (측정 20의 `measure_condition.dart` 방식)

## 4-B. ★ 목표 대조표 — 하한·상한 두 줄로

**이번부터 판정은 두 줄이다.**

| 지역 | 구간 | 하한 목표 | **하한 실측** | 판정 | 상한 목표 | **상한 실측** | 판정 |

- **0-1의 목표표를 그대로** 기준으로 쓴다
- 벗어난 칸은 **몇 %p 벗어났는지** 적는다
- **「너무 쉽다」(상한 초과)도 벗어남으로 센다.** 다만 하한이 목표 안이면
  **「파밍이 보상받는 것」이므로 문제로 보지 않는다** — 그 경우 표에 그렇게 적어라

## 4-C. 그 밖에 낼 표

| # | 표 | 비고 |
|---|---|---|
| 1 | 상점 층 **도달률 · 남은 체력 %** | 측정 20의 4절과 같은 형식 |
| 2 | **지역별 보스만 승률** (9~12지역) | 측정 20은 84.3 / 67.3 / 56.0 / 30.7% 였다 |
| 3 | 전투당 굴림 + 각인 기대 적립 | 측정 12~20과 한 표에 |
| 4 | 전투당 턴 수 · **200턴 안전장치** | 0이어야 한다 |
| 5 | 봇 정책 격차 | **+25%p 넘으면 보고** |

> ⚠ **「전투당 피해 %」는 내지 마라** (생존 편향 — 검토 19).

---

# 작업 5 — 테스트

| # | 검사 |
|---|---|
| 1 | `hp_scale` 12행이 새 표와 일치하고 **단조 증가**한다 |
| 2 | `atk_scale` 12행이 **바뀌지 않았다** |
| 3 | 보스 3종의 HP가 310 / 355 / 405이고 **임계가 `hp ~/ 2`** 다 |
| 4 | 보스 3종의 **공격·강타·방어·2페이즈 패턴 값이 안 바뀌었다** |
| 5 | 1~8지역 `hp_scale`과 9지역 이하 보스 HP가 **안 바뀌었다** |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드 정상 종료, `reports/data/measure9_output.txt` 생성
3. `BALANCE.md`에 **「13차 — 후반 곡선 정렬」** 절을 맨 위에 추가
4. **`reports/21_후반곡선_측정.md`** 작성
   - **맨 앞에 두 줄 목표 대조표.** 「목표에 닿았는가」를 한 줄로 먼저 답할 것
   - 4-A의 조건 3개 비교
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **1~8지역 · `atk_scale` · 보스 공격값 · 난이도 배율 · 층수 · 성장 프로필 ·
  강화율 · 상점 회복 · 전투 승리 회복 조정**
- ❌ 「전투당 피해 %」 표
- ❌ `lib/`에 마법서·강화 시스템 구현
- ❌ 화면 · 각인·층수 표 확정
