# 아트 파이프라인 — 무엇을 가져오고 무엇을 직접 그리는가

> ## ⛔ 폐기됨 (2026-09-02) — 읽지 마라
> **이 문서는 「코드로 도트를 찍는다」를 전제로 쓰였다.**
> 사용자가 **「GPT 원화를 그대로 크게 쓴다」**로 결정하면서 전제가 사라졌다.
> → **`UI_DESIGN.md`가 대체한다.**
>
> 다만 아래 두 규칙은 `UI_DESIGN.md` 3절에 **그대로 살아 있다**:
> **① 빛은 항상 왼쪽 위 · ② 외곽선은 검정이 아니라 그 캐릭터 색의 가장 어두운 단계**

작성: 2026-09-01. 근거: 사용자 결정 A-1, CLAUDE.md 절대 규칙 6(2026-09-01 완화),
`GRAPHICS_PLAN.md`.

> **한 줄 방침**: 기본 재료(폰트·배경 타일)는 CC0에서 가져오고,
> **캐릭터와 몬스터는 전부 직접 그린다.** 그리고 한 번 그린 것은 **박제해서
> 다시는 저절로 변하지 않게** 만든다.

---

## 1. "그릴 때마다 달라지지 않게" — 검토 결과

**결론: 해결 가능하다. 지금 구조가 절반쯤 위험하고, 굽기(bake)로 완전히 막는다.**

### 지금 상태

캐릭터는 `lib/art/sprite_builder.dart`가 타원·다각형을 겹쳐 **앱이 실행될 때마다
계산해서** 그린다. 같은 코드면 항상 같은 그림이 나오므로 **게임 실행 중에는 절대
변하지 않는다.** 이 부분은 안전하다.

진짜 위험은 다른 데 있다. **다음에 내가(또는 다른 세션이) 스프라이트를 손볼 때
생성 코드를 고치면, 손대지 않은 다른 캐릭터까지 통째로 바뀔 수 있다.**
도형 파라미터 하나만 건드려도 명암 계산 전체가 다시 돌기 때문이다.
사용자가 걱정하신 게 정확히 이것이고, 실제로 일어날 수 있는 일이다.

### 해결 — 3중 잠금

1. **굽기(bake)**
   만족스러운 결과가 나오면 그 시점의 픽셀을 **도트 문자열로 뽑아 파일에 박아둔다**
   (`lib/art/sprites_baked_*.dart`). 이후 게임은 계산하지 않고 **읽기만 한다.**
   생성기(`sprite_builder.dart`)는 "새 캐릭터를 만들 때 쓰는 도구"로 역할이 바뀌고,
   완성품과는 분리된다. 코드를 아무리 고쳐도 이미 구운 그림은 그대로다.

2. **체크섬 잠금**
   구운 스프라이트마다 해시(그림 내용을 짧은 숫자로 요약한 값)를 함께 저장하고
   `test/sprite_test.dart`가 매번 검사한다. 누가 실수로 한 픽셀이라도 바꾸면
   **테스트가 즉시 빨간불**을 낸다. 일부러 바꿀 때만 해시를 새로 적는다.

3. **눈으로 볼 수 있는 대장(臺帳)**
   승인된 그림 전체를 `docs/sprites/sheet_v1.png`(가로 480px 시트)로 저장한다.
   나중에 "이거 예전이랑 다른데?" 싶을 때 사용자가 직접 비교할 수 있다.

> 이 3중 잠금이 들어가면 **"그릴 때마다 달라진다"는 상황은 구조적으로 불가능**해진다.

---

## 2. 무엇을 가져오고 무엇을 그리는가

| 대상 | 방침 | 이유 |
|---|---|---|
| **한글 픽셀 폰트** | **CC0/OFL 폰트를 도입** | 지금은 시스템 기본 폰트(둥근 안티앨리어싱)라 픽셀 그림과 따로 논다. **한 줄 바꿔서 얻는 체감이 가장 큰 항목.** |
| 배경 타일·기둥·바닥 질감 | CC0 팩 활용 가능 | 배경은 개성보다 완성도가 중요하고, 코드로 그린 그라데이션엔 질감이 없다 |
| UI 프레임·버튼·막대 | 자체 제작 유지 | 이미 쓸 만하다. 1단계 팔레트 교체로 충분히 살아난다 |
| **캐릭터·마법사·몬스터 30종** | **직접 그린다** | 사용자 결정. CC0는 **참고 자료로만** 본다 |
| 주문·유물 아이콘 17종 | 자체 제작 유지 | 이미 있고 통일돼 있다 |

### 라이선스 안전 규칙 (실수하면 나중에 크게 문제된다)

- **캐릭터 베이스로 삼는다면 CC0만 쓴다.** CC BY는 변형해도 표기 의무가
  따라붙어서, 30종을 섞으면 관리가 지옥이 된다. CC0는 그 의무가 없다.
- **파일을 실제로 게임에 넣는 것은 전부 `ATTRIBUTION.md`에 기록**한다
  (출처 URL·작가·라이선스·받은 날짜).
- **다운로드 전에 라이선스 페이지를 눈으로 확인**한다. 아래 후보는 널리 CC0로
  알려져 있지만, 페이지가 바뀌었을 수 있으므로 **구현 창이 받기 전에 반드시 확인**할 것.

### 후보 (구현 창이 라이선스 확인 후 사용)

| 용도 | 후보 | 알려진 라이선스 |
|---|---|---|
| 한글 픽셀 폰트 | **Galmuri (갈무리)** | OFL 1.1 — 가장 확실 |
| 한글 픽셀 폰트 대안 | 둥근모꼴 / Neo둥근모 | 자유 이용이나 조건이 팩마다 달라 확인 필요 |
| 배경·타일 | Kenney (kenney.nl) 계열 | CC0 |
| 배경·타일 | 0x72 「DungeonTileset II」 | CC0 |
| **몬스터 실루엣 참고 사전** | Dungeon Crawl Stone Soup 타일셋 | CC0 — 몬스터가 수백 종이라 "형태 아이디어"를 얻기에 최적 |

> 절대 하지 않는 것: 딸깍 다이스를 포함한 **상용 게임의 이미지·폰트를 추출하거나
> 그대로 베끼는 것** (절대 규칙 6).

---

## 3. 캐릭터 제작 파이프라인 (독창성 확보)

사용자 요청: *"약간의 변형이 가미되는 수준이더라도 독창적이었으면"*.
아래 **형태 문법**을 전 캐릭터에 강제하면, 참고를 봐도 결과물이 우리 것이 된다.

### 단계

1. **콘셉트 한 줄** — 각 적에게 "한눈에 각인되는 특징 하나"를 정한다
   (슬라임 = 지나치게 큰 눈 / 해골 = 빈 눈구멍 속 불씨 / 로브 = 후드 안의 빛 두 점).
2. **실루엣을 먼저 흑백으로** — 검은 덩어리 상태에서 뭐인지 알아보이지 않으면
   색을 칠해도 실패다. 여기서 걸러낸다.
3. `SpriteBuilder`로 부피·명암을 만든다 (빛은 항상 **왼쪽 위**).
4. **특징을 손으로 덧찍는다** — 자동 생성이 못 하는 부분이 여기다.
5. **굽기 + 해시 + 시트 저장** (1절).

### 전 캐릭터 공통 규칙 — 이게 "우리 화풍"이 된다

- **마법사**: 어깨 폭 > 모자챙 폭 (실루엣이 뭉치지 않게). 지팡이 보석은 항상 발광.
- **모든 적**: 눈이 크고, **포인트색을 딱 하나만** 가진다. 나머지는 무채색에 가깝게.
- **보스**: 일반 적의 1.5배 이상 + 실루엣에 **비대칭 요소**(한쪽 뿔, 접힌 날개 하나).
- **외곽선**: 검정이 아니라 **그 캐릭터 색의 가장 어두운 단계**를 쓴다 (덜 딱딱해 보인다).
- 팔레트는 1단계에서 확정한 색 **밖으로 나가지 않는다.**

### 검증

- `test/sprite_test.dart`에 추가: 줄 길이·크기(기존) + **해시 고정**(신규)
  + "실루엣이 캔버스 밖으로 나가지 않음"(GRAPHICS_PLAN 2단계).
- 한 묶음(5~6종) 끝날 때마다 480px 시트를 만들어 사용자에게 보인다.
  **30종을 다 그린 뒤에 보여주지 않는다** — 화풍이 어긋나 있으면 전부 다시 그려야 한다.

---

## 4. 작업 순서 (GRAPHICS_PLAN 개정)

1. **1단계 — 팔레트·아트 디렉션 교체** (색이 먼저 정해져야 캐릭터 색을 정한다)
2. **폰트 교체** (1단계에 붙여서 함께. 비용 대비 체감이 가장 크다)
3. **2단계 — 캐릭터 재작업 + 굽기** (마법사 → 보스 4종 → 일반 적 26종 순)
4. **3단계 — UI 마무리**
5. **4단계 — 연출 강화**

> 원래 계획은 1 → 3 → 4 → 2였으나 **2를 앞으로 당겼다.** 팔레트를 바꾸면
> 캐릭터 색을 어차피 다시 맞춰야 하므로, 1 직후에 2를 하면 재작업이 없다.
> 3·4(UI·연출)는 캐릭터와 독립적이라 뒤로 밀려도 손해가 없다.
