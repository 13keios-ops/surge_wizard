# 보고서 08 — 데이터 A 창 작업 1·2·3

작성: 2026-09-02 / 근거 지시서: `WORK_ORDER_DATA_A.md` 작업 1·2·3
근거 문서: `SURGE_DESIGN.md` 5절 · `SPELL_CIRCLES.md` 2절

## 1. 한 줄 요약

폭주 80종을 6갈래 체계로 전면 교체하고 주문 70종에 `circle`을 넣었다.
**주문 70종 전부가 정확히 25/20/15/15/15/10% 갈래 비율**을 갖는 것을 테스트로 확인했고,
`flutter test` 152개 전부 통과·`flutter analyze` 경고 0이다.

## 2. 지시서 이행 여부

| 항목 | 상태 | 비고 |
|---|---|---|
| 1-A `surges.json` 80종 전면 교체 | ✅ 완료 | 기존 80종 전부 폐기, `SURGE_DESIGN.md` 5절 그대로 |
| 1-B 항목 형식 유지 (`category` 6종) | ✅ 완료 | `backlash`/`misfire`/`fizzle`/`amplify`/`summon`/`chain` |
| 1-C-1 불발 11종 `"effects": []` | ✅ 완료 | 검사 8번이 지킨다 |
| 1-C-2 태그는 `any` + 속성 5종만 | ✅ 완료 | 부가 태그 12종은 폭주에서 전부 사라졌다. `spells.json`의 부가 태그는 손대지 않음 |
| 1-C-3 가중치 정확 | ✅ 완료 | 총 1600, 갈래별 400/320/240/240/240/160 |
| 2-A 주문 70종 `circle` 추가 | ✅ 완료 | `rarity` 유지, `element` 다음 줄에 삽입 |
| 2-B `gaia_wrath` `dc_modifier` 2→3 | ✅ 완료 | **이것 외 수치 변경 0건** |
| 3-A `test/surge_data_test.dart` 8검사 | ✅ 완료 | 전부 통과 |
| 3-B `test/spell_circle_test.dart` 5검사 | ✅ 완료 | 전부 통과 |
| 3-C 기존 139개 계속 통과 | ⚠ 부분 | 1개를 고쳐야 했다 — 6절 참조 |
| `lib/` 코드 무수정 | ✅ 완료 | `lib/` 한 줄도 안 고쳤다 |
| `flutter analyze` 경고 0 | ✅ 완료 | |

## 3. 실제로 고친 파일

| 파일 | 무엇을 |
|---|---|
| `assets/data/surges.json` | 80종 전면 교체 (id·이름·문장·갈래·가중치·태그·효과 전부) |
| `assets/data/spells.json` | 70종에 `circle` 필드 추가 + `gaia_wrath` `dc_modifier` 2→3 |
| `test/surge_data_test.dart` | **신규** — 폭주 검산 8종 |
| `test/spell_circle_test.dart` | **신규** — 서클 검산 5종 |
| `test/loader_test.dart` | 기존 검사 1개 수정 (6절 참조) |

## 4. 수치 결과

### 4-1. ★ 검사 7 — 주문 70종의 갈래 비율 (지시서가 요구한 표)

**70종 전부가 같은 값이다.** 후보 풀이 `any` 40종 + 자기 속성 8종으로 고정되므로
주문마다 풀 형태가 하나뿐이다 (검증 결과: **서로 다른 풀 형태 = 1가지**).

| 갈래 | 후보 가중치 | 실제 비율 | 목표 | 오차 |
|---|---|---|---|---|
| 역류 `backlash` | 280 | **25.0%** | 25% | **0** |
| 오발 `misfire` | 224 | **20.0%** | 20% | **0** |
| 불발 `fizzle` | 168 | **15.0%** | 15% | **0** |
| 증폭 `amplify` | 168 | **15.0%** | 15% | **0** |
| 소환 `summon` | 168 | **15.0%** | 15% | **0** |
| 연쇄 `chain` | 112 | **10.0%** | 10% | **0** |
| **합** | **1120 (48종)** | **100%** | | |

이 값이 적용되는 주문: **화염 14 · 냉기 14 · 비전 14 · 그림자 14 · 자연 14 = 70종.**
테스트는 평균이 아니라 **주문 1종씩 70번** 정수 곱셈으로 비교하므로 반올림 여지가 없다.

> 참고: 주문의 부가 태그(`explosion` `life` `sky` 등)가 남아 있어도 그 태그를 가진
> 폭주가 없어 풀이 흔들리지 않는다는 것도 이 검사가 함께 증명한다.

### 4-2. 폭주 전체 분포 (검사 3·4·6)

| 갈래 | 종수 | any 합 | 속성 5종 합 | 총 가중치 | 비율 |
|---|---|---|---|---|---|
| 역류 | 20 | 250 | 150 | 400 | 25.0% |
| 오발 | 18 | 200 | 120 | 320 | 20.0% |
| 불발 | 11 | 150 | 90 | 240 | 15.0% |
| 증폭 | 11 | 150 | 90 | 240 | 15.0% |
| 소환 | 11 | 150 | 90 | 240 | 15.0% |
| 연쇄 | 9 | 100 | 60 | 160 | 10.0% |
| **합** | **80** | **1000** | **600** | **1600** | **100%** |

속성별로도 각 8종 / 가중치 120으로 정확히 같다 (화염·냉기·비전·그림자·자연).

### 4-3. 서클 분포 (검사 1~4)

| 서클 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 | 합 |
|---|---|---|---|---|---|---|---|---|---|---|
| 종수 | 10 | 10 | 10 | 10 | 10 | 5 | 5 | 5 | 5 | **70** |
| 속성별 | 각 2 | 각 2 | 각 2 | 각 2 | 각 2 | 각 1 | 각 1 | 각 1 | 각 1 | |
| `rarity` | common | common | common | rare | rare | rare | epic | epic | epic | 30/25/15 |

**모든 서클에 5속성이 전부 존재한다** (검사 3 통과). `circle`↔`rarity` 불일치 0건.

### 4-4. 수치 변경 대조

| 주문 | 항목 | 개정 전 | 개정 후 | 근거 |
|---|---|---|---|---|
| `gaia_wrath` | `dc_modifier` | 2 | **3** | `SPELL_CIRCLES.md` 2절, 2026-09-01 사용자 승인 |

**옮기기 전에 `spells.json` 70종의 `base_damage`·`dc_modifier`를
`SPELL_CIRCLES.md` 2절 표와 전건 대조했다. 차이는 위 1건뿐이었다.**
근거 문서와 기존 데이터가 그 외에는 완전히 일치한다.

## 5. 테스트·검사

- `flutter test`: **152개 전부 통과** (기존 139 + 폭주 8 + 서클 5)
- `flutter analyze`: **경고 0** (`No issues found!`)
- 실제 실행 확인: **하지 않았다.** 이번 작업은 JSON 데이터·테스트만 건드렸고
  화면 확인이 필요한 변경이 없다. 다만 **폭주 팝업은 지금 6갈래 색이 없어
  전부 기본색(`kCharge`)으로 뜬다** — 지시서 C의 작업이다 (7절 참조).
- 재현 명령:
  ```
  flutter test test/surge_data_test.dart test/spell_circle_test.dart
  flutter analyze
  ```

## 6. ★ 지시서를 벗어난 것

| 무엇을 | 왜 | 되돌릴 수 있나 |
|---|---|---|
| `test/loader_test.dart`의 검사 하나(`effect type이 구현 가능한 10종 안이다`)를 고쳤다 | **고치지 않으면 반드시 깨진다.** 이 검사가 (ㄱ) 모든 폭주의 `effects`가 비어 있지 않을 것, (ㄴ) 효과 타입이 옛 10종 안일 것을 요구하는데, v2는 **불발 11종이 빈 `effects`**이고 **새 효과 4종**(`self_damage_spell`·`enemy_heal`·`shield`·`chain_cast`)을 쓴다. 즉 이 검사는 v1 규약을 박제한 것이라 v2 데이터와 양립할 수 없다 | 예 — 검사 1개, 4줄 |

**고친 내용**: 허용 타입을 10종 → **14종**(기존 10 + 신규 4)으로 넓히고,
`effects` 비어 있지 않음 조건을 **`category != 'fizzle'`일 때만** 적용하도록 했다.
불발이 비어 있어야 한다는 것은 새 `surge_data_test.dart` 검사 8번이 더 엄격하게 지킨다.

**지시서 3-C가 "139개가 계속 통과해야 한다"고 못 박았으므로 보고한다.** 데이터를
잘못 옮겨서 깨진 것이 아니라, **지시서 1-C-1(불발은 빈 effects)과 `SURGE_DESIGN.md` 4절
(새 효과 4종)이 그 검사와 정면으로 충돌**하기 때문이다. `lib/` 코드는 손대지 않았다.

그 밖에 지시서를 벗어난 것: **없음.**

### 옮기다 발견한 근거 문서 오류

**없다.** `SURGE_DESIGN.md` 5절 80종과 6절 검산표, `SPELL_CIRCLES.md` 2절 70종은
개수·가중치·합계가 전부 자기 자신과 맞았고, 옮긴 결과가 테스트를 그대로 통과했다.
`spells.json`의 기존 수치와도 `gaia_wrath` 1건 외에 차이가 없었다.

### 참고 — 한글 인코딩 취급

`CLAUDE.md` LESSONS 1번(한글 파일을 PowerShell 텍스트 명령으로 고치지 말 것)을 지켰다.
`surges.json`은 Write 도구로 새로 썼다. `spells.json`은 70곳에 같은 모양의 줄을 넣는
기계적 작업이라 **바이트 단위로만 다루는 스크립트**(디코딩을 아예 하지 않고 ASCII 줄만
삽입)를 썼고, **작업 전후 비ASCII 바이트 수가 2772로 동일함을 스크립트가 단언(assert)**
하도록 했다. 한글이 한 글자도 변형되지 않았다.

## 7. ★ 판단이 필요한 것

**없음.** 다만 **지시서 C가 오기 전까지 게임이 어중간한 상태로 돌아간다**는 점만
알려 둔다 (지시서 0-1이 "그 상태가 정상"이라 명시했으므로 질문이 아니라 확인이다):

- 새 효과 4종은 `core/surge.dart`의 `switch`에 없어 **조용히 무시된다.**
  → 역류 20종·연쇄 9종·오발 일부가 지금은 **아무 효과가 없다.** 앱은 죽지 않는다
- `widgets/surge_popup.dart`의 색상표는 옛 `category` 5종 기준이라
  새 6갈래가 전부 `?? kCharge` 기본색으로 뜬다. 역시 죽지 않는다
- `models/surge_event.dart`의 주석(`유형: self_harm / reversal / ...`)이 낡았다.
  주석뿐이라 동작에는 영향이 없다 — 지시서 C에서 함께 고치면 된다

## 8. 다음 작업 제안

**지시서 C**(새 효과 4종 구현 + 폭주 팝업 6갈래)를 다음에 내는 것이 맞다.
데이터가 먼저 들어가 있어 구현 창이 **실제 값으로 바로 확인**할 수 있고,
지금은 폭주의 절반 가까이가 무효과라 이 상태를 오래 두면 밸런스 측정이 불가능하다.

구현 시 `SURGE_DESIGN.md` 4절의 역류 계산 규칙 3가지를 반드시 함께 볼 것:
**(1) 시전 강도 배율을 곱하지 않는다 · (2) 최대 체력 40% 상한 · (3) 값 구간 60~120%.**
