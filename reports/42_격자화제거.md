# 42. 캐릭터 격자화 제거 (검토 41 승인)

작성: 2026-09-13, 기획 창에서 직접. 근거: `reports/41_격자화_재검토.md` · 사용자 승인.

## 진행

- [1/6] 시작. 격자화를 빼면 **보간도 같이 바꿔야 한다** — 격자 없는 그림을
  최근접으로 줄이면 깨진다. 지금 `kArtFilter = FilterQuality.none`
- [2/6] `tool/bake_art.py` 에 `normalize()` 신설. 격자화가 하던 둘 중
  **자리 잡기만 남기고 양자화는 뺐다** — 실루엣을 잘라 긴 변을 캔버스의 86%로
  줄이고 **발바닥 0.9316**에 앉힌다. 긴 변 기준이라 넓고 낮은 몬스터도 안 넘친다
- [3/6] 캐릭터 3종 재굽기. 마법사·고블린 **512**, 보스 **768**.
  발바닥 셋 다 **0.9316** · 중심 0.499 (실측)
- [4/6] 런타임 보간 `kArtFilter` **none → medium**. 격자가 없으니 최근접으로
  줄이면 깨진다. 🔴 **배경은 `kArtBgFilter = none` 으로 갈라 예전 그대로 뒀다**
- [5/6] ✅ **마법사 걷기 4프레임을 구웠다.** 원화가 이미 있었고 밀도 제약이
  사라져 지금 넣을 수 있게 됐다. `kArtWizardWalkReady = true`
- [6/6] 문서 갱신 — `docs/HANDOFF_ART.md` 밀도 표 무효화 · `tool/gridize.py` 는
  **배경 전용**으로 표시. `flutter analyze` 0 · `flutter test` **362개 통과**

## 화면 확인

`reports/img/roadside/floor_3.png` — 고블린과 마법사가 **또렷하다.** 덩어리로
보이던 블록이 사라졌다. 보스 음영도 깨끗해졌다.

## 무엇이 바뀌었나 한 줄

**원화 1254px 중 37칸만 쓰던 것을, 화면이 받는 만큼 그대로 쓰게 했다.**

## 남은 것

1. **일관성 보증이 자동에서 수동이 됐다.** 참고 게임의 기준은 **팔레트 폭**이다
   (8688px 에 105색). 새 에셋이 들어올 때마다 재는 습관이 필요하다
2. 배경은 아직 2px 블록이다. **캐릭터만 부드러워져 배경과 성질이 달라졌다** —
   화면에서 어긋나 보이면 배경도 같이 봐야 한다
3. `ASSET_LIST.md` 에 **적마다 대표 색**을 배정하는 일은 그대로 남아 있다
