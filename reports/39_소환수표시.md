# 39. 소환수를 화면에 그린다

지시서: `WORK_ORDER_SUMMONS_UI.md` · 창: 기획 창에서 직접 (사용자 지시, 2026-09-13)

## 진행

- [1/4] 시작. `summons` 를 읽는 위젯이 하나도 없는 것을 확인하고 들어간다
- [2/4] `lib/widgets/summon_row.dart` 신설. 아군은 마법사 옆(오른쪽), 적대는 적
  옆(왼쪽). 위력을 원 안에 쓰고, **남은 턴은 짧을 때만** 보여 준다 —
  호위는 999턴이라 숫자가 어지럽다
- [3/4] `battle_stage.dart` 가 302줄이 되어 소환수 겹을 **`StageLayers` 안으로** 넣었다.
  겹 순서(배경 → 길 옆 물체 → 소환수 → 몬스터·캐릭터)는 그대로다. **299줄**
- [4/4] `test/summon_row_test.dart` 7개 + 화면 캡처 1장.
  `flutter analyze` 0 · `flutter test` **355개 전부 통과**

## 화면 확인

`reports/img/roadside/summons.png` — 붉은 원 「4」가 적 높이 왼쪽, 청록 원 「5」가
마법사 높이 오른쪽에 선다. **호위가 때리는 이유가 이제 화면에 있다.**

## 잡은 것

매개변수 이름이 하는 일과 **반대**였다 — `onLeft: true` 가 오른쪽에 놓고 있었다.
화면은 맞게 나왔지만 다음 사람이 반드시 헷갈린다. `toRight` 로 고쳤다.

## 남은 것

- 원화가 없어 **단색 원**이다. 그림이 들어오면 `_SummonChip` 하나만 바뀐다
- 소환수가 **때리는 순간의 연출**이 없다. 지금은 조용히 숫자만 깎인다
