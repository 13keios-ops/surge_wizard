# 작업 지시서 — 그래픽 Phase 1.5 (Scene Proof)

발행: 2026-09-05, 기획 창.
근거: **`GAME_ART_ROADMAP.md` 13.5절** · `reports/28_에셋검수_Phase1_5.md` ·
`UI_DESIGN.md` 1-2·1-2-b절 · `docs/art/RUNTIME_ASSET_SPEC.md`

> ## 한 줄 목표
> **전투 화면 한 장을 통째로 원화로 세운다.**
> 마법사 · 적 · 배경 셋이 **하나의 게임처럼 보이는가**를 처음으로 판정한다.

**원화 3장은 이미 들어와 있고 규격 검수를 통과했다** (`reports/28_*`).
**새로 뽑을 그림은 없다.** 이번 작업은 **굽기 + 배치**다.

---

## 0. 먼저 읽을 것

1. `reports/28_에셋검수_Phase1_5.md` — **3절(남은 문제)이 이 작업의 전부다**
2. `UI_DESIGN.md` — **1-2-b절(논리 칸 = 화면 한 칸 2.0dp)** · 1-3절 · 3-2절
3. `GAME_ART_ROADMAP.md` 13.5절
4. `docs/art/ART_STYLE_GUIDE.md` — 금지 목록

> ### 🔴 하지 않는 것 (계속 유효)
> 팔레트 근사 · 포스터라이즈 · 코드가 외곽선 붙이기 · 강제 색 양자화 ·
> 알파 이진화 · 96×96 격자. `WORK_ORDER_ART_PIPELINE.md`를 읽지 마라.

---

## 1. 원본 — `art_raw/phase1_5_assets/`

| 파일 | 크기 | 실루엣 | 런(칸) |
|---|---|---|---|
| `wizard_back.png` | 512 × 512 | 217 × 413 (발 y=477) | **7px** |
| `enemy_goblin_scout.png` | 512 × 512 | 441 × 413 (발 y=477) | **7px** |
| `bg_forest.png` | 1024 × 1536 | 불투명 | **2px** |

**마법사와 적의 발바닥 y가 477로 같다.** 손보정할 필요가 없다.

---

## 2. 작업

### 2-A. 게임용 에셋 굽기

```
art_raw/phase1_5_assets/wizard_back.png        → assets/art/wizard/body_back.webp
art_raw/phase1_5_assets/enemy_goblin_scout.png → assets/art/enemy/goblin_scout.webp
art_raw/phase1_5_assets/bg_forest.png          → assets/art/bg/forest.webp
```

> ## 🔴 2-A-1. **재축소 금지** — 이번 작업에서 가장 중요하다
>
> **원화는 7px(캐릭터) · 2px(배경) 정사각 블록으로 그려져 있다.**
> **정수배가 아닌 배율로 리샘플하면 그 격자가 깨져 뭉개진다** —
> **그것이 Phase 1이 반려된 바로 그 현상이다.**
>
> - 캐릭터 512 × 512는 **이미 캔버스 규격이다. 크기를 건드리지 마라**
> - 배경 1024 × 1536도 **그대로 굽는다**
> - **알파 250 이상 → 255 스냅**과 **WebP 변환만** 한다
> - 화면에 맞추는 축소는 **런타임이 한다** (`FilterQuality.none`)

**⚠ 앞 지시서의 「몸 높이 425로 정규화」는 폐기한다.**
원화가 **413px(59칸 × 7px)**로 왔고, **413 → 425는 1.029배라 격자가 깨진다.**
**413 그대로 쓴다.**

> Phase 2 지팡이 검산: 지팡이 = 몸 × 1.10 = **454px**.
> 발 y=477 기준 윗끝 = 477 − 454 = **23** → **캔버스 안에 들어간다.** 문제없다.

### 2-B. 렌더 품질 — **`FilterQuality.none`**

`Image` 위젯이 기본 보간(bilinear)으로 줄이면 **블록 경계가 흐려진다.**
**세 장 전부 `filterQuality: FilterQuality.none`** 으로 그린다.

### 2-C. 배경을 무대에 깐다

`lib/widgets/battle_stage.dart` — 지금 코드로 그리는 `StageBackdrop` 자리.

| 규칙 | |
|---|---|
| **맞춤** | `BoxFit.cover` |
| **정렬** | 🔴 **`Alignment.bottomCenter`** — 가운데 정렬하면 **앞쪽 풀밭이 잘린다** |
| **fallback** | **`StageBackdrop`(코드 배경)을 지우지 마라.** 원화가 없는 지역은 그대로 쓴다 |
| 적용 범위 | **1지역(숲)만.** 나머지 11지역은 코드 배경 유지 |

### 2-D. 🔴 적 발선 `0.52` → `0.68`

**`battle_stage.dart:145`** 의 `enemyLine = h * 0.52`.

새 배경은 풀밭이 화면 아래쪽에만 있어 **0.52면 적이 공중에 뜬다.**
기획 창이 0.52 / 0.62 / 0.68을 합성해 비교했고 **0.68에서 풀밭에 선다.**

- **상수로 뺀다** (`constants.dart` — 매직 넘버 금지)
- ⚠ **`constants.dart`가 299줄이다.** 한 줄 더하면 300줄 규칙을 깬다.
  `layout.dart`처럼 갈라 내고 `export` 한다

### 2-E. 적·마법사를 원화로

- 마법사: Phase 1에서 이미 원화를 띄우고 있다 → **파일만 새 것으로**
- 적: `goblin_scout` **한 종만** 원화. 나머지 적은 **`PixelSprite` 유지**
- **`tint`를 입히지 마라** (원화가 제 색을 갖고 있다). 그림자·flash는 유지
- **기존 Transform 연출을 그대로 재사용한다** (숨쉬기·시전 기울기·피격 flash·
  squash·knockback)

### 2-F. 비교 시트

`test_shots/`에 **가로 480px 한 장**, `reports/img/`에 저장.

| 칸 | 무엇 |
|---|---|
| 1 | **기존** — 코드 도트 + 코드 배경 |
| 2 | **신규** — 원화 3장 |
| 3 | 신규 — **피격 순간** |

**실제 기기 비율 400×860으로 찍어라.** 무대 높이가 가변이므로
**작은 기기(640dp)와 큰 기기(860dp) 둘 다** 확인해 적이 어디서든 풀밭에 서는지 본다.

---

## 3. 검산 조건

1. `assets/art/` 3개 파일이 **원본과 같은 크기**다 (512×512 · 512×512 · 1024×1536)
2. **런 최빈값이 원본과 같다** — 캐릭터 7px · 배경 2px.
   달라졌으면 **어딘가에서 리샘플한 것이다**
3. 알파값이 **1~249 구간에 존재한다** (0%면 이진화 사고)
4. 고유색이 **1,000개를 넘는다** (양자화 사고)
5. 원화 3장을 **못 읽어도 전투 화면이 죽지 않고** 코드 도트·코드 배경이 나온다
6. `enemyLine` 상수가 **0.68**이고 하드코딩이 아니다
7. `flutter test` 전부 통과 · `flutter analyze` 0건

**기준선**: 지금 `flutter test` **315개**, `analyze` **0건**.

---

## 4. 기존 테스트 — 미리 조사해 뒀다

| 파일 | 무엇을 단언하나 | 이번에 |
|---|---|---|
| `test/sprite_test.dart` | `PixelSprite` 격자·명암 | **손대지 마라.** fallback으로 산다 |
| `test/widget_test.dart` | 전투 스모크 | 통과해야 한다 (원화 로드 실패해도 fallback) |
| `test_shots/pilot_wizard_test.dart` | 테스트 아님 (그림 생성기) | 건드릴 필요 없다 |

`enemyLine`을 상수로 빼면서 **`stage_backdrop` 관련 단언이 깨지면 보고하라.**

---

## 5. 마무리

1. `flutter analyze` 0건 · `flutter test` 전부 통과
2. **실제로 빌드해 띄우고 1지역 Ⅰ을 한 층 돌아 눈으로 확인한다**
3. `PROGRESS.md` 기록
4. 🔴 **`ATTRIBUTION.md`에 이번 3장 출처를 적는다** (절대 규칙 6).
   **사용자가 2026-09-07에 「ChatGPT로 뽑았다」고 확인했다** — 기존
   「원화 — 마법사 뒷모습」 절과 **같은 권리 근거**(2026-09-01 사용자 확인)다.
   그 절 아래에 **「원화 — Phase 1.5 3장」** 절을 새로 만들어 파일 3개·원본 경로·
   후처리·들인 날짜를 같은 표 형식으로 적어라.
   ⚠ **ComfyUI 미결 경고문은 지우지 마라** — Phase 2부터는 여전히 유효하다
5. **`reports/29_그래픽Phase1_5_구현.md`** 작성 — 보고서 경로 한 줄만 말한다

> ### 🔴 사용자에게 묻기 전에 **먼저 재고 먼저 말한다** (로드맵 23-2절)
> 픽셀 밀도·크기·정렬처럼 **재면 나오는 것**은 구현 창이 판정한다.
> 사용자에게 묻는 것은 **취향 네 가지**뿐이다 —
> ① 같은 게임처럼 보이나 ② (해당 없음) ③ (해당 없음) ④ 마음에 드나
>
> **이번이 세 카테고리(캐릭터·적·배경)의 첫 기준 에셋 승인이다** (로드맵 23절).
> 승인되면 `approved`로 등록하고, **그다음이 Phase 2(지팡이)다.**

## 6. 판단이 필요하면

**임의로 정하지 말고 보고서 「판단이 필요한 것」에 적어라.**
특히 **적 발선 0.68**과 **배경 아래 정렬**은 기획 창이 합성으로 확인한 값이다.
바꿔야 할 이유를 찾으면 **바꾸지 말고 근거 그림과 함께 보고하라.**
