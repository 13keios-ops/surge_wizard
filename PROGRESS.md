# PROGRESS — 무인 작업 진행 상황

작성 시작: 2026-08-31 밤

| 작업 | 상태 |
|---|---|
| 작업 1 — 판정 엔진 (lib/core/) | ✅ 완료 |
| 작업 2 — 단위 테스트 (test/check_test.dart) | ✅ 완료 (29개 전부 통과) |
| 작업 3 — 게임 데이터 JSON 4개 | ✅ 완료 |
| 작업 4 — 모델과 로더 | ✅ 완료 (테스트 18개 통과) |
| 작업 5 — 전투 로직 (battle.dart) | ✅ 완료 (테스트 14개 통과) |
| 작업 6 — 밸런스 시뮬레이션 | ✅ 완료 (BALANCE.md) |

상세 기록은 아래에 작업별로 추가한다.

---

## 작업 1 — 판정 엔진 ✅

`lib/core/` 에 4개 파일 완성:
- `constants.dart` — 모든 밸런싱 수치 상수화
- `dice.dart` — 3d6 굴림, 개별 잠금, 리롤 (최대 3회)
- `combo.dart` — 족보 판정 (트리플 > 스트레이트 > 뱀눈 > 페어)
- `check.dart` — 판정 엔진 (4단계 결과 + 족보 + 마력 축적)

주의: 대성공 경계를 DC+3으로 구현했다 (기획서 3.2절 본문의 +5와 다름).
이유는 BLOCKERS.md 판단 1 참조 — 3.4절 확률표가 +3 기준으로만 일치한다.

## 작업 2 — 단위 테스트 ✅

`flutter test test/check_test.dart` → **29개 전부 통과**

몬테카를로 10만 회 (시드 고정) 실측 vs GAME_DESIGN.md 3.4절 표:

| 강도 | 대성공 (표/실측) | 성공 (표/실측) | 아슬아슬 (표/실측) | 실패 (표/실측) |
|---|---|---|---|---|
| 약하게 DC 7 | 62.5% / 62.8% | 28.2% / 28.0% | 8.8% / 8.6% | 0.5% / 0.5% |
| 보통 DC 10 | 25.9% / 26.1% | 36.6% / 36.7% | 28.2% / 28.0% | 9.3% / 9.1% |
| 전력 DC 13 | 4.6% / 4.7% | 21.3% / 21.4% | 36.6% / 36.7% | 37.5% / 37.2% |

전부 오차 1% 이내. 추가로 216가지 전수(모든 경우) 계산으로도 표와 정확히 일치함을 검증했다.
족보 우선순위는 216가지 조합 전수 검사로 확인.

리포트 재현: `dart run tool/mc_report.dart`

## 작업 3 — 게임 데이터 JSON ✅

전부 실제 파싱으로 문법 검증, id 중복 0 확인 완료. `pubspec.yaml`에 assets 등록.

### spells.json — 주문 70종

| 등급 | 개수 | 평균 대미지 | 평균 DC 보정 |
|---|---|---|---|
| common | 30 | 6.2 | 0 |
| rare | 25 | 10.8 | 0.32 |
| epic | 15 | 17.1 | 1.93 |

- 속성 5종 각 14개 균등 (fire/frost/arcane/shadow/nature)
- effect 보유 21개 (30%) — shield/heal/delay_enemy/mana_restore/check_bonus/extra_die
- 시작 주문 3종(화염구·서리화살·마력방패) 포함
- 예외: 마력방패·치유 약초는 순수 보조 주문이라 base_damage 0 (BLOCKERS.md 판단 4)

### surges.json — 폭주 80종

| 유형 | 개수 | 비율 | 평균 weight |
|---|---|---|---|
| 자해형 self_harm | 24 | 30% | 9.5 |
| 반전형 reversal | 16 | 20% | 5.8 |
| 소환형 summon | 16 | 20% | 8.0 |
| 도박형 gamble | 12 | 15% | 5.2 |
| 순수이득형 boon | 12 | 15% | 5.6 |

- effect type은 지정된 10종만 사용 (self_damage, heal, mana_change, seal_spell,
  swap_hp, summon_ally, damage_multiplier, skip_enemy_turn, extra_die, force_reroll)
- 스키마에 `category` 필드 추가 (유형 통계·검증용)
- 극적인 사고(체력 맞바꿈 등)는 weight 2~3으로 희귀하게

### enemies.json — 적 30종 (일반 26 + 보스 4)

| tier | 마리 수 | 평균 HP | 담당 층 |
|---|---|---|---|
| 1 | 6 | 13.7 | 1~2층 |
| 2 | 5 | 22.0 | 3~4층 |
| 3 | 5 | 29.4 | 5~6층 |
| 4 | 5 | 37.4 | 7~8층 |
| 5 | 5 | 44.2 | 9층 |

- 보스 4종: HP 88~95, 전부 2페이즈 (`phase2_hp_threshold` + `phase2_pattern`)
- 패턴은 3~4개 행동 고정 순환, 랜덤 없음. 행동 종류: attack/charge/defend/heal
- 기획서 4.3절의 오크 전사 예시를 수치 그대로 수록

### relics.json — 유물 40종

- common 18 / rare 14 / epic 8
- 판정 확률 관련 28개 (70%): check_bonus 계열, 리롤 추가, 페어 강화, 1눈 보정 등
- 판정 보정은 epic 포함 최대 +3 (확률 설계 보호)

## 작업 4 — 모델과 로더 ✅

- `lib/models/` — spell, enemy, relic, surge_event + 공용 effect 모델.
  전부 `fromJson` / `toJson` 보유.
- `lib/data/parser.dart` — Flutter 의존성 없는 순수 파서 (테스트·CLI 겸용)
- `lib/data/loader.dart` — 앱에서 assets 로드 (rootBundle)
- `test/loader_test.dart` — **18개 테스트 전부 통과.**
  4개 JSON 파싱, id 중복, 수량, 값 범위, 태그 매칭(모든 주문이 최소
  10종 이상의 폭주와 매칭되는지)까지 검증.

## 작업 5 — 전투 로직 ✅

- `lib/core/battle.dart` — GAME_DESIGN 4.1절 턴 흐름 그대로. UI 없음.
  판정 4단계 반영, 아슬아슬 반동 4종, 폭주 추첨·효과 10종, 스트레이트
  동시 시전, 적 고정 패턴(공격/강타예고/방어/회복), 보스 2페이즈,
  소환수, 방어막 처리 포함. 세부 해석은 BLOCKERS.md 판단 7.
- `test/battle_test.dart` — **14개 테스트 전부 통과.**
  적 30종 전부와의 전투가 정상 종료됨을 확인. 보스 2페이즈 전환 확인.

## 작업 6 — 밸런스 시뮬레이션 ✅

- `tool/simulate.dart` — 1층~10층 1000판 자동 플레이.
- 결과: 완주율 4.7%(봇 기준 최저선), 평균 도달 5.69층,
  폭주 빈도 시전의 11.1%. 상세 분석과 제안은 **BALANCE.md**.
- 수치는 하나도 고치지 않았다.

## 검수·수정 라운드 (REVIEW.md 반영)

- 전체 검수 보고서: REVIEW.md. 지적사항 수정 완료 (조치 결과 표 참조).
- 주요 수정: 예고(telegraph) 정확성 2건, battle.dart → surge.dart 분리,
  check_test → combo_test 분리, 매직 넘버 상수화, 폭주 텍스트 5종 손질,
  소환수 행동 시점 규칙 확정, 시뮬레이터 집계 방식 교체.

## 단계 3 — 전투 화면 (UI) ✅

CLAUDE.md 작업 순서 3단계. 광고·결제·출시 빌드는 여전히 미착수 (계획대로).

- `lib/main.dart` — 템플릿 교체. 다크 테마 + 데이터 로드 관문(FutureBuilder)
- `lib/screens/title_screen.dart` — 타이틀 (전투 시작 버튼, 지도는 4단계에서)
- `lib/screens/battle_controller.dart` — provider(ChangeNotifier) 상태 관리.
  단계: 주문·강도 선택 → 굴림 → 잠금·리롤 → 판정 → 턴 종료.
  리롤은 시전 비용을 남겨두고만 가능 (마나 음수 방지)
- `lib/screens/battle_screen.dart` — 전투 화면 본체 (267줄)
- `lib/widgets/` — dice_widget(눈금 직접 그림, 탭 잠금), spell_card(속성색),
  enemy_panel(HP바 + 행동 예고), surge_popup(폭주 연출), result_banner
- 코어 소폭 확장: `Battle.lastResult`, `SurgeSystem.lastSurge`(UI 표시용),
  `DicePool.seed()`(주사위 추가 효과 연결)
- pubspec에 `provider` 추가 (CLAUDE.md 지정 상태 관리)
- `test/widget_test.dart` — 카운터 템플릿 삭제, 실제 흐름 스모크 테스트로
  교체 (타이틀 → 전투 → 주문 선택 → 굴림 → 시전 → 턴 종료까지 검증)

## 단계 4 — 지도 / 런 진행 / 보상 ✅

권장안 채택: 상점 회복 10, 보상 균등 추첨, 대성공 경계 DC+3 유지
(상세는 BLOCKERS.md 판단 9).

- `models/run_state.dart` — 한 판의 진행 상태 (fromJson/toJson 보유)
- `screens/run_controller.dart` — 층 이동, 적 배정(층→tier 곡선),
  전투 구성, 보상 추첨, 상점, 승리 반영
- `core/relic_powers.dart` — **유물 효과 14종 전부 전투·런에 연결**
  (판정 보정 3종, 대미지, 최대체력, 시작 방어막, 전투 후 회복,
  리롤 +N, 무료 리롤, 페어 강화, 축적 강화, 대성공 회복, 폭주 마나,
  1눈 보정)
- `core/enemy_turn.dart` — 적 턴 로직 분리 (battle.dart 300줄 유지)
- `core/cast_intensity.dart` — 시전 강도 분리
- 화면: `map_screen`(10층 세로 지도, 아래=1층부터 표시), `reward_screen`
  (주문 3택1+슬롯 교체 / 유물 3택1), `result_screen`(승리/사망),
  타이틀 → 지도 → 전투 → 보상 → … → 결과 전체 흐름 연결
- `test/run_test.dart` — 유물 효과 11종 + 런 진행 + 저장 왕복 18개 테스트
- 위젯 스모크 테스트가 지도의 스크롤 문제(1층이 화면 밖)를 잡아내
  ListView를 reverse로 고침 — 테스트가 실제 UX 버그를 발견한 사례
- 시뮬레이터 2차 개선: 리롤 봇 + 유물 전체 반영 → 완주율 31.2%.
  핵심 발견(숙련자에겐 폭주가 사라짐)은 BALANCE.md 참조

## 단계 5 — 메타 성장 + 저장 ✅

권장안: 층당 결정 5 + 보스 보너스 50, epic 주문이 해금 대상,
체력 4·5단계 비용 170/230 (상세는 BLOCKERS.md 판단 11).

- `models/meta_state.dart` — 영구 강화 상태 (fromJson/toJson)
- `services/save_service.dart` — shared_preferences 저장/로드 (오프라인)
- `screens/meta_controller.dart` — 구매 규칙 (비용표·단계 상한·즉시 저장)
- `screens/meta_screen.dart` — 강화 4종 + epic 주문 해금 목록
- 통합: 타이틀에 결정 잔액·강화 버튼, 판 종료 시 결정 지급(결과 화면 표시),
  런에 체력/마나/리롤/시작유물 강화 반영, 미해금 epic은 보상에서 제외
- provider를 MaterialApp 위로 올려 모든 화면에서 메타 접근 가능하게 수정
- `test/meta_test.dart` — 저장 왕복, 구매 규칙, 지급 계산, 런 반영 13개
- `tool/sim_core.dart` 분리 + `tool/meta_progression.dart` 신설:
  200판 연속 플레이로 페이싱 측정 → "소비처 30판 고갈" 발견 (BALANCE.md 3차)

## 단계 6 — 손맛 + 아이콘 + 화면 개편 ✅

GAME_DESIGN 6절(손맛)과 9.5절(딸깍 다이스 참고 방침)을 함께 반영했다.

**아이콘 (절대 규칙 6 준수)**
- game-icons.net 저장소 목록(4,176종)과 우리 JSON의 아이콘 이름 135개를
  전수 대조 → 존재하지 않던 10개를 검증된 이름으로 교정
- SVG 136개 다운로드 (`assets/icons/`), `ATTRIBUTION.md` 자동 생성
  (작가 10명, CC BY 3.0 명시)
- `widgets/game_icon.dart` — SVG를 원하는 색으로 물들여 그리는 위젯
- 전투·지도·보상·메타·타이틀 전 화면에 아이콘 적용

**효과음 (외부 다운로드 없이 직접 합성)**
- `tool/gen_sfx.dart` — 사인파·노이즈로 5종 합성 → `assets/sfx/*.wav`
  (딸깍/주사위 굴림/타격/대성공/폭주). 저작권 문제 없음, 나중에 교체 가능
- `services/sfx_service.dart` — 효과음 + 진동(HapticFeedback) 통합.
  실패해도 게임이 멈추지 않도록 전부 예외 처리

**연출**
- `widgets/dice_widget.dart` — 굴림 애니메이션. 눈이 빠르게 바뀌다가
  마지막에 ease-out으로 감속하며 멈춘다 (6절 "주사위 감속")
- `widgets/battle_fx.dart` — 대성공 흰색 플래시(120ms), 폭주 화면
  흔들림(좌우 8px·300ms), 족보 완성 팝업("트리플!!"이 팡 터짐)
- `widgets/battle_stage.dart` — 마법사 vs 적 대치 구도 + 대미지 숫자가
  위로 튀어오르며 사라지는 연출(600ms)
- 전투 화면 개편: 주사위 82px로 확대(주인공), 손패는 아이콘 카드,
  마나·축적을 점 아이콘으로, 텍스트 축소

## 단계 6-2 — 픽셀아트 전면 교체 + 화면 재설계 ✅

사용자 요청: "딸깍 다이스 정도의 픽셀아트로, 화면이 조악하지 않게".

- **자체 제작 픽셀아트로 전면 교체** (외부 에셋 0개)
  - `lib/art/pixel_sprite.dart` — 도트 문자열을 그대로 그리는 렌더러.
    `1`~`5`는 색조(tint) 기반 명암이라 스프라이트 하나를 색만 바꿔
    여러 몬스터로 재활용한다. 안티앨리어싱을 꺼서 픽셀이 또렷하다.
  - 캐릭터 15종 (16×16): 마법사, 슬라임, 짐승, 박쥐, 버섯, 해골, 고블린,
    거구, 기사, 유령, 골렘, 용, 악마, 로브, 날개
  - 아이템·주문 17종 (12×12): 구슬, 얼음, 방패, 심장, 잎, 별, 해골, 번개,
    주사위, 금화, 반지, 물약, 책, 보석, 편자, 모래시계, 검
  - `sprite_map.dart` — 적 30종을 스프라이트+색조로 매핑, 주문은 효과·속성,
    유물은 효과 종류로 자동 연결
  - flutter_svg 의존성과 SVG 136개 제거 (ATTRIBUTION.md에 경위 기록)
- **화면 재설계** (`widgets/pixel_ui.dart` 공통 디자인)
  - 전투: 마법사와 적이 땅 위에서 마주 선 장면, 각진 체력 막대,
    금색 테두리 패널, 칸으로 나뉜 마나·축적 표시
  - 주사위: 굴릴 때 통통 튀어오르고 눈도 네모로 찍어 픽셀 느낌 유지
  - 타이틀·지도·보상·결과·메타 전 화면을 같은 팔레트로 통일

## 단계 6-3 — 해상도 2배 + 딸깍 다이스식 전투 구도 ✅

- **스프라이트 해상도 2배**: 캐릭터 16×16 → **32×32** (15종),
  아이템 12×12 → **16×16** (17종). 외곽선·명암·표정을 넣을 여유가 생겼다.
- **`test/sprite_test.dart` 신설** — 32개 스프라이트의 줄 길이·크기·색
  글자를 전수 검사한다. 도트를 찍다 섞인 공백 29곳과 폭이 어긋난 1줄을
  이 테스트가 잡아냈다 (눈으로는 못 찾을 오류였다).
- **전투 화면을 세로 대치 구도로 재편** (딸깍 다이스 참고):
  - 맨 위: 적 이름·체력 막대·다음 행동 예고
  - 위쪽: 적이 단 위에 크게 서 있음
  - 아래쪽: 마법사와 플레이어 체력
  - 그 아래: 손패 3장
  - **맨 아래: 주사위 트레이가 화면을 지배** — 마나·축적 표시, 큰 주사위
    3개, 굴리는 중 실시간 족보 표시("트리플! 확정 대성공"), 리롤·시전 버튼
  - 무대 높이가 화면에 맞춰 늘어나고 줄어든다 (작은 기기 대응)

## 단계 6-4 — 딸깍 다이스 실플레이 분석 반영 ✅

사용자의 갤럭시 S20+에 무선 디버깅(adb)으로 접속해 딸깍 다이스를 직접
플레이(챕터1 10층까지)하며 **스크린샷 22장**을 `refs/dicero/`에 수집하고,
분석을 `refs/DICERO_ANALYSIS.md`로 정리한 뒤 아래를 반영했다.

- **전투 구도를 원근법으로** — 좌우 대치 → 복도 안쪽을 보는 시점.
  적은 위쪽에 작게(멀리), 마법사는 아래쪽에 크게 **뒷모습**
  (`kSpriteWizardBack` 신규 32×32). 배경도 위(먼 곳)가 밝은 안개,
  아래(발밑)가 어두운 바닥이라 적 실루엣이 또렷하다.
- **좌측 노드 트랙** — 전투 중에도 앞으로 올 층을 아이콘으로 표시
- **HP 숫자를 막대 위에 겹쳐** 표시 (`PixelBar.label`)
- **매 타격마다 화면 플래시** (대성공은 더 세게) + **오른쪽 가장자리
  피해 배너**가 슬라이드 인
- **보상 화면을 가로 배너 카드로** (`RewardBannerCard`):
  [아이콘 박스] + [이름 알약] + [설명] + 「추천」 뱃지, 등급별 바탕색.
  화면 전체가 보상 색조로 물든다
- **결과 화면**: 리본 배너 + **도달 층수를 84pt 거대 숫자로**
- **상점을 NPC 화면으로** (`ShopDialog`) — 대화상자 대신 로브 상인 그림 +
  큰 회복 타일 + 큰 버튼
- **UI 기반 개선**: 패널에 모서리 장식, 버튼에 눌리는 입체 그림자,
  리본 배너·뱃지 위젯 추가, 잠긴 주사위는 금빛+자물쇠로 강조

가져오지 않은 것(설계 충돌): 주사위 탭=즉시 리롤, 주사위 개수 성장,
스킬을 주사위에 장착, 족보 곱셈 배수, 스태미나 제한. 근거는 분석 문서에 명시.

## 단계 6-5 — 그래픽 전면 재제작 ✅

사용자 지적("조악하다, 딸깍 다이스 수준으로, 이펙트가 아예 없다")에 따라
그림 관련 부분을 원점에서 다시 만들었다.

**스프라이트 시스템을 손도트에서 도형 조합으로 교체**
- `art/sprite_builder.dart` — 타원·다각형·사다리꼴을 겹쳐 실루엣을 만들면
  **빛 방향(왼쪽 위) 기준으로 명암을 자동 계산**한다. 각 픽셀에서 가장 가까운
  바깥점을 너비 우선 탐색으로 찾아 표면 법선을 구하고, 빛과의 각도로
  5단계 음영을 칠한 뒤 색조를 머금은 외곽선을 두른다.
- 손으로 도트를 찍던 방식(32×32)은 명암을 넣을 수 없어 단색 덩어리였다.
  이제 **48×48**이고 입체감이 있다.
- 팔레트를 7단계 명암 + 고정색 18종으로 확장 (`pixel_sprite.dart`)
- 스프라이트에 **발밑 그림자**와 **피격 시 흰색 물들임**(flashAmount) 추가

**배경 아트 신설** (`art/stage_backdrop.dart`)
- 하늘 그라데이션 → 별(깜빡임) → 달(달무리·크레이터) → 산맥 3겹 →
  지평선 안개 → 원근 통로 바닥(이음선) → 기둥 4개 → 발밑 어둠 → 비네트
- 층별 테마 3종 (얼어붙은 하층 / 자줏빛 중층 / 불타는 상층)
- 떠다니는 마법 입자가 천천히 위로 흐른다

**마법 이펙트 신설** (`art/spell_fx.dart`) — 요청하신 부분
- 3단계 재생: **차징**(손끝에 빛이 모임) → **비행**(꼬리를 끌며 날아감) →
  **명중**(팽창하는 링 2겹 + 중심 섬광 + 사방으로 튀는 파편)
- 속성별로 투사체 모양이 다르다: 화염=구슬, 냉기=회전하는 결정,
  비전=회전하는 별, 그림자=해골, 자연=잎사귀, 폭주=보라 해골
- 대성공이면 이펙트가 1.8배로 커진다

**전투 연출**
- 적: 피격 시 흰색 번쩍임 + 뒤로 밀림 + 세로 찌그러짐
- 마법사: 시전 시 뒤로 젖혔다 앞으로 나감
- 양쪽 다 **숨쉬기 애니메이션**(느린 상하 흔들림)

**주사위 재제작** (`dice_widget.dart`)
- 평면 사각형 → **입체 주사위**: 둥근 모서리, 대각선 그라데이션,
  윗면 하이라이트 띠, 아래쪽 안쪽 그림자, 도톰한 눈(그림자 포함)
- 굴릴 때 튀어오르며 회전·찌그러짐, 바닥 그림자가 같이 줄어듦
- 잠그면 금빛으로 물들고 「잠금」 라벨이 붙음

**주문 카드 재제작** — 큰 아이콘 + 속성색 원광 + 등급 테두리 + 위력 알약

## 최종 상태

- `flutter test`: **95개 전부 통과**
- `flutter analyze`: 경고 0
- 파일 300줄 규칙: 전 파일 준수
- Windows 데스크톱 디버그 빌드로 실행 확인 (Android SDK 설정 전 임시 확인 수단)



---

# 그래픽 개선 1단계 — 아트 디렉션 교체 + 폰트 (2026-09-01)

근거: `WORK_ORDER_GRAPHICS.md` 작업 1. 상세 보고서는 `reports/03_graphics_task1.md`.

## 무엇을 했나

1. **팔레트 교체** (`widgets/pixel_ui.dart`) — 명도가 확실히 갈라지는 6단계
   (`kBgWell` → `kBgTray` → `kBgDeep` → `kBgPanel` → `kBgPanelLit` → `kBorderDim`).
   `kChargePurple`(보라)을 **`kCharge`(청록 0xFF4FD1C5)** 로 교체했다.
   보라 배경에 보라 마력 축적이 묻히던 문제가 사라졌다.
2. **전투 배경 층 테마 3종의 채도를 올리고 명도를 벌렸다**
   (`art/stage_backdrop.dart`) — 먼 곳은 밝게, 발밑은 더 어둡게(0.42 → 0.58).
3. **캐릭터 실루엣 테두리**(`PixelSpriteView(halo:)`)를 추가하고 전투 무대의
   마법사·적에 적용. 마법사 로브를 **청록(`kWizardTint`)** 으로 바꿔 배경
   3테마 어느 쪽에서도 실루엣이 뜨게 했다.
4. **손패 카드에서 속성색을 걷어냈다**(`widgets/spell_card.dart`) — 카드 바탕이
   통째로 주황·파랑·보라라서 주사위보다 튀던 문제. 지금은 무채색 패널이고
   속성색은 아이콘과 위쪽 4px 띠에만 남는다.
5. **한글 픽셀 폰트 Galmuri 도입** (OFL 1.1, `assets/fonts/`, 출처는
   `ATTRIBUTION.md`). 앱 기본 글꼴이 되었고, **화면의 모든 fontSize를 정수
   배율 사다리(12·24·36·48·84 / 10·20·30)로 정리**했다.

## 새로 생긴 것 — 화면 캡처 도구

`test_shots/capture_test.dart`. `flutter test` 는 `test/` 만 훑으므로 평소에는
돌지 않는다. 5개 화면을 PNG로 떨어뜨린다:

```
flutter test test_shots/capture_test.dart --dart-define=SHOT_STAGE=after
```

결과는 `reports/img/_shots/<stage>/`. 개정 전/후 비교 시트는
`reports/img/03_graphics_task1.png`.

## 상태

- `flutter test`: **139개 전부 통과**
- `flutter analyze`: 경고 0
- Windows 데스크톱 빌드로 실제 실행 확인 (폰트가 진짜로 적용되는지까지)

---

# 데이터 A — 폭주 6갈래 80종 + 주문 서클 (2026-09-02)

근거: `WORK_ORDER_DATA_A.md`. 상세 보고서는 `reports/08_데이터A_폭주주문.md`.

## 무엇을 했나

1. **`assets/data/surges.json` 80종 전면 교체** — `SURGE_DESIGN.md` 5절 그대로.
   `category`가 옛 5종(`self_harm`/`reversal`/...)에서 **6갈래**
   (`backlash` 역류 20 · `misfire` 오발 18 · `fizzle` 불발 11 · `amplify` 증폭 11 ·
   `summon` 소환 11 · `chain` 연쇄 9)로 바뀌었다.
   태그는 **`any` + 속성 5종만** 쓴다. 부가 태그 12종은 폭주에서 전부 사라졌다.
2. **`assets/data/spells.json` 70종에 `circle`(1~9) 추가** — `rarity`는 그대로 둔다.
   `gaia_wrath`의 `dc_modifier`만 2→3 (2026-09-01 승인된 유일한 수치 변경).
3. **검산 테스트 2개 신규** — `test/surge_data_test.dart`(8검사),
   `test/spell_circle_test.dart`(5검사).

## 핵심 결과

**주문 70종 전부가 갈래 비율 25/20/15/15/15/10%와 오차 0으로 일치한다.**
후보 풀이 `any` 40종(합 1000) + 자기 속성 8종(합 120) = 48종·1120으로 고정되기 때문이다.

## 아직 안 된 것 (지시서 C의 일)

새 효과 4종(`self_damage_spell`·`enemy_heal`·`shield`·`chain_cast`)이
`core/surge.dart`에 없어 **지금은 조용히 무시된다.** 앱은 죽지 않지만
역류·연쇄가 무효과 상태다. 폭주 팝업 색상표도 옛 5종 기준이라 기본색으로 뜬다.

## 상태

- `flutter test`: **152개 전부 통과** (기존 139 + 신규 13)
- `flutter analyze`: 경고 0
- `test/loader_test.dart`의 검사 1개를 v2 규약에 맞게 고쳤다 (보고서 6절)

---

# 2026-09-02 — 코드(폭주) 효과 4종 + 팝업 6갈래

근거: `WORK_ORDER_CODE_SURGE.md`. 상세 보고서는 `reports/10_코드폭주_효과팝업.md`.

## 무엇을 했나

1. **새 효과 4종 구현** (`lib/core/surge.dart`) —
   `self_damage_spell`(역류: `max(주문위력, 4) × N%`, **시전 강도 배율 미적용**) ·
   `enemy_heal`(적 최대 체력에서 절단) · `shield` · `chain_cast`(손패의 다른 주문 |N|개,
   양수=적/부가효과 O, 음수=나/부가효과 X).
   → 데이터에만 있고 조용히 무시되던 **역류 20종·연쇄 9종·오발 8종이 이제 실제로 동작한다.**
2. **자해 상한을 「폭주 전체 합산 후 절단」으로** — `self_damage` + `self_damage_spell` +
   음수 `chain_cast`를 `SurgeOutcome`에 모아 최대 체력의 40%로 자른 뒤 한 번에 입힌다.
   방어막 흡수는 그다음이다. 개별 절단이 아니다.
3. **폭주 팝업 6갈래로 교체** — 갈래(30px) → 숫자(20px) → 한 줄(12px) → 사건 이름(10px).
   색은 역류=적 · 오발=보라 · **불발=무채** · 증폭=금 · 소환=초록 · 연쇄=파랑.
   숫자 칸을 만들기 위해 `SurgeSystem.lastSurgeSummary`를 신설했다 (불발은 빈 문자열).
4. **`test/surge_effect_test.dart` 신규 21개** — 폭주를 추첨에 맡기지 않고 손으로 만들어
   결정적으로 검사한다.

## 새 상수 (`core/constants.dart`)

| 상수 | 값 | 뜻 |
|---|---|---|
| `kBacklashMinPower` | 4 | 위력 0인 보조 주문의 역류 기준 위력 |
| `kPercentBase` | 100 | 백분율 분모 |
| `kSurgeSelfDamageMaxRatio` | 0.4 | 한 폭주의 자해 총합 상한 (최대 체력 대비) |

## 구조가 바뀐 곳

- `Battle.otherCastableSpells()` 신설 — 스트레이트(`_autoCastOther`)와 연쇄가 공유한다
- `Battle._applySpellEffect` → `applySpellEffect` 공개 (연쇄가 같은 규약을 쓴다)
- `lib/core/surge_summary.dart` 신규 — 효과를 팝업 문자열로 옮기는 변환만 담는다

## 상태

- `flutter test`: **196개 전부 통과** (기존 175 + 신규 21)
- `flutter analyze`: 경고 0
- 화면 확인: `reports/images/surge_popup.png`
- 판정 엔진(`check.dart`·`combo.dart`)은 **손대지 않았다**

## 기획 창에 넘긴 질문

**후반 역류가 자해 상한(체력 20 → 8)에 눌려 다시 고정 수치가 된다.**
서클 3급이든 9급이든 역류 피해가 똑같이 8이다. 상한 비율을 유지할지 올릴지는
시뮬레이션 재측정 뒤에 판단하는 것이 맞다 — 보고서 10의 7-1절.

---

# 2026-09-02 — 시전 강도 2단계 개정 (지시서 E)

근거: `WORK_ORDER_INTENSITY.md` / 보고서: `reports/11_강도2단계.md`

## 무엇을 했나

1. **「약하게」 강도를 삭제**했다 — enum·상수·유물 효과(`check_bonus_weak`)·
   화면 버튼·봇 정책까지 전부. 마나 0에 대성공률이 높아 지배 전략이 되던 구멍이었다.
2. **「보통」의 마나 비용을 1 → 0으로 내렸다** (`kManaCostNormal`).
   이제 마나는 「전력 시전과 리롤을 사는 돈」 하나의 뜻만 갖는다.
3. **대성공 강등 경로를 통째로 제거**했다 — `CastIntensity.canCrit`,
   `Battle.effectiveGrade()`, 마력 축적 소모의 `&& canCrit` 예외.
   `lastAppliedGrade`는 UI·통계가 쓰므로 남겼다 (이제 항상 `result.grade`와 같다).
4. **유물 2종 재배치** — `warm_pebble`·`balance_bracelet`이 값은 그대로 두고
   `check_bonus_weak` → `check_bonus_full`이 됐다 (기획 창 결정).
5. **봇 강도 정책 재작성**(`tool/sim_core.dart`) — 「마나가 없어 못 쓴다」 갈래가
   사라져 4갈래가 됐다. 마지막 줄(보통)이 무조건 성립한다.

## 수치로 확인한 것

`kManaCostNormal`만 0↔1로 바꿔 같은 시드로 두 번 돌린 비교 (자세히는 보고서 4-4):

| 지표 | 보통 = 마나 1 | 보통 = 마나 0 |
|---|---|---|
| 유료 리롤 / 시전 | 0.353회 | **0.431회** (+22%) |
| 전력 시전 비율 | 68.5% | **73.1%** |

**수치 해석·밸런스 조정은 하지 않았다.** 지시서 F의 일이다.

## 상태

- `flutter test`: **201개 전부 통과** (기존 196 − 삭제 3 + 신규 8)
- `flutter analyze`: 경고 0
- 신규 테스트: `test/intensity_test.dart` — **enum 인덱스 밀림**을 잡는 표 대조가 핵심
- 화면 확인: `reports/images/intensity_two_step.png` (480×900)
- 판정 엔진(`check.dart`·`combo.dart`·`check_test.dart`)은 **손대지 않았다**

---

# 시뮬레이션 측정 (2026-09-02, 지시서 F `WORK_ORDER_SIM.md`)

**측정만 했다. 밸런스 수치·확률표·데이터를 하나도 바꾸지 않았다.**
결과는 `BALANCE.md` 「5차」 절과 `reports/12_시뮬측정.md`,
원본 출력은 `reports/data/measure_output.txt`.

## 무엇을 만들었나

- `tool/measure.dart` + 보조 6파일 — 1000판 × **모드 3종** × **시드 2개**를 돌린다.
  모드는 **A 순수(리롤✕유물✕) / B 리롤만 / C 전체**다. 3.4절 확률표는
  「보정 0·리롤 없음」의 값이라 실제 플레이(C)와 그대로 비교할 수 없어 나눴다.
- `tool/sim_tally.dart` — 굴림·리롤·턴·폭주 갈래·자해를 세는 집계기.
  대부분은 도구 쪽에서 셀 수 있고, **`lib/` 수정은 폭주 자해 카운터 2개뿐**이다
  (상한이 `SurgeSystem` 안에서 잘려 밖에서는 안 보인다).
- `test/measure_sanity_test.dart` — **측정 코드가 옳은지** 검사하는 12개.
- `tool/simulate.dart`는 **한 글자도 안 고쳤다** (기준선 보존).
  실제로 개정 전후 출력이 동일하다 (완주율 24.4% · 평균 8.68층).

## 재 본 결과 — 문서의 전제 3개가 전부 실측과 다르다

| 전제 | 문서 | 실측 |
|---|---|---|
| 전투당 굴림 (`ENGRAVINGS.md` 4절) | 9굴림 | **2.4굴림** (중앙값 2) |
| 일반 전투 턴 수 (`STAGES.md` 1절) | 5~8턴 | **1.53턴** |
| 보통 시전 실패율 (`GAME_DESIGN` 3.4절) | 2.8% | **9.7%** |

폭주 6갈래 비율(목표 25/20/15/15/15/10%)은 **최대 이탈 2.1%p**로 잘 맞는다.
자해 상한(최대 체력 40%)은 **자해 폭주의 27%**에서 실제로 문다.

## ★ 가장 큰 발견 — 3.4절 확률표가 3.5절과 어긋난다

`resolveCheck`로 3d6 216가지를 전수 계산한 값이 3.4절 표와 다르고,
차이가 정확히 **뱀눈 15/216 = 6.9%p**다. **뱀눈을 「폭주 확정」이 아니라
페어(+3)로 취급해** 계산하면 3.4절 표의 여덟 값이 소수점까지 재현된다.
→ 표가 3.5절의 「뱀눈 = 폭주 확정」 이전 계산이다.

**절대 규칙 3에 따라 수치를 건드리지 않았다.** 선택지는 보고서 12의 7절에 있다.

## 상태

- `flutter test`: **213개 전부 통과** (기존 201 + 신규 12)
- `flutter analyze`: 경고 0
- 재현: `dart run tool/measure.dart` · `dart run tool/simulate.dart`
- 기계적 정리 2건도 함께 처리: `kDcWeak` → `kDcLow` 개명,
  `lastAppliedGrade` 주석 보강 (둘 다 동작 변경 없음)

---

# 진행 구조 교체 · 엔진층 (2026-09-02, 지시서 D `WORK_ORDER_PROGRESSION.md`)

**「1층~10층 단선」을 「지역 12 · 스테이지 100 · 난이도 3」 구조로 갈아끼웠다.**
보고서는 `reports/13_진행구조.md`.

## 무엇을 했나

- **모델 3종 신규** — `Region` · `Stage` · `EnemyVariant`. `Enemy`에 `region`(`int?`) 추가.
  전부 Flutter 무의존이라 `tool/`이 그대로 쓴다
- **로더가 4파일 → 7파일**. `GameDataParser.parseAll(read)` 헬퍼로 테스트·도구의
  중복 7줄을 없앴다
- **`lib/core/enemy_builder.dart`** — 원본 `Enemy`를 안 건드리고
  `기본값 × 지역 배율 × 변종 배율 × 난이도 배율`을 곱한 새 인스턴스를 만든다.
  **보스에는 지역 배율을 곱하지 않는다** (`ENEMIES.md` 3절 표가 최종값)
- **`lib/core/stage_runner.dart`** — 마지막 층이면 지역 보스, 아니면
  tier 풀 → 변종 가중 추첨. 보스 변종은 난이도로 고정(`normal`/`shadow`/`ancient`)
- **`floorEvent(floor, floors)`** — 층수가 3~10으로 가변이라 고정 층 번호를 못 쓴다.
  비율 규칙으로 배치한다. 옛 상수 5종(`kFloorCount` 계열)은 **삭제**했다
- `RunState`·`RunController`가 스테이지 기반으로 재작성됐다.
  진입은 임시 상수 `kEntryRegionId` · `kEntryStageIndex` · `kEntryDifficulty` 고정

## 화면은 「깨지지 않을 만큼만」

지도·결과·전투 무대에서 `kFloorCount` 의존만 걷어냈다. **재설계는 지시서 G의 일이다.**
지도 제목 아래에 「마나의 숲 I · 보통」 한 줄만 추가했다.

## 상태

- `flutter test`: **229개 전부 통과** (기존 213 + 신규 `test/progression_test.dart` 16)
- `flutter analyze`: 경고 0
- `dart run tool/simulate.dart`: 돈다. 단 **「지역 1 · 스테이지 1(3층)」 고정**이라
  ⚠ **지금 나오는 수치를 해석하면 안 된다.** 재측정은 다음 지시서다
- 화면 확인: `flutter test test_shots/capture_test.dart --dart-define=SHOT_STAGE=progression`

## 기획 창에 넘긴 질문 (보고서 13의 7절)

1. **보스 층 주문 보상이 지금은 버려진다** — 받자마자 결과 화면으로 간다
2. **6층 이하 스테이지 32개에 층 중간 주문 보상이 없다** — 「3의 배수」 규칙의 결과
3. **`ENEMIES.md` 5.3절의 난이도별 변종 보정(고대의 +10/+20%p)이 지시서에 없어 안 넣었다**

---

# 지역 배율 분리 (HP/공격) — `WORK_ORDER_SCALE_FIX.md` (2026-09-02)

## 무엇을 했나

**지역 배율 하나를 HP와 공격에 똑같이 곱한 탓에 후반 일반 적이 보스보다
2.3배 아팠다.** 배율을 둘로 나눴다 (`ENEMIES.md` 6절 · 사용자 승인 완료).

- `regions.json` 12행: `scale` → **`hp_scale`(1.00→5.00) + `atk_scale`(1.00→1.45)**
- `Region` 모델: `hpScale` · `atkScale` 두 필드
- `buildEnemy(base, variant, regionHpScale, regionAtkScale, difficulty)` —
  `hp`·`defend`·`heal`은 체력 배율, `attack`·`charge`는 공격 배율.
  **보스는 두 배율 모두 1.0으로 친다** (변종·난이도 배율은 보스에도 그대로)
- **`atk_scale`이 완만한 이유**: tier가 이미 공격을 2.4배 올리고 있다.
  거기에 HP 배율(×5.00)을 또 곱하면 12배가 된다

**이 24개 값 외에 밸런스 수치를 하나도 바꾸지 않았다.**

## 이번 사고를 테스트가 잡게 만들었다

`test/progression_test.dart`의 **S5** — 「12지역 전부에서 보스 공격 ≥ 그 지역
일반 적 평균 공격」. 이 불변식이 깨진 것이 문제의 정체였다.
사람이 표를 다시 보지 않아도 데이터가 어긋나면 즉시 걸린다.
`region_stage_test`의 옛 「6. scale 단조 증가」는 6-1(체력 · 단조 **증가**) ·
6-2(공격 · 단조 **비감소**) · 6-3(옛 `scale` 키 없음)으로 나눴다.

## 재측정

`tool/measure_stage.dart`를 **한 글자도 안 고치고** 그대로 다시 돌렸다
(측정 14와 같은 조건). 결과는 `reports/data/measure3_output.txt`,
전후 대조는 `BALANCE.md` 7차 절과 `reports/15_배율분리_재측정.md`.

- 일반 적 공격이 전 구간 **체력의 15~18%** 로 평평해졌다 (개정 전 18%→61%)
- 완주율(보통): 3지역 29.9%→**44.0%**, 5지역 1.4%→**8.8%**, 10지역 0.1%→**15.0%**
- 일반 전투 평균 턴 2.74 → **3.56**, 보스 턴 6.28 → **6.15**(안 건드렸으니 그대로)
- **7지역 이후는 여전히 0%대다.** 지시대로 더 조정하지 않았다

`tool/measure_reserve.dart`를 새로 만들어 「보통 위주 봇」(`kBotFullManaReserve`=1)도
대표 4지역에 재 봤다. **완주율은 오히려 오르고 턴은 ±0.5턴 안이었다** —
전투 길이의 지배 변수는 봇 성향이 아니라 적 HP와 손패 위력이다.
측정 뒤 상수는 **0으로 되돌렸다.**

## 상태

- `flutter test`: **237개 전부 통과** / `flutter analyze`: 경고 0
- 측정 14 원본 `reports/data/measure2_output.txt`는 **덮어쓰지 않고 보존**했다

## 기획 창에 넘긴 것 (보고서 15의 7절)

1. 🔴 **후반 난이도가 단조롭지 않다** — 10·11지역이 7·8·9지역보다 쉽다.
   가정 성장 프로필의 서클 계단(9→10지역에 6→7) 때문이며 **적 데이터 문제가 아니다**
2. **각인 기대 적립이 문서 기재값의 절반~2/3** (실측 7.7~11.1% vs 13~19%)
3. 📌 `ENEMIES.md` 6절 「개정 전」 표 값이 계산값과 0.1~0.4 다르다 (결론은 같음, 안 고침)
4. 📌 `WORK_ORDER_DATA_B.md` 119행의 `regions.json` 예시에 옛 `"scale"`이 남아 있다

---

# 덱에서 손패 뽑기 + 덱 기반 재측정 — `WORK_ORDER_DECK_SIM.md` (2026-09-02)

## 무엇을 했나

**측정 14·15는 손패를 「그 지역 최고 서클 3장」으로 고정해서 쟀는데, 그건 실제
규칙이 아니었다.** v2 4.2절의 「매 턴 덱에서 3장을 뽑는다」를 엔진에 넣고 다시 쟀다.

- `lib/core/hand_deck.dart` **신규** — 덱 추첨. 중복 없음 · 직전 턴 시전 주문 1턴
  휴식 · 봉인 제외 · 쉬는 규칙보다 **3장 채우기 우선** · `Battle`의 `Random` 사용
- `Battle`에 **`deck`을 선택 인자로** 추가했다. 주지 않으면 지금까지와 **완전히
  같이** 동작한다 → **기존 테스트 237개를 하나도 안 고쳤다**
- `lib/core/spell_effects.dart` **신규** — `battle.dart`가 300줄 규칙을 넘지 않도록
  `applySpellEffect` 본체만 옮겼다 (규칙은 한 줄도 안 바꿨다)
- `tool/measure_profile.dart`: `profileHand` → **`profileDeck`**.
  `SPELL_CIRCLES.md` 부록 슬롯표대로 지역별 덱(1지역 3장 ~ 12지역 19장)을 만들고
  1서클 칸은 **유틸 우선**(회복·방어막·지연)으로 채운다

**밸런스 수치는 하나도 바꾸지 않았다.** 체력·마나 프로필도 측정 15 그대로다.

## 재측정 (측정 16)

같은 조건(36조합 × 300판 × 시드 2개)으로 다시 돌렸다.
출력 `reports/data/measure4_output.txt`, 대조는 `BALANCE.md` 8차 절과
`reports/16_덱기반측정.md`.

- 완주율(보통): 1지역 98.0%→**70.3%**, 3지역 42.3%→**68.3%**,
  10지역 13.3%→**0.0%**. **7~9·12지역은 여전히 0%**
- 전투당 굴림 5.49 → **6.66**, 일반 전투 평균 턴 3.56 → **4.73**
- 자해 상한에 잘린 비율 4.16% → **0.09%**

## 이번에 드러난 것 (기획 창에 넘김 — 보고서 16의 12절)

1. 🔴 **봇이 유틸을 안 쓴다.** 덱의 31~50%가 유틸인데 **시전 중 유틸은 1.6~10%**다.
   봇 정책이 「가장 센 주문」이라 손에 잡힌 회복·방어막을 지나친다
2. 🔴 **1·2지역 덱에 공격 주문이 1장뿐**이라(1서클 3칸을 유틸 우선으로 채운 결과)
   그 한 장이 아슬아슬 반동으로 봉인되면 전투가 교착한다 —
   **200턴 안전장치에 걸린 판이 실제로 나왔다**
3. 10·11지역의 「회복」 편중 정체는 1서클 유틸이 아니라 **`soul_reaper`**
   (7서클 · 위력 15 · 회복 5)였고, 덱을 바꿔도 그대로 남았다

## 상태

- `flutter test`: **244개 전부 통과** (기존 237 + `test/deck_test.dart` 7)
- `flutter analyze`: 경고 0
- 측정 15 원본 `reports/data/measure3_output.txt`는 **덮어쓰지 않고 보존**했다



---

# 덱·봇 수정 + 결정용 재측정 — `WORK_ORDER_DECK_FIX.md` (2026-09-02)

## 무엇을 했나

**측정 12·14·15·16이 전부 전제가 흔들린 상태였다.** 세 가지를 고쳐 전제를 맞췄다.

1. **✅ 승인된 수치 1건** — `assets/data/spells.json`의 `soul_reaper`(영혼 수확)
   `base_damage` **15 → 13**. 회복 5·DC +1은 그대로다.
   7서클 다섯 장 중 혼자 위력이 튀어 후반을 독점하던 것을 태양의 창과 동률로 맞췄다
2. **덱 채우는 순서** — `tool/measure_profile.dart`의 `kCircle1Priority`를
   「위력을 지킨 유틸 3종」(잿불 방패·냉기 손길·영혼 갉기, 전부 위력 5) 먼저로 바꿨다.
   측정 16은 위력 0인 치유 약초·마력방패를 먼저 넣어 **1지역 덱에 적을 깎을 주문이
   한 장뿐**이었고, 그 한 장이 봉인되면 전투가 교착했다
3. **봇 유틸 정책** — `tool/bot_policy.dart` **신규**. 「체력 40% 미만이면 회복」·
   「다음 공격에 죽으면 방어막」 두 줄만 더한 `utility` 정책을 넣었다.
   ★ 정책은 **`const`가 아니라 인자**라서 `greedy`(기존)와 **한 번의 실행에서
   나란히** 돈다 — 측정 15처럼 상수를 고쳐 두 번 돌릴 필요가 없다

**그 밖의 밸런스 수치·체력/마나 프로필·시전 강도는 하나도 바꾸지 않았다.**

## 재측정 (측정 17)

같은 조건(36조합 × 300판 × 시드 2개) + 정책 2개.
출력 `reports/data/measure5_output.txt`, 읽는 법은 `BALANCE.md` 9차 절과
`reports/17_덱봇수정_재측정.md`.

- **200턴 안전장치 0건** (측정 16은 보통 난이도 표본만 세어도 3건). 최장 27턴
- 완주율(보통·시드1): 1지역 70.3%→**96.0%**, 2지역 61.0%→**96.0%**
  (덱 순서 효과), 3지역 66.3%→**71.7%**, 5지역 6.0%→**11.0%** (봇 정책 효과)
- 시전 중 유틸 비율(4~9지역): **1.6~10% → 20~34%**
- 전투당 굴림 6.66 → **5.78**, 보스 전투 평균 턴 9.34 → **5.55**
- **후반(7·9~12지역)은 두 정책 모두 여전히 0%다**

## 이번에 드러난 것 (기획 창에 넘김 — 보고서 17의 11절)

1. 🔴 **후반 0%의 원인이 봇이 아니다.** 봇이 유틸을 20~34% 쓰게 됐는데도
   후반 완주율이 1%도 안 움직였다. 남은 후보는 **적 수치**와 **성장 프로필의 계단**
2. 🔴 **초반 데스는 `utility`가 더 나쁘다** (1지역 48.7%→39.7%).
   지시서가 정한 순서상 회복 규칙이 방어막보다 앞서, 죽기 직전에
   방어막 4 대신 회복 2를 고른다. 순서는 임의로 바꾸지 않았다
3. 각인 기대 적립 **7.7~11.7%** 대 문서 13~19%. 이번 값이 처음으로 교착에
   오염되지 않은 값이다 (전투당 굴림 5.78 vs 문서 전제 9굴림)
4. `soul_reaper`를 13으로 낮춰도 **측정용 덱 채우기는 여전히 그것을 고른다**
   (위력 동률에서 id 순으로 갈린다). 데이터 문제가 아니라 덱 채우기 규칙의 문제다

## 상태

- `flutter test`: **249개 전부 통과** (기존 244 + 신규 5).
  기존 테스트를 **하나도 고치지 않았다**
- `flutter analyze`: 경고 0
- 측정 16 원본 `reports/data/measure4_output.txt`는 **덮어쓰지 않고 보존**했다

---

## 측정 18 — 성장 시스템 반영 + 결정용 최종 측정 ✅ (2026-09-02)

`WORK_ORDER_GROWTH_SIM.md` 수행. **밸런스 수치는 하나도 바꾸지 않았다.**

### 무엇을 했나 (셋)

1. **마법서 강화**를 측정용 프로필에 반영 (`tool/measure_growth.dart` 신규).
   `SPELL_CIRCLES.md` 4절 — 1단계당 위력 +10%, 상한 `11 − 서클`.
   **강화 없음(하한)과 전부 만강(상한)을 한 번의 실행에서 함께** 잰다.
   위력만 오르고 `effect`·`dc_modifier`는 그대로다. **`lib/`에는 안 만들었다**
2. **패시브 최대 체력** `round(20 × (레벨 − 1) / 98)` — 두 열에 똑같이 적용.
   최대 체력 20 … **86** (`GrowthProfile.maxHp` = `baseMaxHp` + `passiveHp`)
3. **봇 정책 순서 교체** — 방어막 규칙이 회복 규칙보다 **앞으로**
   (검토 17 11-1 B). `greedy`는 비교 기준선이라 그대로 뒀다

측정은 **하한/상한 × greedy/utility 4가지**를 한 번에 돌렸다 (13초).
출력 `reports/data/measure6_output.txt`(3028줄), 읽는 법은 `BALANCE.md` 10차 절과
`reports/18_성장반영_측정.md`.

### 결과 (보통 · utility · 시드 20260831)

- 완주율 3지역 72.7% → **93.7%**, 4지역 51.7% → **87.0%**,
  5지역 20.3% → **55.0%**, 6지역 6.3% → **40.0%** (하한 → 상한)
- **11·12지역은 상한에서도 0%**
- 일반 전투 평균 턴: 12지역 8.11 → **5.37** (확인 지점 4턴은 9지역까지 통과)
- 전투당 굴림 5.78(측정 17) → 하한 6.19 / **상한 4.39**
- **200턴 안전장치 0건** — 네 조합 전부

### 이번에 드러난 것 (기획 창에 넘김 — 보고서 18의 10절)

1. 🔴 **후반의 벽은 보스가 아니라 「층 사이 회복」이다.**
   12지역 상한에서 **보스만 붙으면 승률 29%**인데 **평균 3.0층/10층에서 죽는다.**
   층 사이 회복이 **상점 1회 +10**뿐이다 (최대 체력 86의 12%)
2. **마법서 강화 +10%는 3~6지역에는 정확히 맞고 7지역 이상에는 부족하다.**
   다만 1번이 사실이면 강화율을 더 올리는 것은 잘못된 처방이다
3. 각인 실효 보너스가 문서의 **절반**(9.5 / 7.1 / 9.1 / 6.3% 대 17 / 14 / 19 / 13%).
   「전투당 9굴림」 전제는 확실히 틀렸다 — 측정 12~18에서 6.7굴림을 넘은 적이 없다
4. 봇 순서 교체는 초반 데스 격차를 −9.0 → **−7.7%p**로 줄였을 뿐 뒤집지 못했다.
   **방어막 주문의 가치가 낮다**는 신호일 수 있다

### 검산 — 하한이 측정 17과 다른 것은 구현 오류가 아니다

패시브를 끄고 봇 순서를 되돌려 다시 돌렸더니 **12지역 전부 측정 17과 소수점까지
같은 값**이 나왔다. 4지역 +12.7%p 중 +11.3%p가 패시브 체력, +1.4%p가 봇 순서다.
**검산에 쓴 임시 수정은 되돌렸다.**

### 상태

- `flutter test`: **254개 전부 통과** (기존 249 + 신규 5).
  기존 테스트를 **하나도 고치지 않았다**
- `flutter analyze`: 경고 0
- 측정 17 원본 `reports/data/measure5_output.txt`는 **덮어쓰지 않고 보존**했다
- 옛 정책 비교표의 절 번호를 `4-A~4-E` → `P-A~P-E`로 바꿨다
  (이번 지시서의 표가 4-A~4-D를 쓴다. 내용·셈법은 측정 17과 같다)

---

## 측정 19 — 회복 수정(강화가 heal·shield도 · 상점 30%) + 재측정 ✅ (2026-09-03)

`WORK_ORDER_HEAL_FIX.md` 수행. **적 수치 · 지역 배율 · 난이도 배율 · 층수 ·
성장 프로필을 하나도 안 바꿨다.**

### 무엇을 했나 (셋)

1. **마법서 강화가 `heal`·`shield`도 위력과 같은 배수로 올린다**
   (`tool/measure_growth.dart`의 `kUpgradableEffectTypes`).
   마력방패 6 → **12**, 치유 약초 5 → **10**.
   `check_bonus`·`extra_die`·`delay_enemy`·`mana_restore`·`dc_modifier`는 그대로.
   **`lib/`에는 여전히 강화 시스템을 안 만들었다**
2. **상점 회복이 고정 10 → 최대 체력의 30%** (`kShopHealRatio`).
   `kShopHeal` 상수를 지우고 `shopHealAmount(maxHp)` 하나로 통일했다 —
   화면(`shop_dialog`)이 「체력 +10」을 하드코딩하고 있어서 함께 고쳤다
3. **주문 3종 개명** — 용암 침 → **용암 덩이** / 눈덩이 사출 → **눈덩이 투척** /
   수액 분출 → **수액 채취**. `id`와 수치는 그대로다

출력 `reports/data/measure7_output.txt`(3130줄), 읽는 법은 `BALANCE.md` 11차 절과
`reports/19_회복수정_재측정.md`.

### 결과 (상한 · 보통 · utility · 시드 20260831)

- 완주율: 5지역 55.0 → **72.3%**, 6지역 40.0 → **62.3%**, 7지역 7.3 → **21.0%**,
  9지역 1.7 → **8.3%**, 12지역 0.0 → **0.7%** (측정 18 → 이번)
- **막힌 지점이 「7지역부터」에서 「9지역부터」로 두 지역 밀렸다**
- 전투당 방어막·회복(12지역): 1.49 → **2.68** / 4.09 → **7.48**
- 12지역 보스만 승률 29.0 → **34.0%**
- **200턴 안전장치 0건** — 4조합 × 2시드 전부

### 이번에 드러난 것 (기획 창에 넘김 — 보고서 19의 9절)

1. 🔴 **검토 18의 「전투당 피해가 체력의 75%」는 계산 착오였다.**
   같은 값을 수정 전 코드로 **실측**하니 **41.5%**다 (수정 후 40.3%).
   계산식이 방어막·전투 중 회복·「전투가 중간에 끝나는 것」을 다 빼먹었다
2. 그리고 이 지표는 **개선될수록 도로 올라간다** — 완주율이 오르면 더 깊은 층
   전투가 표본에 섞인다 (12지역 평균 도달 층 3.0 → 3.4). **확인 지표로 못 쓴다**
3. **후반 완주율의 목표치가 정해지지 않아** 다음 조정의 크기를 못 정한다
4. 방어막이 12로 커지면서 **`utility` 봇이 `greedy`를 5~7지역에서 +13~18%p
   앞선다** (측정 18에서는 −7~+7%p로 엇갈렸다)

### 검산 — 두 수정을 하나씩 꺼서 갈랐다

- **둘 다 끄면 12지역 × 하한·상한 24칸이 측정 18과 소수점까지 같다.**
  → 이번에 함께 손댄 **전투 계측 카운터·반동 함수 이동이 동작을 안 바꿨다**
- 상점 회복만 켜면 9지역 1.7 → 4.3%, 둘 다 켜면 8.3%.
  **대부분은 작업 1(heal·shield 강화)에서 왔다**
- **검산에 쓴 임시 수정은 되돌렸다** (재실행 결과가 바이트 단위로 같음을 확인)

### 300줄 규칙 때문에 옮긴 것

4-B·4-C를 재려면 `dealToPlayer`·`healPlayer` **안**에서 세야 해서
`lib/core/battle.dart`에 계측 3개를 넣었고, 자리를 만들려고
**아슬아슬 반동 함수를 `lib/core/spell_effects.dart`로 옮겼다.**
난수원을 그대로 넘기므로 굴림 순서가 같다 — 위 검산이 이를 증명한다.

### 상태

- `flutter test`: **256개 전부 통과** (기존 254 + 신규 2)
- `flutter analyze`: 경고 0
- 측정 18 원본 `reports/data/measure6_output.txt`는 **덮어쓰지 않고 보존**했다
- 출력의 절 번호를 정리했다: 옛 4-B → **4-E**, 4-C → **4-D**, 4-D → **4-D3**
  (이번 지시서가 4-B·4-C를 새 표로 쓴다. 옛 표의 내용·셈법은 그대로다)

---

## 측정 20 — 소모전 완화(전투 승리 회복 12% · 후반 hp_scale) + 목표 대조 ✅ (2026-09-03)

`WORK_ORDER_ATTRITION.md` 작업 1~4. 보고서 `reports/20_소모전완화_측정.md`,
원본 출력 `reports/data/measure8_output.txt` (3253줄).

### 바꾼 것 (둘 — 둘 다 사용자 승인된 수치)

1. **전투 승리 회복** — 일반 전투를 이기면 최대 체력의 **12%** 회복
   (`kBattleWinHealRatio`). **보스 층 제외**, 유물 회복을 **대체하지 않고 더한다**
2. **후반 `hp_scale` 완화** — 1.00/1.13/1.30/1.50/1.72/1.95/2.18/2.45/2.75/
   3.10/3.60/**4.20** (`assets/data/regions.json`). **`atk_scale`은 안 건드렸다**

### ★ 처음으로 「목표에 닿았는가」를 판정했다

`GAME_DESIGN.md` 6.2절 목표 대비 **12칸 중 6칸이 목표 안** (시드 2는 7칸).

- ✅ 목표 안: 1·2·3(도입) · 7(중반) · 9·10(후반)
- ❌ **너무 쉽다**: 4(+16.7%p) · 5(+11.0) · 6(+2.7) · 8(+5.7)
- ❌ **아직 어렵다**: 11(−1.0, 거의 닿았다) · **12(−12.0)**

7~11지역이 통째로 살아났다 (7지역 21.0 → **67.7%**, 9지역 8.3 → **44.7%**,
11지역 1.7 → **14.0%**).

### 소모전은 풀렸다 — 새 지표로 확인

「전투당 피해 %」(생존 편향)를 버리고 **판 단위** 지표로 갈아탔다:
**상점 층 도달률**과 **도달했을 때 남은 체력 %**.

- 9지역 도달률 17.7 → **65.0%** / 남은 체력 30.5 → **65.7%**
- 12지역 도달률 2.7 → **18.7%** / 남은 체력 30.8 → **51.5%**

### 검산 — 변인을 하나씩 꺼서 갈랐다 (한 번의 실행, 조건은 인자)

- **기준 조건(회복 ✕ · 옛 배율)이 측정 19와 24칸 소수점까지 같다** (최대 차 0.0%p)
- 기여는 **대부분 전투 승리 회복**에서 왔고, 배율 완화는 혼자서는 작지만
  **겹치면 더해지는 것 이상으로 커진다** (9지역: 회복만 31.0 → 둘 다 44.7%)

### 이번에 드러난 것 (기획 창에 넘김 — 보고서 20의 7절)

1. **도입 구간이 너무 길다.** 4~6·8지역이 목표보다 쉽다
2. **12지역이 남은 이유는 소모전이 아니라 보스다.** 최대 체력에서 바로 붙어도
   화염룡 승률이 **30.7%** (11지역 보스는 56.0%)
3. **봇 정책 격차는 경보선 아래**로 내려왔다 — 최대 12.0%p (측정 19는 +13~18%p)
4. 「전투당 9굴림」 전제는 이번에도 안 맞았다 (실측 **4.53**)

### 상태

- `flutter test`: **258개 전부 통과** (측정 19의 256 + 신규 2)
- `flutter analyze`: 경고 0
- 두 시드 정상 종료 · 같은 명령을 두 번 돌려 **출력이 바이트 단위로 같음** 확인
- 측정 19 원본 `reports/data/measure7_output.txt`는 **덮어쓰지 않고 보존**했다

---

## 측정 21 — 후반 곡선 정렬(hp_scale 9~12 · 보스 HP 10~12) + 두 줄 목표 ✅ (2026-09-03)

`WORK_ORDER_LATE_CURVE.md` 작업 1~5. 보고서 `reports/21_후반곡선_측정.md`,
원본 출력 `reports/data/measure9_output.txt` (3296줄).

### 바꾼 것 (둘 — 둘 다 사용자 승인된 수치)

1. **`hp_scale` 9~12지역 하향** — 2.75/3.10/3.60/4.20 → **2.70/2.95/3.20/3.50**
   (`assets/data/regions.json`). **1~8지역과 `atk_scale`은 한 칸도 안 건드렸다**
2. **보스 HP 10~12지역 하향** — 리치 330→**310** · 거인 400→**355** ·
   화염룡 480→**405**, 2페이즈 임계는 언제나 `hp ~/ 2` (155/177/202).
   **공격·강타·방어·2페이즈 패턴 값은 한 값도 안 바꿨다**

**왜**: 9→12지역에서 만강 덱 위력이 4%밖에 안 오르는데 적은 78% 올랐다(검토 20).
덱 구성도 강화 배수도 의도한 설계이므로 **적 곡선을 화력에 맞췄다.**

### ★ 목표가 두 줄(하한·상한)로 바뀌었다

`GAME_DESIGN.md` 6.2절 개정. **하한 = 강화 없음 · 상한 = 만강**이고,
**두 값의 간격이 그 구간이 요구하는 파밍의 양**이다.

**24칸 중 20칸이 목표 안** (시드 2는 19칸). 벗어난 칸:

- **7지역 두 줄 다** (−8.3%p / −12.3%p) — 중반의 마지막 칸에서 난이도가 튄다
- **4지역 하한 +18.3%p** — 강화 없이도 78.3%가 완주한다
- **12지역 상한 −14.0%p** (3.0 → **11.0%**로 3.7배가 됐지만 목표선 25%에 못 미침)

**11지역이 처음으로 목표 안에 들어왔다** (14.0 → **27.7%**).

### 보스 문제는 풀렸다

최대 체력에서 바로 붙었을 때의 승률(보통·상한·utility):
10지역 67.3 → **70.7%** · 11지역 56.0 → **61.0%** · 12지역 30.7 → **51.7%**.
**11 → 12에서 승률이 절반으로 꺾이던 낙차가 사라졌다.**
상점 층 도달률도 12지역에서 18.7 → **39.0%**, 남은 체력 51.5 → **60.5%**.

### 검산 — 조건 3개를 인자로, 한 번의 실행에서

- **기준 조건(옛 배율 · 옛 보스 HP)이 측정 20을 24칸 소수점까지 재현**했다 (0.0%p)
- **1~8지역은 세 조건이 완전히 같다** — 안 건드렸다는 증거
- 11지역은 배율이, 10지역은 보스가 더 크게 들었고, 12지역은 반씩 나눠 가졌다

### 이번에 드러난 것 (기획 창에 넘김 — 보고서 21의 6절)

1. **7지역을 후반 구간(40~70%)으로 옮기면 두 줄이 한 번에 목표 안에 든다**
   (상한 67.7% · 하한 11.7%) — 구간 정의 문제로 보인다
2. **12지역은 관문이 곱해지는 구조**다 (도달률 39.0% × 보스 승률). 층수를 줄이는
   쪽을 권했다
3. 「전투당 9굴림」 전제는 이번에도 안 맞았다 (실측 **4.49**)

### 상태

- `flutter test`: **262개 전부 통과** (측정 20의 258 + 신규 4)
- `flutter analyze`: 경고 0
- 두 시드 정상 종료 · 같은 명령을 두 번 돌려 **출력 해시가 같음** 확인
- 측정 20 원본 `reports/data/measure8_output.txt`는 **덮어쓰지 않고 보존**했다

---

## 측정 22 — 12지역 층수 하향(9·10 → 7·8) + 곡선 조건 판정 ✅ (2026-09-03)

`WORK_ORDER_FINAL_TUNE.md` 작업 1~4. 보고서 `reports/22_최종조정_측정.md`,
원본 출력 `reports/data/measure10_output.txt` (3372줄).

### 바꾼 것 (하나 — 사용자 승인된 수치)

**12지역 여섯 스테이지의 층수** 9·10·10·10·10·10 → **7·8·8·8·8·8**
(`assets/data/stages.json`). 지역 합 59 → **47**, 총 층수 740 → **728**.
**11지역 이하·권장 레벨·부제·보스 표시는 한 값도 안 건드렸다.**

**왜**: 12지역 보스는 이미 51.7%로 적당한데 완주율이 11.0%였다. 두 관문
(도달률 39.0% × 보스 승률)이 곱해지기 때문이고, 남은 손잡이가
「곱해지는 관문의 수」였다 (측정 21 6-1절 · 검토 21).

### ★ 판정이 「곡선 조건 A·B·C」로 바뀌었다 — 결과는 **A✅ B❌ C✅**

`GAME_DESIGN.md` 6.2절 재개정. 구간 밴드를 버렸다 — 매끄러운 곡선에 계단
목표를 씌우면 경계에서 반드시 어긋나기 때문이다.

- **A** 상한 단조 감소 · 낙차 ≤ 20%p → 최대 **16.0%p** ✅ (측정 21의 16.7보다 낫다)
- **B** 12지역 상한 25~45% → **13.3%** ❌ (−11.7%p. 시드 2는 19.0%)
- **C** 도입 하한 70%+ / 5~10지역 간격 ≥ 30%p → 최소 간격 **37.0%p** ✅

**두 시드가 같은 답을 냈다.**

### 🔴 층수는 관문은 열었는데 완주율은 못 올렸다

| 12지역 (상한 · 보통 · utility) | 측정 21 | **측정 22** |
|---|---|---|
| 상점 층 도달률 | 39.0% | **54.0%** (+15.0%p) |
| 상점 층 진입 시 남은 체력 | 60.5% | **57.0%** (−3.5%p) |
| 완주율 | 11.0% | **13.3%** (+2.3%p) |
| 한 판 평균 턴 | 27.43 | **25.69** (−1.7) |

**층수는 두 방향으로 동시에 작용한다** — 관문의 수를 줄이는 동시에
**층마다 붙은 회복(전투 승리 12% · 상점 30%)의 횟수도 줄인다.**
12지역은 최대 체력이 86이라 회복이 가장 잘 드는 지역이고, 그래서 손해가
이득을 대부분 갉아먹었다. **층수는 이 지역에서 거의 다 쓴 손잡이다.**

### 검산 — 조건 2개를 인자로, 한 번의 실행에서

- **기준 조건(옛 층수)이 측정 21을 24칸 소수점까지 재현**했다 (0.0%p)
- **1~11지역은 두 조건이 완전히 같다** — 안 건드렸다는 증거
- **보스만 승률도 1~11지역이 측정 21과 같다.** 12지역만 51.7 → 47.3%로 보이는데,
  **층이 줄어 보조 측정이 물려받는 난수 흐름이 밀린 것**이다 (시드 2는 방향이
  반대로 +1.0%p). 「기준」 열이 51.7%를 되살리는 것이 그 증거다
- 200턴 안전장치 **0건** · 봇 정책 최대 격차 12.0%p (경보선 아래)
- 전투당 굴림 상한 **4.49** — 측정 21과 같다

### 이번에 드러난 것 (기획 창에 넘김 — 보고서 22의 6절)

1. **남은 것은 「12지역 일반 적을 넘는 힘」이지 관문의 수가 아니다.**
   `hp_scale` 3.50 → 3.1~3.2 하향을 권했다 (검토 21의 안 B)
2. 조건 A의 「단조 감소」를 문서가 정의해 주면 좋겠다 — 1→2지역의 +0.3%p 때문에
   글자 그대로는 매번 미달이 된다. 지금 도구는 **±1.0%p**를 흔들림으로 본다
   (검토 21이 같은 자리를 「표본 흔들림」으로 판정한 것을 이었다)
3. 12지역은 표본이 작아 시드 간 흔들림이 크다 (13.3% ↔ 19.0%).
   B가 목표선 근처에 오면 **판 수를 300 → 1000으로** 올려야 한다
4. **각인 수치표는 확정하지 않았다** — 지시서가 「B가 통과하면」을 조건으로 걸었다

### 지시서와 다른 점 하나

지시서 1-A는 7층에서 「주문 보상 2개」라고 적었으나 **실제로는 1개**다
(`floorEvent`를 실제로 돌려 확인: 7층 ⚔⚔★◈⚔♥⚠ · 8층 ⚔⚔★◈⚔★♥⚠).
**12지역 Ⅰ 스테이지만 주문 보상이 2 → 1로 준다.** 코드는 안 고쳤다.

### 상태

- `flutter test`: **265개 전부 통과** (측정 21의 262 + 신규 3)
- `flutter analyze`: 경고 0
- 두 시드 정상 종료 · 같은 명령을 두 번 돌려 **출력 해시가 같음** 확인
- 측정 21 원본 `reports/data/measure9_output.txt`는 **덮어쓰지 않고 보존**했다

---

## 측정 23 — 12지역 체력 배율 3.50 → 3.35 + 곡선 조건 최종 판정 ✅ (2026-09-04)

지시서 `WORK_ORDER_FINAL_TUNE2.md`. 보고서 `reports/23_배율조정_최종측정.md`,
원본 출력 `reports/data/measure11_output.txt` (3285줄).

### ★ 결론 — **A✅ B1✅ B2✅ C✅** (두 시드 모두)

**곡선 작업이 닫혔다.** 12지역 상한이 13.3% → **18.4%**(시드 20260831),
19.0% → **17.7%**(시드 20260902)로 **B2 밴드(15~25%) 한가운데** 들어왔다.

| 조건 | 요구 | 측정 22 | **측정 23** |
|---|---|---|---|
| A | 상한 단조 감소 · 낙차 ≤ 20%p (흔들림 ±1.0%p) | 16.0%p ✅ | **16.0 / 14.3%p** ✅ |
| B1 | 11지역 상한 25~45% | 27.7% ✅ | **27.7 / 26.0%** ✅ |
| **B2** | **12지역 상한 15~25%** | 13.3% ❌ | **18.4 / 17.7%** ✅ |
| C | 도입 하한 70%+ / 5~10지역 간격 ≥ 30%p | ✅ | **37.0 / 36.7%p** ✅ |

**B가 B1·B2로 쪼개진 것**은 검토 22의 판단이다 — 11지역이 이미 밴드 바닥이라
12지역까지 25% 위로 올리라는 것은 조건 A(단조 감소)와 양립할 수 없었다.

### 무엇을 바꿨나 (한 칸)

`assets/data/regions.json` 12지역 `hp_scale` **3.50 → 3.35**.
11지역 3.20보다 위라 단조 증가는 그대로고, 증가폭이 **+0.30 → +0.15**로 줄었다
(+0.30은 12개 지역 통틀어 가장 큰 계단이었다).
보고서 22가 권한 3.1~3.2는 **11지역이 이미 3.20이라 실행 불가능**했다.

### 배율은 층수와 달리 표적에 맞았다

| 12지역 (상한 · 보통 · utility · 시드1) | 측정 21 | 측정 22 (층수) | **측정 23 (배율)** |
|---|---|---|---|
| 상점 층 도달률 | 39.0% | 54.0% | **52.5%** |
| 상점 층 진입 시 남은 체력 | 60.5% | 57.0% | **61.6%** |
| 완주율 | 11.0% | 13.3% | **18.4%** |
| 한 판 평균 턴 | 27.43 | 25.69 | **25.03** |

층수는 **도달률을 15%p 열면서 남은 체력을 3.5%p 깎아** 완주율이 +2.3%p에
그쳤다. 배율은 **도달률(+1.5%p)과 남은 체력(+2.4%p)을 같은 방향으로 올려**
같은 폭을 얻었다. **관문의 수가 아니라 관문 하나를 넘을 힘이 문제였다.**

### ★ 12지역만 판 수를 1000으로 올렸다 (지역별 판 수, 인자로)

| 12지역 상한 | 시드 20260831 | 시드 20260902 | 차 |
|---|---|---|---|
| 측정 22 (300판) | 13.3% | 19.0% | **5.7%p** |
| **측정 23 (1000판)** | **18.4%** | **17.7%** | **0.7%p** |

검토 22의 6-3이 요구한 것이다. **이 통과는 표본 운이 아니다.**
1~11지역은 300판 그대로여서 기준 조건이 측정 22를 그대로 재현한다.

### 검산 — 조건 2개를 인자로, 한 번의 실행에서

- **기준 조건(옛 배율 3.50)이 측정 22를 1~11지역 22칸 0.0%p로 재현**했다
- **1~11지역은 두 조건이 완전히 같다** — 안 건드렸다는 증거
- **보스만 승률도 1~11지역 12칸이 ±0.0%p** (지역 배율은 보스에 안 붙는다).
  12지역만 47.3 → 44.9%로 보이는데 **판 수가 늘어 난수 흐름이 밀린 것**이다
  — 기준 열(옛 배율)도 42.4%가 나오는 것이 그 증거다
- 200턴 안전장치 **0건** · 봇 정책 최대 격차 12.0%p (경보선 아래)

### ⚠ 36조합 합산 지표는 저울이 바뀌었다

12지역만 판 수가 늘었고 **12지역은 전투가 가장 긴 지역**이라, 「전투당 굴림」
같은 합산 값이 4.49 → 4.84로 올라 보인다. **게임은 하나도 안 바뀌었다.**
출력 4-D에 **「12지역 뺀 줄」**(상한 utility 4.30)을 나란히 찍어 드러냈다.
어느 저울로 봐도 4.0~4.9 대역이라 **각인 확정 근거값 4.49는 그대로 유효하다.**

### 각인 수치표는 재계산하지 않았다

기획 창이 검토 22에서 실측 4.49굴림 기준으로 `ENGRAVINGS.md` 4절을 확정했다.
지시서대로 도구에서도 각인 기대 적립 절 두 개(4-D2 · 3-A2)를 **떼어냈다.**

### 기획 창에 넘긴 것 (보고서 23의 9절)

1. 🟢 곡선 작업이 닫혔음을 확인해 달라 — **조정 제안 없음**
2. 🟡 판 수를 지역별로 다르게 한 것을 앞으로도 유지할지 (권장: 유지)
3. 🟡 `ENEMIES.md` 6절 배율표의 12지역 칸이 아직 3.50이다 (문서 소유가 기획 창)

### 상태

- `flutter test`: **268개 전부 통과** (측정 22의 265 + 신규 3)
- `flutter analyze`: 경고 0
- 두 시드 정상 종료 · 같은 명령을 두 번 돌려 **출력 해시가 같음** 확인
- 측정 22 원본 `reports/data/measure10_output.txt`는 **덮어쓰지 않고 보존**했다
- `measure_goal_report.dart`가 300줄을 넘어 6-C·6-D를
  **`measure_final_report.dart`(신규)로 갈랐다**

---

## 화면 재설계 — 지역·스테이지·난이도 선택 · 지도 개편 · 파일 분리 ✅ (2026-09-04)

`WORK_ORDER_SCREENS.md` (지시서 G). **데이터에만 있던 지역 12 · 스테이지 100 ·
난이도 3을 유저가 고를 수 있게 만들었다.** 밸런스 수치·`assets/data/*.json` 은
한 값도 건드리지 않았다.

### 작업 1 — 진행 기록과 잠금 규칙

- `MetaState`에 **`clearedStageKeys`**(`Set<String>`) 추가. 키는
  `stageClearKey(지역, 스테이지, 난이도)` = `"1-8-normal"` 꼴.
  `toJson`/`fromJson` 왕복에 들어갔고, **옛 저장본에 칸이 없으면 빈 집합**으로 읽는다
- **`lib/core/progress.dart` 신규** — 잠금 판정을 한 곳에 모았다.
  Flutter 의존이 없어 단위 테스트에서 그대로 쓴다
- `MetaController.markStageCleared()` — **판을 끝까지 깼을 때만** 부른다.
  죽거나 나가면 기록하지 않는다

### 작업 2 — 화면 3개

| 화면 | 파일 |
|---|---|
| 지역 선택 (신규) | `screens/region_select_screen.dart` + `widgets/region_card.dart` |
| 스테이지 선택 (신규) | `screens/stage_select_screen.dart` + `widgets/stage_tile.dart` · `difficulty_tabs.dart` |
| 지도 (개편) | `screens/map_screen.dart` + `widgets/next_enemy_card.dart` · `run_summary.dart` |

- 흐름: **타이틀 → 지역 → 스테이지(+난이도 탭) → 지도 → 전투 → 결과**.
  결과 화면에 **「스테이지 선택으로」**를 붙여 반복 플레이가 짧아졌다
- **배경 원화가 아직 없다.** 지역 카드 색을 `lib/art/region_art.dart` **한 곳에서만**
  정하게 했다. 원화가 들어오면 그 파일만 고치면 된다
- 지도는 **아래에서 위로** 오르고 **칸 수가 층수(3~10)를 그대로 따른다**.
  층 아이콘 표(`floorEventLook`)는 **지도와 전투 노드 트랙이 같이 쓰도록** 한 곳에 뒀다

### 작업 3 — 300줄 넘는 파일 3개 분리 (동작 변경 없음)

| 원본 | 분리 전 | 분리 후 | 갈라 낸 파일 |
|---|---|---|---|
| `widgets/battle_stage.dart` | 533 | 259 | `battle_stage_hud.dart` 194 · `battle_popup.dart` 85 |
| `screens/battle_screen.dart` | 478 | 98 | `widgets/dice_tray.dart` 238 · `widgets/cast_controls.dart` 162 |
| `widgets/pixel_ui.dart` | 427 | 10 (모아 놓은 문) | `pixel_palette.dart` 52 · `pixel_panel.dart` 190 · `pixel_button.dart` 201 |

`pixel_ui.dart`는 **export만 하는 문**으로 남겨 쓰는 쪽 import를 하나도 안 고쳤다.
`art/sprites_items.dart`·`stage_backdrop.dart`는 지시대로 **건드리지 않았다**.

### 상태

- `flutter test`: **293개 전부 통과** (측정 23의 268 + 신규 25)
- `flutter analyze`: 경고 0
- **실제 실행 확인** — Windows 데스크톱으로 빌드해 띄우고 타이틀 → 지역 →
  스테이지 → 지도 → 전투까지 눌러서 확인했다. 480px 축소본 `reports/img/24_run/`

---

## 화면 잔손질 3건 (`WORK_ORDER_SCREENS2.md`, 2026-09-05)

검토 24가 남긴 **눈으로 봐야 보이는 것 세 가지**를 고쳤다.
보고서: `reports/25_화면_잔손질.md` · 그림: `reports/img/25_run/`

### 작업 1 — 잠긴 지역 카드

- **검은 막을 덮는 방식을 버렸다.** 테마마다 바탕 밝기가 78~103으로 벌어져
  있어서, 같은 농도를 덮어도 **밝은 사막이 열린 숲보다 밝게 남았다**
- `lib/art/region_art.dart` 에 **`kLockedRegionLook`(공통 회색조)** 를 두고
  `regionLook(region, locked: true)` 가 그것을 준다.
  → **잠긴 12장이 전부 같은 색(밝기 31.0)**, 열린 카드(84.7)가 2.7배 밝다
- 배경 원화가 오면 **그 자리에 그림의 회색조 판**을 깔면 된다 (구조 그대로)

### 작업 2 — 짧은 스테이지의 빈 화면

- **남는 세로 공간 ÷ 칸 수**로 칸 높이를 정한다. 상·하한은
  **`lib/core/layout.dart`** 에 상수로 뒀다 (`constants.dart` 가 `export` 하므로
  쓰는 쪽 import 는 그대로다 — `constants.dart` 가 300줄을 넘어 갈라 냈다)
- 400×860 기기 기준 실측

  | | 개정 전 | 개정 후 |
  |---|---|---|
  | 지도 3층 | 칸 39~46 · 경로 124 / 557 | **칸 120 · 경로 360 / 557** |
  | 지도 10층 | 칸 39~46 · 경로 397 / 557 | **칸 55.7 · 경로 557 / 557** |
  | 스테이지 격자 | 칸 120×96 · 위로 몰림 | **칸 120×120 · 세로 가운데** |

- **칸만 키우면 키 큰 빈 막대가 된다.** 그래서 지도 칸의 적 그림이 칸 높이를
  따라간다 (26 → 최대 72px). **칸이 하한일 때는 26px 그대로**라 10층은 안 바뀐다
- 스테이지 칸의 상한은 **정사각형**이다. 「하한의 1.8배」로 했더니 세로로
  길쭉해져 글자만 가운데 뜬 빈 칸이 됐다 (찍어 보고 되돌렸다)

### 작업 3 — 결과 화면 문구

- `'탑을 정복했다!'` → **`'스테이지 클리어!'`**. 실패 문구는 그대로
- **아직 남은 v1 잔재 3건**(「영구 강화」 안내 · 타이틀의 같은 버튼 ·
  「도달 층수」)은 **고치지 않고 보고서 7-1절에 적었다** — 문구는 기획 창 몫이다

### 상태

- `flutter test`: **298개 전부 통과** (검토 24의 293 + 신규 5).
  **기존 테스트는 한 줄도 안 고쳤다**
- `flutter analyze`: 경고 0
- 300줄 넘는 `lib` 파일은 **검토 24가 남기라고 한 2개뿐**
- **실제 실행 확인** — Windows 데스크톱으로 빌드해 띄우고 지역 선택까지 확인
  (`reports/img/25_run/0_live_region.png`)

## 레벨 · 경험치 · 서클 슬롯표 (`WORK_ORDER_LEVEL.md`, 2026-09-05)

**성장의 뼈대를 세웠다.** 마법서·덱(지시서 I)이 얹힐 자리다.
자세한 것은 `reports/26_레벨_경험치.md`.

### 작업 1 — 경험치와 레벨

- **레벨 L → L+1 = 60 × L**, 최대 99, 1→99 누적 **291,060** (`GROWTH.md` 1.2절)
- 지역별 경험치를 **`regions.json` 의 `exp` 칸**에 뒀다 (25 → 455).
  **다른 칸은 한 값도 안 건드렸다** — 12지역 전수 대조 테스트로 잠갔다
- 상수·계산은 **`lib/core/level.dart`** 에 갈라 냈다 (`constants.dart` 가
  `export` 하므로 쓰는 쪽 import 는 그대로. `constants.dart` 는 **299줄 유지**)
- `MetaState` 에 **`level`·`exp`·`passivePoints`**. 옛 저장본에 칸이 없어도
  기본값으로 읽는다 (클리어 기록과 같은 방식)
- **전투를 이긴 그 자리에서 즉시 주고 즉시 저장한다.** 판 종료가 아니다 —
  **그 판이 뒤에 실패해도 경험치는 남아야 재도전이 파밍이 된다**
  (`RunController.grantExp` → `MetaController.gainExp` → `_persist`)
- 변종 ×1.5를 재려면 「이 적이 어느 변종인가」가 필요해 `Enemy.variantId` 를 더했다.
  **JSON에서 읽지 않고 `buildEnemy` 가 조립할 때만 채운다**
- `kDifficultyScales.expMul` 이 1.6 / 2.5 였는데 `GROWTH.md` 1.3절 확정값
  **1.5 / 2.0** 으로 고쳤다. 아무 데서도 안 쓰이던 임시값이라 밸런스 영향 없음

### 작업 2 — 서클 슬롯표

- **`assets/data/circle_slots.json`** 9행 (레벨 1·10·20·30·45·60·75·90·99)
- 조회는 `GameData.circleSlotsAt(level)` — **표에 없는 레벨은 바로 위 칸**
  (레벨 44 = 레벨 30 행)
- 레벨 90 행이 **19장 · 8서클 1장** — 곡선 측정 덱과 정확히 같다 (`GROWTH.md` 3.1절).
  **이 표를 고치면 곡선 조건 A·B1·B2·C의 근거가 무너진다**

### 작업 3 — 화면

- 결과 화면에 **「경험치 +N」 + 「레벨 업! ×n → Lv.N」** 한 줄. 막대·연출은 없다
- `ScreenHeader` 의 **비워 뒀던 오른쪽 자리에 `Lv.n`** 을 넣었다.
  지역·스테이지 선택에서 **「권장 Lv 1~8」 옆에 「내 레벨」이 서서 뜻이 생긴다**
- 결과 화면에서 경험치는 **「영구 강화」 안내 문구 아래**에 뒀다.
  위에 놓으니 그 문구가 경험치에 붙은 말처럼 읽혔다 (문구 자체는 안 고쳤다)

### 작업 4 — 검산

- **★ `GROWTH.md` 1.4절 표를 코드로 재현해 12지역 전부 ±0으로 일치**
  (`test/level_test.dart`)
- 이때 **「각 스테이지의 마지막 층이 보스」로 계산해야** 표와 맞는다.
  `stage_runner.pickEnemy` 가 실제로 그렇게 세운다. 「지역의 보스 스테이지만
  보스」로 계산하면 12지역이 **84레벨**로 15레벨 모자란다

### 상태

- `flutter test`: **313개 전부 통과** (기존 298 + 신규 15).
  기존 테스트는 **생성자 호출만** 고쳤고 단언은 한 줄도 안 고쳤다
- `flutter analyze`: 경고 0
- **실제 실행 확인 — 전투를 이겨 레벨이 오르는 것까지 손으로 확인했다.**
  1지역 Ⅰ 3층을 전부 클리어해 **경험치 +175 → Lv.2** (`reports/img/26_run/0_live_result.png`).
  175 = 25 + 25 + 25×5(보스) 로 손계산과 일치한다
