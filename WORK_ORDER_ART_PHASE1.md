# 작업 지시서 — 그래픽 Phase 1 (Runtime Proof)

발행: 2026-09-05, 기획 창. 근거: **`GAME_ART_ROADMAP.md` 13절(Phase 1)** ·
`docs/art/RUNTIME_ASSET_SPEC.md` · `docs/art/ART_STYLE_GUIDE.md` · `UI_DESIGN.md` 1-3·3-2절

> ## ⚠ 2026-09-05 — **완료됐으나 그림은 반려됐다** (검토 27)
> **코드는 승인.** `assets/art/` 등록 · 원화 표시 · fallback · 연출 재사용은 **살아 있다.**
> **화면은 반려.** 단 **원화 화풍은 반려가 아니다** — 사용자 말은
> 「원화 퀄에 비해 나머지가 다 떨어진다」였다. **화풍은 이후 에셋의 기준이다.**
> 원인은 **칸 밀도 7.7배 차이**(코드 48칸 vs 원화 308칸)이고,
> **적·배경이 코드 도트인 한 어떤 원화도 안 맞는다.**
> → **다음은 지팡이가 아니라 `Phase 1.5`(마법사+적1+배경1)다.** `HANDOFF.md` 7-3절.
> **이 지시서로 다시 작업하지 마라.**

> ## 한 줄 목표
> **외부 원화 마법사 뒷모습 1장을 실제 전투 화면에 세운다.**
> 코드 도트(`kSpriteWizardBack`) 대신 원화가 보이고, 기존 연출이 그대로 돈다.

**이 지시서는 마법사 몸 1장만 다룬다.** 지팡이·모자·로브·적·배경은 **Phase 2 이후**다.
**손대지 마라.**

---

## 0. 먼저 읽을 것

1. `GAME_ART_ROADMAP.md` — 특히 **2·3·7·13절**
2. `docs/art/RUNTIME_ASSET_SPEC.md` — 원본/게임용 분리, 정규화
3. `docs/art/ART_STYLE_GUIDE.md` — **금지 목록을 반드시 볼 것**
4. `UI_DESIGN.md` — **머리말의 「뒤집힌 3곳」 표를 먼저 보고** 1-3절(캔버스 배치)·3-2절(후처리)

> ### 🔴 이 셋은 **더 이상 하지 않는다** (2026-09-05 개정)
> **팔레트 근사 · 포스터라이즈 · 코드가 외곽선 붙이기.**
> 옛 문서(`WORK_ORDER_ART_PIPELINE.md`)의 **96×96 격자 · 강제 양자화 ·
> 알파 이진화**도 전부 폐기됐다. **그 문서를 읽지 마라.**
>
> **실제로 사고를 냈다** — 「색 24~32색」 규격이 24색 양자화를 불러
> 지팡이 볼륨을 소멸시켰다 (`CLAUDE.md` LESSONS 2026-09-04).

---

## 1. 원본 — 이미 저장소에 있다

| | |
|---|---|
| 파일 | **`art_raw/wizard/_turn_back.png`** |
| 크기 | **316 × 618** |
| 알파≥16 실루엣 | **313 × 616** (x 0~312, y 0~615) — **여백이 거의 없다** |
| 알파 분포 | a=0 31.8% · 250~255 53.8% · 중간 계조 6.5% → **정상** |
| 고유색 | 약 49,800 |

**기획 창이 눈으로 확인했다 — 뒷모습이 맞다.**

> ### ⚠ `wizard_body.png`는 **정면**이다 (2026-09-05 확인)
> 지팡이 검수(`ASSET_LIST_EQUIP.md` 2절 · `art_raw/README.md`)가 기준으로 쓰는
> **`wizard_body.png`(316×599)는 정면**이고, 전투 화면에 서는 것은 **뒷모습**이다.
> 게다가 **신장이 599 vs 618로 3% 다르다.**
> **Phase 1에서는 상관없다** (몸만 세운다). **Phase 2에서 지팡이 배율·앵커를
> 뒷모습 기준으로 다시 재야 한다.** 지금 재지 마라.

---

## 2. 작업

### 2-A. 게임용 에셋 굽기

**`art_raw/wizard/_turn_back.png` → `assets/art/wizard/body_back.webp`**

**정규화 규격** (`UI_DESIGN.md` 1-3절):

| 항목 | 값 |
|---|---|
| 캔버스 | **512 × 512** |
| 발바닥 | **y = 470** |
| 좌우 중심 | **x = 256** |
| **몸 높이** | **425 px** ← 2-A-1 참조 |

**절차는 이 넷뿐이다** (`UI_DESIGN.md` 3-2절):

1. 알파 경계로 크롭 (문턱값 **알파 ≥ 16**)
2. 위 규격으로 512×512에 배치 — 축소는 **LANCZOS 등 면적 기반**.
   `NEAREST`로 줄이면 얇은 선이 사라진다
3. **알파 250 이상 → 255로 스냅**
4. WebP로 굽기

**⛔ 하지 마라**: 팔레트 근사 · 포스터라이즈 · 색 수 제한 · 외곽선 추가 ·
**알파 이진화(0/255)**. 반투명·glow를 그대로 남긴다.

#### 2-A-1. 🔴 몸 높이를 **430(최대)이 아니라 425**로 하는 이유

캔버스 규격만 보면 `470 − 40(머리 위 여백) = 430`까지 쓸 수 있다.
**그런데 Phase 2에서 지팡이가 안 들어간다.**

```
지팡이 길이 = 몸 높이 × 1.10   (UI_DESIGN 2-2절, 발 기준 정렬)
몸 430 → 지팡이 473 → 윗끝 y = 470 − 473 = −3   ⛔ 캔버스 밖
몸 425 → 지팡이 468 → 윗끝 y = 470 − 468 =  2   ✅ 겨우 들어간다
```

**지금 430으로 구우면 Phase 2에서 몸을 다시 구워야 한다.** 425로 한다.

참고 배율: `425 ÷ 616 ≈ 0.690`.

### 2-B. Flutter 등록

`pubspec.yaml`의 `assets:`에 **`- assets/art/`** 를 더한다.

> ⚠ 바로 위 주석 **「그림은 lib/art/ 의 픽셀아트 스프라이트로 코드에서 직접 그린다」**
> 가 이제 사실이 아니다. **함께 고쳐라.**

### 2-C. 전투 화면 교체

**`lib/widgets/battle_stage.dart:209`** 의 `PixelSpriteView(kSpriteWizardBack, …)` 자리.

| 반드시 지킬 것 | |
|---|---|
| **기존 Transform 연출을 그대로 재사용한다** | 숨쉬기 · 시전 기울기 · 피격 flash · squash · knockback · ambient (로드맵 7절) |
| **`PixelSprite`를 지우지 마라** | 원화를 못 읽으면 **기존 도트로 떨어진다**(fallback). 로드맵 13절 · `docs/art/RUNTIME_ASSET_SPEC.md` |
| **`wizardSize`가 정하던 화면 크기를 유지한다** | 원화가 갑자기 커지거나 작아지면 비교가 안 된다 |
| **`tint`·`halo`** | 원화에는 **청록 색조를 입히지 않는다.** 원화가 이미 제 색을 갖고 있다. 그림자는 유지 |

**이번에 만들지 않는 것**: 별도 idle/walk/cast **프레임 애니메이션** (Phase 8),
포즈 추가, 앵커, 스켈레톤 (로드맵 22절).

### 2-D. 비교 시트

`test_shots/`에 **가로 480px 한 장**을 만든다 (`reports/img/`에 저장).

| 칸 | 무엇 |
|---|---|
| 1 | **기존** 전투 화면 (코드 도트) |
| 2 | **신규** 전투 화면 (원화) |
| 3 | 신규 — **피격 순간** (flash·squash가 원화에도 먹는지) |

**실제 기기 비율 400×860으로 찍어라** (`HANDOFF.md` 6절 함정 3).

---

## 3. 검산 조건

동작으로 건다. **문자열 규칙을 걸지 마라.**

1. `assets/art/wizard/body_back.webp` 가 **512×512**이고,
   **알파≥16 실루엣의 아랫끝 y = 470 ± 1**, **가로 중심 x = 256 ± 2**,
   **실루엣 높이 = 425 ± 2**
2. **알파값이 1~249 구간에 실제로 존재한다** (0%면 알파 이진화 사고)
3. **고유색이 1,000개를 넘는다** (24~64개면 강제 양자화 사고)
4. 원화 파일을 **못 읽는 상황에서도 전투 화면이 죽지 않고** 기존 스프라이트가 나온다
5. 전투 화면 위젯 테스트가 **기존과 같이 통과**한다

---

## 4. 기존 테스트 — 미리 조사해 뒀다

| 파일 | 무엇을 단언하나 | 이번에 |
|---|---|---|
| `test/sprite_test.dart` | `PixelSprite` 48×48·16×16 격자, 채워진 화소 수, 명암 단계 | **손대지 마라.** fallback으로 남으므로 그대로 통과해야 한다 |
| `test_shots/pilot_wizard_test.dart` | 테스트가 아니라 **보고서 그림 생성기**다 | 건드릴 필요 없다 |
| `test/widget_test.dart` | 타이틀→지역→스테이지→지도→전투 스모크 | **통과해야 한다.** 원화 로드가 테스트 환경에서 실패해도 fallback으로 살아야 한다 |

**기준선**: 지금 `flutter test` **313개 전부 통과**, `flutter analyze` **0건**.

---

## 5. 마무리

1. `flutter analyze` 0건 · `flutter test` 전부 통과
2. **실제로 빌드해 띄우고 전투를 한 턴 돌려 눈으로 확인한다**
3. `PROGRESS.md`에 기록
4. **`reports/27_그래픽Phase1_원화마법사.md`** 작성 (양식 `reports/_TEMPLATE.md`)
   — 보고서 경로 한 줄만 말하고 내용을 대화에 붙여넣지 마라

> ### 사용자에게 물을 것은 **네 가지뿐이다** (로드맵 10절)
> ① 같은 게임처럼 보이나 ② 장비가 자연스럽게 붙나(이번엔 해당 없음)
> ③ 얼굴·몸을 가리나(이번엔 해당 없음) ④ 마음에 드나
>
> **규격·알파·파일·코드·테스트 검증은 전부 구현 창이 맡는다.**
> 🔴 **마법사 몸은 「캐릭터」 카테고리의 첫 기준 에셋이다.**
> **사용자 승인을 받아야 `approved`가 된다** (로드맵 23절).

## 6. 판단이 필요하면

**임의로 정하지 말고 보고서 「판단이 필요한 것」에 적어라.**
특히 **몸 높이 425**를 바꿔야 할 이유를 발견하면 **바꾸지 말고 보고하라** —
Phase 2의 지팡이가 여기에 걸려 있다.
