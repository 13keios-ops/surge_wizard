# 작업 지시서 — 걸어가는 연출 (배경 세로 스크롤)

> ⛔ **기제가 틀렸다 (2026-09-12). `WORK_ORDER_ROADSIDE.md` 가 걷어낸다.**
> 「그림 전체를 위아래로 민다」는 원근과 양립할 수 없다 (`GAME_DESIGN.md` 6.7절 ①).
> **구현은 끝나 테스트도 통과하지만 멀쩡한 것이 아니다.**

발행: 2026-09-11, 기획 창.
근거: `GAME_DESIGN.md` **6.7절 ①** (사용자 요청 원문) · `ART_REQUEST_BG_V4.md` ·
`docs/GPT_PROJECT_INSTRUCTIONS.md` 5절.

> ### 🪟 창 운영
> **구현 창. `/clear` 하고 시작한다.** 이미지를 거의 안 보는 작업이다.

---

## 0. 한 줄

**층을 하나 올라갈 때마다 배경이 아래로 흘러 캐릭터가 앞으로 걸어간 것처럼 보이게 한다.**

사용자 요구 원문 (6.7절):

> 「한 스테이지가 끝날 때마다 캐릭터가 걸어가고 **배경도 같이 움직여서**
> 걸어가는 느낌을 주고 있어서 반영되었으면 좋겠고」

---

## 1. 지금 어떻게 돼 있나 (읽고 시작할 것)

| 무엇 | 어디 |
|---|---|
| 배경을 까는 위젯 | `lib/art/art_sprite.dart` **`ArtBackdropView`** — `BoxFit.cover` + `Alignment.bottomCenter` **고정** |
| 그것을 쓰는 곳 | `lib/widgets/battle_stage.dart` **201행** `Positioned.fill` |
| 층 정보 | `BattleStage`가 이미 `floor`·`floors`를 받고 있다 (32~35행) |
| 층이 오르는 자리 | `lib/screens/map_screen.dart:121` `run.advanceFloor()` |
| 화면 흐름 | 지도 → 전투(push) → 보상 → `advanceFloor()` → 지도 |

🔴 **전투 화면은 층마다 새로 열린다.** 그러니 **전투 화면이 열릴 때 한 번**
이전 층 위치에서 지금 층 위치로 밀면 된다. **네비게이션을 건드리지 마라.**

---

## 2. 할 일

### 2-1. 배경 위치를 층에 따라 정한다

`ArtBackdropView`에 **`progress` (0.0~1.0)** 를 받는다.

```
alignment: Alignment(0, 1 - 2 * progress)
```

- `progress = 0` → `Alignment.bottomCenter` (그림의 **아래**, 지역 출발점)
- `progress = 1` → `Alignment.topCenter` (그림의 **위**, 목표 건물)

**progress 계산**: `floors <= 1` 이면 **0**, 아니면 `(floor - 1) / (floors - 1)`.
🔴 **0으로 나누는 것을 막아라.** 층이 1인 스테이지가 실제로 있다.

### 2-2. 전투 화면이 열릴 때 한 번 민다

`BattleStage`가 뜰 때 **이전 층 위치 → 지금 층 위치**로 애니메이션한다.

- 한 번만 재생한다. 전투 중에는 배경이 움직이지 않는다
- **1층은 밀지 않는다** (이전 위치가 없다)
- 길이는 `core/constants.dart`에 상수로 둔다. **매직 넘버 금지** —
  권장 **900ms · `Curves.easeInOut`**
- 미는 동안 **입력을 막을 필요는 없다.** 주사위를 굴려도 된다

### 2-3. 마법사 걷기 프레임 (에셋이 있을 때만)

`assets/art/wizard/back_walk_1.webp` … `_4.webp` 가 **있으면** 미는 동안 프레임을
순환시키고, **없으면 지금 정지 그림을 그대로 쓴다.**

🔴 **에셋은 아직 없다.** 없을 때 정상 동작하는 것이 이번 납품 조건이고,
있을 때의 경로는 **한 줄짜리 분기**로 준비만 해 둔다. 프레임을 기다리지 마라.

### 2-4. 배경 원화가 없는 지역

`kArtRegionBackdrops`에 없는 지역은 코드 배경(`StageBackdrop`)을 쓴다.
**그쪽은 밀지 않는다.** 지금 그대로 둔다.

---

## 3. 🔴 파일 길이 — 먼저 읽어라

`lib/widgets/battle_stage.dart`가 **이미 299줄**이다. 규칙은 300줄이다.
**여기에 애니메이션 코드를 넣으면 그 자리에서 넘는다.**

→ **슬라이드 로직은 `lib/art/art_sprite.dart` 쪽에 두거나 새 파일로 뺀다.**
`BattleStage`는 `progress`만 넘기는 한 줄이 되게 한다.

---

## 4. 테스트 (필수)

`test/` 에 추가한다. **UI 없이 도는 것부터.**

| 무엇 | 기대 |
|---|---|
| progress 계산 | floor 1 → 0.0 · 마지막 층 → 1.0 · 중간 층 → 그 사이 |
| 🔴 `floors == 1` | **0.0** (0으로 나누지 않는다) |
| Alignment 변환 | progress 0 → y = 1 · progress 1 → y = -1 |
| 위젯 | 층이 다르면 `ArtBackdropView`의 alignment가 다르다 |

`flutter test` **전부 통과**해야 한다 (지금 317개). `flutter analyze` 경고 0.

---

## 5. 보고서

🔴 **작업을 시작할 때** `reports/35_걸어가는연출.md`를 먼저 만든다.
양식은 `reports/_TEMPLATE.md`. **한 덩어리 끝날 때마다 한 줄씩 덧붙인다.**
끊기면 「다음: ~」 한 줄을 남긴다.

사용자에게는 **"보고서 reports/35_걸어가는연출.md 작성 완료" 한 줄만** 말한다.

---

## 6. 하지 않을 것

- 네비게이션 구조 변경 (지도 화면을 없애거나 전투를 한 화면에 합치는 것)
- 배경을 무한 반복(loop)시키는 것 — **한 지역을 한 번만 지나간다**
- 적·보스가 같이 움직이게 하는 것 — **배경만 민다**
- 밸런스 수치·데이터 파일
