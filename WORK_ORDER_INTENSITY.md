# 작업 지시서 E — 시전 강도 2단계 개정 (구현 창용)

발행: 2026-09-02, 기획 창. **사용자 승인 완료된 작업이다** (`GAME_DESIGN.md` v2 3.3절).

근거 문서: **`GAME_DESIGN.md` 3.3절**
> ⚠ `DICE_DESIGN.md`와 `WORK_ORDER_BALANCE.md`는 **v1 기준이라 낡았다. 보지 마라.**
> 그 문서들은 「약하게」를 전제로 쓰였다.

> ### 🪟 창 운영
> **새 창(`/clear` 후)에서 시작한다.** 코드 여러 파일을 함께 고치는 작업이다.
> 화면 확인은 **전투 화면 스크린샷 1장(가로 480px)** 이면 충분하다.

---

## 0. 왜 지금 이걸 하나 — 시뮬레이션을 막고 있다

`GAME_DESIGN.md` v2 3.3절은 **강도를 2단계로 줄이고 「보통」을 마나 0으로 내렸다.**
그런데 **코드에는 아직 「약하게」가 그대로 있고 「보통」이 마나 1이다.**

> 이 상태로 시뮬레이션을 돌리면 **v1 게임을 측정하게 된다.**
> 봇이 마나가 없을 때마다 「약하게」를 쓰기 때문에 굴림 횟수·턴 수·등급 분포가
> 전부 우리가 만들 게임과 다른 값이 나온다.

**이 작업이 끝나야 시뮬레이션(지시서 F) 숫자가 의미를 갖는다.**

## 0-1. 개정 내용 (v2 3.3절)

| 강도 | DC | 위력 배율 | 마나 | 대성공 |
|---|---|---|---|---|
| **보통** | 10 | 1.0배 | **0** ← 1에서 내림 | 2배 + 마나 1 환급 |
| **전력** | 13 | 3.0배 | 2 | 2배 + 마나 2 환급 |

**「약하게」는 삭제한다.** 마나 0에 대성공률이 높아 지배 전략이 되는 구멍이었다.
이제 **마나는 「위험과 재시도를 사는 돈」 하나의 뜻만 갖는다** (전력 시전 + 리롤).

## 0-2. 절대 지킬 것

- ⚠ **`test/check_test.dart`를 고치지 마라.** 3.4절 확률표를 검증하는 테스트다.
  이 테스트가 `kDcWeak`(=7)를 **DC 값으로** 쓰고 있으므로 **상수 `kDcWeak`는 남긴다**
  (아래 1-A). 「약하게」는 사라져도 **DC 7에서의 확률 검증은 계속 필요하다**
  (`CLAUDE.md` 테스트 규칙)
- **`core/check.dart`·`core/combo.dart`도 고치지 마라.** 판정 자체는 안 바뀐다
- 매직 넘버 금지. 파일 300줄 / 함수 40줄 규칙 유지
- `flutter analyze` 경고 0, `flutter test` 전부 통과 상태로 끝낼 것

---

# 작업 1 — 코어

## 1-A. `lib/core/constants.dart`

| 상수 | 조치 |
|---|---|
| `kManaCostNormal` | **1 → 0** ★ 이 작업의 핵심 |
| `kPowerWeak` (0.5) | **삭제** |
| `kManaCostWeak` (0) | **삭제** |
| `kCritManaRefundWeak` (0) | **삭제** |
| `kDcWeak` (7) | ⚠ **남긴다.** 주석을 고쳐라 — 「약하게」는 삭제됐지만 **DC 7은 3.4절 확률표 검증(`check_test`)에 계속 쓰인다」** |

## 1-B. `lib/core/cast_intensity.dart`

```dart
enum CastIntensity { normal, full }
```

- `dc` / `power` / `manaCost` / `critManaRefund` / `label`의 **const 리스트에서
  첫 칸(weak)을 빼라.** enum 순서에 인덱스로 의존하는 구조이므로 **한 칸만 어긋나도
  전력이 보통 값을 쓰게 된다.** 테스트로 반드시 잡을 것 (작업 3-A 1번)
- **`canCrit`을 삭제한다.** 강도가 둘 다 대성공이 가능해져 항상 true다

## 1-C. `lib/core/battle.dart` — 강등 로직 제거

`canCrit`이 없어지면 **「약하게 대성공 → 성공 강등」 경로가 통째로 죽는다.**
죽은 채로 두면 다음 사람이 "대성공이 강등될 수 있다"고 오해한다.

- **`effectiveGrade()` 메서드를 삭제**하고, `applyGrade`가 `result.grade`를 직접 쓴다
- ⚠ **마력 축적 소모 조건도 함께 고쳐라.** 지금은 이렇다:

  ```dart
  final consumed = result.chargeConsumed && intensity.canCrit;
  ```

  → **`final consumed = result.chargeConsumed;`**
  (게이지 3의 확정 대성공이 강등돼 날아가는 것을 막던 예외였다. 강등이 없으면 불필요)
- `lastAppliedGrade`는 **남긴다.** UI·통계가 쓰고 있고, 이제 항상 `result.grade`와 같다

## 1-D. `lib/core/relic_powers.dart`

`checkBonusWeak` 필드와 `'check_bonus_weak'` case를 **삭제**한다.

---

# 작업 2 — 데이터·화면·도구

## 2-A. ✅ `assets/data/relics.json` — 유물 2종 재배치 (기획 창 결정)

`check_bonus_weak`를 쓰는 유물이 **2종** 있다. 그대로 두면 **효과가 없는 유물**이 된다.

| 유물 | 조치 |
|---|---|
| `warm_pebble` | `check_bonus_weak` → **`check_bonus_full`** |
| `balance_bracelet` | `check_bonus_weak` → **`check_bonus_full`** |

- **`value`는 그대로 둔다.** 타입만 바꾼다
- `description` 문구도 함께 고칠 것 (「약하게」가 들어 있으면 「전력」으로)
- **왜 `check_bonus_full`인가**: 「약하게」가 사라지면 남은 둘 중 **위험한 쪽을 돕는 것**이
  v2 설계(마나 = 위험을 사는 돈)와 맞는다. `check_bonus`(전체 +N)로 바꾸면
  이미 5종이나 있는 효과와 겹치고 유물이 지나치게 세진다

## 2-B. `lib/screens/battle_screen.dart`

- 시전 강도 **버튼 3개 → 2개**
- **「약하게」는 대성공이 되지 않는다」 안내문(226~231행 부근)을 삭제**한다.
  이제 사실이 아니다
- 버튼이 둘로 줄어 남는 자리는 **넓게 쓰라.** 손가락으로 누르는 화면이다

## 2-C. `lib/screens/battle_controller.dart`

206행 부근에 마나가 없을 때 `CastIntensity.weak`로 떨어지는 갈래가 있다.
**「보통」이 마나 0이므로 이제 떨어질 곳은 「보통」이다.**

## 2-D. 주석 정리

`lib/widgets/result_banner.dart` · `lib/widgets/battle_fx.dart` ·
`lib/art/sprite_map.dart`(`'check_bonus_weak'` case)에 「약하게」 흔적이 남아 있다.
**전부 정리한다.** `grep -rn "약하게\|Weak\|weak" lib/ tool/` 로 확인할 것.

## 2-E. ★ `tool/sim_core.dart` — 봇 정책 다시 쓰기

**가장 신중해야 할 곳이다.** 시뮬레이션 결과가 여기에 달려 있다.

`chooseIntensity`가 「약하게」를 전제로 5갈래였다. **2단계용으로 다시 쓴다:**

```
1) 마력 축적이 만땅이면 → 전력 (확정 대성공을 크게 쓴다)
2) 전력으로 이번 턴에 적을 끝낼 수 있고 마나가 되면 → 전력
3) 마나가 2 이상이고 다음 적 공격을 버틸 수 있으면 → 전력
4) 그 밖에는 → 보통 (마나 0이므로 언제나 가능하다)
```

- ⚠ **「마나가 없어서 못 쓴다」는 경우가 사라졌다.** 보통은 항상 낼 수 있다
- `rollWithRerolls`의 `intensityBonus`에서 `weak` 갈래를 뺀다
- ⚠ **리롤 예산 계산을 다시 볼 것.** 지금은 `battle.mana - intensity.manaCost < cost`로
  판단하는데, **보통이 마나 0이 되면서 리롤에 쓸 수 있는 마나가 늘었다.**
  로직은 그대로 맞지만 **행동이 크게 달라진다.** 보고서에 리롤 횟수 변화를 적을 것
- `tool/mc_report.dart`의 `kDcWeak`는 **DC 값으로 쓰는 것이니 그대로 둔다**

---

# 작업 3 — 테스트

## 3-A. `test/intensity_test.dart` (신규)

| # | 검사 |
|---|---|
| 1 | ★ **enum 인덱스 정합성** — `normal`의 DC 10·위력 1.0·마나 **0**·환급 1, `full`의 DC 13·위력 3.0·마나 2·환급 2. **한 칸 밀림을 잡는 검사다** |
| 2 | `CastIntensity.values.length == 2` |
| 3 | **마나 0에서도 「보통」 시전이 성공한다** (예전엔 마나 1이 필요했다) |
| 4 | 마나 1에서 「전력」 시전은 **거부된다** (`castSpell`이 false) |
| 5 | 「보통」 대성공이 **강등되지 않는다** — 위력 2배가 그대로 들어간다 |
| 6 | 마력 축적 3에서 시전하면 **강도와 무관하게** 게이지가 소모된다 |

## 3-B. ⚠ 기존 테스트 — 미리 조사했다

**기획 창이 `test/` 전체를 훑었다.** 아래가 전부다.

### 3-B-1. `test/battle_test.dart` — **삭제·수정 허가**

| 위치 | 조치 |
|---|---|
| 36행 `battle.mana >= 1 ? normal : weak` | **항상 `normal`로** 고친다 |
| 307·315·324·350행 부근 — **「약하게」 강등을 검증하는 테스트들** | **삭제한다.** 없어진 기능을 검사하는 테스트다 |
| 379·402행 `selectIntensity(CastIntensity.weak)` | **`normal`로** 바꾼다. 402행은 「시전 비용 0으로 리롤만 측정」이 목적인데 **이제 보통이 마나 0이라 의도가 그대로 성립한다** |

### 3-B-2. `test/loader_test.dart` 180행 — **수정 허가**

허용 유물 효과 목록에서 **`'check_bonus_weak'`를 뺀다.**

### 3-B-3. `test/check_test.dart` — **절대 고치지 마라**

`kDcWeak`를 **DC 값으로** 쓴다. 상수를 남기므로 그대로 통과한다.
여기가 깨지면 **판정 엔진을 건드린 것이다. 되돌려라.**

### 3-B-4. 그 밖의 기존 테스트를 고치지 마라

깨지면 구현이 틀린 것이다. 특히 **enum 인덱스 밀림**을 먼저 의심하라.

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. `grep -rn "약하게" lib/ tool/` 결과가 **0줄**인지 확인
3. **`dart run tool/simulate.dart`를 한 번 돌려 본다.**
   ⚠ **수치를 해석하거나 밸런스를 조정하지 마라.** 그건 지시서 F의 일이다.
   **죽지 않고 돌아가는지만** 확인하고, 출력을 보고서에 그대로 붙인다
4. **화면 확인** — 전투 화면 스크린샷 1장(가로 480px). 버튼이 2개이고 배치가 어색하지 않은지
5. **`reports/11_강도2단계.md`를 새로 만든다** (양식 `reports/_TEMPLATE.md`)
   - **작업 3-A 1번(enum 정합성) 결과를 표로** 넣을 것
   - `simulate.dart` 출력 전문을 붙일 것 (해석은 하지 말 것)
   - 봇 정책을 어떻게 다시 썼는지 **의사코드로** 적을 것
   - 근거 문서 오류를 발견하면 **고치지 말고 적어라**
6. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만** 말한다

## 하지 않을 것

- ❌ **시뮬레이션 수치 해석·밸런스 조정** (지시서 F)
- ❌ 확정 굴림(Confirm Roll) 구현 — v2 3.2절에 있지만 **아직 지시하지 않았다**
- ❌ 모델·로더·진행 구조 (지시서 D)
- ❌ `check.dart`·`combo.dart`·`check_test.dart` 수정
- ❌ 3.4절 확률표에 손대기
