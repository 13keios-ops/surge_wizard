# 작업 지시서 M — 회복 수정 + 재측정

발행: 2026-09-02, 기획 창. **작업 1·2는 사용자 승인 완료된 수치 변경이다.**

근거 문서: **`SPELL_CIRCLES.md`** (0절 이름 개정 · 4절 강화 규칙) · 검토 18

> ### 🪟 창 운영
> **새 창(`/clear` 후).** **스크린샷이 필요 없다.**

---

## 0. 목적 (한 줄)

**측정 18이 「보스는 괜찮고, 층을 지나며 깎이는 체력이 문제」라고 결론 냈다.
그 원인 둘을 고치고 다시 잰다.**

## 0-1. 무엇이 문제였나 (검토 18)

| # | 문제 | 고칠 것 |
|---|---|---|
| 1 | **저서클 유틸의 절대값이 그대로**라 후반에 무의미해진다.<br>마력방패 방어막 **6**인데 12지역 적 공격은 **12**다 | 강화가 **`heal`·`shield`도** 올리게 |
| 2 | 상점 회복 **고정 10**은 v1의 체력 20 기준(50%)이다.<br>지금 체력 86에서는 **12%** | **최대 체력의 30%** 로 |

**한 전투가 최대 체력의 20%(1지역) → 75%(12지역)를 먹는다.**
1지역에서 방어막 6이 적 공격 3.4를 완전히 막던 관계가 후반에 무너진 것이 핵심이다.

## 0-2. 절대 지킬 것

- **작업 1·2·3 외에 수치를 바꾸지 마라.** 완주율이 낮아도 그대로 적어라
- **적 데이터 · 지역 배율 · 난이도 배율 · 층수 · 성장 프로필을 건드리지 마라**
- `core/check.dart` · `combo.dart` · `surge.dart`를 열지 마라
- 매직 넘버 금지 · 파일 300줄 · 함수 40줄 · 주석 한국어
- `flutter analyze` 경고 0, `flutter test` 전부 통과

---

# 작업 1 — ✅ 강화가 `heal`·`shield`도 올린다

`tool/measure_growth.dart`의 강화 적용을 고친다.
배수는 **위력에 쓰는 것과 같다** (서클별 만강 ×2.00 ~ ×1.20).

| 효과 | 강화 | 왜 |
|---|---|---|
| `base_damage` | ✅ 올린다 (지금과 같음) | |
| **`heal`** | ✅ **올린다** | 치유 약초 5 → 10 |
| **`shield`** | ✅ **올린다** | 마력방패 6 → **12** (12지역 적 공격 12를 한 턴 완전 차단) |
| `check_bonus` | ❌ **올리지 마라** | **3.4절 확률표에 직접 개입한다** |
| `extra_die` | ❌ 올리지 마라 | 같은 이유 |
| `delay_enemy` | ❌ 올리지 마라 | 적 지연 누적 상한 2턴(3.2절)과 충돌한다 |
| `mana_restore` | ❌ 올리지 마라 | 최대 마나가 3~5뿐이라 의미가 없다 |

- `dc_modifier`도 그대로다
- 반올림은 위력과 같은 방식
- **`lib/`에 강화 시스템을 만들지 마라.** `tool/`에만 있는 측정용 가정이다

---

# 작업 2 — ✅ 상점 회복을 비율로

## 2-A. 상수

`lib/core/constants.dart`

```
kShopHeal = 10  (삭제)
→ kShopHealRatio = 0.30   // 최대 체력의 30%
```

회복량 = `(최대 체력 × 0.30).round()`

## 2-B. ⚠ 닿는 곳 — 기획 창이 전수 조사했다

| 파일 | 조치 |
|---|---|
| `lib/screens/run_controller.dart` 209행 | `state.hp + kShopHeal` → 비율 계산 |
| `lib/widgets/shop_dialog.dart` 58행 | **「체력 +10」이 하드코딩돼 있다.** 실제 회복량을 계산해 보여줄 것 |
| `tool/sim_core.dart` 113행 | 비율 계산 |
| `tool/measure_stage_run.dart` 147행 | 비율 계산 |
| **`test/run_test.dart` 243~246행** | `expect(run.state.hp, 5 + kShopHeal)` — **고쳐도 된다.** 비율로 바꾸되 **검사 의도(상점이 회복시킨다·최대치를 안 넘는다)는 유지**할 것 |

**그 밖이 깨지면 구현이 틀린 것이다.**

---

# 작업 3 — 주문 3종 개명 (어감)

`assets/data/spells.json`. **`name`과 `description`만 고치고 수치는 건드리지 마라.**

| id | 원래 | **바꿀 이름** | `description` |
|---|---|---|---|
| `magma_spit` | 용암 침 | **용암 덩이** | 「끈적한 용암 덩이를 던진다.」 |
| `snow_bolt` | 눈덩이 사출 | **눈덩이 투척** | 지금 문구(「단단히 뭉쳐 던진다.」) **그대로 둔다** |
| `sap_surge` | 수액 분출 | **수액 채취** | 「달콤한 수액을 거둬 힘으로 바꾼다.」 |

> **왜**: 한글로만 보이면 「침」이 타액으로, 「사출」·「수액 분출」이 다른 뜻으로 읽힌다
> (`SPELL_CIRCLES.md` 0절). **`id`는 바꾸지 마라** — 코드·테스트가 id로 찾는다.

---

# 작업 4 — 재측정 (측정 18과 같은 조건)

- 지역 12 × 난이도 3 = **36조합 × 300판**, 시드 **2개**
- **강화 하한/상한 × 봇 greedy/utility = 4조합** (측정 18과 동일)
- 성장 프로필(체력·마나·덱 구성)은 **그대로**
- 출력 **`reports/data/measure7_output.txt`**

## 내보낼 표 — 측정 18과 나란히

### 4-A. ★ 완주율

| 지역 | 측정 18 하한 | 측정 18 상한 | **이번 하한** | **이번 상한** |

**상한 열에서 7~12지역이 얼마나 올라오는지**가 이번 수정의 성패다.

### 4-B. ★ 전투당 피해가 체력의 몇 %인가 — 원인을 직접 본다

검토 18이 쓴 표를 **측정값으로** 낸다.

| 지역 | 최대 체력 | 일반 전투 평균 턴 | **전투당 받은 피해** | **체력 대비 %** |

- 측정 18은 1지역 20% → 12지역 **75%** 였다
- **후반이 40% 아래로 내려오는지**가 확인 지점이다
- 「받은 피해」는 **방어막에 막힌 것을 뺀 실제 체력 감소**로 센다

### 4-C. 방어막·회복이 실제로 얼마나 막고 회복했나

| 지역 | 방어막으로 막은 총량 | 회복한 총량 | 시전 유틸 비율 |

**작업 1이 실제로 들었는지**를 여기서 본다.

### 4-D. 전투당 굴림 + 각인 기대 적립 · 턴 수 · 200턴 안전장치

측정 12~18과 한 표에. 안전장치는 **0이어야 한다.**

---

# 작업 5 — 테스트

| # | 검사 |
|---|---|
| 1 | 강화가 `heal`·`shield`를 **위력과 같은 배수**로 올린다 (마력방패 6 → 12, 치유 약초 5 → 10) |
| 2 | 강화가 `check_bonus`·`extra_die`·`delay_enemy`·`mana_restore`·`dc_modifier`를 **바꾸지 않는다** (주문 70종 전수) |
| 3 | 상점 회복이 **최대 체력의 30%**(반올림)이고 최대치를 넘지 않는다 |
| 4 | 개명 3종의 `name`이 새 이름이고 **`id`·수치는 그대로**다 |

---

# 마무리

1. `flutter analyze` 경고 0 / `flutter test` 전부 통과
2. 두 시드 정상 종료, `reports/data/measure7_output.txt` 생성
3. `BALANCE.md`에 **「11차 — 회복 수정」** 절을 맨 위에 추가
4. **`reports/19_회복수정_재측정.md`** 작성
   - **4-A와 4-B를 맨 앞에.** 「후반이 되는가」를 한 줄로 먼저 답할 것
   - **판단·조정 제안은 「판단이 필요한 것」 절에만**
5. 사용자에게는 **「보고서 <경로> 작성 완료」 한 줄만**

## 하지 않을 것

- ❌ **적 수치 · 지역 배율 · 난이도 배율 · 층수 · 성장 프로필 조정**
- ❌ 강화율(+10%/단계) 변경 — **위력 쪽은 3~6지역에서 이미 맞았다**
- ❌ `check_bonus`·`extra_die`·`delay_enemy` 강화
- ❌ `lib/`에 마법서·강화 시스템 구현
- ❌ 화면 · 각인·층수 표 확정
