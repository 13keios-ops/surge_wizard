# 작업 지시서 — 원화 v3 검수·굽기 (그래픽 창)

> ⛔ **끝난 지시서다** (검토 34 — 원화 4장 굽기·보스 등록 완료). 지금 상태는 `docs/HANDOFF_ART.md`.

발행: 2026-09-08, 기획 창. 대상: `art_raw/` 에 2026-09-08 들어온 **4장**.

> ### 🪟 창 운영
> **그래픽 창 (`/clear` 후 새로).** 이미지가 쌓이는 작업이라 오래 끌지 않는다.
> 읽을 것: **`docs/HANDOFF_ART.md` 머리말** → `ART_REQUEST_V3.md` 「검수 합격선」
> → `art_raw/README.md` 「검수 절차」 → `docs/LESSONS.md` **B묶음**.

---

## 0. 들어온 것

| 파일 | 받은 캔버스 | 요청한 캔버스 |
|---|---|---|
| `art_raw/bg/bg_forest.png` | 841 × 1870 | 1024 × 2276 |
| `art_raw/wizard/wizard_back.png` | 1254 × 1254 | 512 × 512 |
| `art_raw/enemy/enemy_goblin_scout.png` | 1254 × 1254 | 512 × 512 |
| `art_raw/boss/boss_elder_slime.png` | 1254 × 1254 | 768 × 768 |

**캔버스 크기가 어긋난 것 자체는 문제가 아니다** — 후처리가 정규화한다.
**비율은 맞다** (배경 0.4497 vs 목표 0.4499).

## 1. 🔴 기획 창이 미리 재 본 결과 — **두 장이 미달일 가능성이 높다**

정식 검수 전에 런 길이만 거칠게 재 봤다. **이 숫자를 그대로 믿지 말고 네가 다시 재라.**
(색 허용오차 28을 줘서 안티에일리어싱을 흡수한 뒤 최빈 런을 잡은 값이다)

| | 몸 높이 | 블록 ≈ | **키(칸)** | 요청 | **화면 한 칸** | 판정 |
|---|---|---|---|---|---|---|
| 마법사 | 1016 | 18px | **≈56칸** | 46칸 | **≈1.61dp** | 🔴 미달 |
| 일반 적 | 805 | 17~18px | **≈46칸** | 31칸 | **≈1.33dp** | 🔴 미달 |
| 보스 | 731 | 9~10px | **≈77칸** | 69칸 | **≈1.89dp** | ⚠ 경계 |

**합격 범위는 1.9 ~ 2.1dp** (`UI_DESIGN.md` 1-2-b, 2026-09-08 신설).
**1.61dp는 그래픽 Phase 1이 반려된 바로 그 값이다.**

### 🔴 그리고 더 걱정되는 것 — **격자가 무르다**

허용오차 **0**으로 재면 세 장 다 **1px 런이 82~96%**다.
**Phase 1.5에 통과한 원화는 같은 조건에서 7px 런이 100%였다** (`reports/28_*` 1절).

**즉 블록 경계가 딱 떨어지지 않고 안티에일리어싱·그라디언트로 뭉개져 있다.**
칸 수만 맞춰도 이게 남으면 화면에서 「하나만 매끈한」 현상이 다시 난다.

## 2. 할 일

### 작업 1 — 정식 검수 (숫자 먼저, 그림은 나중)

`ART_REQUEST_V3.md` 맨 아래 **「검수 합격선」 표**대로 8항목을 잰다.
`art_raw/README.md` 「검수 절차」의 재는 법을 쓴다.

🔴 **위 1절과 다른 값이 나오면 네 값이 맞다.** 내 것은 거친 사전 점검이다.

### 작업 2 — 판정

| 결과 | 다음 |
|---|---|
| **합격** | 작업 3으로 |
| **미달** | **여기서 멈춘다.** 3절대로 재요청문을 만들어 사용자에게 준다. **굽지 마라** |

**부분 합격(예: 보스만 통과)이면 통과한 것만 굽고 나머지는 재요청**한다.

### 작업 3 — 굽기 (합격한 것만)

후처리는 **모양을 안 건드리는 넷뿐**이다 (`art_raw/README.md`):
**배경 제거 → 알파 경계 크롭 후 정규화 → 알파 250↑ 255 스냅 → WebP.**
❌ 팔레트 근사·포스터라이즈·외곽선 추가·알파 이진화 **금지.**

| 파일 | 굽는 곳 | 정규화 |
|---|---|---|
| `wizard_back.png` | `assets/art/wizard/body_back.webp` (덮어쓰기) | 512 × 512 · 발바닥 y=477 · 중심 x=256 |
| `enemy_goblin_scout.png` | `assets/art/enemy/goblin_scout.webp` (덮어쓰기) | 위와 같음 |
| `boss_elder_slime.png` | `assets/art/boss/elder_slime.webp` (**신규**) | **768 × 768 · 발바닥 y=716 · 중심 x=384** |
| `bg_forest.png` | `assets/art/bg/forest.webp` (덮어쓰기) | **1024 × 2276.** 위아래를 자르지 말고 **전체를 그대로** |

### 작업 4 — 보스를 코드에 등록한다 (**새 카테고리다**)

지금 `lib/art/art_sprite.dart` 에는 **보스 경로가 없다.** 일반 적과 같은 맵을 쓰면
보스도 `kEnemySize`(76dp)로 그려진다 — **`kBossSize`(180dp)로 가야 한다.**

- `battle_stage.dart` 는 이미 `isBoss ? kBossSize : kEnemySize` 로 갈라 놓았다.
  **그림 경로만 보스용으로 찾아가게** 하면 된다
- 보스는 캔버스가 **768**이라 발바닥 비율이 같은지(716/768 = 477/512) 확인할 것.
  같으므로 `kArtFootLine` 은 **그대로 쓴다**

### 작업 5 — 확인·검증

- `flutter analyze` 경고 0건 · `flutter test` 전부 통과 (지금 **316개**)
- `test_shots/ui6a_test.dart` 로 **360×640 한 장**을 다시 찍어
  **적이 길 위에 서 있는지** 눈으로 확인 (지금 배경엔 길이 없어 허공에 떴었다)
- 🔴 **480px 축소본만 본다**

## 3. 미달일 때 재요청문에 넣을 것

`ART_REQUEST_V3.md` 를 **고치지 말고**, 아래를 **덧붙이는 짧은 추가 문단**으로 만든다.

- **몇 칸으로 나왔고 몇 칸이어야 하는지**를 그림마다 숫자로 (「56칸으로 나왔습니다. 46칸이어야 합니다」)
- 🔴 **「블록 안쪽을 단색으로 칠해 주세요. 블록 경계에 흐림(안티에일리어싱)을 넣지 마세요」**
  — 이번에 가장 크게 어긋난 것이다
- **「이미 그린 그림을 축소하지 말고, 그 칸 수에 맞춰 다시 그려 주세요」**
  (`ART_REQUEST_PHASE1_5.md` 에서 효과가 있었던 문장이다)
- 잘 나온 장이 있으면 **그 장을 첨부해 「이 굵기로」** 라고 한다

## 4. 하지 마라

- ❌ **미달인 것을 「일단 구워 보고」 하지 마라.** 그 순간 화면 판단이 오염된다
- ❌ 기획 문서(`GAME_DESIGN.md` `UI_DESIGN.md` `HANDOFF*.md`)를 고치지 마라
- ❌ 배경 나머지 7종·적 25종을 뽑지 마라 — **이 4장이 승인돼야 batch로 간다** (로드맵 23절)
- ❌ 전투 화면 레이아웃을 손보지 마라 — **6-A는 끝났다.** 수정 3건은 **구현 창** 몫이다

## 5. 보고서

🔴 **시작할 때 `reports/32_원화v3_검수.md` 를 먼저 만든다** (양식 `reports/_TEMPLATE.md`).
**한 덩어리 끝날 때마다 한 줄씩** 덧붙인다.
사용자에게는 **「보고서 `reports/32_원화v3_검수.md` 작성 완료」 한 줄만** 말한다.

🔴 **끝나고 요약·판정 절을 다시 써라.** 중간 저장 문장이 그대로 남으면
다음 창이 정반대로 읽는다 (`docs/LESSONS.md` C묶음, 검토 31).
