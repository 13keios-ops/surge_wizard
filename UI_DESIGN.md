# UI·아트 설계 v2 — 원화 기반

> 2026-09-02 기획 창 작성. **사용자 결정: 「원화를 그대로 크게 쓴다」**
> `ART_PIPELINE.md`·`GRAPHICS_PLAN.md`(v1, 픽셀아트 전제)를 **대체한다.**
> 화면을 만드는 모든 지시서는 이 문서를 근거로 한다.

> ## 🔴 2026-09-05 — `GAME_ART_ROADMAP.md`가 위에 얹혔다
> **판단 우선순위**: `GAME_DESIGN.md` > **이 문서** > `ASSET_LIST*.md` >
> **`GAME_ART_ROADMAP.md` + `docs/art/`** > `art_raw/README.md`.
>
> 다만 **로드맵이 이 문서의 다음 세 곳을 명시적으로 뒤집었다.** 그 세 곳에서는
> **로드맵이 이긴다** — 각 자리에 🔴로 표시해 두었다.
>
> | 뒤집힌 것 | 이 문서 | 지금 기준 |
> |---|---|---|
> | **후처리 팔레트 근사·포스터라이즈** | 3-2절 1·2번 | **하지 않는다** (`docs/art/ART_STYLE_GUIDE.md`) |
> | **후처리 외곽선 추가** | 3-2절 3번 | 코드가 붙이지 않고 **원화가 갖고 나온다** (colored outline) |
> | **몸 1장 고정** | 2-1절 | **view/pose 3종** (`docs/art/EQUIPMENT_LAYER_SPEC.md`) |
>
> ### 🔴 2026-09-05 추가 — **논리 칸 수(화면 한 칸 = 2.0px)**
> 규격표에 **없던 항목을 신설했다** (1-2-b절). **그래픽 Phase 1 반려의 원인**이므로
> **원화를 발주하기 전에 반드시 읽어라.**

---

## 0. 무엇이 바뀌었나

| | v1 (폐기) | **v2 (이 문서)** |
|---|---|---|
| 캐릭터·적 | 코드로 찍은 도트 | **GPT 원화 PNG를 그대로 크게** |
| 배경 | 코드로 그린 무대 | **원화 배경 8종** (2026-09-05 4테마→8종) |
| UI(패널·버튼·글꼴) | 픽셀 | **픽셀 유지** |
| 이펙트 | 코드 | **원화 + 코드 혼합** |

> ⚠ **가장 큰 위험은 「일러스트 + 픽셀 UI」의 톤 불일치다.**
> 이 문서 3절의 후처리 규칙이 그것을 막는 장치다. **그 규칙을 건너뛰면
> 화면이 두 개의 다른 게임처럼 보인다.**

---

## 1. 기준 격자 — 모든 그림이 여기에 맞춰진다

### 1-1. 논리 해상도

- **기준 논리 폭 360dp** (세로 고정). 높이는 640~900dp 가변
- 실기기 대부분이 3배 밀도(1080px 폭)이므로 **자산은 3배 기준으로 만든다**
- 화면은 **`Expanded` + `LayoutBuilder`** 로 짠다. 고정 높이 금지
  (`CLAUDE.md` LESSONS — 작은 기기에서 넘친다)

### 1-2. 자산 규격표 ★ 이 표를 벗어나지 마라

> ## 🔴 2026-09-08 — **6-A(검토 31)가 이 표의 크기를 전부 확정했다**
> 「약 120~150dp」 같은 **범위가 없어졌다.** 아래 값이 `lib/core/layout.dart` 의
> 상수와 **1:1로 같다.** 근거는 `reports/31_UI재설계_6A.md` 4-8·4-9절
> (참고 게임 화면을 행마다 실측해 옮긴 값) + `reports/31_UI재설계_6A_REVIEW.md`.

> ## 🔴 2026-09-09 — **캔버스는 이제 「고정 규격」이 아니다** (검토 34)
> 후처리에 **격자화**가 들어오면서 (3-2절 2-b) **캔버스는 「목표 칸 수 × 한 칸 px」로
> 그때그때 정해진다.** 아래는 **지금 구워져 있는 값**이지 지켜야 할 규격이 아니다.
> 🔴 **생성기에 요구하는 것은 「가로 1024px 이상 · 최대한 세밀하게」뿐이다.**

| 자산 | 캔버스(지금) | 화면 표시 크기 | 배경 | 형식 |
|---|---|---|---|---|
| **일반 적** | 1024 × 1024 | **76dp** (`kEnemySize`) | 투명 | WebP |
| **보스** | **880 × 880** (= 110칸 × 8px) | **180dp** (`kBossSize`) | 투명 | WebP |
| **마법사 (레이어당)** | 1024 × 1024 | **112dp** (`kWizardSize`) | 투명 | WebP |
| **배경(테마)** | 🔴 **1024 × 2276 (9:20)** | **화면 전체 채움** (아래 정렬) | 불투명 | WebP |
| **아이템·주문 아이콘** | **256 × 256** | 32~64dp | 투명 | WebP |
| **이펙트 낱장** | **512 × 512** | 가변 | 투명 | WebP |

> ### 🔴 배경이 「무대 상자」가 아니라 **화면 전체**를 덮게 됐다
> v3 전투 화면은 배경 위에 UI가 얹히는 구조다 (`GAME_DESIGN.md` 6.7절).
> 옛 2:3 규격으로 뽑으면 `BoxFit.cover` 에서 **좌우가 잘려 길 가장자리가 날아간다.**
> **가장 긴 폰(20:9) 기준으로 뽑고 `bottomCenter` 로 붙이면** 16:9~20:9 어디서도
> 좌우가 안 잘리고 짧은 폰에서 **위쪽 숲만** 잘린다.
> **좌우 10%는 안전 여백** — 중요한 것을 두지 마라.

### 1-2-b. 🔴 논리 칸 수 — **화면 한 칸 = 1.55 ~ 1.75 dp** (2026-09-09 개정, 검토 34)

> ⛔ **옛 제목은 「= 2.0 px」이었다.** 2026-09-05에 정한 값이고, **2026-09-09에
> 사용자 요청으로 내려왔다.** 아래 상자들 중 **날짜가 가장 최근인 것이 이긴다.**

> **위 규격표에는 캔버스 크기만 있고 「얼마나 굵은 픽셀로 그리나」가 없었다.**
> **그 빈칸이 그래픽 Phase 1 반려의 원인이다** — 캔버스만 맞추면 밀도는 제각각이 되고,
> 마법사 원화만 화면에서 한 칸 **0.383 dp**(308칸)로 뭉개져
> 코드 도트(2.96 dp, 48칸)와 **7.7배** 어긋났다
> (`reports/27_그래픽Phase1_REVIEW.md`).

**모든 원화는 화면에서 「한 칸 = 1.55 ~ 1.75 dp」로 읽히게 굽는다.**
**그리고 한 화면에 같이 서는 것들끼리 ±10% 안이어야 한다** (아래).

> ## 🔴🔴 2026-09-08 — **재는 대상은 「상자」가 아니라 「몸」이다** (검토 31 5절)
> 이 표가 오래 두 갈래로 읽혀 왔다. **코드가 그리는 dp는 512 캔버스 「상자」 전체**이고,
> **몸은 그 상자의 413/512 = 80.7%**다 (실측: `reports/28_에셋검수_Phase1_5.md`).
>
> **그래서 「상자 dp ÷ 2.0」으로 칸 수를 내면 반드시 1.61dp가 나온다** —
> **그래픽 Phase 1이 반려된 바로 그 값이다.**
>
> 🔴 **칸 수 = 상자dp × 0.807 ÷ 2.0.** 아래 표는 그렇게 다시 낸 값이다.

> ## 🔴🔴 2026-09-09 — **밴드가 1.9~2.1 → 1.55~1.75dp 로 내려왔다** (검토 34)
> 사용자가 **「캐릭터가 흐릿하다. 밀도를 올려 달라」**고 두 번 요청했고,
> **받은 원화가 담고 있는 한계까지** 올렸다.
>
> ### 왜 2.0이 정답이 아니었나 — **기준으로 삼았던 것이 사라졌다**
> 2.0dp는 **코드 도트(2.46~2.96dp)와 톤을 맞추려고** 정한 값이다 (아래 옛 상자).
> 그런데 **그 코드 도트가 원화로 하나씩 대체되고 있다.** 기준이 없어졌는데
> 숫자만 남아 있었다.
>
> ### 🔴 그리고 아무도 **배경**을 표에 안 넣었다
> 캐릭터를 2.0dp로 맞춰 놓고 「합격」이라 적었는데 **배경이 0.70dp라 3배 촘촘**했다.
> 규칙에 「배경은 대상 아님」이라 적혀 있어서 **이 어긋남을 아무도 못 봤고**,
> 사용자가 「흐릿하다」고 말해서야 드러났다.
> **→ 아래 표에 배경을 넣는다. 규칙 대상은 아니어도 「같이 재는 것」에는 넣는다.**

| 자산 | 캔버스(지금) | **상자(화면)** | **칸** | **캔버스 안 한 칸** | **화면 한 칸** |
|---|---|---|---|---|---|
| **마법사** | 1024 × 1024 | 112 dp | **55칸**(키) | 15 px | **1.641dp** ✅ |
| **일반 적** | 1024 × 1024 | 76 dp | **37칸**(키) | 22 px | **1.633dp** ✅ |
| **보스** | **880 × 880** | 180 dp | **110칸**(긴 변) | 8 px | **1.636dp** ✅ |
| **배경** | 1024 × 2276 | 화면 전체 | 512 × 1138칸 | 2 px | *0.70dp* (규칙 밖 — 기록만) |

> ## 🔴 진짜 불변식은 절대값이 아니라 **「서로 얼마나 다른가」**다
> Phase 1이 반려된 이유는 **0.383dp라서**가 아니라 **옆에 선 것과 7.7배 달라서**였다.
> **한 화면에 같이 서는 것들의 화면 한 칸은 서로 ±10% 안**이어야 한다.
> 지금 세 장은 1.633 ~ 1.641로 **0.5% 안**이다.
>
> **밴드(1.55~1.75dp)는 「지금 어디쯤인가」를 적어 둔 것이고, ±10%가 규칙이다.**
> 밴드를 또 옮기려면 **세 장을 같은 비율로 함께** 옮긴다. 한 장만 올리면
> Phase 1이 반려된 그 현상이 그대로 재현된다.

> ### ⚠ 여기가 **지금 원화의 바닥**이다 (검토 34 12-5절)
> 격자화는 **「잔 것을 굵게」만** 된다. 그래서 화면 밀도의 하한은
> **받은 원화가 물리적으로 담고 있는 칸 수**다.
>
> | | 원화가 담은 칸 | 화면 한 칸의 **하한** |
> |---|---|---|
> | 마법사 | 56.5칸 | **1.60dp** ← 지금 1.641 이라 거의 바닥 |
> | 일반 적 | 39.9칸 | **1.50dp** |
> | 보스 | 격자 없음 (제한 없음) | — |
>
> **더 올리려면 원화를 더 잘게 받아야 한다** → 3-0절 「생성기에 요구할 것」.

> ### 🔴 보스만 **「긴 변」으로 잰다** (2026-09-08 확정, 검토 32)
> 보스 12종에는 **넓고 낮은 것**(슬라임·두꺼비)과 **세로로 솟은 것**(거인·기사·용)이
> 섞여 있다. **「키 N칸」으로 못 박으면 넓은 보스는 폭이 캔버스를 넘는다** —
> 실제로 태고의 슬라임이 **267px 넘쳤다.**
>
> | 보스 모양 | **110칸**으로 재는 변 | 화면에서 |
> |---|---|---|
> | 넓고 낮은 것 | **가로** | 폭 180dp · 높이는 그 결과 |
> | 세로로 솟은 것 | **세로** | 높이 180dp · 폭은 그 결과 |
>
> 캔버스가 정사각이고 코드가 그 상자를 `kBossSize`(180dp)로 통째로 그리므로,
> **「실루엣의 긴 변이 캔버스를 채운다」로 두면 모양이 달라도 한 칸이 항상 1.636dp**다.
> **숫자 하나로 12종이 다 덮인다.** (2026-09-09에 88 → **110칸**. 밴드가 내려왔다)
>
> ⚠ 화면에서 보스가 **약해 보이면 고칠 것은 모양이 아니라 `kBossSize`**다.
> 그때 칸 수도 같은 비율로 다시 낸다.

> ### 🔴 배경은 **확대해 써도 된다** (2026-09-08, 검토 32)
> 배경은 이 규칙의 대상이 아니고(한 칸 0.70dp), 화면 물리 해상도(1080px)보다 작게
> 들어와도 **1.3배 안쪽이면 확대해 쓴다.** 세로로 잘라 이어 붙이지 마라 —
> **이음매 관리 비용이 8종 × 앞으로 계속이다.**
> 🔴 **확대할 때는 배경만 `FilterQuality.medium`.** 배율이 정수가 아니면
> `none` 으로 두었을 때 **줄이 불규칙하게 굵어진다.** 캐릭터는 `none` 그대로.

> ### ⛔ 옛 허용 오차 **1.9 ~ 2.1 dp** (2026-09-08) — **2026-09-09에 1.55~1.75로 대체됨**
> 이 상자가 남아 있는 이유는 **「범위를 적어야 한다」는 교훈 자체는 살아 있기** 때문이다.
> 그전까지 이 문서는 **「2.0dp」라고만 적고 얼마까지 벗어나도 되는지를 안 적었다.**
> 그래서 1.94는 통과(검토 28), 1.61은 반려(검토 27)인데 **그 선이 어디인지
> 아무도 몰랐다.** **수치 규격에는 반드시 범위를 함께 적는다.**

> ### 🟡 **배경은 밴드의 「판정 대상」은 아니다 — 그러나 반드시 함께 잰다** (2026-09-09 개정)
> 512칸 배경을 360dp 폭에 깔면 **한 칸 0.70dp**다. 배경을 캐릭터와 같은 굵기로
> 그리면 **길이 뭉갠다.** 그래서 **밴드로 반려하지는 않는다.**
>
> 🔴 **그런데 2026-09-08까지 「대상이 아니다」를 「안 재도 된다」로 읽었다.**
> 그 결과 **배경이 캐릭터보다 3배 촘촘한 것을 아무도 못 봤고**, 사용자가
> 「캐릭터가 흐릿하다」고 말해서야 드러났다 (검토 34 12-1절).
> **→ 배경은 위 표에 항상 함께 적는다.** 판정은 안 해도 **눈에는 보이게 둔다.**

- **「논리 칸」은 그림이 실제로 쓰는 색 덩어리의 크기**다. 캔버스 해상도와 다르다
- **재는 법**: 가로로 같은 색이 이어지는 길이(런)의 최빈값.
  `_turn_back.png`은 런의 85%가 2px이었다 → 논리 해상도 **158 × 308칸**
- ⚠ **칸 수는 이미지를 줄여도 변하지 않는다.** 줄어드는 것은 한 칸의 화면 크기다
- **이 값을 안 지키면 화면이 또 따로 논다.** 캔버스 규격만 맞추는 것으로는 부족하다

> ### ⛔ 아래 상자(2026-09-05)는 **v3에서 뒤집혔다**
> **배경은 지금 화면 전체를 덮는다.** 1024×1536이 아니라 **세로로 훨씬 길다** —
> 구워진 것이 1024×2276이고 **새 요청은 세로:가로 3.3 이상**이다
> (`GAME_DESIGN.md` 6.7절 · `ART_REQUEST_BG_V4.md`). 아래는 배경이 무대 상자에만
> 깔리던 시절의 기록이다. **「아래 정렬로 자른다」만 아직 유효하다.**

> ### 🔴 2026-09-05 정정 — 배경은 **1080×1920(9:16)이 아니라 1024×1536(2:3)**이다
> 옛 규격은 **배경이 화면 전체를 채운다는 전제**로 쓰였다. 실제로 배경이 보이는 곳은
> **`BattleStage` 상자**이고, 그 상자는 **약 344 × 400~470 dp (세로비 0.73~0.86)**다.
> 9:16(0.5625)으로 뽑으면 **잘려 나가는 양이 더 늘어난다.**
>
> **그리고 무대에 깔 때 `Alignment.bottomCenter`로 자른다** —
> 가운데 정렬하면 **캐릭터가 설 앞쪽 지면이 잘려 나간다**
> (`reports/28_에셋검수_Phase1_5.md` 3-1절).

> **왜 2.0px인가** — UI가 이미 픽셀이다 (Galmuri11은 12px 기준 정수 배율 폰트,
> 패널·버튼도 픽셀). 3절이 경고한 **「일러스트 + 픽셀 UI」 톤 불일치**의 완충 장치
> (팔레트 근사·포스터라이즈)를 2026-09-05에 없앴으므로,
> **이제 톤을 맞출 유일한 수단이 「원화도 픽셀로 읽히게 하는 것」이다.**
> 2.0px이면 지금 코드 도트(2.46~2.96px)와 가까워 게임 인상도 안 바뀐다.

- **전부 WebP.** PNG로 두면 앱 용량이 두 배가 된다
- 예상 총량: 적 26 + **보스 12**(13번째 `boss_dice_devourer`는 지역 미배정 보류) + **배경 8** + 마법사 레이어 + 아이콘 → **12~18MB** 선
- **정사각 캔버스를 고집하는 이유**: 좌우 반전·확대·색조 처리를 코드가
  한 규칙으로 처리할 수 있다. 캔버스가 제각각이면 배치가 매번 손보정이 된다

### 1-3. 캐릭터 캔버스 안 배치 규약 (중요)

🔴 **2026-09-09 개정 — 픽셀이 아니라 「비율」로 적는다** (검토 34).
캔버스가 512·792·880·1024로 계속 바뀌었는데 **규약은 비율 하나면 충분하다.**

> ## **발바닥 = 캔버스 높이의 0.931 · 좌우 중심 = 가로의 0.5**
>
> | 캔버스 | 발바닥 y | 중심 x |
> |---|---|---|
> | 512 | 477 | 256 |
> | 880 (지금 보스) | 819 | 440 |
> | 1024 (지금 마법사·적) | 953 | 512 |
>
> 코드는 `kArtFootLine = 477 / 512 = 0.93164` **비율 상수 하나**를 쓰고,
> `ArtSpriteView` 가 캔버스를 상자에 통째로 맞춘다.
> **정사각이기만 하면 캔버스 크기는 코드에 아무 영향이 없다.**

> 🔴 **2026-09-08 정정 — 470이 아니라 477이다** (검토 31 6-1절).
> 이 문서만 **470**이었고, 실제로 납품된 원화 3장의 실측(`reports/28_에셋검수_Phase1_5.md`)과
> 코드(`lib/art/art_sprite.dart:17` `kArtFootLine = 477 / 512`)는 **477**이다.
> **이대로 뒀으면 재발주 요청서가 470으로 나가 새 에셋만 7px 어긋났다.**

```
   0                256               512
 0 ┌─────────────────┬─────────────────┐
   │                 │                 │
   │        (머리 위 여백 40 이상)      │
150│ ─ ─ ─ ─ 머리 중심선 ─ ─ ─ ─ ─ ─ ─ │  ← 모자 레이어 기준
   │                 │                 │
   │              캐릭터               │
477│ ━━━━━━━ 발바닥 기준선 ━━━━━━━━━━ │
512└─────────────────┴─────────────────┘
```

- **모든 캐릭터가 이 규약을 지키면** 코드가 위치를 계산하지 않아도 발이 바닥에 선다
- 크기 차이는 **캐릭터를 캔버스 안에서 크게/작게 그려** 표현한다.
  코드에서 배율을 다르게 주지 않는다 (그러면 픽셀 밀도가 제각각이 된다)

---

## 2. 마법사 4레이어 — 장비가 바뀌어도 안 어긋나게

`GAME_DESIGN.md` 5.5절: 외관에 반영되는 슬롯은 **모자 / 로브 / 지팡이** 3종.

### 2-1. 레이어 구성과 순서

| 순서 | 레이어 | 파일 | 비고 |
|---|---|---|---|
| 1 (뒤) | **로브** | `wizard/robe_<id>.webp` | 몸보다 뒤에 겹치는 부분 포함 |
| 2 | **몸** | `wizard/body.webp` | **1장 고정.** 장비와 무관 |
| 3 | **모자** | `wizard/hat_<id>.webp` | 머리 중심선 기준 |
| 4 (앞) | **지팡이** | `wizard/staff_<id>.webp` | 손 앞에 온다 |

- 레이어는 **같은 512×512 캔버스**에 그린다. 코드는 **그냥 겹쳐 그린다**

> ### 🔴 2026-09-05 개정 — 「몸 1장 고정」을 **view/pose 3종**으로 바꿨다
> 옛 문장은 「몸은 딱 1장이다」였다. 로드맵 4절이 이것을 다음으로 대체했다.
>
> | view/pose | 무엇 |
> |---|---|
> | `wizard_front_idle` | 정면 (지도·메뉴·결과 화면) |
> | `wizard_back_idle` | 뒷모습 (전투 무대 기본) |
> | **`wizard_back_staff_idle`** | 뒷모습 + **지팡이를 쥔 손·팔 자세**. ⚠ **지팡이 자체는 그리지 않는다** |
>
> **`wizard_back_staff_idle` 에 지팡이를 그려 넣으면 안 된다.** 지팡이는 독립
> 에셋으로 올라오므로, 몸에도 그려져 있으면 **두 자루가 겹친다.**
>
> **레이어는 `body → equipment` 로 시작한다.** 손 가림·앞뒤 교차·모자 충돌은
> **실제로 문제가 났을 때만** 쪼갠다 (`docs/art/EQUIPMENT_LAYER_SPEC.md`).
> 아래 2-1 표의 4레이어 순서는 **그 확장형**이고, 지금 당장 만들 구조가 아니다.

### 2-2. ⚠ GPT는 정합을 정확히 못 맞춘다 — 오프셋 데이터로 흡수한다

원화 생성기는 「같은 캔버스에 모자만」 같은 지시를 **정확히 지키지 못한다.**
그래서 코드가 보정할 여지를 데이터로 남긴다.

`assets/data/equip_anchors.json` (신규)

```json
{ "hat_apprentice": { "dx": 0, "dy": -4, "scale": 1.0 } }
```

- 원화를 넣고 **화면을 보며 세 값만 조정**하면 맞는다
- **이 파일이 없으면 장비를 넣을 때마다 코드를 고치게 된다.** 반드시 만든다

> ### 🔴 recipe json과 겹치지 않게 — 배치값은 여기 하나만 둔다 (2026-09-05)
> `docs/art/ASSET_RECIPE_SCHEMA.json`에도 `dx`·`dy`·`scale` 칸이 있다.
> **같은 뜻의 값이 두 곳에 있으면 반드시 어긋난다.**
>
> - **런타임이 읽는 것은 `assets/data/equip_anchors.json` 하나다** (절대 규칙 2)
> - **recipe json은 「어떻게 다시 뽑나」의 기록**이다 — `workflow`·`seed`·
>   `ipadapter`·`sha256`·`approved`. **배치 세 값은 recipe에 쓰지 않는다**
> - 값이 서로 다르면 **`equip_anchors.json`이 이긴다**

#### ✅ 실측된 기본값 (2026-09-03, 지팡이 9종으로 확인)

| 항목 | 값 |
|---|---|
| `wizard_body` 원본 크기 | **316 × 599** (투명 여백 제거 후) |
| 지팡이 원화 캔버스 | 941 × 1672 (시트 크기 그대로 옴) → **알파 경계로 잘라서 쓴다** |
| **지팡이 기본 배율** | **몸 높이의 1.10** (2026-09-04 개정 — 아래) |
| **지팡이 기본 x 위치** | 몸 좌표 **x = 76** (지팡이 가로 중심) |
| 지팡이 기본 y 위치 | **바닥(발) 기준으로 정렬** |
| 겹치는 순서 | 로브 → 몸 → 모자 → **지팡이(맨 앞)** |

- ⚠⚠ **2026-09-04 개정 — 실제 합성 시험으로 정했다.**
  v1 15종을 캐릭터에 붙여 본 결과, **헤드가 얼굴 높이(위에서 44~54%)까지
  내려온 것들이 얼굴을 가렸다.** 반대로 **지팡이를 신장보다 8% 길게** 하면
  헤드가 **머리 위 빈 공간으로 솟아** 폭 322px짜리도 아무것도 가리지 않았다.
  → **기본 배율 1.10.** 신규 원화는 길이를 신장의 108~112%로 그린다
  (`ASSET_LIST_EQUIP.md` 2-1절)
- **몸통 폭이 진짜 제약이다.** 길이의 22~24%짜리(v1 주사위·용뼈)는
  **몸통에 감긴 천이 캐릭터 몸을 통째로 덮었다.** 신규 원화는 42px 이내
- 옛 원화 중 **옹이·참나무·폭풍 3종은 배율 1.00~1.08로 지금도 쓸 수 있다.**
  나머지는 가림이 발생하므로 교체 대상
- 지팡이마다 미세 조정이 필요하면 이 값에서 `dx`·`dy`·`scale`만 흔든다
- ⚠ **원화의 알파 최대가 253~254다** (완전 불투명이 거의 없다).
  후처리에서 **알파 250 이상은 255로 스냅**한다 — 안 하면 배경이 미세하게 비친다

### 2-3. 슬롯 6종 중 3종은 그림이 없다

신발 · 귀걸이 · 수정구는 **외관에 반영하지 않는다** (5.5절). **아이콘만 있으면 된다.**

---

## 3. ★ 톤 불일치를 막는 규칙 — 이게 이 문서의 핵심

일러스트 캐릭터와 픽셀 UI를 한 화면에 놓으면 **따로 논다.**
**원화 쪽에 규칙을 걸어 UI 쪽으로 끌어온다.**

### 3-0. ★ 생성기에 무엇을 어떤 형식으로 요구하나

> ## 🔴🔴 2026-09-09 — **「칸 수」를 생성기에 요구하지 않는다** (검토 34 확정)
>
> 요청서에 **「N칸으로 그려 주세요」**를 세 번 적었고 **세 번 다 안 지켜졌다**
> (마법사 46칸 요청 → 56.4 → 56.5칸). **이 생성기는 격자를 세지 못한다.**
> 그리고 **지켜졌더라도 그 칸 수가 상한이 되어** 나중에 밀도를 못 올린다 —
> 실제로 2026-09-09에 그 벽에 부딪혔다 (마법사 원화가 56.5칸이라 1.60dp가 바닥).
>
> ### 그래서 이렇게 요구한다 (요청서에 이 문장 그대로)
>
> > **「격자를 만들려고 애쓰지 마세요. 부드럽게 그려도 됩니다.
> > 최대한 세밀하게만 그려 주세요. 칸은 저희가 맞춥니다.」**
>
> | 요구할 것 | 요구하지 말 것 |
> |---|---|
> | 구도 · 색 · 자세 · **발바닥 위치** | ❌ 칸 수 |
> | **가로 1024px 이상** (배경은 세로:가로 3.3 이상) | ❌ 「격자에 딱 맞게」 |
> | **목표 칸 수의 2배보다 잘게** (마법사 목표 60칸 → **120칸보다 잘게**) | ❌ 정확한 캔버스 크기 |
>
> **잔 것을 굵게 만드는 것은 후처리가 한다 (3-2절 2-b 격자화). 그 반대는 못 한다.**
> **잘게 받아 둘수록 나중에 어느 쪽으로든 갈 수 있다.**

> 🔴 **2026-09-05 — 생성기가 「GPT 채팅」에서 「ComfyUI」로 바뀐다** (로드맵 8절).
> **이 절의 요구 내용은 그대로 유효하다** (무손실 PNG · 투명 · 크게 받아 줄이기 ·
> 한 장에 하나 · 글자 금지). 바뀌는 것은 **전달 경로와 반복 방법**이다 —
> 스타일 유지를 **IPAdapter**가 맡고, 자세·길이가 반복해 흔들릴 때만 **ControlNet**을 쓴다.
> **아래에서 「GPT」는 「생성기」로 읽으면 된다.**

> **원본은 항상 무손실 PNG로 받는다. WebP는 후처리를 끝낸 뒤 도구가 굽는다.**
> 생성기에 WebP·JPG를 요구하면 **크롭·정규화 전에 이미 손실이 생긴다.**

#### 투명 배경이 필요한 것

| 자산 | 배경 |
|---|---|
| 캐릭터 · 적 · 보스 · 아이템 아이콘 · 이펙트 | **투명** |
| **배경 8종** (`ASSET_LIST_BG.md`) | **불투명** — 그 자체가 바닥이다 |
| 버튼 · 패널 | **GPT로 만들지 않는다.** 픽셀 UI는 코드로 그린다 |

#### ⚠ 투명은 요구하되 믿지 마라

**GPT는 투명을 자주 실패한다.** 특히 채팅 화면에서는 **투명 대신 회색 체커보드
무늬를 「그려서」** 준다 — 알파 채널이 아니라 그림이다.

1. 먼저 **「투명 배경 PNG」**로 요구한다
2. 안 되면 **선명한 마젠타(`#FF00FF`) 단색 배경**으로 요구한다.
   캐릭터에 절대 안 쓰이는 색이라 후처리가 깨끗이 지운다
3. **받은 파일을 눈으로 확인한다.** 체커보드가 그려져 있으면 2번으로 다시 뽑는다

> **후처리 도구는 두 경우를 다 처리한다** — 알파가 있으면 그대로,
> 없으면 배경색을 키로 지운다. **투명 PNG의 반투명 가장자리**(게임에서 흰 테두리처럼
> 보이는 것)도 여기서 정리한다.

#### 해상도 — 크게 받아 줄인다

**줄이면 좋아지고 늘리면 뭉개진다.**

> ### 🔴 2026-09-11 정정 — **아래 「최종」 칸은 낡았다**
> 캔버스는 **정사각이고 발바닥이 `kArtFootLine`(477/512) 비율에 있기만 하면**
> 크기가 코드에 아무 영향이 없다 (1-3절). 지금 구워진 것은 **마법사·적 1024 · 보스 880**이다.
> **배경은 1024×1536이 아니다** — 구워진 것이 1024×2276이고,
> **새로 요청할 때는 세로:가로 3.3 이상**이다 (`ART_REQUEST_BG_V4.md`).

| 자산 | 생성기에 요구 | 최종 |
|---|---|---|
| 캐릭터 · 적 · 이펙트 | **정사각 1024 이상** | 정사각이면 무관 (지금 1024) |
| 보스 | **정사각 1024 이상** | 정사각이면 무관 (지금 880) |
| 아이템 아이콘 | **1024 × 1024** | 256 |
| 배경 | **가로 1024 이상 · 세로:가로 3.3 이상** | **그대로 쓴다** (재축소 없음) |

#### 흔한 함정

- **글자를 넣지 마라.** GPT는 글자를 제대로 못 쓴다 (3-1 금지 목록)
- **한 장에 하나만.** 「시트로 여러 개」는 크기·조명이 제각각이라 잘라 써도 안 맞는다
- **파일명은 정해진 규칙으로** 받는다 (6절 자산 목록의 이름 그대로)

### 3-1. 원화 프롬프트 공통 문구 (전 캐릭터 고정)

매번 다른 화풍이 나오지 않게 **아래를 프롬프트에 항상 붙인다.**

| 항목 | 고정값 |
|---|---|
| 시점 | **정면에 가까운 3/4, 전신, 눈높이** |
| 조명 | **왼쪽 위에서 오는 단일 광원** (`ART_PIPELINE` v1과 동일 규칙 계승) |
| 배경 | **완전 투명 / 단색** |
| 채도 | 중간 이상. 파스텔 금지, 사실적 질감 금지 |
| 외곽 | **선명한 외곽선이 있는 스타일** |
| 금지 | 사진풍, 3D 렌더풍, 과한 그라디언트, 글자·워터마크 |

### 3-2. 들어오는 원화에 반드시 거는 후처리 3가지

**사람이 눈으로 맞추지 말고 도구(`tool/`)가 일괄 처리한다.**

> ## 🔴 2026-09-05 — 이 절의 세 처리 중 **둘을 버렸다**
> `docs/art/ART_STYLE_GUIDE.md`가 **「출력 색 수를 기계적으로 제한하지 않음」**과
> **「24~64색 강제 quantization 금지」**를 명시했다.
>
> | 옛 처리 | 지금 |
> |---|---|
> | ~~팔레트 근사~~ | ⛔ **안 한다** |
> | ~~포스터라이즈 24~32단계~~ | ⛔ **안 한다** |
> | ~~외곽선 추가 (코드가 2~3px)~~ | ⛔ **코드가 붙이지 않는다.** 원화가 **colored outline**을 갖고 나온다 |
>
> **왜 뒤집혔나** — 실제로 사고를 냈다. 「색 24~32색」 규격 한 줄이
> **정확히 24색 양자화를 불러 지팡이 볼륨을 소멸**시켰다
> (`CLAUDE.md` LESSONS 2026-09-04). **톤 통일은 후처리가 아니라
> 생성 단계(IPAdapter + 공통 스타일 문구)에서 잡는다.**

**지금 남은 후처리는 「모양을 안 건드리는 것」뿐이다** (`docs/art/RUNTIME_ASSET_SPEC.md`).

| # | 처리 | 왜 |
|---|---|---|
| 1 | **배경 제거** — 알파가 있으면 그대로, 없으면 마젠타 키잉 | 3-0절 「투명은 요구하되 믿지 마라」 |
| 2 | **알파 경계 크롭 → 512×512 정규화** (발바닥 y=477 · 중심 x=256) | 1-3절 배치 규약 |
| 2-b | 🔴 **격자화** — 면적 평균으로 **규격 칸 수까지 줄이고 정수배로 최근접 확대** (`tool/gridize.py`) | **2026-09-08 신설.** 아래 상자 |
| 3 | **알파 250 이상 → 255 스냅** | 원화 알파 최대가 253~254라 배경이 미세하게 비친다 |
| 4 | **WebP 굽기** → `assets/art/` | 용량 |

- **알파 이진화(0/255)는 하지 않는다.** 반투명·glow를 허용한다

> ## 🔴 2026-09-08 신설 — **격자화(2-b)** (검토 32 승인)
>
> ### 왜 넣었나 — **생성기가 「칸 수」를 못 지킨다**
> 마법사 46칸을 **두 번** 요청했는데 **56.4칸 → 56.5칸**(0.1칸 움직였다),
> 배경 1024를 두 번 요청했는데 **두 번 다 841**이었다. 보스는 두 번 다
> **격자가 아예 없었다.** **숫자를 더 세게 적어서 되는 종류의 문제가 아니다.**
>
> ### 왜 해도 되나 — **정보를 버리는 방향이라 이쪽에서 된다**
> 받은 그림은 언제나 **필요한 것보다 잘게** 그려져 온다.
> **잔 것을 굵게 만드는 것은 되고, 그 반대는 안 된다.**
> 네 장에 걸어 보니 **격자 적합이 전부 0.00** — Phase 1.5 합격 원화와 같은 값이다.
> **색·자세·구도는 한 화소도 안 바뀐다.** 잃는 것은 **요청한 만큼의 잔 디테일뿐**이다.
>
> ### 🔴 이것은 위에서 버린 세 처리와 다르다
> 팔레트 근사·포스터라이즈·외곽선 추가는 **색과 모양을 만들어 내는** 처리라
> 볼륨을 죽였다. 격자화는 **해상도만 낮춘다.** 색을 새로 만들지 않는다.
>
> ### 남는 제약 — **한 방향으로만 된다**
> 원화가 규격 칸 수보다 **잘 때만** 굵게 찍을 수 있다.
> → **요청서에서 「칸 수」 협상을 빼고 「이보다 잘게만 그려 주세요」로 바꾼다.**

### 3-3. 결과 고정 (v1의 「3중 잠금」 계승)

- 후처리한 결과물을 `assets/art/`에 **굽고**, 파일 해시를 테스트로 고정한다
- 🔴 **원화 원본은 `art_raw/`에 둔다** (2026-09-05 정정). 옛 문장은 「저장소에 넣지
  않는다」였는데 `art_raw/README.md`·로드맵 3절과 어긋났다. **무거워지면 그때 뺀다**
- 라이선스: GPT 원화는 상업 이용 확인됨(2026-09-01). **`ATTRIBUTION.md`에
  도구·확인 날짜를 기록**한다 (`GAME_DESIGN.md` 10절)

---

## 4. 변종 6종을 일러스트에서 어떻게 보이나 ★ 새 그림 0장 유지

**「적 30종 → 180가지」는 색조를 곱하는 전제로 설계됐다** (`ENEMIES.md` 5절).
일러스트에 색조를 그대로 곱하면 **탁해진다.** 방식을 바꾸되 **새 그림은 여전히 0장이다.**

| 변종 | 표현 |
|---|---|
| 기본 | 그대로 |
| **굶주린** | 붉은 색조 **20%** + 붉은 외곽 광채 |
| **부패한** | 녹색 색조 **20%** + 아래로 흐르는 입자 |
| **얼어붙은** | 청색 색조 **25%** + 표면 하이라이트 |
| **그림자** | **밝기 −35%** + 보라 외곽 광채 (색조는 거의 안 씌운다) |
| **고대의** | 금빛 외곽 광채 **강하게** + 크기 **×1.15** + 미세 입자 |

- **색조는 약하게, 외곽 광채는 강하게.** 원화의 형태·질감을 죽이지 않으면서
  「다른 놈이다」가 즉시 읽힌다
- 「고대의」만 크기를 키운다 — 종반 보스급 변종이므로 **실루엣부터 다르게**
- 전부 코드로 처리한다 (`ColorFiltered` + 외곽 광채). **원화는 1장뿐이다**

---

## 5. 화면 구도 6종

**공통**: 배경은 원화, 패널·버튼·글꼴은 픽셀. 글꼴 Galmuri(10·20·30에서만 또렷).

### 5-1. 전투 화면 (가장 중요 — 여기서 다 논다)

> ## ⛔ 2026-09-08 — **아래 옛 도식은 통째로 무효다** (검토 31)
> 상단 12% 적 이름·HP / 손패 15% / 조작 25% / 「보통·전력」 버튼이 **전부 사라졌다.**
> **무대 상자와 조작부라는 구분 자체가 없어졌다** — 배경이 화면 100%를 덮고
> 그 위에 UI가 얹힌다.
>
> ### 지금 기준 — **모든 좌표를 「화면 아래에서 몇 dp」로 잰다**
> 기기 비율이 16:9 ~ 20:9로 제각각이라 **위에서 재면 기기마다 구도가 달라진다.**
> 아래에서 재면 화면이 길어져도 **위쪽 배경만 더 보이고** 관계는 그대로다
> (세 기기 실측이 완전히 일치함을 검토 31이 재현했다).
>
> | 화면 아래에서 | 무엇 | 상수 |
> |---|---|---|
> | 8 ~ 142dp | 주사위 3개(삼각편대) + 리롤·시전 버튼 | `kCtrlBottom` |
> | **144 ~ 208dp** | **주문 슬롯 5칸** (안 산 칸은 잠금) | `kSlotsBottom` `kSlotsHeight` |
> | **230dp** | **마법사 발** | `kHeroFoot` |
> | **390dp** | 보스 발 | `kBossFoot` |
> | 403 / 449dp | 앞줄 적 발 / 뒷줄 적 발 (다중 적) | `kFoeFrontFoot` `kFoeBackFoot` |
> | **377dp** | 🔴 **길이 화면 폭을 다 채우는 높이. 이 아래로 나무 금지** | `kRoadOpen` |
> | 585dp | 길이 안개에 잠기는 높이 | `kRoadMist` |
>
> 🔴 **144가 `kSlotsBottom` 의 하한이다** — 그 아래는 족보 줄(116~140), 그 아래가
> 주사위(8~112)라 **더 내리면 겹친다** (2026-09-09 사용자 요청으로 152 → 144,
> `kHeroFoot` 250 → 230, `kResBottom` 228 → 208. 검토 34 9-1절).
> `kUnitPlateHeight` 는 글자를 12px 로 키우면서 20 → **22**.
>
> 화면 **위**에 고정되는 것은 둘뿐이다 — **상단 띠 44dp**(던전 이름·설정)와
> **보스 체력바**(y 48, 높이 20, 가로 전체). **보스는 발밑에 체력바를 두지 않는다.**
>
> 정본은 `lib/core/layout.dart` 이고 근거는 `reports/31_UI재설계_6A.md` 4-8·4-9절이다.
> **여기에 그 표를 다시 옮겨 적지 마라** (`HANDOFF.md` 9절).

<details><summary>옛 도식 (v2 · 무효)</summary>

```
┌────────────────────────────────┐
│ [적 이름]        ▓▓▓▓▓░░ HP    │  상단 12%
│        ⚑ 다음 행동 예고         │
├────────────────────────────────┤
│                                │
│         (배경 원화 · 테마)       │
│              🗿 적              │  무대 48%
│                                │
│           🧙 마법사             │
│                     ▓▓▓ 내 HP  │
├────────────────────────────────┤
│   [주문1]   [주문2]   [주문3]   │  손패 15%
├────────────────────────────────┤
│ 마나 ◆◆◆        마력 축적 □□□  │
│   ⚀ ⚀ ⚀   (주사위)             │  조작 25%
│  [ 보통 ]        [ 전력 ]       │
│  [    주사위 굴리기    ]        │
└────────────────────────────────┘
```

</details>

- **적이 위, 마법사가 아래.** 🔴 **크기 원근을 쓰지 않는다** — 적은 여럿이어도 **같은 크기**이고,
  깊이는 **배경의 길과 양옆 기둥이 작아지는 것**으로 만든다 (그래야 밀도 규칙과 안 부딪힌다)
- ⛔ **「적 원화가 화면의 주인공이다」는 무효.** 주인공은 **주사위와 길**이다.
  적은 작아지고 멀어졌다 (마법사 112 · 일반 적 76dp)
- 폭주 팝업은 이 위에 뜬다 — **갈래(큰 글씨) → 숫자 → 한 줄** (`SURGE_DESIGN.md` 1절)
- 주사위는 **각인 테두리 색**을 가진다 (`ENGRAVINGS.md` 8절, 잠금 금색과 안 겹치게)

### 5-2. 지역 선택

```
┌────────────────────────────────┐
│  ← 지역                 Lv.42  │
├────────────────────────────────┤
│ ┌────────────────────────────┐ │
│ │ (배경 원화 축소)  ①마나의 숲 │ │
│ │  권장 Lv 1~8      ★★★ 완료  │ │
│ └────────────────────────────┘ │
│ ┌────────────────────────────┐ │
│ │ (배경 원화 축소)  ②이끼 낀…  │ │
│ │  권장 Lv 8~16     ★★☆      │ │
│ └────────────────────────────┘ │
│              ⋮ (12개 세로 스크롤) │
└────────────────────────────────┘
```

- **지역 카드의 그림 = 그 지역의 배경 원화를 잘라 쓴다.** 새 그림 0장
- 카드에 **권장 레벨**과 **난이도 3단계 클리어 표시(★)**
- 잠긴 지역은 어둡게 + 해금 조건 한 줄

### 5-3. 스테이지 선택

```
┌────────────────────────────────┐
│  ← 마나의 숲            Lv.42  │
│  [보통] [하드] [데스]           │  ← 난이도 탭
├────────────────────────────────┤
│  Ⅰ 3층 ★   Ⅱ 3층 ★   Ⅲ 3층 ★ │
│  Ⅳ 4층 ★   Ⅴ 4층 ☆   Ⅵ 4층 🔒│
│  Ⅶ 5층 🔒                      │
│  ┌──────────────────────────┐  │
│  │ Ⅷ  태고의 웅덩이   👑보스 │  │  ← 보스 스테이지는 크게
│  │    5층 · 권장 Lv 8        │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘
```

- **난이도는 탭으로 위에.** 스테이지를 다시 고르지 않아도 난이도만 바꾼다
- **보스 스테이지만 카드가 크고 부제가 보인다** (`STAGES.md` 3절의 12개)

### 5-4. 지도 (스테이지 안, 층 진행)

```
┌────────────────────────────────┐
│ 마나의 숲 Ⅷ · 태고의 웅덩이     │
│ ▓▓▓▓▓▓▓░░░  3 / 5층            │
├────────────────────────────────┤
│      ⑤ 👑  (보스)               │
│      ④ ♥   (상점)               │
│   ▶ ③ ⚔   ← 지금                │
│      ② ★   (주문 보상)          │
│      ① ✔                       │
├────────────────────────────────┤
│  다음 상대                      │
│  [적 원화]  굶주린 고블린 정찰병 │
│             HP 11 · 공격 5      │
├────────────────────────────────┤
│  [ 들어가기 ]     [ 철수하기 ]  │
└────────────────────────────────┘
```

- **아래에서 위로 오르는 세로 경로.** 층수가 3~10으로 가변이므로 **칸 수가 유동적**
- **다음 상대를 원화로 미리 보여준다** — 변종 색조도 여기서 보인다
- **철수**(6.4절)는 여기에 둔다

### 5-5. 덱 편성 (서클 슬롯)

```
┌────────────────────────────────┐
│  덱 편성            21 / 21칸   │
├────────────────────────────────┤
│ 1서클 ●●●●●  [🔥][❄][✦][🌑][🌿] │
│ 2서클 ●●●●   [🔥][❄][  ][  ]   │
│ 3서클 ●●●    [  ][  ][  ]      │
│  ⋮                             │
├────────────────────────────────┤
│  보유 마법 (서클별 탭)          │
│  [아이콘][아이콘][아이콘]…       │
└────────────────────────────────┘
```

- **서클별로 줄이 나뉘고 슬롯 수가 눈에 보인다** (`SPELL_CIRCLES.md` 부록)
- 빈 칸이 보여야 「몇 개 더 넣을 수 있는지」가 즉시 읽힌다

### 5-6. 장비·각인

```
┌────────────────────────────────┐
│      [ 마법사 4레이어 미리보기 ] │  ← 장비 바꾸면 즉시 반영
│   모자  로브  지팡이             │
│   신발  귀걸이 수정구            │
├────────────────────────────────┤
│  각인 주사위                    │
│    ⚀      ⚁      ⚂            │
│  [보상]  [확률]  [폭주]         │  ← 테두리 = 등급 색
└────────────────────────────────┘
```

- **장비를 바꾸면 위 미리보기가 바로 바뀐다.** 이게 장비 수집의 동기다
- 외관에 반영되는 3종(모자·로브·지팡이)을 **위 줄에** 둔다

---

## 6. 자산 목록과 우선순위

**전부 한꺼번에 만들지 않는다.** 아래 순서로 채운다.

| 순위 | 자산 | 수량 | 없으면 |
|---|---|---|---|
| **1** | 배경 (🔴 **4테마가 아니라 8종**) | 8 | 게임이 안 예쁘다 (체감 1위) |
| **1** | 마법사 몸 + 시작 장비 3종 | 4 | 주인공이 없다 |
| **2** | tier 1~2 일반 적 | 11 | 도입 3개 지역을 못 채운다 |
| **2** | 지역 1~3 보스 | 3 | |
| **3** | tier 3~5 일반 적 | 15 | |
| **3** | 지역 4~12 보스 | 9 | |
| **4** | 장비 원화 (모자·로브·지팡이 등급별) | 15~30 | 아이콘으로 버틴다 |
| **5** | 주문·유물 아이콘 | 110 | **기존 도트 아이콘으로 버틴다** |

### ★ 플레이스홀더 전략 — 기존 도트를 지우지 마라

> **원화가 들어온 것부터 하나씩 갈아끼운다.**
> `sprite_map.dart`가 **「원화 파일이 있으면 원화, 없으면 기존 도트」** 로
> 떨어지게 만든다. 그러면 **자산이 반만 있어도 게임이 계속 돌아간다.**

- `lib/art/`의 코드 도트는 **폴백으로 남긴다.** 전부 교체된 뒤에 지운다
- `test/sprite_test.dart`도 그때까지 유지한다

---

## 7. 지금 코드에서 무엇이 바뀌나

| 파일 | 조치 |
|---|---|
| `lib/art/sprite_map.dart` | **원화 우선 + 도트 폴백** 구조로 (핵심) |
| `lib/art/stage_backdrop.dart` | 원화 배경으로 대체. 코드 배경은 폴백 |
| `lib/art/sprites_*.dart` | **당분간 유지** (폴백) |
| `lib/art/spell_fx.dart` | 유지. 이펙트는 코드 + 원화 혼합 |
| `pubspec.yaml` | `assets/art/` 추가 |
| `tool/` | **원화 후처리 도구 신규** (3-2의 4단계 — 모양을 안 건드리는 것만) |

---

## 8. 남은 결정 〔미정〕

1. ~~원화 화풍의 구체적 레퍼런스~~ → **닫혔다.** 로드맵 8절이 정한 방식은
   **승인된 첫 에셋을 IPAdapter로 참조**하는 것이다. 스타일 문구는
   `docs/art/ART_STYLE_GUIDE.md`에 있다
2. ~~팔레트를 원화에 맞출지, 원화를 팔레트에 맞출지~~ → **닫혔다.**
   **둘 다 안 한다.** 강제 quantization 금지 (3-2절 개정)
3. ~~포스터라이즈 단계 수~~ → **닫혔다.** 포스터라이즈를 하지 않는다
4. **앱 용량 상한** (WebP 품질 결정에 필요) — 유일하게 남은 미결
5. 🔴 **ComfyUI 체크포인트·LoRA·IPAdapter 가중치의 상업 이용 가부**
   (`GAME_DESIGN.md` 10절 · 14절 18번). **에셋을 뽑기 전에 정한다**
