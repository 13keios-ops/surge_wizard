# BLOCKERS — 무인 작업 중 내린 판단과 막힌 지점

작성: 2026-08-31 밤 (무인 작업 세션)

---

## 판단 1 — 대성공 경계값: 기획서 내부 모순 (중요)

**상황:** GAME_DESIGN.md 3.2절 표는 대성공 조건을 "판정값 ≥ DC + 5"라고 적었지만,
3.4절 확률표의 수치는 "판정값 ≥ DC + 3"으로 계산해야만 정확히 일치한다.

검산 (3d6 합계 분포, 총 216가지 기준):
- DC 7 대성공 62.5% = 합계 10 이상일 확률 (10 = 7+3). 7+5=12 이상이면 37.5%가 나와서 표와 안 맞음.
- DC 10 대성공 25.9% = 합계 13 이상일 확률 (13 = 10+3).
- DC 13 대성공 4.6% = 합계 16 이상일 확률 (16 = 13+3).

**판단:** CLAUDE.md 절대 규칙 3번이 "3.4절 확률표가 기준"이라고 명시했으므로
**대성공 = 판정값 ≥ DC + 3** 으로 구현했다. 성공 구간도 자동으로 [DC, DC+3)이 된다.
아슬아슬 [DC−3, DC), 실패 < DC−3 은 양쪽 절이 일치하므로 그대로.

**아침에 확인할 것:** DC+5가 의도였다면 3.4절 표를 다시 계산해야 하고,
constants.dart 의 `critMargin` 상수 하나만 5로 바꾸면 코드는 대응된다.

## 판단 2 — 마력 축적 3칸 vs 뱀눈 충돌

**상황:** "마력 축적 3칸이면 다음 시전은 무조건 대성공"과 "뱀눈은 폭주 확정"이
동시에 일어날 수 있다 (게이지 3인 상태에서 1이 2개 나온 경우).

**판단:** 마력 축적이 이긴다 (무조건 대성공). 근거:
- 기획서가 마력 축적을 "좌절 방지 장치 — 필수"라고 부르고, "무조건"이라는 단어를 썼다.
- 테스트 요구사항 "마력 축적 3칸에서 다음 시전이 무조건 대성공인지"에 예외 언급이 없다.
- 뱀눈 테스트는 게이지 3 미만 상태에서 검증하도록 작성했다.

## 판단 3 — 스트레이트와 페어 보너스

스트레이트(연속 3개)는 세 눈이 전부 다르므로 페어와 동시에 성립할 수 없다.
스트레이트의 효과("다른 주문 1개 자동 동시 시전")는 판정값을 건드리지 않고
결과에 플래그로만 표시하며, 실제 동시 시전은 전투 로직(battle.dart)이 처리한다.

## 판단 4 — 보조 주문의 base_damage 0 예외

**상황:** 작업 지시는 common 주문의 base_damage를 5~9로 규정했지만,
시작 주문 "마력방패"는 공격이 아니라 방어 주문이다. 대미지 5~9를 주면
방어 주문이 아니게 된다.

**판단:** 순수 보조 주문 2종(마력방패, 치유 약초)만 base_damage 0 + effect로 구현.
나머지 68종은 규정 범위를 지켰다. 시작 주문 구성(화염구/서리화살/마력방패)이
기획서 4.2절에 명시돼 있어 마력방패를 빼는 선택지는 없었다.

## 판단 5 — surges.json에 category 필드 추가

기획서 5.2 스키마에는 없지만, 유형 비율(자해 30% 등) 검증과 밸런스 통계를
위해 `category` 필드를 추가했다. 로더는 이 필드를 무시해도 게임 동작에
지장이 없다 (통계·디버깅용).

## 판단 6 — 폭주 effect의 값 규약

- `summon_ally`: 양수 = 아군 소환(매턴 그 값만큼 적 공격), 음수 = 적대 소환(나를 공격)
- `mana_change`: 양수 = 회복, 음수 = 소모
- `swap_hp`: value 미사용(0), 플레이어와 적의 현재 HP 교환
- `damage_multiplier`: 이번 폭주를 일으킨 주문의 대미지에 곱해 적에게 적용
  (실패해도 "폭주로 인해" 대미지가 나가는 도박형/이득형 전용)

## 판단 7 — 전투 로직의 세부 해석 (기획서에 명시 없던 것들)

- **스트레이트 동시 시전:** 손패의 다른 주문 1개가 같은 강도로, 추가 판정
  없이 "성공" 취급으로 공짜 발동한다 (마나 소모 없음, 재귀 없음).
  시전이 실패(폭주)했으면 동시 시전도 없다 — 주문 자체가 불발이므로.
- **적의 charge(강타 준비):** 예고 턴에는 대미지가 없고, 그 다음 적 턴에
  예고된 값만큼 터진다. 그동안 플레이어는 방어막 등으로 대비할 수 있다.
- **아슬아슬 반동 "적 행동 앞당김":** 적이 그 턴에 행동을 2회 한다.
- **전투 간 상태:** 체력·마력 축적은 유지, 마나는 전투마다 최대치로 초기화,
  봉인은 그 전투에서만 유효 (기획서 문구 "이번 전투 동안"에 근거).

## 기록 — PowerShell로 한글 파일을 고치면 안 된다

battle_test.dart를 PowerShell `Get-Content | Set-Content`로 일괄 치환했다가
한글이 전부 깨졌다 (Get-Content에 UTF-8 지정 누락). Write 도구로 전체를
다시 써서 복구했다. 이후 파일 수정은 전부 Edit/Write 도구만 썼다.

## 판단 8 — 검수(REVIEW.md) 수정 라운드에서 내린 결정

- **보스 2페이즈 전환 시점:** REVIEW의 원래 제안("대미지 직후 전환 + 예고
  갱신")은 유저가 이미 본 예고를 바꿔버려 또 다른 거짓말이 된다.
  대신 **예고된 행동을 그대로 실행한 뒤(적 턴 종료 시) 전환**하도록 했다.
  유저와의 약속(예고)이 항상 지켜진다.
- **소환수 행동 시점:** "적 턴 시작 시"로 규칙을 정했다. 시전을 못 하는
  턴에도 소환수가 멈추지 않는다.
- **폭주 텍스트 톤:** "영끌"·"통장" 같은 현대 은어는 세계관 안 표현으로
  교체했다 (유머는 유지). 원문이 더 좋았다면 되돌릴 수 있다.
- **수정하지 않은 것 (사용자 결정 대기):** 대성공 경계 DC+3 확정(판단 1),
  트리플 시 마력 축적 게이지 유지 여부, tier 풀의 층별 HP ±4 편차,
  main.dart 템플릿 교체(UI 단계에서).

## 판단 9 — 4단계(런 진행)의 권장안 설계 (사용자 지시: 비필수 항목은 권장안으로)

- **상점(5층):** 기획서에 경제 설계가 없어 지금은 "휴식 — 체력 10 회복"만
  있다 (`kShopHeal = 10`). 재화·구매 목록은 메타 성장 단계에서 마력 결정과
  함께 설계하는 게 자연스럽다.
- **보상 추첨:** 균등 무작위 (손패/보유 중인 것 제외). 등급별 수량 차이
  (주문 30/25/15, 유물 18/14/8)가 자연 가중치 역할을 한다.
- **주문 보상 UX:** 3장 중 1장 선택 → 교체할 손패 슬롯 선택 → 교체.
  건너뛰기 가능.
- **최대 체력 유물:** 획득 즉시 현재 체력도 같은 양만큼 오른다 (일반적인
  로그라이트 관례).
- **유물 효과 14종 전부 전투·런에 실제 연결** (`core/relic_powers.dart`).
  지연된 것 없음.

## 판단 10 — 2차 시뮬레이션의 핵심 발견 (수치 미변경)

리롤을 최적으로 쓰는 봇은 실패율이 1.9%까지 떨어져 폭주(이 게임의
차별점)가 거의 발생하지 않는다. 상세와 제안 3가지는 BALANCE.md 2차
시뮬레이션 절 참조. 밸런스 수치는 절대 규칙 3에 따라 건드리지 않았다.

## 판단 11 — 5단계(메타 성장)의 권장안 설계

기획서 5.5절에 없는 부분을 이렇게 정했다:

- **마력 결정 지급량:** 돌파한 층당 5개 + 보스 클리어 보너스 50 (완주 = 100).
  약한 플레이어도 3~4판이면 첫 강화(50)를 살 수 있는 페이싱.
- **체력 강화 4·5단계 비용:** 170, 230 (기획서의 50→80→120 증가폭 연장).
  리롤 2단계 비용: 300 (마나 강화의 배수 규칙과 동일).
- **"신규 주문 해금"의 의미:** epic 주문 15종은 기본 잠금이고, 80개씩
  해금하면 전투 보상 추첨에 등장한다. common/rare는 처음부터 등장.
- **"시작 유물 1개"의 의미:** 판 시작 시 무작위 common 유물 1개 지급.
- 저장 키: `meta_state_v1` (shared_preferences). 판 중간 저장(앱 강제 종료
  시 런 복구)은 미구현 — 필요해지면 RunState.toJson이 이미 있어서 붙이기
  쉽다.

메타 페이싱 측정 결과와 "소비처 30판이면 고갈" 발견은 BALANCE.md 3차 참조.

## 판단 12 — 효과음을 직접 합성했다

절대 규칙 6이 외부 에셋 임의 다운로드를 금지하므로, 효과음을 인터넷에서
받지 않고 `tool/gen_sfx.dart`로 파형을 합성해 만들었다 (딸깍·주사위·타격·
대성공·폭주 5종). 저작권 문제가 없고 재생성도 자유롭다.
나중에 더 좋은 음원으로 교체하려면 같은 파일명으로 덮어쓰면 된다.

## 판단 13 — Windows 확인용 빌드 환경 문제 2건 (안드로이드와 무관)

1. **NuGet 저장소 미설정** — audioplayers 플러그인의 Windows 빌드가
   NuGet 패키지를 받아야 하는데 이 PC의 전역 NuGet 설정에 저장소 주소가
   없었다. 전역 설정을 건드리지 않으려고 프로젝트에 `NuGet.config`를 두어
   해결했다. 안드로이드 빌드에는 영향이 없다.
2. **build 폴더를 수동 삭제하면 안 된다** — `native_assets` 폴더가 사라져
   설치 단계가 실패한다. 반드시 `flutter clean`을 쓸 것.

---

(이하 작업 중 발생한 항목이 추가된다)
